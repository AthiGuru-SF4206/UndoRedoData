@inherits GridTestBase
@using Syncfusion.Blazor.Grids
@using Syncfusion.Blazor.Tests

<SfFixture @ref="fixture" TestAsync="GridBasicRendering" Description="">
    <ComponentUnderTest>
        <SfGrid DataSource="GenerateCheckBoxData()" AllowFiltering="true" AllowPaging="true" Toolbar="@ToolbarItems">

            <GridColumns>
                <GridColumn Field=@nameof(Order.OrderID) HeaderText="Order ID" TextAlign="TextAlign.Right" Width="120"></GridColumn>
                <GridColumn Field=@nameof(Order.CustomerID) HeaderText="Customer Name" Width="150"></GridColumn>
                <GridColumn Field=@nameof(Order.OrderDate) HeaderText=" Order Date" Format="d" Type="ColumnType.Date" TextAlign="TextAlign.Right" Width="130"></GridColumn>
                <GridColumn Field=@nameof(Order.Freight) HeaderText="Freight" Format="C2" TextAlign="TextAlign.Right" IsFrozen="true" Width="120"></GridColumn>
            </GridColumns>
        </SfGrid>
    </ComponentUnderTest>
</SfFixture>

@code{
    SfFixture fixture { get; set; }
    public List<Order> OrdersData { get; set; }
    public string[] ToolbarItems = new string[] { "ColumnChooser" };
    public string[] HideItems = new string[] { "Freight" };

    async Task GridBasicRendering(Fixture fixture)
    {
        var cut = fixture.GetComponentUnderTest<SfGrid<Order>>();
        var Grid = cut.Instance;
        var a = Grid.GetVisibleColumnsAsync().Result.Count();
        Assert.True(Grid.GetVisibleColumnsAsync().Result.Count() == 4, "Total columns rendered properly");
        await Grid.HideColumnsAsync(HideItems, "Freight");
        Assert.True(Grid.GetVisibleColumnsAsync().Result.Count() == 3, "Columns rendered properly after hidden");
    }
   
    public List<Order>
GenerateCheckBoxData()
    {
        return OrdersData = Enumerable.Range(1, 75).Select(x => new Order()
        {
            OrderID = 1000 + x,
            CustomerID = (new string[] { "datagrid", "Datagrid", "pgAdmin 4", "pgadmin 4" })[new Random().Next(4)],
            Freight = 2.1 * x,
            OrderDate = (new DateTime[] { new DateTime(2010, 5, 1), new DateTime(2010, 5, 2), new DateTime(2010, 5, 3), })[new Random().Next(3)],
        }).ToList();
    }

    public class Order
    {
        public int? OrderID { get; set; }
        public string CustomerID { get; set; }
        public DateTime? OrderDate { get; set; }
        public double? Freight { get; set; }
    }
}
