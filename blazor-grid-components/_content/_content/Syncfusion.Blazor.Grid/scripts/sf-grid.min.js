(window["webpackJsonp"] = window["webpackJsonp"] || []).push([["_content/Syncfusion.Blazor.Grid/scripts/sf-grid"],{

/***/ "./bundles/sf-grid.js":
/*!****************************!*\
  !*** ./bundles/sf-grid.js ***!
  \****************************/
/*! no exports provided */
/*! all exports used */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _modules_sf_grid_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../modules/sf-grid.js */ "./modules/sf-grid.js");
/* harmony import */ var _modules_sf_grid_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_modules_sf_grid_js__WEBPACK_IMPORTED_MODULE_0__);


/***/ }),

/***/ "./modules/sf-grid.js":
/*!****************************!*\
  !*** ./modules/sf-grid.js ***!
  \****************************/
/*! no static exports found */
/***/ (function(module, exports) {

function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
window.sfBlazor = window.sfBlazor || {};
window.sfBlazor.Grid = function () {
  'use strict';

  var __spreadArray$2 = undefined && undefined.__spreadArray || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar) ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
  };
  /**
   * The function used to update DOM using requestAnimationFrame.
   *
   * @param {Function} updateFunction Function that contains the actual update action.
   * @param {Function} callBack Callback function to execute after the update.
   * @returns {void}
   * @hidden
   */
  function getUpdateUsingRaf(updateFunction, callBack) {
    requestAnimationFrame(function () {
      try {
        callBack(null, updateFunction());
      } catch (e) {
        callBack(e);
      }
    });
  }
  /**
   * @param {HTMLElement} node - Defines the row
   * @param {number} width - Defines the width
   * @param {boolean} isRtl - Boolean property
   * @param {string} position - Defines the position
   * @returns {void}
   * @hidden
   */
  function applyStickyLeftRightPosition(node, width, isRtl, position) {
    if (node == null) {
      return;
    }
    if (position === 'Left') {
      if (isRtl) {
        node.style.right = width + 'px';
      } else {
        node.style.left = width + 'px';
      }
    }
    if (position === 'Right') {
      if (isRtl) {
        node.style.left = width + 'px';
      } else {
        node.style.right = width + 'px';
      }
    }
  }
  /** @hidden */
  var scrollWidth = null;
  /**
   * @hidden
   * Retrieves the width of the scrollbar on the current browser.
   *
   * @returns {number} The width of the scrollbar in pixels.
   */
  function getScrollBarWidth() {
    if (scrollWidth !== null) {
      return scrollWidth;
    }
    var divNode = document.createElement('div');
    var value = 0;
    divNode.style.cssText = 'width:100px;height: 100px;overflow: scroll;position: absolute;top: -9999px;';
    document.body.appendChild(divNode);
    value = divNode.offsetWidth - divNode.clientWidth | 0;
    document.body.removeChild(divNode);
    return scrollWidth = value;
  }
  /**
   * Retrieves the cumulative height of previous and next siblings of a given element.
   *
   * @param {HTMLElement} element The element whose siblings' heights are to be calculated.
   * @returns {number} The cumulative height of previous and next siblings.
   */
  function getSiblingsHeight(element) {
    var previous = getHeightFromDirection(element, 'previous');
    var next = getHeightFromDirection(element, 'next');
    return previous + next;
  }
  /**
   * Retrieves the cumulative height of siblings in a specific direction until encountering specified classes.
   *
   * @param {HTMLElement} element The starting element to traverse from.
   * @param {string} direction The direction ('previous' or 'next') to traverse sibling elements.
   * @returns {number} The cumulative height of sibling elements with specified classes.
   */
  function getHeightFromDirection(element, direction) {
    var sibling = element[direction + 'ElementSibling'];
    var result = 0;
    var classList = ['e-gridheader', 'e-gridfooter', 'e-groupdroparea', 'e-gridpager', 'e-toolbar'];
    while (sibling) {
      if (classList.some(function (value) {
        return sibling.classList.contains(value);
      })) {
        result += sibling.offsetHeight;
      }
      sibling = sibling[direction + 'ElementSibling'];
    }
    return result;
  }
  /**
   * @hidden
   * Traverses up the DOM tree from a given element until it finds a parent element that matches the selector.
   *
   * @param {Element} elem The starting element for traversal.
   * @param {string} selector The selector to match against parent elements.
   * @param {boolean} [isID=false] Optional flag indicating if the selector is an ID.
   * @returns {Element} The matching parent element, or null if none found.
   */
  function parentsUntil(elem, selector, isID) {
    var parent = elem;
    while (parent) {
      if (isID ? parent.id === selector : parent.classList.contains(selector)) {
        break;
      }
      parent = parent.parentElement;
    }
    return parent;
  }
  /** @hidden */
  // eslint-disable-next-line @typescript-eslint/no-namespace
  var Global;
  (function (Global) {
    // eslint-disable-next-line prefer-const
    Global.timer = null;
  })(Global || (Global = {}));
  /**
   * @hidden
   * Retrieves the index of an element within an array of elements.
   *
   * @param {Element} element The element to search for in the array.
   * @param {Element[]} elements The array of elements to search within.
   * @returns {number} The index of the element if found, otherwise -1.
   */
  function getElementIndex(element, elements) {
    var index = -1;
    for (var i = 0, len = elements.length; i < len; i++) {
      if (elements[parseInt(i.toString(), 10)].isEqualNode(element)) {
        index = i;
        break;
      }
    }
    return index;
  }
  /**
   * @hidden
   * Checks if a value exists in an array and returns its index if found, otherwise returns -1.
   *
   * @param {Object} value The value to search for in the array.
   * @param {Object[]} collection The array to search within.
   * @returns {number} The index of the value if found, otherwise -1.
   */
  function inArray(value, collection) {
    for (var i = 0, len = collection.length; i < len; i++) {
      if (collection[parseInt(i.toString(), 10)] === value) {
        return i;
      }
    }
    return -1;
  }
  /**
   * Retrieves the position (x, y coordinates) from a MouseEvent or TouchEvent.
   *
   * @param {MouseEvent | TouchEvent} e The MouseEvent or TouchEvent from which to retrieve the position.
   * @returns {IPosition} An object containing x and y coordinates.
   */
  function getPosition(e) {
    var position = {};
    position.x = sf.base.isNullOrUndefined(e.clientX) ? e.changedTouches[0].clientX : e.clientX;
    position.y = sf.base.isNullOrUndefined(e.clientY) ? e.changedTouches[0].clientY : e.clientY;
    return position;
  }
  /**
   * Iterates over an array or object and applies a predicate function to each item.
   *
   * @param {Array|Object} collection The array or object to iterate over.
   * @param {Function} predicate The function to apply to each item.
   * @returns {Array} A new array with the results of the predicate function.
   */
  function iterateArrayOrObject(collection, predicate) {
    var result = [];
    for (var i = 0, len = collection.length; i < len; i++) {
      var pred = predicate(collection[parseInt(i.toString(), 10)], i);
      if (!sf.base.isNullOrUndefined(pred)) {
        result.push(pred);
      }
    }
    return result;
  }
  /**
   * Checks if an action should be prevented based on the presence of updated elements and the state of a dialog.
   *
   * @param {HTMLElement} element The HTML element to check.
   * @returns {boolean} Returns true if the action should be prevented, false otherwise.
   */
  function isActionPrevent(element, editMode) {
    var dlg = element.querySelector('#' + element.id + 'EditConfirm');
    var updatedElements = element.querySelectorAll('.e-updatedtd');
    return !sf.base.isNullOrUndefined(editMode) && editMode === 'Batch' && updatedElements.length > 0 ? false : updatedElements.length > 0 && (dlg ? dlg.classList.contains('e-popup-close') : true);
  }
  /**
   * Determines if the grid is in a group adaptive state.
   *
   * @param {SfGrid} grid - The grid instance to check.
   * @returns {boolean} - Returns `true` if the grid is group adaptive, otherwise `false`.
   *
   * @hidden
   */
  function isGroupAdaptive(grid) {
    return grid.options.enableVirtualization && grid.options.groupCount > 0 && (grid.options.offline || grid.options.url === '');
  }
  /** @hidden */
  var rowHeight;
  /**
   * Get the row height by creating a temporary table inside the given element.
   *
   * @param {HTMLElement} [element] - The element to append the temporary table to.
   * @returns {number} - The calculated height of a table row.
   */
  function getRowHeight(element) {
    if (rowHeight !== undefined) {
      return rowHeight;
    }
    var table = sf.base.createElement('table', {
      className: 'e-table',
      styles: 'visibility: hidden'
    });
    table.innerHTML = '<tr><td class="e-rowcell">A<td></tr>';
    element.appendChild(table);
    var rect = table.querySelector('td').getBoundingClientRect();
    element.removeChild(table);
    rowHeight = Math.ceil(rect.height);
    return rowHeight;
  }
  /**
   * Removes elements that match the given selector from the target element.
   *
   * @param {Element} target - The target element from which to remove the matching elements.
   * @param {string} selector - The CSS selector to identify the elements to be removed.
   * @returns {void}
   *
   * @hidden
   */
  function removeElement(target, selector) {
    var elements = [].slice.call(target.querySelectorAll(selector));
    for (var i = 0; i < elements.length; i++) {
      sf.base.remove(elements[parseInt(i.toString(), 10)]);
    }
  }
  /**
   * Adds or removes active classes to/from the given cells.
   *
   * @param {Element[]} cells - The list of cell elements to modify.
   * @param {boolean} add - A flag indicating whether to add or remove the classes.
   * @param {...string[]} args - The classes to add or remove.
   * @returns {void}
   *
   * @hidden
   */
  function addRemoveActiveClasses(cells, add) {
    var args = [];
    for (var _i = 2; _i < arguments.length; _i++) {
      args[_i - 2] = arguments[_i];
    }
    for (var i = 0, len = cells.length; i < len; i++) {
      if (add) {
        sf.base.classList(cells[parseInt(i.toString(), 10)], __spreadArray$2([], args, true), []);
        cells[parseInt(i.toString(), 10)].setAttribute('aria-selected', 'true');
      } else {
        sf.base.classList(cells[parseInt(i.toString(), 10)], [], __spreadArray$2([], args, true));
        cells[parseInt(i.toString(), 10)].removeAttribute('aria-selected');
      }
    }
  }
  /**
   * Recursively finds the root parent grid element for a given element.
   *
   * @param {HTMLElement | null} element - The starting HTML element to find the parent grid.
   * @returns {HTMLElement | null} The root parent grid element or null if not found.
   */
  function getRootElement(element) {
    if (!element) {
      return null;
    }
    var parentGrid = parentsUntil(element, 'e-grid');
    if (!parentGrid) {
      return null;
    }
    return parentsUntil(parentGrid, 'e-detailrow') ? getRootElement(parentGrid.parentElement) : parentGrid;
  }
  /**
   * @hidden
   * Retrieves a cell element from the DOM given a row UID and column index.
   *
   * @param {HTMLElement} parentElement The root grid element containing the rows.
   * @param {string} rowUid The unique identifier (data-uid) of the target row.
   * @param {number} cellColIndex The column index (aria-colindex) of the target cell.
   * @returns {HTMLElement | null} The matching cell element if found, otherwise null.
   */
  function getCellByRowUidAndColIndex(parentElement, rowUid, cellColIndex) {
    var cell = null;
    var rows = parentElement.querySelectorAll("[data-uid=\"".concat(rowUid, "\"]"));
    if (!sf.base.isNullOrUndefined(rows) && rows.length > 0 && !sf.base.isNullOrUndefined(parentElement)) {
      var row = null;
      for (var i = 0; i < rows.length; i++) {
        var el = rows[parseInt(i.toString(), 10)];
        if (!sf.base.isNullOrUndefined(el) && parentsUntil(el, 'e-grid') === parentElement) {
          row = el;
          break;
        }
      }
      if (!sf.base.isNullOrUndefined(row)) {
        cell = row.querySelector("[aria-colindex=\"".concat(cellColIndex, "\"]"));
      }
    }
    return cell;
  }

  /**
   * The `Scroll` module is used to handle scrolling behaviour.
   */
  var Scroll = /** @class */function () {
    /**
     * Constructor for the Grid scrolling.
     *
     * @param {SfGrid} [parent] - Optional. The parent grid instance.
     * @hidden
     */
    function Scroll(parent) {
      //To maintain scroll state on grid actions.
      this.previousValues = {
        top: 0,
        left: 0
      };
      this.oneTimeReady = true;
      this.parent = parent;
      this.addEventListener();
      this.setHeight();
      this.setPadding();
    }
    /**
     * Sets the height for something (describe what is being set the height for).
     *
     * @returns {void}
     * @hidden
     */
    Scroll.prototype.setHeight = function () {
      var mHdrHeight = 0;
      var content = this.parent.element.querySelector('.e-content');
      if (this.parent.options.frozenRows && this.parent.options.height !== 'auto' && !this.parent.options.height.match(/%/g)) {
        var tbody = this.parent.element.querySelector('.e-headercontent').querySelector('tbody');
        mHdrHeight = tbody ? tbody.offsetHeight : 0;
        content.style.height = sf.base.formatUnit(parseInt(this.parent.options.height, 10) - mHdrHeight);
      }
    };
    /**
     * @hidden
     */
    Scroll.prototype.removeUnwantedScroll = function (offsetValue, minWidth) {
      if (minWidth === void 0) {
        minWidth = 0;
      }
      var isFrozenColumn = false;
      var isFrozenRowWidth = this.parent.content.offsetWidth >= this.parent.getContentTable().offsetWidth;
      var isFrozenRow = this.parent.options.frozenRows !== 0 && this.parent.options.frozenColumns === 0;
      //const movablescrollbarDiv: HTMLElement = this.parent.element.querySelector('.e-movablescrollbar');
      //let movableScrollbarHeight: number = movablescrollbarDiv ? movablescrollbarDiv.offsetHeight : 0;
      if (this.parent.options.frozenColumns !== 0 && !sf.base.isNullOrUndefined(this.getMovableContent())) {
        isFrozenColumn = this.getMovableContent().offsetWidth >= this.getMovableContentTable().offsetWidth + minWidth - 2 || this.getMovableContent().offsetWidth === 0;
      }
      var isHorizontalScrollBarRendered = this.parent.content.scrollWidth > this.parent.content.offsetWidth;
      var tableHeight = this.parent.getContentTable().offsetHeight;
      var actualScrollHeight = tableHeight + (isHorizontalScrollBarRendered ? Scroll.getScrollBarWidth() : 0);
      if (offsetValue === 'Height' && this.parent.content.offsetHeight >= actualScrollHeight) {
        return true;
      }
      if (offsetValue === 'Width' && (isFrozenColumn || isFrozenRow && isFrozenRowWidth)) {
        if (this.parent.options.height === '100%') {
          this.frozenContentElement().style.borderBottom = 'none';
        }
        return true;
      }
      return false;
    };
    /**
     * @hidden
     */
    Scroll.prototype.getMovableContent = function () {
      return this.parent.element.querySelector('.e-movablecontent');
    };
    /**
     * @hidden
     */
    Scroll.prototype.getMovableContentTable = function () {
      return this.parent.element.querySelector('.e-movablecontent .e-table');
    };
    /**
     * @hidden
     */
    Scroll.prototype.frozenContentElement = function () {
      return this.parent.element.querySelector('.e-frozencontent');
    };
    /**
     * Retrieves the scrollbar element within the parent element.
     *
     * @returns {HTMLElement} The scrollbar element.
     * @hidden
     */
    Scroll.prototype.getScrollbar = function () {
      return this.parent.element.querySelector('.e-scrollbar');
    };
    /**
     * Sets padding for the content based on certain conditions.
     *
     * @returns {void}
     * @hidden
     */
    Scroll.prototype.setPadding = function () {
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      var content = this.parent.element.querySelector('.e-gridheader');
      if (sf.base.isNullOrUndefined(content)) {
        return;
      }
      if (this.parent.options.height === 'auto' && this.parent.options.frozenName === 'None' && this.parent.options.frozenColumns === 0) {
        return;
      }
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      var footer = this.parent.element.querySelector('.e-gridfooter');
      if (this.removeUnwantedScroll('Height')) {
        this.content.style.overflow = this.parent.options.frozenColumns && this.parent.options.enableColumnVirtualization ? 'hidden auto' : 'auto';
        content.querySelector('.e-headercontent').style.borderRightWidth = '';
        content.style.paddingRight = '';
        if (!sf.base.isNullOrUndefined(footer) && footer) {
          footer.style.paddingRight = '';
        }
        return;
      }
      var scrollWidth = Scroll.getScrollBarWidth() - this.getThreshold();
      var cssProps = this.getCssProperties();
      if (this.parent.options.enableRtl) {
        content.style['padding-right'] = '';
      } else {
        content.style['padding-left'] = '';
      }
      var contentElement = this.parent.element.querySelector('.e-content');
      var overflowy = window.getComputedStyle(contentElement)['overflow-y'];
      var overflowHeight = contentElement.clientHeight - this.parent.getContentTable().clientHeight;
      if (!this.parent.options.enableVirtualization && this.parent.options.frozenColumns > 0 && (this.parent.options.height === 'auto' || overflowHeight >= 0) && overflowy === 'auto' && (this.parent.options.allowGrouping || !this.parent.options.hasDetailTemplate)) {
        scrollWidth = 0;
      }
      content.style[cssProps.padding] = scrollWidth > 0 ? scrollWidth + 'px' : '0px';
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      content.querySelector('.e-headercontent').style[cssProps.border] = scrollWidth > 0 ? '1px' : '0px';
      if (footer) {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        var footerContent = footer.querySelector('.e-summarycontent');
        if (!this.parent.options.enableAdaptiveUI) {
          footerContent.style[cssProps.border] = scrollWidth > 0 ? '1px' : '0px';
        }
        footer.style[cssProps.padding] = scrollWidth > 0 ? scrollWidth + 'px' : '0px';
      }
    };
    /**
     * Removes padding from header and its parent element based on the right-to-left (RTL) mode.
     *
     * @param {boolean} [rtl] - Optional. Specifies whether to enable RTL mode.
     * @returns {void}
     * @hidden
     */
    Scroll.prototype.removePadding = function (rtl) {
      var cssProps = this.getCssProperties(rtl);
      var hDiv = this.parent.getHeaderContent();
      hDiv.style[cssProps.border] = '';
      hDiv.parentElement.style[cssProps.padding] = '';
      //const footerDiv: HTMLDivElement = (<HTMLDivElement>this.parent.getFooterContent());
    };
    /**
     * Refresh makes the Grid adoptable with the height of parent container.
     *
     * > The [`height`](grid/#height/) must be set to 100%.
     *
     * @returns {void}
     */
    Scroll.prototype.refresh = function () {
      if (this.parent.options.height !== '100%') {
        return;
      }
      var content = this.parent.element.querySelector('.e-gridcontent');
      var height = getSiblingsHeight(content);
      content.style.height = 'calc(100% - ' + height + 'px)'; //Set the height to the '.e-gridcontent';
    };
    Scroll.prototype.getThreshold = function () {
      /* Some browsers places the scroller outside the content,
       * hence the padding should be adjusted.*/
      var appName = sf.base.Browser.info.name;
      var userAgent = navigator.userAgent;
      if (userAgent.indexOf('Edg/') > -1) {
        return 0.75;
      }
      if (appName === 'mozilla') {
        return 0.5;
      }
      return 1;
    };
    /**
     *
     * @returns {void}
     * @hidden
     */
    Scroll.prototype.addEventListener = function () {
      this.wireEvents();
      // this.parent.on(onEmpty, this.wireEvents, this);
      // this.parent.on(contentReady, this.wireEvents, this);
      // this.parent.on(uiUpdate, this.onPropertyChanged, this);
      // this.parent.on(textWrapRefresh, this.wireEvents, this);
      // this.parent.on(headerRefreshed, this.setScrollLeft, this);
    };
    // private setScrollLeft(): void {
    //     if (this.parent.options.frozenColumns) {
    //         (<HTMLElement>(<SfGrid>this.parent).headerModule.getMovableHeader()).scrollLeft = this.previousValues.left;
    //     }
    // }
    Scroll.prototype.onContentScroll = function (scrollTarget) {
      var _this = this;
      var element = scrollTarget;
      var isHeader = element.classList.contains('e-headercontent');
      return function (e) {
        if (_this.content.querySelector('tbody') === null || _this.parent.options.isPreventScrollEvent) {
          return;
        }
        var target = e.target;
        var left = target.scrollLeft;
        var gridElement = parentsUntil(target, 'e-grid');
        if (!sf.base.isNullOrUndefined(gridElement) && gridElement.querySelectorAll('.e-filter-popup.e-popup-open').length > 0 && _this.previousValues.left !== left) {
          _this.parent.dotNetRef.invokeMethodAsync('FilterPopupClose');
        }
        // Close enhanced operator dropdowns on scroll
        // Enhanced operator dropdowns are rendered outside the grid element in the DOM
        if (!sf.base.isNullOrUndefined(gridElement) && _this.previousValues.left !== left) {
          _this.parent.closeOperatorDropdownIfOpen();
        }
        //const sLimit: number = target.scrollWidth;
        _this.updateFrozenShadow(target);
        var frozenRightColumns = _this.parent.getColumns().filter(function (a) {
          return a.isFrozen && a.freeze === 'Right';
        });
        if (_this.content.scrollTop > 0 && _this.parent.options.frozenRows) {
          _this.parent.element.classList.add('e-top-shadow');
        } else {
          _this.parent.element.classList.remove('e-top-shadow');
        }
        var widthVal = Math.round(target.scrollWidth - target.scrollLeft);
        var gridcontent = _this.parent.getContent();
        //The small margin (e.g., 1) accounts for floating-point precision.
        var scrollReachRightEnd = gridcontent.scrollLeft >= gridcontent.scrollWidth - gridcontent.clientWidth - 1;
        var parentElement = _this.parent.element;
        if (frozenRightColumns.length > 0 && (widthVal === target.offsetWidth || scrollReachRightEnd)) {
          parentElement.classList.remove('e-right-shadow');
        } else {
          parentElement.classList.add('e-right-shadow');
        }
        var isFooter = target.classList.contains('e-summarycontent');
        if (_this.parent.options.enableInfiniteScrolling && !_this.parent.options.isEdit) {
          if (!_this.parent.infiniteScrollModule.isLazyChildLoad) {
            _this.parent.infiniteScrollModule.infiniteScrollHandler(target, _this.previousValues.left, false);
          }
        }
        if (_this.parent.options.groupCount > 0 && _this.parent.options.enableLazyLoading) {
          var isDown = _this.previousValues.top < target.scrollTop;
          if (isDown) {
            _this.parent.infiniteScrollModule.lazyLoadInfiniteScrollHandler(isDown);
          }
        }
        if (_this.previousValues.left === left) {
          _this.previousValues.top = !isHeader ? _this.previousValues.top : target.scrollTop;
          return;
        }
        element.scrollLeft = left;
        var footer = _this.parent.element.querySelector('.e-summarycontent');
        if (footer) {
          footer.scrollLeft = left;
        }
        if (isFooter) {
          _this.header.scrollLeft = left;
        }
        _this.previousValues.left = left;
      };
    };
    Scroll.prototype.onFreezeContentScroll = function (scrollTarget) {
      var _this = this;
      var element = scrollTarget;
      return function (e) {
        if (_this.content.querySelector('tbody') === null) {
          return;
        }
        var target = e.target;
        var top = target.scrollTop;
        if (_this.previousValues.top === top) {
          return;
        }
        element.scrollTop = top;
        _this.previousValues.top = top;
      };
    };
    Scroll.prototype.updateFrozenShadow = function (target) {
      var frozenLeftColumns = this.parent.getColumns().filter(function (a) {
        return a.isFrozen && a.freeze === 'Left';
      });
      var frozenRightColumns = this.parent.getColumns().filter(function (a) {
        return a.isFrozen && a.freeze === 'Right';
      });
      if (target.scrollLeft !== 0 && (this.parent.options.frozenColumns > 0 && frozenRightColumns.length === 0 || frozenLeftColumns.length > 0)) {
        this.parent.element.classList.add('e-left-shadow');
      } else if (this.parent.element.classList.contains('e-left-shadow')) {
        this.parent.element.classList.remove('e-left-shadow');
      }
    };
    Scroll.prototype.onCustomScrollbar = function (mCont, mHdr) {
      var _this = this;
      var content = mCont;
      var header = mHdr;
      var mfooter;
      return function (e) {
        if (_this.content.querySelector('tbody') === null) {
          return;
        }
        var target = e.target;
        var left = target.scrollLeft;
        if (_this.previousValues.left === left) {
          return;
        }
        _this.updateFrozenShadow(target);
        if (_this.parent.options.aggregatesCount) {
          mfooter = _this.parent.element.querySelector('.e-movablefootercontent');
        }
        content.scrollLeft = left;
        header.scrollLeft = left;
        if (mfooter) {
          mfooter.scrollLeft = left;
        }
        _this.previousValues.left = left;
      };
    };
    Scroll.prototype.onWheelScroll = function (scrollTarget) {
      var _this = this;
      var element = scrollTarget;
      return function (e) {
        if (_this.content.querySelector('tbody') === null) {
          return;
        }
        var top = element.scrollTop + (e.deltaMode === 1 ? e.deltaY * 30 : e.deltaY);
        if (_this.previousValues.top === top) {
          return;
        }
        e.preventDefault();
        _this.parent.getContent().querySelector('.e-frozencontent').scrollTop = top;
        element.scrollTop = top;
        _this.previousValues.top = top;
      };
    };
    Scroll.prototype.onTouchScroll = function (scrollTarget) {
      var _this = this;
      var element = scrollTarget;
      return function (e) {
        if (e.pointerType === 'mouse') {
          return;
        }
        var isFrozen = _this.parent.options.frozenColumns > 0;
        var pageXY = _this.getPointXY(e);
        var left = element.scrollLeft + (_this.pageXY.x - pageXY.x);
        var mHdr = isFrozen ? _this.parent.getHeaderContent() : _this.parent.getHeaderContent().querySelector('.e-headercontent');
        var mCont = isFrozen ? _this.parent.getContent() : _this.parent.getContent().querySelector('.e-content');
        if (_this.previousValues.left === left || left < 0 || mHdr.scrollWidth - mHdr.clientWidth < left) {
          return;
        }
        if (e.cancelable) {
          e.preventDefault();
        }
        mHdr.scrollLeft = left;
        mCont.scrollLeft = left;
        if (isFrozen) {
          var scrollBar = _this.parent.element.querySelector('.e-movablescrollbar');
          scrollBar.scrollLeft = left;
        }
        _this.pageXY.x = pageXY.x;
        _this.previousValues.left = left;
        // let cont: Element;
        // let mHdr: Element;
        // let pageXY: { x: number, y: number } = this.getPointXY(e);
        // let top: number = element.scrollTop + (this.pageXY.y - pageXY.y);
        // let left: number = element.scrollLeft + (this.pageXY.x - pageXY.x);
        // if (this.parent.getHeaderContent().contains(e.target as Element)) {
        //     mHdr = this.parent.options.frozenColumns ?
        //         this.parent.getHeaderContent().querySelector('.e-movableheader') :
        //         this.parent.getHeaderContent().querySelector('.e-headercontent') as Element;
        //     if (this.previousValues.left === left || (left < 0 || (mHdr.scrollWidth - mHdr.clientWidth) < left)) {
        //         return;
        //     }
        //     e.preventDefault();
        //     mHdr.scrollLeft = left;
        //     element.scrollLeft = left;
        //     this.pageXY.x = pageXY.x;
        //     this.previousValues.left = left;
        // } else {
        //     cont = this.parent.getContent().querySelector('.e-frozencontent');
        //     if (this.previousValues.top === top && (top < 0 || (cont.scrollHeight - cont.clientHeight) < top)
        //         || (top < 0 || (cont.scrollHeight - cont.clientHeight) < top)) {
        //         return;
        //     }
        //     e.preventDefault();
        //     cont.scrollTop = top;
        //     element.scrollTop = top;
        //     this.pageXY.y = pageXY.y;
        //     this.previousValues.top = top;
        // }
      };
    };
    Scroll.prototype.setPageXY = function () {
      var _this = this;
      return function (e) {
        if (e.pointerType === 'mouse') {
          return;
        }
        _this.pageXY = _this.getPointXY(e);
      };
    };
    Scroll.prototype.getPointXY = function (e) {
      var pageXY = {
        x: 0,
        y: 0
      };
      if (e.touches && e.touches.length) {
        pageXY.x = e.touches[0].pageX;
        pageXY.y = e.touches[0].pageY;
      } else {
        pageXY.x = e.pageX;
        pageXY.y = e.pageY;
      }
      return pageXY;
    };
    Scroll.prototype.wireEvents = function () {
      var _this = this;
      if (this.oneTimeReady) {
        this.content = this.parent.getContent();
        this.header = this.parent.getHeaderContent();
        var mScrollBar = this.content.parentElement.querySelector('.e-movablescrollbar');
        var root = this.parent && this.parent.element;
        if (root && !root.classList.contains('e-device')) {
          var inferred = 'ontouchstart' in window && window.matchMedia('(pointer: coarse)').matches && window.matchMedia('(hover: none)').matches;
          if (inferred) {
            root.classList.add('e-device');
          }
        }
        //Need for custom scrollbar
        if (this.parent.options.frozenColumns > 0 && this.parent.options.enableColumnVirtualization) {
          sf.base.EventHandler.add(mScrollBar, 'scroll', this.onCustomScrollbar(this.content, this.header), this);
          sf.base.EventHandler.add(this.content, 'scroll', this.onCustomScrollbar(mScrollBar, this.header), this);
          sf.base.EventHandler.add(this.header, 'scroll', this.onCustomScrollbar(mScrollBar, this.content), this);
          sf.base.EventHandler.add(this.header, 'touchstart pointerdown', this.setPageXY(), this);
          sf.base.EventHandler.add(this.content, 'touchstart pointerdown', this.setPageXY(), this);
          sf.base.EventHandler.add(this.content, 'touchmove pointermove', this.onTouchScroll(this.header), this);
        } else {
          sf.base.EventHandler.add(this.content, 'scroll', this.onContentScroll(this.header), this);
          sf.base.EventHandler.add(this.header, 'scroll', this.onContentScroll(this.content), this);
        }
        if (this.parent.options.aggregatesCount) {
          var footer = this.parent.element.querySelector('.e-summarycontent');
          if (!sf.base.isNullOrUndefined(footer)) {
            sf.base.EventHandler.add(footer, 'scroll', this.onContentScroll(this.content), this);
          }
        }
        if (this.parent.options.enableStickyHeader) {
          this.addStickyListener(true);
        }
        this.refresh();
        this.oneTimeReady = false;
      }
      var table = this.parent.getContent().querySelector('.e-table');
      var sLeft;
      var sHeight;
      var clientHeight;
      getUpdateUsingRaf(function () {
        sLeft = _this.header.scrollLeft;
        sHeight = table.scrollHeight;
        clientHeight = _this.parent.getContent().clientHeight;
      }, function () {
        if (!_this.parent.options.enableVirtualization) {
          if (sHeight < clientHeight) {
            sf.base.addClass(table.querySelectorAll('tr:last-child td'), 'e-lastrowcell');
          }
          _this.header.scrollLeft = _this.previousValues.left;
          _this.content.scrollLeft = _this.previousValues.left;
          _this.content.scrollTop = _this.previousValues.top;
        }
        if (!_this.parent.options.enableColumnVirtualization) {
          _this.content.scrollLeft = sLeft;
        }
        if (_this.parent.options.frozenColumns && _this.parent.getHeaderContent()) {
          _this.parent.getHeaderContent().scrollLeft = _this.parent.getContent().scrollLeft;
        }
      });
    };
    /**
     *  Retrieves CSS properties based on the right-to-left (RTL) mode.
     *
     * @param {boolean} [rtl] - Optional. Specifies whether to enable RTL mode.
     * @returns {ScrollCss} The CSS properties object based on the RTL mode.
     * @hidden
     */
    Scroll.prototype.getCssProperties = function (rtl) {
      var css = {};
      var enableRtl = sf.base.isNullOrUndefined(rtl) ? this.parent.options.enableRtl : rtl;
      css.border = enableRtl ? 'borderLeftWidth' : 'borderRightWidth';
      css.padding = enableRtl ? 'paddingLeft' : 'paddingRight';
      return css;
    };
    /**
     * Returns the scrollable parent element of the grid parent node.
     *
     * @param {HTMLElement} gridParentNode - The grid parent node to find the scrollable parent from.
     * @returns {HTMLElement|null} The scrollable parent element, or null if not found.
     * @hidden
     */
    Scroll.prototype.getScrollableParent = function (gridParentNode) {
      if (gridParentNode === null) {
        return null;
      }
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      var gridParent = sf.base.isNullOrUndefined(gridParentNode.tagName) ?
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      gridParentNode.scrollingElement : gridParentNode;
      var overflowY = document.defaultView.getComputedStyle(gridParent, null).overflowY;
      if (gridParent.scrollHeight > gridParent.clientHeight && overflowY !== 'hidden' && overflowY !== 'visible' || gridParentNode.tagName === 'HTML' || gridParentNode.tagName === 'ARTICLE') {
        return gridParentNode;
      } else {
        return this.getScrollableParent(gridParentNode.parentNode);
      }
    };
    Scroll.prototype.addStickyListener = function (isAdd) {
      this.gridParentElement = this.getScrollableParent(this.parent.element.parentElement);
      if (isAdd) {
        if (this.gridParentElement) {
          sf.base.EventHandler.add(this.gridParentElement.tagName === 'HTML' || this.gridParentElement.tagName === 'ARTICLE' ? document : this.gridParentElement, 'scroll', this.makeStickyHeader, this);
        } else {
          sf.base.EventHandler.remove(this.gridParentElement, 'scroll', this.makeStickyHeader);
        }
      }
    };
    Scroll.prototype.makeStickyHeader = function () {
      if (this.parent.options.enableStickyHeader && this.parent.element && !sf.base.isNullOrUndefined(this.gridParentElement) && this.parent.getContent()) {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        var contentRect = this.parent.getContent().parentElement.getBoundingClientRect();
        if (contentRect) {
          var headerElement = this.parent.getHeaderContent().parentElement;
          var toolbarElement = this.parent.element.querySelector('.e-toolbar');
          var groupElement = this.parent.element.querySelector('.e-groupdroparea');
          var ccElement = this.parent.element.querySelector('.e-ccdlg');
          var ccToolbar = this.parent.element.querySelector('.e-cc-toolbar');
          var height = headerElement.offsetHeight + (toolbarElement ? toolbarElement.offsetHeight : 0) + (groupElement ? groupElement.offsetHeight : 0);
          var parentTop = this.gridParentElement.getBoundingClientRect().top;
          var top1 = contentRect.top - (parentTop < 0 ? 0 : parentTop);
          var left = contentRect.left;
          if (top1 < height && contentRect.bottom > 0) {
            headerElement.classList.add('e-sticky');
            var elemTop = 0;
            if (groupElement) {
              this.setSticky(groupElement, elemTop, contentRect.width, left, true);
              elemTop += groupElement.getBoundingClientRect().height - 2;
            }
            if (toolbarElement) {
              this.setSticky(toolbarElement, elemTop, contentRect.width, left, true);
              elemTop += toolbarElement.getBoundingClientRect().height - 2;
            }
            this.setSticky(headerElement, elemTop, contentRect.width, left, true);
          } else {
            if (headerElement.classList.contains('e-sticky')) {
              this.setSticky(headerElement, null, null, null, false);
            }
            if (groupElement) {
              this.setSticky(groupElement, null, null, null, false);
            }
            if (toolbarElement) {
              this.setSticky(toolbarElement, null, null, null, false);
            }
            if (ccElement && !sf.base.isNullOrUndefined(ccToolbar)) {
              // eslint-disable-next-line @typescript-eslint/no-explicit-any
              var position = sf.popups.calculateRelativeBasedPosition(ccToolbar, ccElement);
              var ccTop = position.top + ccToolbar.getBoundingClientRect().height;
              var elementVisible = ccElement.style.display;
              ccElement.style.display = 'block';
              // eslint-disable-next-line @typescript-eslint/no-explicit-any
              var ccRect = ccElement.getBoundingClientRect();
              var left_1 = ccToolbar.getBoundingClientRect().left - contentRect.left - ccRect.width + ccToolbar.clientWidth + 2;
              ccElement.style.display = elementVisible;
              this.setSticky(ccElement, ccTop, ccRect.width, left_1, false);
            }
          }
        }
      }
    };
    Scroll.prototype.setSticky = function (element, top, width, left, isAdd) {
      if (isAdd) {
        element.classList.add('e-sticky');
      } else {
        element.classList.remove('e-sticky');
      }
      element.style.width = width != null ? width + 'px' : '';
      element.style.top = top != null ? top - 2 + 'px' : '';
      element.style.left = left !== null ? parseInt(element.style.left, 10) !== left ? left + 'px' : element.style.left : '';
    };
    Scroll.prototype.destroy = function () {
      var gridElement = this.parent.element;
      if (!gridElement || !gridElement.querySelector('.e-gridheader') && !gridElement.querySelector('.e-gridcontent')) {
        return;
      }
      if (this.parent.options.enableStickyHeader) {
        this.addStickyListener(false);
      }
      //Remove padding
      this.removePadding();
      //Remove Dom event
      sf.base.EventHandler.remove(this.parent.getContent(), 'scroll', this.onContentScroll);
      sf.base.EventHandler.remove(this.header, 'scroll', this.onContentScroll);
      if (this.parent.options.aggregatesCount) {
        var footer = this.parent.element.querySelector('.e-summarycontent');
        if (!sf.base.isNullOrUndefined(footer)) {
          sf.base.EventHandler.remove(footer, 'scroll', this.onContentScroll);
        }
      }
    };
    /**
     * Function to get the scrollbar width of the browser.
     *
     * @returns {number} The width of the scrollbar in pixels.
     * @hidden
     */
    Scroll.getScrollBarWidth = function () {
      return getScrollBarWidth();
    };
    return Scroll;
  }();

  /**
   * Frozen rows and column handling
   */
  var Freeze = /** @class */function () {
    function Freeze(parent) {
      this.parent = parent;
    }
    Freeze.prototype.refreshFreeze = function (obj) {
      if (obj["case"] === 'textwrap' || obj["case"] === 'refreshHeight') {
        var fRows = void 0;
        var mRows = void 0;
        var frRows = void 0;
        var fHdr = this.getFrozenHeader();
        var mHdr = this.getMovableHeader();
        var cont = this.parent.getContent();
        var wrapMode = this.parent.options.wrapMode;
        if (sf.base.isNullOrUndefined(fHdr) && sf.base.isNullOrUndefined(mHdr)) {
          return;
        }
        if (obj["case"] === 'textwrap') {
          if (wrapMode === 'Both' || obj.isModeChg) {
            fRows = fHdr.querySelectorAll('tr');
            mRows = mHdr.querySelectorAll('tr');
            if (this.parent.options.frozenName === 'LeftRight') {
              frRows = this.parent.element.querySelectorAll('.e-frozenheader')[1].querySelectorAll('tr');
            }
          } else {
            fRows = fHdr.querySelector(wrapMode === 'Content' ? 'tbody' : 'thead').querySelectorAll('tr');
            mRows = mHdr.querySelector(wrapMode === 'Content' ? 'tbody' : 'thead').querySelectorAll('tr');
          }
          if (!this.parent.getHeaderContent().querySelectorAll('.e-stackedheadercell').length) {
            this.setWrapHeight(fRows, mRows, obj.isModeChg, false, false, frRows);
          }
          this.refreshStackedHdrHgt();
        } else if (obj["case"] === 'refreshHeight') {
          if (this.parent.options.frozenName === 'LeftRight') {
            frRows = cont.querySelector('.e-frozen-right-content').querySelectorAll('tr');
          }
          this.setWrapHeight(cont.querySelector('.e-frozencontent').querySelectorAll('tr'), cont.querySelector('.e-movablecontent').querySelectorAll('tr'), obj.isModeChg, false, false, frRows);
          if (!this.parent.getHeaderContent().querySelectorAll('.e-stackedheadercell').length) {
            if (this.parent.options.frozenName === 'LeftRight') {
              frRows = this.parent.element.querySelectorAll('.e-frozenheader')[1].querySelectorAll('tr');
            }
            this.setWrapHeight(fHdr.querySelectorAll('tr'), mHdr.querySelectorAll('tr'), obj.isModeChg, false, false, frRows);
          }
        }
      }
    };
    Freeze.prototype.updateResizeHandler = function () {
      var elements = [].slice.call(this.parent.getHeaderContent().querySelectorAll('.e-rhandler'));
      for (var i = 0; i < elements.length; i++) {
        var headerCellContainer = elements[parseInt(i.toString(), 10)].parentElement;
        if (!sf.base.isNullOrUndefined(headerCellContainer) && !sf.base.isNullOrUndefined(headerCellContainer.parentElement)) {
          elements[parseInt(i.toString(), 10)].style.height = headerCellContainer.parentElement.offsetHeight + 'px';
        }
      }
    };
    Freeze.prototype.setWrapHeight = function (fRows, mRows, isModeChg, isContReset, isStackedHdr, frRows) {
      var fRowHgt;
      var mRowHgt;
      var frRowHgt;
      var maxHeight;
      var isWrap = this.parent.options.allowTextWrap;
      var wrapMode = this.parent.options.wrapMode;
      var tHead = this.parent.getHeaderContent().querySelector('thead');
      var tBody = this.parent.getHeaderContent().querySelector('tbody');
      var height = [];
      var width = [];
      var rightHeight = [];
      for (var i = 0, len = fRows.length; i < len; i++) {
        //separate loop for performance issue
        if (!sf.base.isNullOrUndefined(fRows[parseInt(i.toString(), 10)]) && !sf.base.isNullOrUndefined(mRows[parseInt(i.toString(), 10)])) {
          if (frRows) {
            rightHeight[parseInt(i.toString(), 10)] = frRows[parseInt(i.toString(), 10)].getBoundingClientRect().height;
          }
          height[parseInt(i.toString(), 10)] = fRows[parseInt(i.toString(), 10)].getBoundingClientRect().height; //https://pagebuildersandwich.com/increased-plugins-performance-200/
          width[parseInt(i.toString(), 10)] = mRows[parseInt(i.toString(), 10)].getBoundingClientRect().height;
        }
      }
      for (var i = 0, len = fRows.length; i < len; i++) {
        if (isModeChg && (wrapMode === 'Header' && isContReset || wrapMode === 'Content' && tHead.contains(fRows[parseInt(i.toString(), 10)]) || wrapMode === 'Header' && tBody.contains(fRows[parseInt(i.toString(), 10)])) || isStackedHdr) {
          if (frRows[parseInt(i.toString(), 10)]) {
            frRows[parseInt(i.toString(), 10)].style.height = null;
          }
          fRows[parseInt(i.toString(), 10)].style.height = null;
          mRows[parseInt(i.toString(), 10)].style.height = null;
        }
        fRowHgt = height[parseInt(i.toString(), 10)];
        mRowHgt = width[parseInt(i.toString(), 10)];
        frRowHgt = rightHeight[parseInt(i.toString(), 10)] ? rightHeight[parseInt(i.toString(), 10)] : 0;
        if (this.parent.options.rowHeight !== 0) {
          maxHeight = this.parent.options.rowHeight;
        } else {
          maxHeight = Math.max(fRowHgt, mRowHgt, frRowHgt);
          mRows[parseInt(i.toString(), 10)].style.height = maxHeight + 'px';
          fRows[parseInt(i.toString(), 10)].style.height = maxHeight + 'px';
        }
        if (frRows) {
          frRows[parseInt(i.toString(), 10)].style.height = maxHeight + 'px';
        }
        //TODO: check below commented code is not working hence used above
        // if (!isNullOrUndefined(fRows[i]) && fRows[i].childElementCount && ((isWrap && fRowHgt < mRowHgt) ||
        //     (!isWrap && fRowHgt < mRowHgt))) {p
        //     fRows[i].style.height = mRowHgt + 'px';
        // }
        // if (mRows && !isNullOrUndefined(mRows[i]) && mRows[i].childElementCount && ((isWrap && fRowHgt > mRowHgt) ||
        //     (!isWrap && fRowHgt > mRowHgt))) {
        //     mRows[i].style.height = fRowHgt + 'px';
        // }
      }
      if (isWrap) {
        this.setFrozenHeight();
      }
    };
    Freeze.prototype.setFrozenHeight = function (minWidth) {
      if (this.parent.options.isPreventScrollEvent) {
        return;
      }
      var movableContent = this.parent.element.querySelector('.e-content');
      var frozenscrollbarX = this.parent.element.querySelectorAll('.e-frozenscrollbar');
      var movablescrollbarX = this.parent.element.querySelector('.e-movablescrollbar');
      var movableChildScrollBarX = this.parent.element.querySelector('.e-movablechild');
      var content = this.parent.element.querySelector('.e-content');
      if (this.parent.options.frozenColumns > 0 && this.parent.options.height === '100%' && this.parent.options.enableColumnVirtualization) {
        var scrollBar = this.parent.element.querySelector('.e-movablescrollbar');
        var scrollBarHeight = scrollBar.offsetHeight;
        content.style.height = 'calc(100% - ' + scrollBarHeight + 'px)';
      }
      if (!sf.base.isNullOrUndefined(movableContent)) {
        var columns = Array.from(movableContent.getElementsByClassName('e-table')[0].querySelectorAll('col'));
        var autoWidthColumns = columns.filter(function (col) {
          return col.getAttribute('style') === 'width: auto';
        });
        minWidth = autoWidthColumns.length > 0 && minWidth ? minWidth : 0;
      }
      if (movablescrollbarX && this.parent.options.height !== '100%' && this.parent.options.height !== 'auto') {
        var parentHeight = parseInt(this.parent.options.height.split ? this.parent.options.height.split('px')[0] : this.parent.options.height, 10);
        var contentHeight = parentHeight - movablescrollbarX.offsetHeight;
        content.style.height = contentHeight + 'px';
      }
      if ((this.parent.options.enableVirtualization && this.parent.options.enableColumnVirtualization || this.parent.options.enableColumnVirtualization) && frozenscrollbarX) {
        var columns = this.parent.options.columns;
        var totalColumnWidth = 0;
        for (var i = 0; i < columns.length; i++) {
          if (columns[parseInt(i.toString(), 10)].visible && !sf.base.isNullOrUndefined(columns[parseInt(i.toString(), 10)].width)) {
            totalColumnWidth += parseFloat(columns[parseInt(i.toString(), 10)].width.split ? columns[parseInt(i.toString(), 10)].width.split('px')[0] : columns[parseInt(i.toString(), 10)].width.toString());
          }
        }
        movableChildScrollBarX.style.width = totalColumnWidth + getScrollBarWidth() + 'px';
      } else {
        if (!sf.base.isNullOrUndefined(movablescrollbarX) && !sf.base.isNullOrUndefined(movableChildScrollBarX)) {
          movablescrollbarX.style.width = movableContent.offsetWidth + 'px';
          movableChildScrollBarX.style.width = movableContent.getElementsByClassName('e-table')[0].offsetWidth + minWidth + getScrollBarWidth() + 'px';
        }
      }
      if (!sf.base.isNullOrUndefined(movableContent)) {
        this.parent.scrollModule.setPadding();
      }
      //if (movableContent.scrollWidth - movableContent.clientWidth) {
      //TODO: why we need commented code?
      // frozenContent.style.height = movableContentHeight - height + 'px';
      // frozenContent.style.borderBottom = '';
      // } else {
      //     frozenContent.style.height = movableContentHeight + 'px';
      //     if ((frozenContent.scrollHeight <= frozenContent.clientHeight) ||
      //         (movableContent.scrollHeight <= movableContent.clientHeight)) {
      //         this.parent.scrollModule.removePadding();
      //     }
      //     frozenContent.style.borderBottom = '0px';
      // }
    };
    Freeze.prototype.updateStackedFrozenHeight = function (fTr, mTr) {
      var fRowSpan;
      var mRowSpan;
      for (var i = 0, len = fTr.length; i < len; i++) {
        fRowSpan = this.getRowSpan(fTr[parseInt(i.toString(), 10)]);
        mRowSpan = this.getRowSpan(mTr[parseInt(i.toString(), 10)]);
        if (fRowSpan.min > 1) {
          this.updateStackedHdrRowHgt(i, fRowSpan.max, fTr[parseInt(i.toString(), 10)], mTr);
        } else if (mRowSpan.min > 1) {
          this.updateStackedHdrRowHgt(i, mRowSpan.max, mTr[parseInt(i.toString(), 10)], fTr);
        }
      }
    };
    Freeze.prototype.refreshStackedHdrHgt = function () {
      var fRTr;
      var fRTrL;
      var maxLenCol;
      var fTr = this.getFrozenHeader().querySelectorAll('.e-columnheader');
      var mTr = this.getMovableHeader().querySelectorAll('.e-columnheader');
      if (this.parent.options.frozenName === 'LeftRight') {
        fRTrL = this.parent.element.querySelectorAll('.e-frozenheader')[0].querySelectorAll('.e-columnheader');
        fRTr = this.parent.element.querySelectorAll('.e-frozenheader')[1].querySelectorAll('.e-columnheader');
        maxLenCol = mTr.length > fRTrL.length ? mTr.length > fRTr.length ? mTr : fRTr : fRTrL.length > fRTr.length ? fRTrL : fRTr;
        if (maxLenCol === fRTr) {
          this.updateStackedFrozenHeight(fRTrL, maxLenCol);
          this.updateStackedFrozenHeight(fRTr, mTr);
        } else if (maxLenCol === fRTrL) {
          this.updateStackedFrozenHeight(fRTrL, mTr);
          this.updateStackedFrozenHeight(fRTr, maxLenCol);
        } else {
          this.updateStackedFrozenHeight(fRTrL, maxLenCol);
          this.updateStackedFrozenHeight(fRTr, mTr);
        }
      } else {
        this.updateStackedFrozenHeight(fTr, mTr);
      }
      if (this.parent.options.allowResizing) {
        this.updateResizeHandler();
      }
    };
    Freeze.prototype.getRowSpan = function (row) {
      var rSpan;
      var minRowSpan;
      var maxRowSpan;
      for (var i = 0, len = row.childElementCount; i < len; i++) {
        if (i === 0) {
          minRowSpan = row.children[0].rowSpan;
        }
        rSpan = row.children[parseInt(i.toString(), 10)].rowSpan;
        minRowSpan = Math.min(rSpan, minRowSpan);
        maxRowSpan = Math.max(rSpan, minRowSpan);
      }
      return {
        min: minRowSpan,
        max: maxRowSpan
      };
    };
    Freeze.prototype.updateStackedHdrRowHgt = function (idx, maxRowSpan, row, rows) {
      var height = 0;
      for (var i = 0; i < maxRowSpan; i++) {
        height += rows[idx + i].style.height ? parseInt(rows[idx + i].style.height, 10) : rows[idx + i].offsetHeight;
      }
      row.style.height = height + 'px';
    };
    Freeze.prototype.getFrozenHeader = function () {
      return this.frozenHeader;
    };
    Freeze.prototype.getMovableHeader = function () {
      return this.movableHeader;
    };
    Freeze.prototype.refreshRowHeight = function () {
      if (this.parent.options.rowHeight !== 0) {
        return;
      }
    };
    Freeze.prototype.clearWrapHeight = function () {
      var fn = function fn(fRows, mRows) {
        for (var i = 0, len = fRows.length; i < len; i++) {
          if (!sf.base.isNullOrUndefined(fRows[parseInt(i.toString(), 10)]) && !sf.base.isNullOrUndefined(mRows[parseInt(i.toString(), 10)])) {
            fRows[parseInt(i.toString(), 10)].style.height = null;
            mRows[parseInt(i.toString(), 10)].style.height = null;
          }
        }
      };
      var fRows;
      var mRows;
      if (this.parent.options.frozenColumns) {
        if (this.parent.options.frozenRows || this.parent.options.wrapMode === 'Both' || this.parent.options.wrapMode === 'Header') {
          fRows = this.parent.element.querySelector('.e-frozenheader').querySelectorAll('tr');
          mRows = this.parent.element.querySelector('.e-movableheader').querySelectorAll('tr');
          fn(fRows, mRows);
        }
        fRows = this.parent.element.querySelector('.e-frozencontent').querySelectorAll('tr');
        mRows = this.parent.element.querySelector('.e-movablecontent').querySelectorAll('tr');
        fn(fRows, mRows);
      }
      if (this.parent.options.frozenRows && this.parent.options.frozenColumns === 0) {
        fRows = this.parent.element.querySelector('.e-headercontent').querySelectorAll('tr');
        mRows = this.parent.element.querySelector('.e-content').querySelectorAll('tr');
        fn(fRows, mRows);
      }
    };
    Freeze.prototype.updateFrozenColumnStyles = function () {
      this.updateLeftFreezePosition(this.parent.getHeaderContent().querySelectorAll('tr'), 'th');
      this.updateLeftFreezePosition(Array.from(this.parent.getDataRows()), 'td');
    };
    Freeze.prototype.updateLeftFreezePosition = function (rows, cellSelector) {
      for (var i = 0; i < rows.length; i++) {
        var cells = rows[parseInt(i.toString(), 10)].querySelectorAll(cellSelector);
        for (var j = 0; j < cells.length; j++) {
          if (cells[parseInt(j.toString(), 10)].classList.contains('e-leftfreeze')) {
            cells[parseInt(j.toString(), 10)].style.left = j === 0 ? '0px' : this.calculatedLeftValue(j - 1) + 'px';
          }
        }
      }
    };
    Freeze.prototype.calculatedLeftValue = function (index) {
      var columns = this.parent.columnModel;
      if (columns === null || columns.length === 0 || index < 0) {
        return 0;
      }
      var width = 0;
      var maxIndex = Math.min(index, columns.length - 1);
      for (var i = 0; i <= maxIndex; i++) {
        if (columns[parseInt(i.toString(), 10)] != null) {
          var columnWidth = columns[parseInt(i.toString(), 10)].width;
          width += parseInt(columnWidth.toString(), 10);
        }
      }
      return width;
    };
    return Freeze;
  }();

  /**
   * ColumnWidthService
   *
   * @hidden
   */
  var ColumnWidthService = /** @class */function () {
    function ColumnWidthService(parent) {
      this.parent = parent;
    }
    ColumnWidthService.prototype.setMinwidthBycalculation = function (tWidth) {
      var difference = 0;
      var collection = this.parent.getColumns().filter(function (a) {
        return sf.base.isNullOrUndefined(a.width) || a.width === 'auto' || a.width === '';
      });
      if (collection.length) {
        if (!sf.base.isNullOrUndefined(this.parent.options.width) && this.parent.options.width !== 'auto' && this.parent.options.width.toString().indexOf('%') === -1) {
          difference = (typeof this.parent.options.width === 'string' ? parseInt(this.parent.options.width, 10) : this.parent.options.width) - tWidth;
        } else {
          difference = this.parent.element.getBoundingClientRect().width - tWidth;
        }
        var tmWidth = 0;
        var minWidth = 0;
        for (var _i = 0, collection_1 = collection; _i < collection_1.length; _i++) {
          var cols = collection_1[_i];
          tmWidth += !sf.base.isNullOrUndefined(cols.minWidth) ? typeof cols.minWidth === 'string' ? parseInt(cols.minWidth, 10) : cols.minWidth : 0;
        }
        var minWidthValues = {};
        for (var i = 0; i < collection.length; i++) {
          if (tWidth === 0 && this.parent.options.allowResizing && this.isWidthUndefined() && i !== collection.length - 1) {
            this.setUndefinedColumnWidth(collection);
          }
          if (tWidth !== 0 && difference < tmWidth) {
            minWidthValues[collection[parseInt(i.toString(), 10)].field] = collection[parseInt(i.toString(), 10)].minWidth + 'px';
            minWidth += parseInt(collection[parseInt(i.toString(), 10)].minWidth.toString(), 10);
          } else if (tWidth !== 0 && difference > tmWidth) {
            minWidthValues[collection[parseInt(i.toString(), 10)].field] = '';
            minWidth += 0;
          }
        }
        this.parent.dotNetRef.invokeMethodAsync('SetMinWidth', minWidthValues);
        if (this.parent.options.frozenColumns) {
          this.parent.freezeModule.setFrozenHeight(minWidth);
        }
      }
    };
    ColumnWidthService.prototype.setUndefinedColumnWidth = function (collection) {
      for (var k = 0; k < collection.length; k++) {
        if (k !== collection.length - 1) {
          collection[parseInt(k.toString(), 10)].width = 200;
          this.setWidth(200, this.parent.getColumnIndexByField(collection[parseInt(k.toString(), 10)].field));
        }
      }
    };
    ColumnWidthService.prototype.setColumnWidth = function (column, index, module, allowStopEvent, virtualAutoFit) {
      if (allowStopEvent === void 0) {
        allowStopEvent = true;
      }
      if (virtualAutoFit === void 0) {
        virtualAutoFit = false;
      }
      if (this.parent.getColumns(virtualAutoFit).length < 1) {
        return;
      }
      var columnIndex;
      if (this.parent.options.enableColumnVirtualization) {
        columnIndex = this.parent.options.virtualizedColumns.findIndex(function (col) {
          return col.uid === column.uid;
        }) + this.parent.getIndentCount();
      } else {
        columnIndex = sf.base.isNullOrUndefined(index) ? this.parent.getNormalizedColumnIndex(column.uid) : index;
      }
      var cWidth = this.getWidth(column);
      var tgridWidth = this.getTableWidth(this.parent.getColumns(virtualAutoFit));
      if (cWidth !== null) {
        this.setWidth(cWidth, columnIndex);
        if (this.parent.options.width !== 'auto' && this.parent.options.width.toString().indexOf('%') === -1) {
          this.setMinwidthBycalculation(tgridWidth);
        }
        if (this.parent.options.enableColumnVirtualization && module === 'resize') {
          this.parent.options.virtualizedColumns.filter(function (col) {
            return col.uid === column.uid;
          })[0].width = cWidth;
          this.parent.virtualContentModule.refreshOffsets();
          this.parent.virtualContentModule.setVirtualHeight();
        } else if (this.parent.options.enableColumnVirtualization && virtualAutoFit) {
          this.parent.options.columns.filter(function (col) {
            return col.uid === column.uid;
          })[0].width = cWidth;
          this.parent.virtualContentModule.refreshOffsets();
        }
        if (this.parent.options.allowResizing && module === 'resize' || this.parent.options.frozenColumns && this.parent.options.allowResizing) {
          this.setWidthToTable(null, false, 'resize');
        }
        if (allowStopEvent) {
          if (cWidth.toString().indexOf('px') > 0) {
            cWidth = cWidth.toString().replace('px', '');
          }
          var isResizing = module === 'resize' ? true : false;
          this.parent.dotNetRef.invokeMethodAsync('ColumnWidthChanged', {
            index: columnIndex,
            width: cWidth,
            columnUid: column.uid
          }, isResizing);
        }
      }
    };
    ColumnWidthService.prototype.setWidth = function (width, index, clear) {
      var chrome = 'chrome';
      var webstore = 'webstore';
      if (typeof width === 'string' && width.indexOf('%') !== -1 && !(Boolean(window["".concat(chrome)]) && Boolean(window["".concat(chrome)]["".concat(webstore)])) && this.parent.options.allowGrouping) {
        var elementWidth = this.parent.element.offsetWidth;
        width = parseInt(width, 10) / 100 * elementWidth;
      }
      var header = this.parent.getHeaderTable();
      var content = this.parent.getContentTable();
      var fWidth = sf.base.formatUnit(width);
      var headerCol = header.querySelector('colgroup').children[parseInt(index.toString(), 10)];
      if (headerCol && !clear) {
        headerCol.style.width = fWidth;
      } else if (headerCol && clear) {
        headerCol.style.width = ' ';
      }
      var contentCol = content.querySelector('colgroup').children[parseInt(index.toString(), 10)];
      if (contentCol && !clear) {
        contentCol.style.width = fWidth;
      } else if (contentCol && clear) {
        contentCol.style.width = ' ';
      }
      if (this.parent.options.aggregatesCount !== 0) {
        var tcolGroup = this.parent.getFooterContent().querySelector('colgroup');
        var footerCol = !sf.base.isNullOrUndefined(tcolGroup) ? tcolGroup.children[parseInt(index.toString(), 10)] : null;
        if (contentCol && footerCol && !clear) {
          footerCol.style.width = fWidth;
        } else if (contentCol && footerCol && clear) {
          footerCol.style.width = ' ';
        }
      }
      var edit = this.parent.element.querySelectorAll('.e-table.e-inline-edit');
      var editTableCol = [];
      for (var i = 0; i < edit.length; i++) {
        if (parentsUntil(edit[parseInt(i.toString(), 10)], 'e-grid').id === this.parent.element.id) {
          for (var j = 0; j < edit[parseInt(i.toString(), 10)].querySelector('colgroup').children.length; j++) {
            editTableCol.push(edit[parseInt(i.toString(), 10)].querySelector('colgroup').children[parseInt(j.toString(), 10)]);
          }
        }
      }
      if (edit.length && editTableCol.length) {
        editTableCol[parseInt(index.toString(), 10)].style.width = fWidth;
      }
      if (this.parent.options.frozenColumns !== 0 && !this.parent.options.enableColumnVirtualization) {
        this.parent.freezeModule.setFrozenHeight();
      }
    };
    ColumnWidthService.prototype.getColumnLevelFrozenColgroup = function (index, left, movable, ele) {
      if (!ele || !ele.querySelector('colgroup')) {
        return null;
      }
      var columns = this.parent.options.enableColumnVirtualization ? this.parent.options.virtualizedColumns : this.parent.frozenColumnModel;
      var headerCol;
      var colGroup = [].slice.call(ele.querySelector('colgroup').children);
      if (columns[parseInt(index.toString(), 10)].freeze === 'Left' && columns[parseInt(index.toString(), 10)].isFrozen) {
        headerCol = colGroup[parseInt(index.toString(), 10)];
      } else if (columns[parseInt(index.toString(), 10)].freeze === 'Right' && columns[parseInt(index.toString(), 10)].isFrozen) {
        headerCol = colGroup[index - (left + movable)];
      } else {
        headerCol = colGroup[index - left];
      }
      return headerCol;
    };
    ColumnWidthService.prototype.isWidthUndefined = function () {
      var isWidUndefCount = this.parent.getColumns().filter(function (col) {
        return sf.base.isNullOrUndefined(col.width) && sf.base.isNullOrUndefined(col.minWidth);
      }).length;
      return this.parent.getColumns().length === isWidUndefCount;
    };
    ColumnWidthService.prototype.getWidth = function (column) {
      //TODO: move it to c# side
      // if (isNullOrUndefined(column.width) && this.parent.options.allowResizing
      //     && isNullOrUndefined(column.minWidth) && !this.isWidthUndefined()) {
      //     column.width = 200;
      // }
      // if (this.parent.options.frozenColumns && isNullOrUndefined(column.width) &&
      //     column.index < this.parent.options.frozenColumns) {
      //     column.width = 200;
      // }
      if (!column.width) {
        return null;
      }
      var width = parseInt(column.width.toString(), 10);
      if (column.minWidth && width < parseInt(column.minWidth.toString(), 10)) {
        return column.minWidth;
      } else if (column.maxWidth && width > parseInt(column.maxWidth.toString(), 10)) {
        return column.maxWidth;
      } else {
        return column.width;
      }
    };
    ColumnWidthService.prototype.getTableWidth = function (columns) {
      var tWidth = 0;
      for (var _i = 0, columns_1 = columns; _i < columns_1.length; _i++) {
        var column = columns_1[_i];
        var cWidth = this.getWidth(column);
        if (column.width === 'auto') {
          cWidth = 0;
        }
        if (column.visible !== false && cWidth !== null) {
          tWidth += parseInt(cWidth.toString(), 10);
        }
      }
      return tWidth;
    };
    ColumnWidthService.prototype.calcMovableOrFreezeColWidth = function (tableType) {
      var columns = this.parent.frozenColumnModel.length !== 0 ? this.parent.frozenColumnModel.slice() : this.parent.getColumns().slice();
      var frozenColumnsCount = 0;
      if (!this.parent.options.frozenLeftColumnsCount && !this.parent.options.frozenRightColumnsCount) {
        for (var i = 0; i < columns.length; i++) {
          if (columns[parseInt(i.toString(), 10)].index < this.parent.options.actualFrozenColumns && !columns[parseInt(i.toString(), 10)].isFrozen) {
            frozenColumnsCount++;
          }
        }
        this.parent.options.actualFrozenColumns = frozenColumnsCount;
      }
      var left = this.parent.options.frozenLeftColumnsCount || this.parent.options.actualFrozenColumns;
      var movable = columns.length - this.parent.options.frozenColumns;
      if (tableType === 'movable') {
        if (this.parent.options.frozenRightColumnsCount) {
          columns.splice(left + movable, columns.length);
        }
        if (left) {
          columns.splice(0, left);
        }
      } else if (tableType === 'freeze-left') {
        columns.splice(left, columns.length);
      } else if (tableType === 'freeze-right') {
        columns.splice(0, left + movable);
      }
      return sf.base.formatUnit(this.getTableWidth(columns));
    };
    ColumnWidthService.prototype.setWidthToFrozenLeftTable = function (width) {
      var freezeWidth = sf.base.isNullOrUndefined(width) ? this.calcMovableOrFreezeColWidth('freeze-left') : width;
      freezeWidth = this.parent.getContent().querySelector('.e-frozen-left-content').classList.contains('e-frozenborderdisabled') ? '0' : freezeWidth;
      this.parent.getHeaderTable().style.width = freezeWidth;
      this.parent.getContentTable().style.width = freezeWidth;
      this.parent.resizeModule.leftFrozenTableWidth = freezeWidth;
      if (this.parent.getFooterContent() && !sf.base.isNullOrUndefined(this.parent.getFooterContent().querySelector('.e-frozen-left-footercontent'))) {
        this.parent.getFooterContent().querySelector('.e-frozen-left-footercontent').style.width = freezeWidth;
      }
    };
    ColumnWidthService.prototype.setWidthToFrozenRightTable = function (width) {
      var freezeWidth = sf.base.isNullOrUndefined(width) ? this.calcMovableOrFreezeColWidth('freeze-right') : width;
      freezeWidth = this.parent.getContent().querySelector('.e-frozen-right-content').classList.contains('e-frozenborderdisabled') ? '0' : freezeWidth;
      if (!this.parent.options.enableColumnVirtualization) {
        this.parent.getHeaderContent().querySelector('.e-frozen-right-header').querySelector('.e-table').style.width = freezeWidth;
        this.parent.getContent().querySelector('.e-frozen-right-content').querySelector('.e-table').style.width = freezeWidth;
        this.parent.resizeModule.rightFrozenTableWidth = freezeWidth;
        if (this.parent.getFooterContent() && !sf.base.isNullOrUndefined(this.parent.getFooterContent().querySelector('.e-frozen-right-footercontent'))) {
          this.parent.getFooterContent().querySelector('.e-frozen-right-footercontent').style.width = freezeWidth;
        }
      }
    };
    ColumnWidthService.prototype.setWidthToMovableTable = function (width) {
      var movableWidth = '';
      if (sf.base.isNullOrUndefined(width)) {
        var isColUndefined = this.parent.getColumns().filter(function (a) {
          return sf.base.isNullOrUndefined(a.width);
        }).length >= 1;
        var isWidthAuto = this.parent.getColumns().filter(function (a) {
          return a.width === 'auto';
        }).length >= 1;
        if (typeof this.parent.options.width === 'number' && !isColUndefined && !isWidthAuto) {
          movableWidth = sf.base.formatUnit(this.parent.options.width - parseInt(this.calcMovableOrFreezeColWidth('freeze').split('px')[0], 10) - 5);
        } else if (!isColUndefined && !isWidthAuto) {
          movableWidth = this.calcMovableOrFreezeColWidth('movable');
        }
      } else {
        movableWidth = width;
      }
      if (this.parent.getHeaderContent().querySelector('.e-movableheader').firstElementChild && !this.parent.options.enableColumnVirtualization) {
        this.parent.getHeaderContent().querySelector('.e-movableheader').firstElementChild.style.width = movableWidth;
      }
      if (this.parent.getFooterContent() && this.parent.getFooterContent().querySelector('.e-movablefootercontent').firstElementChild && !this.parent.options.enableColumnVirtualization) {
        this.parent.getFooterContent().querySelector('.e-movablefootercontent').firstElementChild.style.width = movableWidth;
      }
      if (!this.parent.options.enableColumnVirtualization) {
        this.parent.getContent().querySelector('.e-movablecontent').firstElementChild.style.width = movableWidth;
        this.parent.resizeModule.tableWidth = movableWidth;
      }
    };
    ColumnWidthService.prototype.setWidthToFrozenEditTable = function () {
      var freezeWidth = this.calcMovableOrFreezeColWidth('freeze');
      this.parent.element.querySelectorAll('.e-table.e-inline-edit')[0].style.width = freezeWidth;
    };
    ColumnWidthService.prototype.setWidthToMovableEditTable = function () {
      var movableWidth = this.calcMovableOrFreezeColWidth('movable');
      if (!sf.base.isNullOrUndefined(this.parent.element.querySelectorAll('.e-table.e-inline-edit')[1])) {
        this.parent.element.querySelectorAll('.e-table.e-inline-edit')[1].style.width = movableWidth;
      }
    };
    ColumnWidthService.prototype.setPersistedWidth = function (column) {
      if (this.parent.options.frozenColumns) {
        if (this.parent.options.frozenRightColumnsCount !== 0 || this.parent.options.frozenLeftColumnsCount !== 0) {
          if (this.parent.options.frozenLeftColumnsCount !== 0) {
            this.setWidthToFrozenLeftTable(column.leftFrozenTableWidth);
          }
          if (this.parent.options.frozenRightColumnsCount !== 0) {
            this.setWidthToFrozenRightTable(column.rightFrozenTableWidth);
          }
          this.setWidthToMovableTable(column.tableWidth);
        } else {
          this.setWidthToFrozenLeftTable(column.leftFrozenTableWidth);
          this.setWidthToMovableTable(column.tableWidth);
        }
      } else {
        this.parent.getHeaderTable().style.width = column.tableWidth;
        this.parent.getContentTable().style.width = column.tableWidth;
        if (this.parent.options.aggregatesCount !== 0) {
          this.parent.getFooterContent().querySelector('.e-table').style.width = column.tableWidth;
        }
      }
    };
    ColumnWidthService.prototype.setWidthToTable = function (columns, tableWidth, module) {
      if (columns === void 0) {
        columns = null;
      }
      if (tableWidth === void 0) {
        tableWidth = false;
      }
      if (module === void 0) {
        module = '';
      }
      var tWidth;
      if (this.parent.options.enableColumnVirtualization && module === 'resize') {
        tWidth = sf.base.formatUnit(this.getTableWidth(this.parent.options.virtualizedColumns));
      } else {
        tWidth = sf.base.formatUnit(this.getTableWidth(columns != null ? columns : this.parent.getColumns()));
      }
      var autoFitColumns = this.parent.getColumns().filter(function (c) {
        return c.autoFit === true && c.visible === true;
      });
      if (!this.parent.options.frozenColumns || this.parent.options.frozenColumns && !this.parent.options.enableColumnVirtualization && (autoFitColumns.length > 0 || this.parent.options.autoFit) || this.parent.options.frozenColumns && module === 'resize') {
        if (this.parent.options.hasDetailTemplate) {
          this.setWidth('30', 0);
        }
        if (tableWidth) {
          tWidth = '';
        }
        this.parent.resizeModule.tableWidth = tWidth;
        this.parent.getHeaderTable().style.width = tWidth;
        this.parent.getContentTable().style.width = tWidth;
        if (this.parent.options.aggregatesCount !== 0 && !sf.base.isNullOrUndefined(this.parent.getFooterContent().querySelector('.e-table'))) {
          this.parent.getFooterContent().querySelector('.e-table').style.width = tWidth;
        }
      }
      var edit = this.parent.element.querySelector('.e-table.e-inline-edit');
      if (edit && this.parent.options.frozenColumns) {
        this.setWidthToFrozenEditTable();
        this.setWidthToMovableEditTable();
      } else if (edit) {
        edit.style.width = tWidth;
      }
    };
    return ColumnWidthService;
  }();

  // eslint-disable-next-line valid-jsdoc, jsdoc/require-param, jsdoc/require-returns
  /**
   * Constructor for the HeaderDragDrop class.
   *
   */
  var HeaderDragDrop = /** @class */function () {
    /**
     * Constructor for HeaderDragDrop class.
     *
     * @param {SfGrid} parent - The parent SfGrid instance.
     */
    function HeaderDragDrop(parent) {
      var _this = this;
      // eslint-disable-next-line valid-jsdoc, jsdoc/require-param
      /**
       * Function handler for drag start event.
       *
       * @returns {void}
       */
      this.dragStart = function (e) {
        var gObj = _this.parent;
        document.body.classList.add('e-prevent-select');
        var popup = gObj.element.querySelector('.e-gridpopup');
        if (popup) {
          popup.style.display = 'none';
        }
        _this.parent.reorderModule.dragStart({
          target: e.target,
          column: _this.column,
          event: e.event
        });
        _this.parent.groupModule.columnDragStart({
          target: e.target,
          column: _this.column,
          event: e.event
        });
        e.bindEvents(e.dragElement);
      };
      this.drag = function (e) {
        var gObj = _this.parent;
        var target = e.target;
        if (target) {
          var closest = sf.base.closest(target, '.e-grid');
          var contentElement = sf.base.closest(target, '.e-gridcontent');
          var pagerElement = sf.base.closest(target, '.e-gridpager');
          var cloneElement = _this.parent.element.querySelector('.e-cloneproperties');
          if (!closest || closest.getAttribute('id') !== gObj.element.getAttribute('id') || contentElement && closest.getAttribute('id') === sf.base.closest(contentElement, '.e-grid').getAttribute('id') || pagerElement) {
            sf.base.classList(cloneElement, ['e-notallowedcur'], ['e-defaultcur']);
            if (gObj.options.allowReordering) {
              var upArrowElement = gObj.options.enableColumnVirtualization ? gObj.element.querySelector('.e-reorderuparrow-virtual') : gObj.element.querySelector('.e-reorderuparrow');
              var downArrowElement = gObj.options.enableColumnVirtualization ? gObj.element.querySelector('.e-reorderdownarrow-virtual') : gObj.element.querySelector('.e-reorderdownarrow');
              if (upArrowElement) {
                upArrowElement.style.display = 'none';
              }
              if (downArrowElement) {
                downArrowElement.style.display = 'none';
              }
            }
            if (!gObj.options.groupReordering) {
              return;
            }
          }
          if (gObj.options.allowReordering) {
            _this.parent.reorderModule.drag({
              target: e.target,
              column: _this.column,
              event: e.event
            });
          }
          if (gObj.options.allowGrouping) {
            _this.parent.groupModule.columnDrag({
              target: e.target
            });
          }
        }
      };
      this.dragStop = function (e) {
        var gObj = _this.parent;
        document.body.classList.remove('e-prevent-select');
        var cancel;
        var popup = gObj.element.querySelector('.e-gridpopup');
        if (popup) {
          popup.style.display = 'none';
        }
        if (!parentsUntil(e.target, 'e-headercell') && !parentsUntil(e.target, 'e-groupdroparea') || !gObj.options.allowReordering && parentsUntil(e.target, 'e-headercell') || !e.helper.getAttribute('e-mappinguid') && parentsUntil(e.target, 'e-groupdroparea')) {
          sf.base.remove(e.helper);
          cancel = true;
          if (gObj.options.allowGrouping && _this.parent.groupModule) {
            sf.base.EventHandler.remove(window, 'touchmove', _this.parent.groupModule.preventTouchOnWindow);
          }
        }
        if (gObj.options.allowReordering) {
          _this.parent.reorderModule.dragStop({
            target: e.target,
            event: e.event,
            column: _this.column,
            cancel: cancel
          });
        }
      };
      this.drop = function (e) {
        var gObj = _this.parent;
        var closest = sf.base.closest(e.target, '.e-grid');
        sf.base.remove(e.droppedElement);
        if (closest && closest.getAttribute('id') !== gObj.element.getAttribute('id') || !(gObj.options.allowReordering || gObj.options.allowGrouping)) {
          return;
        }
        if (gObj.options.allowReordering) {
          _this.parent.reorderModule.headerDrop({
            target: e.target
          });
        }
        if (gObj.options.allowGrouping && gObj.options.showDropArea) {
          _this.parent.groupModule.columnDrop({
            target: e.target,
            droppedElement: e.droppedElement
          });
        }
        //gObj.notify(events.headerDrop, { target: e.target, uid: uid, droppedElement: e.droppedElement });
      };
      this.helper = function (e) {
        var gObj = _this.parent;
        var target = e.sender.target;
        if (gObj.options.isFixedColumnPresent && e.currentTargetElement && e.sender.target) {
          var currentTargetHeader = parentsUntil(e.currentTargetElement, 'e-headercell');
          var senderTargetHeader = parentsUntil(e.sender.target, 'e-headercell');
          if (currentTargetHeader && senderTargetHeader) {
            target = e.currentTargetElement;
          }
        }
        var closest = sf.base.closest(target, '.e-headercell:not(.e-stackedHeaderCell)');
        if (closest) {
          var dropElement = closest.querySelector('.e-headercelldiv') || closest.querySelector('.e-stackedheadercelldiv');
          var uID = dropElement.getAttribute('e-mappinguid');
          var column = gObj.getColumnByUid(uID);
          if (!sf.base.isNullOrUndefined(column) && !column.allowGrouping && !column.allowReordering) {
            return false;
          }
        }
        var parentEle = parentsUntil(target, 'e-headercell');
        if (gObj.getContent().classList.contains('e-freezeline-moving') || !(gObj.options.allowReordering || gObj.options.allowGrouping) || !sf.base.isNullOrUndefined(parentEle) && parentEle.querySelectorAll('.e-checkselectall').length > 0) {
          return false;
        }
        var visualElement = sf.base.createElement('div', {
          className: 'e-cloneproperties e-dragclone e-headerclone'
        });
        var element = target.classList.contains('e-headercell') ? target : parentEle;
        if (!element || !gObj.options.allowReordering && element.classList.contains('e-stackedheadercell')) {
          return false;
        }
        var height = element.offsetHeight;
        var headercelldiv = element.querySelector('.e-headercelldiv') || element.querySelector('.e-stackedheadercelldiv');
        var col;
        if (headercelldiv) {
          if (element.querySelector('.e-stackedheadercelldiv')) {
            col = gObj.getStackedHeaderColumnByHeaderText(headercelldiv.innerText.trim(), gObj.options.columns);
          } else {
            col = gObj.getColumnByUid(headercelldiv.getAttribute('e-mappinguid'));
          }
          _this.column = col;
          var stackedColumns = _this.column.columns;
          var fixedColumnsOrder = _this.parent.reorderModule.processStackedColumns(_this.parent.options.columns);
          var indexOftheElement = parseInt(element.getAttribute('aria-colindex'), 10) - 1;
          var isStackedChildAllLocked = !sf.base.isNullOrUndefined(stackedColumns) && stackedColumns.length > 0 ? _this.parent.reorderModule.isAnyColumnFixed(fixedColumnsOrder[parseInt(indexOftheElement.toString(), 10)]) : false;
          if (_this.column.fixedColumn || isStackedChildAllLocked) {
            return false;
          }
          visualElement.setAttribute('e-mappinguid', headercelldiv.getAttribute('e-mappinguid'));
        }
        visualElement.innerText = headercelldiv ? sf.base.isNullOrUndefined(col.headerText) ? col.field : col.headerText : element.innerText;
        visualElement.style.width = element.offsetWidth + 'px';
        visualElement.style.height = element.offsetHeight + 'px';
        visualElement.style.lineHeight = (height - 6).toString() + 'px';
        gObj.element.appendChild(visualElement);
        return visualElement;
      };
      this.parent = parent;
      if (this.parent.options.allowGrouping || this.parent.options.allowReordering) {
        this.initializeHeaderDrag();
        this.initializeHeaderDrop();
      }
    }
    /**
     * Initializes header drag functionality.
     *
     * @public
     * @returns {void}
     */
    HeaderDragDrop.prototype.initializeHeaderDrag = function () {
      var gObj = this.parent;
      if (!(this.parent.options.allowReordering || this.parent.options.allowGrouping && this.parent.options.showDropArea)) {
        return;
      }
      var headerRows = [].slice.call(gObj.getHeaderContent().querySelectorAll('.e-columnheader'));
      for (var i = 0, len = headerRows.length; i < len; i++) {
        this.draggable = new sf.base.Draggable(headerRows[parseInt(i.toString(), 10)], {
          dragTarget: '.e-headercell',
          distance: 5,
          helper: this.helper,
          dragStart: this.dragStart,
          drag: this.drag,
          dragStop: this.dragStop,
          abort: '.e-rhandler',
          isPreventSelect: false
        });
      }
    };
    /**
     * Initializes header drop functionality.
     *
     * @private
     * @returns {void}
     */
    HeaderDragDrop.prototype.initializeHeaderDrop = function () {
      var gObj = this.parent;
      this.droppable = new sf.base.Droppable(gObj.getHeaderContent(), {
        accept: '.e-dragclone',
        drop: this.drop
      });
    };
    HeaderDragDrop.prototype.destroy = function () {
      if (!sf.base.isNullOrUndefined(this.draggable)) {
        this.draggable.destroy();
      }
      if (!sf.base.isNullOrUndefined(this.droppable)) {
        this.droppable.destroy();
      }
    };
    return HeaderDragDrop;
  }();

  /**
   * ColumnDrop Handling
   */
  var ContentDragDrop = /** @class */function () {
    function ContentDragDrop(parent) {
      var _this = this;
      /**
       * Handles the drop event for columns.
       *
       * @param {DropEventArgs} e - The event arguments for the drop event.
       * @returns {void}
       */
      this.drop = function (e) {
        var targetElementGrid = parentsUntil(e.target, 'e-grid');
        var cloneElementGrid = e.droppedElement.parentElement;
        var elementsNullCaseCheck = !sf.base.isNullOrUndefined(targetElementGrid) && !sf.base.isNullOrUndefined(cloneElementGrid);
        if (_this.parent.options.allowRowDragAndDrop && _this.parent.options.rowDropTarget) {
          if (elementsNullCaseCheck && targetElementGrid.id !== cloneElementGrid.id && _this.parent.options.groupCount === 0) {
            _this.parent.rowDragAndDropModule.columnDrop({
              target: e.target,
              droppedElement: e.droppedElement,
              mouseEvent: e.event
            });
          }
        } else {
          _this.parent.groupModule.columnDrop({
            target: e.target,
            droppedElement: e.droppedElement
          });
        }
        sf.base.remove(e.droppedElement);
      };
      this.parent = parent;
      if (this.parent.options.allowGrouping) {
        this.initializeContentDrop();
      }
    }
    ContentDragDrop.prototype.initializeContentDrop = function () {
      var gObj = this.parent;
      new sf.base.Droppable(gObj.getContent(), {
        accept: '.e-dragclone',
        drop: this.drop
      });
    };
    return ContentDragDrop;
  }();

  /**
   * Column reorder handling
   */
  var Reorder = /** @class */function () {
    function Reorder(parent) {
      this.parent = parent;
      if (parent.options.allowReordering) {
        this.createReorderElement();
      }
    }
    Reorder.prototype.chkDropPosition = function (srcElem, destElem) {
      if (sf.base.isNullOrUndefined(srcElem.parentElement)) {
        return false;
      }
      var mappingElement = destElem.querySelector('[e-mappinguid]');
      var mappingUid = !sf.base.isNullOrUndefined(mappingElement) ? mappingElement.getAttribute('e-mappinguid') : '';
      var col = this.parent.getColumnByUid(mappingUid);
      var bool = col ? !col.fixedColumn : true;
      return (srcElem.parentElement.isEqualNode(destElem.parentElement) || this.parent.options.frozenColumns && Array.prototype.indexOf.call(sf.base.closest(srcElem, 'thead').children, srcElem.parentElement) === Array.prototype.indexOf.call(sf.base.closest(destElem, 'thead').children, destElem.parentElement)) && this.targetParentContainerIndex(srcElem, destElem) > -1 && bool;
    };
    Reorder.prototype.chkDropAllCols = function (srcElem, destElem) {
      var isFound;
      var headers = this.getHeaderCells();
      var header;
      while (!isFound && headers.length > 0) {
        header = headers.pop();
        isFound = srcElem !== header && this.targetParentContainerIndex(srcElem, destElem) > -1;
      }
      return isFound;
    };
    Reorder.prototype.findColParent = function (col, cols, parent) {
      for (var i = 0, len = cols.length; i < len; i++) {
        if (col === cols[parseInt(i.toString(), 10)]) {
          return true;
        } else if (cols[parseInt(i.toString(), 10)].columns) {
          var cnt = parent.length;
          parent.push(cols[parseInt(i.toString(), 10)]);
          if (!this.findColParent(col, cols[parseInt(i.toString(), 10)].columns, parent)) {
            parent.splice(cnt, parent.length - cnt);
          } else {
            return true;
          }
        }
      }
      return false;
    };
    Reorder.prototype.getColumnsModel = function (cols) {
      var columnModel = [];
      var subCols = [];
      for (var i = 0, len = cols.length; i < len; i++) {
        columnModel.push(cols[parseInt(i.toString(), 10)]);
        if (cols[parseInt(i.toString(), 10)].columns) {
          subCols = subCols.concat(cols[parseInt(i.toString(), 10)].columns);
        }
      }
      if (subCols.length) {
        columnModel = columnModel.concat(this.getColumnsModel(subCols));
      }
      return columnModel;
    };
    Reorder.prototype.headerDrop = function (e) {
      if (sf.base.isNullOrUndefined(this.element)) {
        return;
      }
      var gObj = this.parent;
      var dropElement = this.element.querySelector('.e-headercelldiv') || this.element.querySelector('.e-stackedheadercelldiv');
      if (this.parent.options.enableColumnVirtualization) {
        dropElement = this.draggedHeader.querySelector('.e-headercelldiv') || this.draggedHeader.querySelector('.e-stackedheadercelldiv');
      }
      var uId = dropElement.getAttribute('e-mappinguid');
      var column = gObj.getColumnByUid(uId);
      var isStackedColumnLocked = false;
      if (!sf.base.isNullOrUndefined(this.draggedHeader.querySelector('.e-stackedheadercelldiv'))) {
        var headerCells = this.getHeaderCells();
        var stackedHdrColumn = this.parent.getStackedColumns(this.parent.options.columns);
        var stackedCols = stackedHdrColumn.length > 0 ? this.getAllStackedheaderParentColumns(headerCells) : [];
        var columnsList = this.processStackedColumns(this.parent.options.columns);
        var columnsOrder = stackedHdrColumn.length > 0 && stackedCols.length > 0 ? columnsList : this.parent.options.columns;
        var flatColumns = this.getColumnsModel(columnsOrder);
        if (!sf.base.isNullOrUndefined(dropElement.parentElement) && !sf.base.isNullOrUndefined(dropElement.parentElement.parentElement)) {
          var thElement = dropElement.parentElement.parentElement;
          isStackedColumnLocked = this.stackedLockedColumn(flatColumns, thElement);
        }
      }
      if (!sf.base.closest(e.target, 'th') || !sf.base.isNullOrUndefined(column) && (!column.allowReordering || column.fixedColumn) || isStackedColumnLocked) {
        return;
      }
      var destElem = sf.base.closest(e.target, '.e-headercell');
      if (sf.base.isNullOrUndefined(destElem)) {
        return;
      }
      var destElemDiv = destElem.querySelector('.e-headercelldiv') || destElem.querySelector('.e-stackedheadercelldiv');
      var destElemUid = destElemDiv.getAttribute('e-mappinguid');
      if (!sf.base.isNullOrUndefined(destElemUid)) {
        var destColumn = gObj.getColumnByUid(destElemUid);
        if (sf.base.isNullOrUndefined(destColumn) || !destColumn.allowReordering || destColumn.fixedColumn) {
          return;
        }
      }
      if (destElem && !(!this.chkDropPosition(this.element, destElem) || !this.chkDropAllCols(this.element, destElem))) {
        if (this.parent.options.enableColumnVirtualization) {
          var columns = this.parent.options.columns;
          var sourceUid_1 = this.draggedHeader.querySelector('.e-headercelldiv').getAttribute('e-mappinguid');
          var col = this.parent.getColumns(false).filter(function (col) {
            return col.uid === sourceUid_1;
          });
          var colMatchIndex_1 = null;
          var column_1 = col[0];
          var destUid_1 = destElem.querySelector('.e-headercelldiv').getAttribute('e-mappinguid');
          columns.some(function (col, index) {
            if (col.uid === destUid_1) {
              colMatchIndex_1 = index;
              return col.uid === destUid_1;
            }
            return false;
          });
          if (!sf.base.isNullOrUndefined(colMatchIndex_1)) {
            this.moveColumns(colMatchIndex_1, column_1);
          }
        } else {
          var newIndex = this.targetParentContainerIndex(this.element, destElem);
          var mappingElement = this.element.querySelector('[e-mappinguid]');
          var uid = !sf.base.isNullOrUndefined(mappingElement) ? mappingElement.getAttribute('e-mappinguid') : '';
          this.destElement = destElem;
          if (uid) {
            this.moveColumns(newIndex, this.parent.getColumnByUid(uid));
          } else {
            var headers = this.getHeaderCells();
            var oldIdx = getElementIndex(this.element, headers);
            var columns = this.getColumnsModel(this.parent.options.columns);
            var column_2 = columns[parseInt(oldIdx.toString(), 10)];
            this.moveColumns(newIndex, column_2);
          }
        }
      }
    };
    Reorder.prototype.isActionPrevent = function (gObj) {
      return isActionPrevent(gObj.element, gObj.options.editMode);
    };
    Reorder.prototype.moveColumns = function (destIndex, column, reorderByColumn, preventRefresh) {
      var gObj = this.parent;
      if (this.isActionPrevent(gObj)) {
        //gObj.notify(events.preventBatch, { instance: this, handler: this.moveColumns, arg1: destIndex, arg2: column });
        return;
      }
      var parent = this.getColParent(column, this.parent.options.columns);
      var cols = parent ? parent.columns : this.parent.options.columns;
      var srcIdx = inArray(column, cols);
      if ((this.parent.options.frozenColumns && parent || this.parent.checkFixedColumns(this.parent.options.columns)) && !reorderByColumn) {
        for (var i = 0; i < cols.length; i++) {
          var gridcolumn = cols[parseInt(i.toString(), 10)];
          if (gridcolumn.field === column.field && gridcolumn.index === column.index) {
            srcIdx = i;
            break;
          }
        }
        var mappingElement = this.destElement.querySelector('[e-mappinguid]');
        var mappingUid = !sf.base.isNullOrUndefined(mappingElement) ? mappingElement.getAttribute('e-mappinguid') : '';
        var col = this.parent.getColumnByUid(mappingUid);
        if (col) {
          for (var i = 0; i < cols.length; i++) {
            var gridcolumn = cols[parseInt(i.toString(), 10)];
            if (gridcolumn.field === col.field && gridcolumn.index === col.index) {
              destIndex = i;
              break;
            }
          }
        } else {
          for (var i = 0; i < cols.length; i++) {
            var gridcolumn = cols[parseInt(i.toString(), 10)];
            if (gridcolumn.headerText === this.destElement.innerText.trim()) {
              destIndex = i;
            }
          }
        }
        var destStackedHeader = this.destElement.classList.contains('e-stackedheadercell');
        var srcStackedHeader = this.element.classList.contains('e-stackedheadercell');
        if (destStackedHeader && srcStackedHeader && (this.isAnyColumnFixed(cols[parseInt(destIndex.toString(), 10)]) || this.isAnyColumnFixed(cols[parseInt(srcIdx.toString(), 10)]))) {
          return;
        }
      }
      if (!gObj.options.allowReordering || srcIdx === destIndex || srcIdx === -1 || destIndex === -1) {
        return;
      }
      cols.splice(destIndex, 0, cols.splice(srcIdx, 1)[0]);
      gObj.getColumns(true);
      //gObj.notify(events.columnPositionChanged, { fromIndex: destIndex, toIndex: srcIdx });
      if (preventRefresh !== false) {
        //TODO: reorder from here
        setTimeout(function () {
          gObj.dotNetRef.invokeMethodAsync('ColumnReordered', {
            requestType: 'reorder',
            fromIndex: destIndex,
            toIndex: srcIdx,
            toColumnUid: column.uid
          });
        }, 10);
      }
    };
    Reorder.prototype.targetParentContainerIndex = function (srcElem, destElem) {
      var headers = this.getHeaderCells();
      if (this.parent.options.frozenName !== 'None') {
        this.parent.updateColumnLevelFrozen();
      }
      var cols = this.parent.options.columns;
      var lockedOrderedColumns = this.processStackedColumns(cols);
      var flatColumns = this.parent.options.frozenName === 'None' ? this.getColumnsModel(lockedOrderedColumns) : this.parent.frozenColumnModel.slice();
      var parent = this.getColParent(flatColumns[getElementIndex(srcElem, headers)], cols);
      cols = parent ? parent.columns : lockedOrderedColumns;
      if (srcElem.classList.contains('e-stackedheadercell') && destElem.classList.contains('e-stackedheadercell')) {
        var srcMappingElement = srcElem.querySelector('[e-mappinguid]');
        var srcElemntUID_1 = !sf.base.isNullOrUndefined(srcMappingElement) ? srcMappingElement.getAttribute('e-mappinguid') : '';
        var destMappingElement = srcElem.querySelector('[e-mappinguid]');
        var destElementUID_1 = !sf.base.isNullOrUndefined(destMappingElement) ? destMappingElement.getAttribute('e-mappinguid') : '';
        var sourceColumn = cols.filter(function (col) {
          return col.uid === srcElemntUID_1;
        })[0];
        var destColumn = cols.filter(function (col) {
          return col.uid === destElementUID_1;
        })[0];
        if (this.isAnyColumnFixed(sourceColumn) || this.isAnyColumnFixed(destColumn)) {
          return -1;
        }
      }
      return inArray(flatColumns[getElementIndex(destElem, headers)], cols);
    };
    Reorder.prototype.getHeaderCells = function () {
      return [].slice.call(this.parent.element.getElementsByClassName('e-headercell'));
    };
    Reorder.prototype.getColParent = function (column, columns) {
      var parents = [];
      this.findColParent(column, columns, parents);
      return parents[parents.length - 1];
    };
    Reorder.prototype.reorderSingleColumn = function (fromFName, toFName) {
      var fColumn = this.parent.getColumnByField(fromFName);
      var toColumn = this.parent.getColumnByField(toFName);
      if (!sf.base.isNullOrUndefined(fColumn) && (!fColumn.allowReordering || fColumn.fixedColumn) || !sf.base.isNullOrUndefined(toColumn) && (!toColumn.allowReordering || toColumn.fixedColumn)) {
        return;
      }
      var column = this.parent.getColumnByField(toFName);
      var parent = this.getColParent(column, this.parent.options.columns);
      var columns = parent ? parent.columns : this.parent.options.columns;
      var destIndex = inArray(column, columns);
      if (destIndex > -1) {
        this.moveColumns(destIndex, this.parent.getColumnByField(fromFName), true);
      }
    };
    Reorder.prototype.reorderMultipleColumns = function (fromFNames, toFName) {
      var toIndex = this.parent.getColumnIndexByField(toFName);
      var toColumn = this.parent.getColumnByField(toFName);
      if (toIndex < 0 || !sf.base.isNullOrUndefined(toColumn) && (!toColumn.allowReordering || toColumn.fixedColumn)) {
        return;
      }
      for (var i = 0; i < fromFNames.length; i++) {
        var column = this.parent.getColumnByField(fromFNames[parseInt(i.toString(), 10)]);
        if (!sf.base.isNullOrUndefined(column) && (!column.allowReordering || column.fixedColumn)) {
          return;
        }
      }
      for (var i = 0; i < fromFNames.length; i++) {
        var column = this.parent.getColumnByIndex(toIndex);
        var parent_1 = this.getColParent(column, this.parent.options.columns);
        var columns = parent_1 ? parent_1.columns : this.parent.options.columns;
        var destIndex = inArray(column, columns);
        if (destIndex > -1) {
          this.moveColumns(destIndex, this.parent.getColumnByField(fromFNames[parseInt(i.toString(), 10)]), true, false);
        }
        if (this.parent.getColumnIndexByField(fromFNames[i + 1]) >= destIndex) {
          toIndex++; //R to L
        }
      }
      var cols = this.parent.getColumns();
      this.parent.dotNetRef.invokeMethodAsync('ColumnReordered', {
        fromColumnUid: fromFNames.map(function (name) {
          return cols.filter(function (col) {
            return col.field === name;
          })[0].uid;
        }),
        toColumnUid: toColumn.uid,
        isMultipleReorder: true,
        requestType: 'reorder',
        type: 'actionBegin'
      });
    };
    Reorder.prototype.moveTargetColumn = function (column, toIndex) {
      if (toIndex > -1) {
        this.moveColumns(toIndex, column, true);
      }
    };
    Reorder.prototype.reorderSingleColumnByTarget = function (fieldName, toIndex) {
      var column = this.parent.getColumnByField(fieldName);
      this.moveTargetColumn(column, toIndex);
    };
    Reorder.prototype.reorderMultipleColumnByTarget = function (fieldName, toIndex) {
      for (var i = 0; i < fieldName.length; i++) {
        this.reorderSingleColumnByTarget(fieldName[parseInt(i.toString(), 10)], toIndex);
      }
    };
    /**
     * Changes the position of the Grid columns by field names.
     *
     * @param  {string | string[]} fromFName - Defines the origin field names.
     * @param  {string} toFName - Defines the destination field name.
     * @returns {void}
     */
    Reorder.prototype.reorderColumns = function (fromFName, toFName) {
      if (typeof fromFName === 'string') {
        this.reorderSingleColumn(fromFName, toFName);
        this.fromCol = fromFName;
      } else {
        this.reorderMultipleColumns(fromFName, toFName);
        this.fromCol = fromFName[0];
      }
    };
    /**
     * Changes the position of the Grid columns by field index.
     *
     * @param  {number} fromIndex - Defines the origin field index.
     * @param  {number} toIndex - Defines the destination field index.
     * @returns {void}
     */
    Reorder.prototype.reorderColumnByIndex = function (fromIndex, toIndex) {
      var column = this.parent.getColumnByIndex(fromIndex);
      this.moveTargetColumn(column, toIndex);
    };
    /**
     * Changes the position of the Grid columns by field index.
     *
     * @param  {string | string[]} fieldName - Defines the field name.
     * @param  {number} toIndex - Defines the destination field index.
     * @returns {void}
     */
    Reorder.prototype.reorderColumnByTargetIndex = function (fieldName, toIndex) {
      if (typeof fieldName === 'string') {
        this.reorderSingleColumnByTarget(fieldName, toIndex);
      } else {
        this.reorderMultipleColumnByTarget(fieldName, toIndex);
      }
    };
    Reorder.prototype.createReorderElement = function () {
      var header = this.parent.element.querySelector('.e-headercontent');
      this.upArrow = header.appendChild(sf.base.createElement('div', {
        className: 'e-icons e-icon-reorderuparrow e-reorderuparrow',
        styles: 'display:none'
      }));
      this.downArrow = header.appendChild(sf.base.createElement('div', {
        className: 'e-icons e-icon-reorderdownarrow e-reorderdownarrow',
        styles: 'display:none'
      }));
      if (this.parent.options.enableColumnVirtualization) {
        this.upArrow.classList.replace('e-reorderuparrow', 'e-reorderuparrow-virtual');
        this.downArrow.classList.replace('e-reorderdownarrow', 'e-reorderdownarrow-virtual');
      }
    };
    /**
     * The function used to trigger onActionComplete
     *
     * @return {void}
     * @hidden
     */
    // public onActionComplete(e: NotifyArgs): void {
    //     if (isBlazor() && !this.parent.isJsComponent) {
    //         e.rows = null;
    //     }
    //     this.parent.trigger(events.actionComplete, extend(e, { type: events.actionComplete }));
    //     let target: Element = this.fromCol && this.parent.getColumnHeaderByField(this.fromCol);
    //     if (target) {
    //         this.parent.focusModule.onClick({ target }, true);
    //     }
    // }
    /**
     * To destroy the reorder
     *
     * @returns {void}
     * @hidden
     */
    Reorder.prototype.destroy = function () {
      if (this.upArrow) {
        sf.base.remove(this.upArrow);
      }
      if (this.downArrow) {
        sf.base.remove(this.downArrow);
      }
      //call ejdrag and drop destroy
    };
    Reorder.prototype.keyPressHandler = function (e) {
      switch (e.action) {}
    };
    Reorder.prototype.drag = function (e) {
      var gObj = this.parent;
      var target = e.target;
      var cloneElement = gObj.element.querySelector('.e-cloneproperties');
      if (!e.column.allowReordering || e.column.fixedColumn) {
        sf.base.classList(cloneElement, ['e-notallowedcur'], ['e-defaultcur']);
        return;
      }
      var closest = sf.base.closest(target, '.e-headercell:not(.e-stackedHeaderCell)');
      var isLeft = this.x > getPosition(e.event).x + gObj.getContent().firstElementChild.scrollLeft;
      sf.base.removeClass(gObj.element.querySelector('.e-headercontent').querySelectorAll('.e-reorderindicate'), ['e-reorderindicate']);
      this.setDisplay('none');
      this.stopTimer();
      sf.base.classList(cloneElement, ['e-defaultcur'], ['e-notallowedcur']);
      this.updateScrollPostion(e.event);
      if (closest && !closest.isEqualNode(this.element)) {
        target = closest;
        //consider stacked, detail header cell
        var dropElement = closest.querySelector('.e-headercelldiv') || closest.querySelector('.e-stackedheadercelldiv');
        var uID = dropElement.getAttribute('e-mappinguid');
        var column = gObj.getColumnByUid(uID);
        if (!(!this.chkDropPosition(this.element, target) || !this.chkDropAllCols(this.element, target)) && column.allowReordering) {
          this.updateArrowPosition(target, isLeft);
          sf.base.classList(target, ['e-allowDrop', 'e-reorderindicate'], []);
        } else if (!(gObj.options.allowGrouping && parentsUntil(e.target, 'e-groupdroparea'))) {
          sf.base.classList(cloneElement, ['e-notallowedcur'], ['e-defaultcur']);
        }
      }
      //gObj.trigger(events.columnDrag, { target: target, draggableType: 'headercell', column: e.column });
    };
    Reorder.prototype.updateScrollPostion = function (e) {
      var _this = this;
      var x = getPosition(e).x;
      var cliRect = this.parent.element.getBoundingClientRect();
      var cliRectBaseLeft = cliRect.left;
      var cliRectBaseRight = cliRect.right;
      var scrollElem = this.parent.getContent();
      if (this.parent.options.frozenName !== 'None') {
        this.updateFrozenScrollPosition(x, cliRect);
      } else {
        if (x > cliRectBaseLeft && x < cliRectBaseLeft + 35) {
          this.timer = window.setInterval(function () {
            _this.setScrollLeft(scrollElem, true);
          }, 50);
        } else if (x < cliRectBaseRight && x > cliRectBaseRight - 35) {
          this.timer = window.setInterval(function () {
            _this.setScrollLeft(scrollElem, false);
          }, 50);
        }
      }
    };
    Reorder.prototype.updateFrozenScrollPosition = function (x, cliRect) {
      var _this = this;
      var scrollElem = this.parent.getContent().querySelector('.e-movablecontent');
      var mhdrCliRect = this.parent.element.querySelector('.e-movableheader').getBoundingClientRect();
      var left = this.parent.options.frozenLeftCount || this.parent.options.actualFrozenColumns;
      var right = this.parent.options.frozenRightCount;
      var cliRectBaseRight = right ? mhdrCliRect.right : cliRect.right;
      var cliRectBaseLeft = left ? mhdrCliRect.left : cliRect.left;
      if (x > cliRectBaseLeft && x < cliRectBaseLeft + 35) {
        this.timer = window.setInterval(function () {
          _this.setScrollLeft(scrollElem, true);
        }, 50);
      } else if (x < cliRectBaseRight && x > cliRectBaseRight - 35) {
        this.timer = window.setInterval(function () {
          _this.setScrollLeft(scrollElem, false);
        }, 50);
      }
    };
    Reorder.prototype.setScrollLeft = function (scrollElem, isLeft) {
      var scrollLeft = scrollElem.scrollLeft;
      scrollElem.scrollLeft = scrollElem.scrollLeft + (isLeft ? -5 : 5);
      if (scrollLeft !== scrollElem.scrollLeft) {
        this.setDisplay('none');
      }
    };
    Reorder.prototype.stopTimer = function () {
      window.clearInterval(this.timer);
    };
    Reorder.prototype.updateArrowPosition = function (target, isLeft) {
      var cliRect = target.getBoundingClientRect();
      var cliRectBase = this.parent.element.getBoundingClientRect();
      if (isLeft && cliRect.left < cliRectBase.left || !isLeft && cliRect.right > cliRectBase.right) {
        return;
      }
      this.upArrow.style.top = cliRect.top + cliRect.height - cliRectBase.top - 7 + 'px';
      this.downArrow.style.top = cliRect.top - cliRectBase.top - 2 + 'px';
      this.upArrow.style.left = this.downArrow.style.left = (isLeft ? cliRect.left : cliRect.right) - cliRectBase.left - 4 + 'px';
      if (this.parent.options.enableColumnVirtualization) {
        this.upArrow.style.left = this.downArrow.style.left = Number(this.upArrow.style.left.replace('px', '')) + this.parent.getContent().scrollLeft + 'px';
      }
      this.setDisplay('');
    };
    Reorder.prototype.dragStart = function (e) {
      var gObj = this.parent;
      document.body.classList.add('e-prevent-select');
      var target = e.target;
      this.element = target.classList.contains('e-headercell') ? target : parentsUntil(target, 'e-headercell');
      if (sf.base.isNullOrUndefined(this.element)) {
        this.element = e.event.target.classList.contains('e-headercell') ? e.event.target : parentsUntil(e.event.target, 'e-headercell');
      }
      this.draggedHeader = this.element.cloneNode(true);
      if (!e.column.allowReordering || e.column.fixedColumn) {
        return;
      }
      this.x = getPosition(e.event).x + gObj.getContent().firstElementChild.scrollLeft;
      // gObj.trigger(events.columnDragStart, {
      //     target: target as Element, draggableType: 'headercell', column: e.column
      // });
    };
    Reorder.prototype.dragStop = function (e) {
      var gObj = this.parent;
      if (this.parent.options.allowGrouping && e.event.type === 'touchend') {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        sf.base.EventHandler.remove(window, 'touchmove', this.parent.groupModule.preventTouchOnWindow);
      }
      document.body.classList.remove('e-prevent-select');
      this.setDisplay('none');
      this.stopTimer();
      if (!e.cancel) ;
      sf.base.removeClass(gObj.element.querySelector('.e-headercontent').querySelectorAll('.e-reorderindicate'), ['e-reorderindicate']);
    };
    Reorder.prototype.setDisplay = function (display) {
      if (this.upArrow) {
        this.upArrow.style.display = display;
      }
      if (this.downArrow) {
        this.downArrow.style.display = display;
      }
    };
    Reorder.prototype.stackedLockedColumn = function (columnsList, dropElement) {
      var dropIndex = parseInt(dropElement.getAttribute('aria-colindex'), 10) - 1;
      if (!columnsList.some(function (column) {
        return column.fixedColumn;
      })) {
        return false;
      }
      for (var i = 0; i < columnsList.length; i++) {
        var column = columnsList[parseInt(i.toString(), 10)];
        if (!sf.base.isNullOrUndefined(column.columns) && column.columns.length > 0) {
          // Recursively check nested columns
          var isLockedChild = this.stackedLockedColumn(column.columns, dropElement);
          if (isLockedChild) {
            return true; // Return true if any of the child level column is locked
          }
        }
        // Check if the looped index matches dropIndex
        if (i === dropIndex) {
          return column.fixedColumn ? true : false; // Return false if not locked
        }
      }
      return false;
    };
    Reorder.prototype.getAllStackedheaderParentColumns = function (headers) {
      var stackedColumns = [];
      for (var i = 0; i < headers.length; i++) {
        var headerElement = headers[parseInt(i.toString(), 10)];
        if (headerElement.classList.contains('e-hide')) {
          headers.splice(i, 1);
          i--;
        } else if (headerElement.closest('thead').firstChild === headerElement.parentElement) {
          var mappingElement = headerElement.querySelector('[e-mappinguid]');
          var mappingUID = !sf.base.isNullOrUndefined(mappingElement) ? mappingElement.getAttribute('e-mappinguid') : '';
          stackedColumns.push(this.parent.getColumnByUid(mappingUID));
        }
      }
      return stackedColumns;
    };
    Reorder.prototype.isAnyColumnFixed = function (column) {
      if (!sf.base.isNullOrUndefined(column)) {
        // Check if the current column is locked
        if (column.fixedColumn) {
          return true;
        }
        // Check recursively if any of the nested child columns are locked
        if (!sf.base.isNullOrUndefined(column.columns) && column.columns.length > 0) {
          // Recursively checking each child column
          for (var j = 0; j < column.columns.length; j++) {
            if (this.isAnyColumnFixed(column.columns[parseInt(j.toString(), 10)])) {
              return true;
            }
          }
        }
      }
      return false;
    };
    Reorder.prototype.setStackedFixedColumns = function (column, isLocked) {
      var jsonString = JSON.stringify(column);
      var stackedColumn = JSON.parse(jsonString);
      var columnsRemoveCount = stackedColumn.columns.length;
      for (var j = 0; j < column.columns.length; j++) {
        var innerColumn = column.columns[parseInt(j.toString(), 10)];
        if (!innerColumn.columns) {
          if (innerColumn.fixedColumn && isLocked) {
            stackedColumn.columns.push(innerColumn);
          } else if (!innerColumn.fixedColumn && !isLocked) {
            stackedColumn.columns.push(innerColumn);
          }
        } else {
          var stackedLockedcolumn = this.setStackedFixedColumns(innerColumn, isLocked);
          if (stackedLockedcolumn.columns.length !== 0) {
            stackedColumn.columns.push(stackedLockedcolumn);
          }
        }
      }
      if (columnsRemoveCount !== 0) {
        stackedColumn.columns.splice(0, columnsRemoveCount);
      }
      return stackedColumn;
    };
    Reorder.prototype.processStackedColumns = function (stackedColumns) {
      var _this = this;
      var fixedColumns = [];
      var unfixedColumns = [];
      if (this.parent.checkFixedColumns(stackedColumns)) {
        stackedColumns.forEach(function (column) {
          if (column.columns && _this.parent.checkFixedColumns(column.columns)) {
            var fixedColumn = _this.setStackedFixedColumns(column, true);
            var normalColumn = _this.setStackedFixedColumns(column, false);
            if (fixedColumn.columns.length > 0) {
              fixedColumns.push(fixedColumn);
            }
            if (normalColumn.columns.length > 0) {
              unfixedColumns.push(normalColumn);
            }
          } else if (column.fixedColumn) {
            fixedColumns.push(column);
          } else {
            unfixedColumns.push(column);
          }
        });
        unfixedColumns.sort(function (a, b) {
          return a.index - b.index;
        });
        var allColumns = fixedColumns.concat(unfixedColumns);
        return allColumns; // Processed list of all columns
      }
      return stackedColumns; // Return original if no frozen columns found
    };
    /**
     * For internal use only - Get the module name.
     *
     * @private
     * @returns {string} The name of the module.
     */
    Reorder.prototype.getModuleName = function () {
      return 'reorder';
    };
    return Reorder;
  }();
  var resizeClassList = {
    root: 'e-rhandler',
    suppress: 'e-rsuppress',
    icon: 'e-ricon',
    helper: 'e-rhelper',
    header: 'th.e-headercell',
    cursor: 'e-rcursor'
  };
  /**
   * Resize handler
   */
  var Resize = /** @class */function () {
    function Resize(parent) {
      this.tapped = false;
      this.isDblClk = true;
      this.parent = parent;
      this.widthService = new ColumnWidthService(this.parent);
    }
    /**
     * Resize by field names.
     *
     * @param  {string|string[]} fName - Defines the field name.
     * @returns {void}
     */
    Resize.prototype.autoFitColumns = function (fName) {
      var virtualAutoFit = this.parent.options.enableColumnVirtualization ? true : false;
      var columnName = [];
      if (fName === undefined || fName === null || fName.length <= 0) {
        if (this.parent.options.frozenColumns > 0) {
          columnName = this.parent.autofitFrozenColumns(true);
        } else {
          columnName = this.parent.getColumns(virtualAutoFit).map(function (x) {
            return x.field || x.uid;
          });
        }
      } else {
        columnName = typeof fName === 'string' ? [fName] : fName;
      }
      this.findColumn(columnName);
      if (this.parent.options.frozenColumns > 0) {
        this.widthService.setWidthToTable();
      }
    };
    Resize.prototype.autoFit = function () {
      var newarray;
      if (this.parent.options.frozenColumns || this.parent.options.actualFrozenColumns > 0) {
        newarray = this.parent.autofitFrozenColumns();
      } else {
        newarray = this.parent.getColumns().filter(function (c) {
          return c.autoFit === true;
        }).map(function (c) {
          return c.field || c.uid;
        });
      }
      if (newarray.length > 0) {
        this.autoFitColumns(newarray);
      }
    };
    /* tslint:disable-next-line:max-func-body-length */
    Resize.prototype.resizeColumn = function (fName, index, id, allowStopEvent) {
      if (allowStopEvent === void 0) {
        allowStopEvent = false;
      }
      var gObj = this.parent;
      var tWidth = 0;
      var footerTable;
      var headerDivTag = 'e-gridheader';
      var contentDivTag = 'e-gridcontent';
      var footerDivTag = 'e-gridfooter';
      var indentWidth = 0;
      var autoFitVirtual = this.parent.options.enableColumnVirtualization ? true : false;
      var uid = id ? id : this.parent.getUidByColumnField(fName, autoFitVirtual);
      uid = uid ? uid : fName;
      var columnIndex = this.parent.getNormalizedColumnIndex(uid, autoFitVirtual);
      //let headerTextClone: Element;
      var contentTextClone;
      var footerTextClone;
      var columnIndexByField = this.parent.getColumnIndexByField(fName, autoFitVirtual);
      columnIndexByField = columnIndexByField === -1 ? this.parent.getColumnIndexByUid(fName, autoFitVirtual) : columnIndexByField;
      var frzCols = gObj.options.frozenColumns;
      if (!sf.base.isNullOrUndefined(gObj.getFooterContent())) {
        footerTable = gObj.getFooterContent().querySelector('.e-table');
      }
      var headerTable = gObj.getHeaderTable();
      var contentTable = gObj.getContentTable();
      var headerTextClone = sf.base.closest(headerTable.querySelector('[e-mappinguid="' + uid + '"]'), 'th').cloneNode(true);
      if (frzCols) {
        var ariaColIndex = headerTextClone.getAttribute('aria-colindex');
        contentTextClone = contentTable.querySelectorAll('[aria-colindex="' + ariaColIndex + '"]');
        if (footerTable) {
          footerTextClone = footerTable.querySelectorAll("td:nth-child(".concat(columnIndex + 1, ")"));
        }
      } else {
        contentTextClone = contentTable.querySelectorAll("td:nth-child(".concat(columnIndex + 1, "):not(.e-groupcaption)"));
        if (footerTable) {
          footerTextClone = footerTable.querySelectorAll("td:nth-child(".concat(columnIndex + 1, "):not(.e-groupcaption)"));
        }
      }
      var indentWidthClone = headerTable.querySelector('tr').querySelectorAll('.e-grouptopleftcell');
      if (indentWidthClone.length > 0) {
        for (var i = 0; i < indentWidthClone.length; i++) {
          indentWidth += indentWidthClone[parseInt(i.toString(), 10)].offsetWidth;
        }
      }
      var detailsElement = contentTable.querySelector('.e-detailrowcollapse') || contentTable.querySelector('.e-detailrowexpand');
      if (this.parent.options.hasDetailTemplate && detailsElement) {
        indentWidth += detailsElement.offsetWidth;
      }
      var headerText = [headerTextClone];
      var contentText = [];
      var footerText = [];
      if (footerTable) {
        for (var i = 0; i < footerTextClone.length; i++) {
          footerText[parseInt(i.toString(), 10)] = footerTextClone[parseInt(i.toString(), 10)].cloneNode(true);
        }
      }
      for (var i = 0; i < contentTextClone.length; i++) {
        contentText[parseInt(i.toString(), 10)] = contentTextClone[parseInt(i.toString(), 10)].cloneNode(true);
      }
      var wHeader = this.createTable(headerTable, headerText, headerDivTag);
      var wContent = this.createTable(contentTable, contentText, contentDivTag);
      var wFooter = null;
      if (footerText.length) {
        wFooter = this.createTable(footerTable, footerText, footerDivTag);
      }
      var columnbyindex = gObj.getColumns(autoFitVirtual)[parseInt(columnIndexByField.toString(), 10)];
      var width = columnbyindex.width = sf.base.formatUnit(Math.max(wHeader, wContent, wFooter));
      if (!sf.base.isNullOrUndefined(columnbyindex.maxWidth) && columnbyindex.maxWidth !== '' && parseInt(width, 10) > parseInt(columnbyindex.maxWidth.toString(), 10)) {
        columnbyindex.width = columnbyindex.maxWidth.toString();
      }
      this.widthService.setColumnWidth(gObj.getColumns(autoFitVirtual)[parseInt(columnIndexByField.toString(), 10)]);
      var result = gObj.getColumns(autoFitVirtual).some(function (x) {
        return (x.width === null || x.width === undefined || x.width.length <= 0) && x.visible;
      });
      if (result === false) {
        var element = gObj.getColumns(autoFitVirtual);
        for (var i = 0; i < element.length; i++) {
          if (element[parseInt(i.toString(), 10)].visible) {
            tWidth = tWidth + parseFloat(element[parseInt(i.toString(), 10)].width);
          }
        }
      }
      var calcTableWidth = tWidth + indentWidth;
      if (tWidth > 0 && !gObj.options.frozenColumns) {
        //TODO: why this?
        if (this.parent.options.hasDetailTemplate) {
          //this.widthService.setColumnWidth(new Column({ width: '30px' }));
          this.widthService.setWidth('30', 0);
        }
        headerTable.style.width = sf.base.formatUnit(calcTableWidth);
        contentTable.style.width = sf.base.formatUnit(calcTableWidth);
        if (!sf.base.isNullOrUndefined(footerTable)) {
          footerTable.style.width = sf.base.formatUnit(calcTableWidth);
        }
      }
      gObj.addTableBorderClass();
      this.parent.freezeModule.refreshRowHeight();
      if (width.toString().indexOf('px') > 0) {
        width = width.replace('px', '');
      }
      this.parent.options.isResizedGrid = true;
      this.parent.dotNetRef.invokeMethodAsync('ColumnWidthChanged', {
        width: width,
        columnUid: uid,
        allowStopEvent: allowStopEvent
      }, false);
    };
    /**
     * To destroy the resize
     *
     * @returns {void}
     * @hidden
     */
    Resize.prototype.destroy = function () {
      var gridElement = this.parent.element;
      if (!gridElement || !gridElement.querySelector('.e-gridheader') && !gridElement.querySelector('.e-gridcontent')) {
        return;
      }
      this.widthService = null;
      this.unwireEvents();
      //this.removeEventListener();
    };
    /**
     * For internal use only - Get the module name.
     *
     * @private
     * @returns {string} The name of the module.
     */
    Resize.prototype.getModuleName = function () {
      return 'resize';
    };
    Resize.prototype.findColumn = function (fName) {
      var _loop_1 = function _loop_1(i) {
        var fieldName = fName[parseInt(i.toString(), 10)];
        var autoFitVirtual = this_1.parent.options.enableColumnVirtualization ? true : false;
        var columnIndex = this_1.parent.getColumnIndexByField(fieldName, autoFitVirtual);
        columnIndex = columnIndex === -1 ? this_1.parent.getColumnIndexByUid(fieldName, autoFitVirtual) : columnIndex;
        var column = this_1.parent.getColumns(autoFitVirtual)[parseInt(columnIndex.toString(), 10)];
        if (columnIndex > -1 && !sf.base.isNullOrUndefined(column) && column.visible === true) {
          if (!(this_1.parent.options.allowGrouping && !sf.base.isNullOrUndefined(this_1.parent.options.initGroupingField) && this_1.parent.options.initGroupingField.some(function (x) {
            return x === column.field;
          }) && !this_1.parent.options.showGroupedColumn)) {
            this_1.resizeColumn(fieldName, columnIndex);
          }
        }
      };
      var this_1 = this;
      for (var i = 0; i < fName.length; i++) {
        _loop_1(i);
      }
    };
    /**
     * To create table for autofit
     *
     * @hidden
     * @param {Element} table - The table element to create.
     * @param {Element[]} text - Array of text elements.
     * @param {string} tag - The tag string.
     * @returns {number} The created table's number.
     */
    Resize.prototype.createTable = function (table, text, tag) {
      var myTableDiv = sf.base.createElement('div');
      myTableDiv.className = this.parent.element.className;
      myTableDiv.style.cssText = 'display: inline-block;visibility:hidden;position:absolute';
      var mySubDiv = sf.base.createElement('div');
      mySubDiv.className = tag;
      var myTable = sf.base.createElement('table');
      myTable.className = table.className;
      myTable.classList.add('e-resizetable');
      myTable.style.cssText = 'table-layout: auto;width: auto';
      var thead = null;
      if (tag === 'e-gridheader') {
        thead = sf.base.createElement('thead');
        myTable.appendChild(thead);
      }
      var myTr = sf.base.createElement('tr');
      for (var i = 0; i < text.length; i++) {
        var tr = myTr.cloneNode();
        tr.className = table.querySelector('tr').className;
        tr.appendChild(text[parseInt(i.toString(), 10)]);
        if (thead) {
          thead.appendChild(tr);
        } else {
          myTable.appendChild(tr);
        }
      }
      mySubDiv.appendChild(myTable);
      myTableDiv.appendChild(mySubDiv);
      document.body.appendChild(myTableDiv);
      var offsetWidthValue = myTable.getBoundingClientRect().width;
      document.body.removeChild(myTableDiv);
      return Math.ceil(offsetWidthValue);
    };
    /**
     * @hidden
     */
    // public addEventListener(): void {
    //     if (this.parent.isDestroyed) {
    //         return;
    //     }
    //     this.parent.on(events.headerRefreshed, this.refreshHeight, this);
    //     this.parent.on(events.initialEnd, this.wireEvents, this);
    //     this.parent.on(events.contentReady, this.autoFit, this);
    // }
    /**
     * @hidden
     */
    // public removeEventListener(): void {
    //     if (this.parent.isDestroyed) {
    //         return;
    //     }
    //     this.parent.off(events.headerRefreshed, this.refreshHeight);
    //     this.parent.off(events.initialEnd, this.wireEvents);
    // }
    /**
     * @hidden
     * @returns {void}
     */
    Resize.prototype.render = function () {
      this.unwireEvents();
      this.wireEvents();
      this.setHandlerHeight();
    };
    Resize.prototype.refreshHeight = function () {
      var element = this.getResizeHandlers();
      for (var i = 0; i < element.length; i++) {
        var headerCellElement = element[parseInt(i.toString(), 10)];
        var headerCellContainer = headerCellElement.parentElement;
        if (!sf.base.isNullOrUndefined(headerCellContainer) && !sf.base.isNullOrUndefined(headerCellContainer.parentElement) && headerCellContainer.parentElement.offsetHeight > 0) {
          element[parseInt(i.toString(), 10)].style.height = headerCellContainer.parentElement.offsetHeight + 'px';
        }
      }
      this.setHandlerHeight();
    };
    Resize.prototype.wireEvents = function () {
      sf.base.EventHandler.add(this.parent.getHeaderContent(), sf.base.Browser.touchStartEvent, this.resizeStart, this);
      sf.base.EventHandler.add(this.parent.getHeaderContent(), 'dblclick', this.callAutoFit, this);
    };
    Resize.prototype.unwireEvents = function () {
      sf.base.EventHandler.remove(this.parent.getHeaderContent(), sf.base.Browser.touchStartEvent, this.resizeStart);
      sf.base.EventHandler.remove(this.parent.getHeaderContent(), 'dblclick', this.callAutoFit);
    };
    Resize.prototype.getResizeHandlers = function () {
      return this.parent.options.frozenColumns ? [].slice.call(this.parent.getHeaderContent().querySelectorAll('.' + resizeClassList.root)) : [].slice.call(this.parent.getHeaderContent().querySelector('.e-table').querySelectorAll('.' + resizeClassList.root));
    };
    Resize.prototype.setHandlerHeight = function () {
      var element = [].slice.call(this.parent.getHeaderContent().querySelector('.e-table').querySelectorAll('.' + resizeClassList.suppress));
      for (var i = 0; i < element.length; i++) {
        var headerCellContainer = element[parseInt(i.toString(), 10)].parentElement;
        if (!sf.base.isNullOrUndefined(headerCellContainer) && !sf.base.isNullOrUndefined(headerCellContainer.parentElement)) {
          element[parseInt(i.toString(), 10)].style.height = headerCellContainer.parentElement.offsetHeight + 'px';
        }
      }
    };
    Resize.prototype.callAutoFit = function (e) {
      if (e.target.classList.contains('e-rhandler')) {
        var col = this.getTargetColumn(e);
        if (col.columns) {
          return;
        }
        this.resizeColumn(col.field, this.parent.getNormalizedColumnIndex(col.uid), col.uid, this.isDblClk ? true : false);
        var header = sf.base.closest(e.target, resizeClassList.header);
        header.classList.add('e-resized');
      }
    };
    Resize.prototype.resizeStart = function (e) {
      if (e.target.classList.contains('e-rhandler')) {
        var columnList = [];
        var columnData = {};
        if (this.parent.isMacSafariBrowser()) {
          document.body.classList.add('e-prevent-select'); // This is being used to prevent text highlighting issues in Mac OS Safari browser.
        }
        if (!this.helper) {
          if (this.getScrollBarWidth() === 0) {
            if (this.parent.options.allowGrouping) {
              for (var i = 0; i < this.parent.options.groupCount; i++) {
                this.widthService.setWidth('30px', i);
              }
            }
            var refreshedColumns = this.refreshColumnWidth();
            for (var _i = 0, refreshedColumns_1 = refreshedColumns; _i < refreshedColumns_1.length; _i++) {
              var col = refreshedColumns_1[_i];
              this.widthService.setColumnWidth(col, null, null, false);
              columnData = {
                width: col.width.toString(),
                columnUid: col.uid
              };
              columnList.push(columnData);
            }
            this.widthService.setWidthToTable();
          }
          this.refreshStackedColumnWidth();
          this.element = e.target;
          //TODO: rowheight
          // if (this.parent.getVisibleFrozenColumns()) {
          //     let mtbody: Element = this.parent.getContent().querySelector('.e-movablecontent').querySelector('tbody');
          //     let ftbody: Element = this.parent.getContent().querySelector('.e-frozencontent').querySelector('tbody');
          //     let mtr: NodeListOf<HTMLElement> = mtbody.querySelectorAll('tr');
          //     let ftr: NodeListOf<HTMLElement> = ftbody.querySelectorAll('tr');
          //     for (let i: number = 0; i < mtr.length; i++) {
          //         if (this.parent.rowHeight) {
          //             mtr[i].style.height = this.parent.rowHeight + 'px';
          //             ftr[i].style.height = this.parent.rowHeight + 'px';
          //         } else {
          //             mtr[i].style.removeProperty('height');
          //             ftr[i].style.removeProperty('height');
          //         }
          //     }
          // }
          this.parentElementWidth = this.parent.element.getBoundingClientRect().width;
          this.appendHelper();
          this.column = this.getTargetColumn(e);
          this.pageX = this.getPointX(e);
          if (this.column.freeze === 'Right') {
            if (this.parent.options.enableRtl) {
              this.minMove = (this.column.minWidth ? parseFloat(this.column.minWidth.toString()) : 0) - parseFloat(sf.base.isNullOrUndefined(this.column.width) ? '' : this.column.width.toString());
            } else {
              this.minMove = parseFloat(sf.base.isNullOrUndefined(this.column.width) ? '' : this.column.width.toString()) - (this.column.minWidth ? parseFloat(this.column.minWidth.toString()) : 0);
            }
          } else if (this.parent.options.enableRtl) {
            this.minMove = parseFloat(this.column.width.toString()) - (this.column.minWidth ? parseFloat(this.column.minWidth.toString()) : 0);
          } else {
            this.minMove = (this.column.minWidth ? parseFloat(this.column.minWidth.toString()) : 0) - parseFloat(sf.base.isNullOrUndefined(this.column.width) ? '' : this.column.width.toString());
          }
          this.minMove += this.pageX;
        }
        if (sf.base.Browser.isDevice && !this.helper.classList.contains(resizeClassList.icon)) {
          this.helper.classList.add(resizeClassList.icon);
          sf.base.EventHandler.add(document, sf.base.Browser.touchStartEvent, this.removeHelper, this);
          sf.base.EventHandler.add(this.helper, sf.base.Browser.touchStartEvent, this.resizeStart, this);
        } else {
          // let args: ResizeArgs = {
          //     e: isBlazor() && !this.parent.isJsComponent ? null : e,
          //     column: this.column
          // };
          // this.parent.trigger(events.resizeStart, args, (args: ResizeArgs) => {
          //     if (args.cancel || this.parent.isEdit) {
          //         this.cancelResizeAction();
          //         return;
          //     }
          sf.base.EventHandler.add(document, sf.base.Browser.touchEndEvent, this.resizeEnd, this);
          this.parent.dotNetRef.invokeMethodAsync('ResizeStarted', {
            columnUid: this.column.uid,
            columnList: columnList
          });
          // });
        }
        if (this.parent.options.enableVirtualization && this.parent.options.frozenColumns) {
          this.parent.element.querySelector('.e-virtualtable').style.position = '';
        }
      }
    };
    /**
     * Prevents or allows resize actions based on the provided flag.
     *
     * @param {boolean} isCancel - Flag indicating whether to cancel the resize action.
     * @returns {void}
     * @hidden
     */
    Resize.prototype.preventResizeAction = function (isCancel) {
      if (isCancel) {
        this.cancelResizeAction();
      } else {
        sf.base.EventHandler.add(this.parent.element, sf.base.Browser.touchMoveEvent, this.resizing, this);
        this.updateCursor('add');
      }
    };
    Resize.prototype.cancelResizeAction = function (removeEvents) {
      if (removeEvents) {
        sf.base.EventHandler.remove(this.parent.element, sf.base.Browser.touchMoveEvent, this.resizing);
        sf.base.EventHandler.remove(document, sf.base.Browser.touchEndEvent, this.resizeEnd);
        this.updateCursor('remove');
      }
      if (sf.base.Browser.isDevice) {
        sf.base.EventHandler.remove(document, sf.base.Browser.touchStartEvent, this.removeHelper);
        sf.base.EventHandler.remove(this.helper, sf.base.Browser.touchStartEvent, this.resizeStart);
      }
      sf.base.detach(this.helper);
      this.refresh();
    };
    Resize.prototype.getWidth = function (width, minWidth, maxWidth) {
      if (minWidth && width < minWidth) {
        return minWidth;
      } else if (maxWidth && width > maxWidth) {
        return maxWidth;
      } else {
        return width;
      }
    };
    Resize.prototype.updateResizeEleHeight = function () {
      var elements = [].slice.call(this.parent.getHeaderContent().querySelectorAll('.e-rhandler'));
      for (var i = 0; i < elements.length; i++) {
        elements[parseInt(i.toString(), 10)].style.height = elements[parseInt(i.toString(), 10)].parentElement.parentElement.offsetHeight + 'px';
      }
    };
    Resize.prototype.getColData = function (column, mousemove) {
      return {
        width: parseFloat(sf.base.isNullOrUndefined(this.widthService.getWidth(column)) || this.widthService.getWidth(column) === 'auto' ? '0' : this.widthService.getWidth(column).toString()) + mousemove,
        minWidth: column.minWidth ? parseFloat(column.minWidth.toString()) : null,
        maxWidth: column.maxWidth ? parseFloat(column.maxWidth.toString()) : null
      };
    };
    Resize.prototype.resizing = function (e) {
      if (sf.base.isNullOrUndefined(this.column)) {
        return;
      }
      var offsetWidth = 0;
      if (sf.base.isNullOrUndefined(this.column)) {
        offsetWidth = parentsUntil(this.element, 'th').offsetWidth;
      }
      if (this.parent.options.allowTextWrap) {
        this.updateResizeEleHeight();
        this.setHelperHeight();
        this.parent.scrollModule.refresh();
      }
      var pageX = this.getPointX(e);
      var mousemove = this.parent.options.enableRtl ? -(pageX - this.pageX) : pageX - this.pageX;
      if (this.column.freeze === 'Right' && this.column.isFrozen) {
        mousemove = this.parent.options.enableRtl ? pageX - this.pageX : this.pageX - pageX;
      }
      var colData = this.getColData(this.column, mousemove);
      if (!colData.width) {
        colData.width = sf.base.closest(this.element, 'th').offsetWidth;
      }
      var width = this.getWidth(colData.width, colData.minWidth, colData.maxWidth);
      if (this.column.freeze !== 'Right' && (!this.parent.options.enableRtl && this.minMove >= pageX - 10 || this.parent.options.enableRtl && this.minMove <= pageX + 10) || this.column.freeze === 'Right' && (this.parent.options.enableRtl && this.minMove >= pageX - 10 || !this.parent.options.enableRtl && this.minMove <= pageX + 10)) {
        width = this.column.minWidth ? parseFloat(this.column.minWidth.toString()) : 10;
        this.pageX = pageX = this.minMove;
      }
      if (width !== parseFloat(sf.base.isNullOrUndefined(this.column.width) || this.column.width === 'auto' ? offsetWidth.toString() : this.column.width.toString())) {
        this.pageX = pageX;
        this.column.width = sf.base.formatUnit(width);
        // let args: ResizeArgs = {
        //     e: e,
        //     column: this.column
        // };
        //this.parent.trigger(events.onResize, args);
        // if (args.cancel) {
        //     this.cancelResizeAction(true);
        //     return;
        // }
        var columns = [this.column];
        var finalColumns = [this.column];
        if (this.column.columns) {
          columns = this.getSubColumns(this.column, []);
          columns = this.calulateColumnsWidth(columns, false, mousemove);
          finalColumns = this.calulateColumnsWidth(columns, true, mousemove);
        }
        for (var _i = 0, finalColumns_1 = finalColumns; _i < finalColumns_1.length; _i++) {
          var col = finalColumns_1[_i];
          this.widthService.setColumnWidth(col, null, 'resize');
        }
        this.updateHelper();
      }
      this.refreshResizeFrozenColumns();
      this.isDblClk = false;
    };
    Resize.prototype.refreshResizeFrozenColumns = function () {
      var _this = this;
      var translateX = this.parent.options.enableColumnVirtualization ? this.getTranslateX() : 0;
      if (this.column.freeze === 'Left' && this.column.isFrozen || this.parent.options.frozenLeftColumnsCount === 0 && this.parent.options.frozenRightColumnsCount === 0 && this.column.index < this.parent.options.frozenColumns) {
        var width_1 = this.parent.getIndentCount() * 30;
        var columns = this.parent.getColumns(true).filter(function (col) {
          return col.freeze === 'Left' && col.isFrozen || _this.parent.options.frozenLeftCount === 0 && _this.parent.options.frozenRightCount === 0 && col.index < _this.parent.options.frozenColumns;
        });
        this.frozenHeaderRefresh('Left');
        for (var i = 0; i < columns.length; i++) {
          if (columns[parseInt(i.toString(), 10)].index > this.column.index) {
            var elements = [];
            if (this.parent.options.frozenRows) {
              elements = [].slice.call(this.parent.getHeaderContent().querySelectorAll('td[aria-colindex="' + (i + 1) + '"]')).concat([].slice.call(this.parent.getContent().querySelectorAll('td[aria-colindex="' + (i + 1) + '"]')));
            } else {
              elements = [].slice.call(this.parent.getContent().querySelectorAll('td[aria-colindex="' + (i + 1) + '"]'));
            }
            elements.filter(function (cell) {
              applyStickyLeftRightPosition(cell, width_1 - translateX, _this.parent.options.enableRtl, 'Left');
            });
            if (this.parent.options.enableColumnVirtualization) {
              columns[parseInt(i.toString(), 10)].valueX = width_1;
            }
          }
          if (columns[parseInt(i.toString(), 10)].visible) {
            columns[parseInt(i.toString(), 10)].translateLeftRightValue = width_1;
            width_1 += parseFloat(columns[parseInt(i.toString(), 10)].width.toString());
          }
        }
        this.refreshResizeFixedCols('Left');
      }
      if (this.column.freeze === 'Right' && this.column.isFrozen) {
        var width_2 = 0;
        var columns = this.parent.getColumns(true);
        this.frozenHeaderRefresh('Right');
        var columnsRight = columns.filter(function (col) {
          return col.freeze === 'Right' && col.isFrozen;
        });
        for (var i = columns.length - 1; i >= columns.length - columnsRight.length; i--) {
          var elements = [];
          if (this.parent.options.frozenRows) {
            elements = [].slice.call(this.parent.getHeaderContent().querySelectorAll('td[aria-colindex="' + (i + 1) + '"]')).concat([].slice.call(this.parent.getContent().querySelectorAll('td[aria-colindex="' + (i + 1) + '"]')));
          } else {
            elements = [].slice.call(this.parent.getContent().querySelectorAll('td[aria-colindex="' + (i + 1) + '"]'));
          }
          elements.filter(function (cell) {
            applyStickyLeftRightPosition(cell, width_2 + translateX, _this.parent.options.enableRtl, 'Right');
          });
          if (this.parent.options.enableColumnVirtualization) {
            columns[parseInt(i.toString(), 10)].valueX = width_2;
          }
          if (columns[parseInt(i.toString(), 10)].visible) {
            columns[parseInt(i.toString(), 10)].translateLeftRightValue = width_2;
            width_2 = width_2 + parseFloat(columns[parseInt(i.toString(), 10)].width.toString());
          }
        }
        this.refreshResizeFixedCols('Right');
      }
      if (this.column.isFrozen && this.column.freeze === 'Fixed') {
        this.refreshResizeFixedCols('Left');
        this.refreshResizeFixedCols('Right');
        this.frozenHeaderRefresh('Left');
        this.frozenHeaderRefresh('Right');
      }
    };
    Resize.prototype.frozenHeaderRefresh = function (pos) {
      var _this = this;
      var translateX = this.parent.options.enableColumnVirtualization ? this.getTranslateX() : 0;
      if (pos === 'Left') {
        var tr = [].slice.call(this.parent.getHeaderContent().querySelector('thead').querySelectorAll('tr'));
        for (var i = 0; i < tr.length; i++) {
          var th = [].slice.call(tr[parseInt(i.toString(), 10)].querySelectorAll('.e-leftfreeze,.e-fixedfreeze'));
          var _loop_2 = function _loop_2(j) {
            var node = th[parseInt(j.toString(), 10)];
            if (node.classList.contains('e-rowdragheader') || node.classList.contains('e-detailheadercell') || node.classList.contains('e-grouptopleftcell')) {
              return "continue";
            }
            var column = this_2.getParticularColumn(node);
            var cols = this_2.parent.getColumns(true);
            var width = 0;
            var summarycell = [];
            if (this_2.parent.options.aggregatesCount && this_2.parent.getFooterContent()) {
              if (this_2.parent.getFooterContent().querySelectorAll('.e-summaryrow').length) {
                var summaryRows = [].slice.call(this_2.parent.getFooterContent().querySelectorAll('.e-summaryrow'));
                summaryRows.filter(function (row) {
                  summarycell.push(row.querySelector('[e-mappinguid="' + column.uid + '"]'));
                });
              }
              summarycell = summarycell.concat([].slice.call(this_2.parent.getFooterContent().querySelectorAll('[e-mappinguid="' + column.uid + '"]')));
            }
            if (node.classList.contains('e-fixedfreeze')) {
              if (this_2.parent.getFrozenLeftColumn().length) {
                width = this_2.parent.getIndentCount() * 30;
              }
              for (var w = 0; w < cols.length; w++) {
                if (column.index > cols[parseInt(w.toString(), 10)].index) {
                  if (column.uid === cols[parseInt(w.toString(), 10)].uid) {
                    break;
                  }
                  if ((cols[parseInt(w.toString(), 10)].freeze === 'Left' || cols[parseInt(w.toString(), 10)].freeze === 'Fixed') && cols[parseInt(w.toString(), 10)].isFrozen) {
                    if (cols[parseInt(w.toString(), 10)].visible) {
                      width += parseInt(cols[parseInt(w.toString(), 10)].width.toString(), 10);
                    }
                  }
                }
              }
              if (summarycell && summarycell.length) {
                summarycell.filter(function (cell) {
                  applyStickyLeftRightPosition(cell, width - translateX, _this.parent.options.enableRtl, 'Left');
                });
              }
              applyStickyLeftRightPosition(node, (width === 0 ? width : width - 1) - translateX, this_2.parent.options.enableRtl, 'Left');
            } else {
              width = this_2.parent.getIndentCount() * 30;
              if (column.index === 0) {
                if (summarycell && summarycell.length) {
                  summarycell.filter(function (cell) {
                    applyStickyLeftRightPosition(cell, width - translateX, _this.parent.options.enableRtl, 'Left');
                  });
                }
                applyStickyLeftRightPosition(node, width - translateX, this_2.parent.options.enableRtl, 'Left');
                if (this_2.parent.options.enableColumnVirtualization) {
                  column.valueX = width;
                }
              } else {
                for (var k = 0; k < cols.length; k++) {
                  if (column.index < cols[parseInt(k.toString(), 10)].index || column.uid === cols[parseInt(k.toString(), 10)].uid) {
                    break;
                  }
                  if (cols[parseInt(k.toString(), 10)].visible) {
                    width += parseInt(cols[parseInt(k.toString(), 10)].width.toString(), 10);
                  }
                }
                if (summarycell && summarycell.length) {
                  summarycell.filter(function (cell) {
                    applyStickyLeftRightPosition(cell, width - translateX, _this.parent.options.enableRtl, 'Left');
                  });
                }
                applyStickyLeftRightPosition(node, width - translateX, this_2.parent.options.enableRtl, 'Left');
                if (this_2.parent.options.enableColumnVirtualization) {
                  column.valueX = width;
                }
              }
            }
          };
          var this_2 = this;
          for (var j = 0; j < th.length; j++) {
            _loop_2(j);
          }
        }
      }
      if (pos === 'Right') {
        var tr = [].slice.call(this.parent.getHeaderContent().querySelector('thead').querySelectorAll('tr'));
        for (var i = 0; i < tr.length; i++) {
          var th = [].slice.call(tr[parseInt(i.toString(), 10)].querySelectorAll('.e-rightfreeze, .e-fixedfreeze'));
          var _loop_3 = function _loop_3(j) {
            var node = th[parseInt(j.toString(), 10)];
            var column = this_3.getParticularColumn(node);
            var cols = this_3.parent.getColumns(true);
            var width = 0;
            var summarycell = [];
            if (this_3.parent.options.aggregatesCount && this_3.parent.getFooterContent()) {
              if (this_3.parent.getContent().querySelectorAll('.e-summaryrow').length) {
                var summaryRows = [].slice.call(this_3.parent.getContent().querySelectorAll('.e-summaryrow'));
                summaryRows.filter(function (row) {
                  summarycell.push(row.querySelector('[e-mappinguid="' + column.uid + '"]'));
                });
              }
              summarycell = summarycell.concat([].slice.call(this_3.parent.getFooterContent().querySelectorAll('[e-mappinguid="' + column.uid + '"]')));
            }
            if (node.classList.contains('e-fixedfreeze')) {
              width = 0;
              for (var w = cols.length - 1; w >= 0; w--) {
                if (column.index < cols[parseInt(w.toString(), 10)].index) {
                  if ((cols[parseInt(w.toString(), 10)].freeze === 'Right' || cols[parseInt(w.toString(), 10)].freeze === 'Fixed') && cols[parseInt(w.toString(), 10)].isFrozen) {
                    if (cols[parseInt(w.toString(), 10)].visible) {
                      width += parseFloat(cols[parseInt(w.toString(), 10)].width.toString());
                    }
                  }
                }
              }
              if (summarycell.length) {
                summarycell.filter(function (cell) {
                  applyStickyLeftRightPosition(cell, width + translateX, _this.parent.options.enableRtl, 'Right');
                });
              }
              applyStickyLeftRightPosition(node, width + translateX, this_3.parent.options.enableRtl, 'Right');
            } else {
              width = 0;
              for (var k = cols.length - 1; k >= 0; k--) {
                if (column.index > cols[parseInt(k.toString(), 10)].index || column.uid === cols[parseInt(k.toString(), 10)].uid) {
                  break;
                }
                if (cols[parseInt(k.toString(), 10)].visible) {
                  width += parseInt(cols[parseInt(k.toString(), 10)].width.toString(), 10);
                }
              }
              if (summarycell.length) {
                summarycell.filter(function (cell) {
                  applyStickyLeftRightPosition(cell, width + translateX, _this.parent.options.enableRtl, 'Right');
                });
              }
              applyStickyLeftRightPosition(node, width + translateX, this_3.parent.options.enableRtl, 'Right');
              if (this_3.parent.options.enableColumnVirtualization) {
                column.valueX = width;
              }
            }
          };
          var this_3 = this;
          for (var j = th.length - 1; j >= 0; j--) {
            _loop_3(j);
          }
        }
      }
    };
    Resize.prototype.refreshResizeFixedCols = function (pos) {
      var cols = this.parent.getColumns(true);
      var translateX = this.parent.options.enableColumnVirtualization ? this.getTranslateX() : 0;
      var th = [].slice.call(this.parent.getHeaderContent().querySelector('tbody').querySelectorAll('.e-fixedfreeze')).concat([].slice.call(this.parent.getContent().querySelectorAll('.e-fixedfreeze')));
      for (var i = 0; i < th.length; i++) {
        var node = th[parseInt(i.toString(), 10)];
        var column = void 0;
        if (node.classList.contains('e-summarycell')) {
          var uid = node.getAttribute('e-mappinguid');
          column = this.parent.getColumnByUid(uid);
        } else {
          var index = parseInt(node.getAttribute('aria-colindex'), 10) - 1;
          column = cols[parseInt(index.toString(), 10)];
        }
        var width = 0;
        if (pos === 'Left') {
          if (this.parent.getFrozenLeftColumn().length) {
            width = this.parent.getIndentCount() * 30;
          }
          for (var j = 0; j < cols.length; j++) {
            if (column.index > cols[parseInt(j.toString(), 10)].index) {
              if (column.uid === cols[parseInt(j.toString(), 10)].uid) {
                break;
              }
              if ((cols[parseInt(j.toString(), 10)].freeze === 'Left' || cols[parseInt(j.toString(), 10)].freeze === 'Fixed') && cols[parseInt(j.toString(), 10)].isFrozen) {
                if (cols[parseInt(j.toString(), 10)].visible) {
                  width += parseFloat(cols[parseInt(j.toString(), 10)].width.toString());
                }
              }
            }
          }
          applyStickyLeftRightPosition(node, (width === 0 ? width : width - 1) - translateX, this.parent.options.enableRtl, 'Left');
        }
        if (pos === 'Right') {
          width = 0;
          for (var j = cols.length - 1; j >= 0; j--) {
            if (column.uid === cols[parseInt(j.toString(), 10)].uid) {
              break;
            }
            if ((cols[parseInt(j.toString(), 10)].freeze === 'Right' || cols[parseInt(j.toString(), 10)].freeze === 'Fixed') && cols[parseInt(j.toString(), 10)].isFrozen) {
              if (cols[parseInt(j.toString(), 10)].visible) {
                width += parseFloat(cols[parseInt(j.toString(), 10)].width.toString());
              }
            }
          }
          applyStickyLeftRightPosition(node, width + translateX, this.parent.options.enableRtl, 'Right');
        }
      }
    };
    Resize.prototype.getParticularColumn = function (node) {
      var uid = node.classList.contains('e-filterbarcell') ? node.getAttribute('e-mappinguid') : node.querySelector('[e-mappinguid]').getAttribute('e-mappinguid');
      return this.parent.getColumnByUid(uid);
    };
    Resize.prototype.getTranslateX = function () {
      if (!this.parent.options.enableColumnVirtualization) {
        return 0;
      }
      var element = this.parent.getContent().getElementsByClassName('e-virtualtable')[0];
      var startIndex = element.style.transform.indexOf('(') + 1;
      var endIndex = element.style.transform.indexOf('p');
      var translateXValue = parseInt(element.style.transform.slice(startIndex, endIndex), 10);
      return translateXValue;
    };
    Resize.prototype.calulateColumnsWidth = function (columns, isUpdate, mousemove) {
      var finalColumns = [];
      for (var _i = 0, columns_1 = columns; _i < columns_1.length; _i++) {
        var col = columns_1[_i];
        var totalWidth = 0;
        for (var i = 0; i < columns.length; i++) {
          totalWidth += parseFloat(columns[parseInt(i.toString(), 10)].width.toString());
        }
        var colData = this.getColData(col, parseFloat(col.width) * mousemove / totalWidth);
        var colWidth = this.getWidth(colData.width, colData.minWidth, colData.maxWidth);
        if (colWidth !== parseFloat(col.width.toString())) {
          if (isUpdate) {
            col.width = sf.base.formatUnit(colWidth < 1 ? 1 : colWidth);
          }
          finalColumns.push(col);
        }
      }
      return finalColumns;
    };
    Resize.prototype.getSubColumns = function (column, subColumns) {
      for (var _i = 0, _a = column.columns; _i < _a.length; _i++) {
        var col = _a[_i];
        if (col.visible !== false && col.allowResizing) {
          if (col.columns) {
            this.getSubColumns(col, subColumns);
          } else {
            subColumns.push(col);
          }
        }
      }
      return subColumns;
    };
    Resize.prototype.resizeEnd = function (e) {
      if (!this.helper) {
        return;
      }
      if (this.parent.isMacSafariBrowser()) {
        document.body.classList.remove('e-prevent-select');
      }
      sf.base.EventHandler.remove(this.parent.element, sf.base.Browser.touchMoveEvent, this.resizing);
      sf.base.EventHandler.remove(document, sf.base.Browser.touchEndEvent, this.resizeEnd);
      this.updateCursor('remove');
      sf.base.detach(this.helper);
      // let args: ResizeArgs = {
      //     e: isBlazor() && !this.parent.isJsComponent ? null : e,
      //     column: this.column
      // };
      // let cTable: HTMLElement = content.querySelector('.e-movablecontent') ? content.querySelector('.e-movablecontent') : content;
      // if (cTable.scrollHeight >= cTable.clientHeight) {
      //     this.parent.scrollModule.setPadding();
      //     cTable.style.overflowY = 'scroll';
      // }
      //this.parent.trigger(events.resizeStop, args);
      sf.base.closest(this.element, '.e-headercell').classList.add('e-resized');
      if (parentsUntil(this.element, 'e-frozenheader')) {
        this.isFrozenColResized = true;
      } else {
        this.isFrozenColResized = false;
      }
      if (this.parent.options.frozenColumns) {
        this.parent.freezeModule.refreshRowHeight();
        this.parent.freezeModule.setFrozenHeight();
      }
      if (this.parent.options.allowTextWrap) {
        this.updateResizeEleHeight();
      }
      var width = this.column.width.toString();
      width = width.replace('px', '');
      this.parent.dotNetRef.invokeMethodAsync('ColumnWidthChanged', {
        width: width,
        columnUid: this.column.uid,
        allowStopEvent: !this.isDblClk,
        tableWidth: this.tableWidth,
        leftFrozenTableWidth: this.leftFrozenTableWidth,
        rightFrozenTableWidth: this.rightFrozenTableWidth
      }, false);
      if (!this.parent.options.enableColumnVirtualization) {
        this.parent.updateColumnWidth(this.parent.columnModel);
      }
      this.parent.addTableBorderClass();
      this.refresh();
      this.doubleTapEvent(e);
      this.isDblClk = true;
    };
    Resize.prototype.getPointX = function (e) {
      if (e.touches && e.touches.length) {
        return e.touches[0].pageX;
      } else {
        return e.pageX;
      }
    };
    Resize.prototype.refreshColumnWidth = function () {
      var columns = this.parent.getColumns();
      var headerCells = [].slice.apply(this.parent.getHeaderContent().querySelectorAll('th.e-headercell'));
      for (var _i = 0, headerCells_1 = headerCells; _i < headerCells_1.length; _i++) {
        var ele = headerCells_1[_i];
        for (var _a = 0, columns_2 = columns; _a < columns_2.length; _a++) {
          var column = columns_2[_a];
          if (ele.querySelector('[e-mappinguid]') && ele.querySelector('[e-mappinguid]').getAttribute('e-mappinguid') === column.uid && column.visible) {
            column.width = ele.getBoundingClientRect().width ? ele.getBoundingClientRect().width : column.width;
            break;
          }
          if (!column.visible && typeof column.width === 'string' && column.width.includes('px')) {
            column.width = Number(column.width.replace('px', ''));
          }
        }
      }
      return columns;
    };
    Resize.prototype.refreshStackedColumnWidth = function () {
      for (var _i = 0, _a = this.parent.getStackedColumns(this.parent.options.columns); _i < _a.length; _i++) {
        var stackedColumn = _a[_i];
        stackedColumn.width = this.getStackedWidth(stackedColumn, 0);
      }
    };
    Resize.prototype.getStackedWidth = function (column, width) {
      for (var _i = 0, _a = column.columns; _i < _a.length; _i++) {
        var col = _a[_i];
        if (col.visible !== false) {
          if (col.columns) {
            width += this.getStackedWidth(col, 0);
          } else {
            width += col.width;
          }
        }
      }
      return width;
    };
    Resize.prototype.getTargetColumn = function (e) {
      var cell = sf.base.closest(e.target, resizeClassList.header);
      cell = cell.querySelector('.e-headercelldiv') || cell.querySelector('.e-stackedheadercelldiv');
      var uid = cell.getAttribute('e-mappinguid');
      return this.parent.getColumnByUid(uid);
    };
    Resize.prototype.updateCursor = function (action) {
      var headerRows = [].slice.call(this.parent.getHeaderContent().querySelectorAll('th'));
      headerRows.push(this.parent.element);
      for (var _i = 0, headerRows_1 = headerRows; _i < headerRows_1.length; _i++) {
        var row = headerRows_1[_i];
        row.classList["".concat(action)](resizeClassList.cursor);
      }
    };
    Resize.prototype.refresh = function () {
      this.column = null;
      this.pageX = null;
      this.element = null;
      this.helper = null;
    };
    Resize.prototype.appendHelper = function () {
      this.helper = sf.base.createElement('div', {
        className: resizeClassList.helper
      });
      this.parent.element.appendChild(this.helper);
      this.setHelperHeight();
    };
    Resize.prototype.setHelperHeight = function () {
      var height = this.parent.getContent().offsetHeight - (this.parent.options.frozenColumns ? 0 : this.getScrollBarWidth());
      var rect = sf.base.closest(this.element, resizeClassList.header);
      var tr = [].slice.call(this.parent.getHeaderContent().querySelectorAll('tr'));
      for (var i = tr.indexOf(rect.parentElement); i < tr.length; i++) {
        height += tr[parseInt(i.toString(), 10)].offsetHeight;
      }
      var pos = this.calcPos(rect);
      if (parentsUntil(rect, 'e-frozen-right-header')) {
        pos.left += this.parent.options.enableRtl ? rect.offsetWidth - 2 : 0 - 1;
      } else {
        pos.left += this.parent.options.enableRtl ? 0 - 1 : rect.offsetWidth - 2;
      }
      this.helper.style.cssText = 'height: ' + height + 'px; top: ' + pos.top + 'px; left:' + Math.floor(pos.left) + 'px;';
    };
    Resize.prototype.getScrollBarWidth = function (height) {
      var ele = this.parent.getContent();
      return ele.scrollHeight > ele.clientHeight && height || ele.scrollWidth > ele.clientWidth ? getScrollBarWidth() : 0;
    };
    Resize.prototype.removeHelper = function (e) {
      var cls = e.target.classList;
      if (!(cls.contains(resizeClassList.root) || cls.contains(resizeClassList.icon)) && this.helper) {
        sf.base.EventHandler.remove(document, sf.base.Browser.touchStartEvent, this.removeHelper);
        sf.base.EventHandler.remove(this.helper, sf.base.Browser.touchStartEvent, this.resizeStart);
        sf.base.detach(this.helper);
        this.refresh();
      }
    };
    Resize.prototype.updateHelper = function () {
      var rect = sf.base.closest(this.element, resizeClassList.header);
      if (sf.base.isNullOrUndefined(rect)) {
        return;
      }
      var left = Math.floor(this.calcPos(rect).left + (this.parent.options.enableRtl ? 0 - 1 : rect.offsetWidth - 2));
      var borderWidth = 2; // to maintain the helper inside of grid element.
      if (parentsUntil(rect, 'e-frozen-right-header')) {
        left = Math.floor(this.calcPos(rect).left + (this.parent.options.enableRtl ? rect.offsetWidth - 2 : 0 - 1));
      }
      if (left > this.parentElementWidth) {
        left = this.parentElementWidth - borderWidth;
      }
      if (this.parent.options.frozenColumns) {
        var table = sf.base.closest(rect, '.e-table');
        var fLeft = table.offsetLeft;
        if (left < fLeft) {
          left = fLeft;
        }
      }
      this.helper.style.left = left + 'px';
    };
    Resize.prototype.calcPos = function (elem) {
      var parentOffset = {
        top: 0,
        left: 0
      };
      if (sf.base.isNullOrUndefined(elem)) {
        return parentOffset;
      }
      var offset = elem.getBoundingClientRect();
      var doc = elem.ownerDocument;
      var offsetParent = parentsUntil(elem, 'e-grid') || doc.documentElement;
      while (offsetParent && (offsetParent === doc.body || offsetParent === doc.documentElement) && offsetParent.style.position === 'static') {
        offsetParent = offsetParent.parentNode;
      }
      if (offsetParent && offsetParent !== elem && offsetParent.nodeType === 1) {
        parentOffset = offsetParent.getBoundingClientRect();
      }
      return {
        top: offset.top - parentOffset.top,
        left: offset.left - parentOffset.left
      };
    };
    Resize.prototype.doubleTapEvent = function (e) {
      if (this.getUserAgent() && this.isDblClk) {
        if (!this.tapped) {
          // eslint-disable-next-line @typescript-eslint/no-explicit-any
          this.tapped = setTimeout(this.timeoutHandler.bind(this), 300);
        } else {
          clearTimeout(this.tapped);
          this.callAutoFit(e);
          this.tapped = null;
        }
      }
    };
    Resize.prototype.getUserAgent = function () {
      var userAgent = sf.base.Browser.userAgent.toLowerCase();
      return /iphone|ipod|ipad/.test(userAgent);
    };
    Resize.prototype.timeoutHandler = function () {
      this.tapped = null;
    };
    return Resize;
  }();

  // eslint-disable-next-line valid-jsdoc, jsdoc/require-param
  /**
   *
   * The `Group` module is used to handle group action.
   */
  var Group = /** @class */function () {
    /**
     *
     * Constructor for Grid group module
     *
     * @param {SfGrid} parent - Specifies that parent is an optional parameter of type SfGrid
     * @hidden
     */
    function Group(parent) {
      var _this = this;
      this.visualElement = sf.base.createElement('div', {
        className: 'e-cloneproperties e-dragclone e-gdclone',
        styles: 'line-height:23px',
        attrs: {
          action: 'grouping'
        }
      });
      /**
       *
       * Helper function used for handling group action.
       *
       * @param {MouseEvent} e - The MouseEvent object.
       * @param {HTMLElement} e.sender - The sender element of the event.
       * @returns {void}
       */
      this.helper = function (e) {
        var gObj = _this.parent;
        var target = e.sender.target;
        var element = target.classList.contains('e-groupheadercell') ? target : parentsUntil(target, 'e-groupheadercell');
        if (!element || !target.classList.contains('e-drag') && _this.parent.options.groupReordering) {
          return false;
        }
        var groupingContainer = element.closest('.e-groupdroparea') || element.parentElement;
        if (!groupingContainer) {
          return false;
        }
        var groupingFieldIndex = Array.prototype.indexOf.call(groupingContainer.querySelectorAll('.e-groupheadercell'), element);
        if (groupingFieldIndex === -1) {
          return false;
        }
        var initGroupingFields = gObj.options.initGroupingField;
        var groupingField = initGroupingFields[Number(groupingFieldIndex)];
        _this.column = gObj.getColumnByField(groupingField);
        if (!_this.column) {
          return false;
        }
        _this.visualElement.textContent = element.textContent;
        _this.visualElement.style.width = element.offsetWidth + 2 + 'px';
        _this.visualElement.style.height = element.offsetHeight + 2 + 'px';
        _this.visualElement.setAttribute('e-mappinguid', _this.column.uid);
        gObj.element.appendChild(_this.visualElement);
        return _this.visualElement;
      };
      this.dragStart = function (e) {
        _this.parent.element.classList.add('e-ungroupdrag');
        document.body.classList.add('e-prevent-select');
        e.bindEvents(e.dragElement);
      };
      this.drag = function (e) {
        // if (this.groupSettings.allowReordering) {
        //     this.animateDropper(e);
        // }
        var target = e.target;
        var cloneElement = _this.parent.element.querySelector('.e-cloneproperties');
        // this.parent.trigger(events.columnDrag, { target: target, draggableType: 'headercell', column: this.column });
        if (!_this.parent.options.groupReordering) {
          sf.base.classList(cloneElement, ['e-defaultcur'], ['e-notallowedcur']);
          if (!(parentsUntil(target, 'e-gridcontent') || parentsUntil(target, 'e-headercell'))) {
            sf.base.classList(cloneElement, ['e-notallowedcur'], ['e-defaultcur']);
          }
        }
      };
      this.dragStop = function (e) {
        document.body.classList.remove('e-prevent-select');
        _this.parent.element.classList.remove('e-ungroupdrag');
        var preventDrop = !(parentsUntil(e.target, 'e-gridcontent') || parentsUntil(e.target, 'e-gridheader'));
        // if (this.groupSettings.allowReordering && preventDrop) {     //TODO: reordering
        //     remove(e.helper);
        //     if (parentsUntil(e.target, 'e-groupdroparea')) {
        //         this.rearrangeGroup(e);
        //     } else if (!(parentsUntil(e.target, 'e-grid'))) {
        //         let field: string = this.parent.getColumnByUid(e.helper.getAttribute('e-mappinguid')).field;
        //         if (this.groupSettings.columns.indexOf(field) !== -1) {
        //             this.ungroupColumn(field);
        //         }
        //     }
        //     return;
        // } else
        if (preventDrop) {
          sf.base.remove(e.helper);
          return;
        }
      };
      //TODO: reordering
      // private animateDropper: Function = (e: { target: HTMLElement, event: MouseEventArgs, helper: Element }) => {
      //     let uid: string = this.parent.element.querySelector('.e-cloneproperties').getAttribute('e-mappinguid');
      //     let dragField: string = this.parent.getColumnByUid(uid).field;
      //     let parent: Element = parentsUntil(e.target, 'e-groupdroparea');
      //     let dropTarget: Element = parentsUntil(e.target, 'e-group-animator');
      //     // tslint:disable-next-line
      //     let grouped: string[] = [].slice.call(this.element.querySelectorAll('.e-groupheadercell'))
      //         .map((e: Element) => e.querySelector('div').getAttribute('ej-mappingname'));
      //     let cols: string[] = JSON.parse(JSON.stringify(grouped));
      //     if (dropTarget || parent) {
      //         if (dropTarget) {
      //             let dropField: string = dropTarget.querySelector('div[ej-mappingname]').getAttribute('ej-mappingname');
      //             let dropIndex: number = +(dropTarget.getAttribute('index'));
      //             if (dropField !== dragField) {
      //                 let dragIndex: number = cols.indexOf(dragField);
      //                 if (dragIndex !== -1) {
      //                     cols.splice(dragIndex, 1);
      //                 }
      //                 let flag: boolean = dropIndex !== -1 && dragIndex === dropIndex;
      //                 cols.splice(dropIndex + (flag ? 1 : 0), 0, dragField);
      //             }
      //         } else if (parent && cols.indexOf(dragField) === -1) {
      //             cols.push(dragField);
      //         }
      //         this.element.innerHTML = '';
      //         if (cols.length && !this.element.classList.contains('e-grouped')) {
      //             this.element.classList.add('e-grouped');
      //         }
      //         this.reorderingColumns = cols;
      //         for (let c: number = 0; c < cols.length; c++) {
      //             this.addColToGroupDrop(cols[c]);
      //         }
      //     } else {
      //         this.addLabel();
      //         this.removeColFromGroupDrop(dragField);
      //     }
      // }
      // private rearrangeGroup(e: { target: HTMLElement, event: MouseEventArgs, helper: Element }): void {
      //     this.sortRequired = false;
      //     this.updateModel();
      // }
      this.preventTouchOnWindow = function (e) {
        e.preventDefault();
      };
      this.drop = function (e) {
        var gObj = _this.parent;
        var column = gObj.getColumnByUid(e.droppedElement.getAttribute('e-mappinguid'));
        gObj.element.querySelector('.e-groupdroparea').classList.remove('e-hover');
        sf.base.remove(e.droppedElement);
        if (gObj.options.allowGrouping) {
          // eslint-disable-next-line @typescript-eslint/no-explicit-any
          sf.base.EventHandler.remove(window, 'touchmove', _this.preventTouchOnWindow);
        }
        _this.parent.element.querySelector('.e-groupdroparea').removeAttribute('aria-dropeffect');
        _this.parent.element.querySelector('[aria-grabbed=true]').setAttribute('aria-grabbed', 'false');
        if (sf.base.isNullOrUndefined(column) || column.allowGrouping === false || parentsUntil(gObj.getColumnHeaderByUid(column.uid), 'e-grid').getAttribute('id') !== gObj.element.getAttribute('id')) {
          return;
        }
        gObj.dotNetRef.invokeMethodAsync('GroupColumn', column.field, 'Group');
      };
      this.parent = parent;
      if (this.parent.options.allowGrouping && this.parent.options.showDropArea) {
        this.initDragAndDrop();
      }
    }
    Group.prototype.columnDrag = function (e) {
      if (this.parent.options.groupReordering) ;
      var gObj = this.parent;
      var cloneElement = gObj.element.querySelector('.e-cloneproperties');
      var column = gObj.getColumnByUid(cloneElement.getAttribute('e-mappinguid'));
      if (parentsUntil(e.target, 'e-groupdroparea')) {
        sf.base.classList(cloneElement, ['e-defaultcur'], ['e-notallowedcur']);
      }
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      sf.base.EventHandler.add(window, 'touchmove', this.preventTouchOnWindow);
      if (!parentsUntil(e.target, 'e-groupdroparea') && !(gObj.options.allowReordering && parentsUntil(e.target, 'e-headercell')) || !column.allowGrouping && parentsUntil(e.target, 'e-groupdroparea')) {
        sf.base.classList(cloneElement, ['e-notallowedcur'], ['e-defaultcur']);
      }
      if (gObj.options.showDropArea) {
        if (e.target.classList.contains('e-groupdroparea')) {
          gObj.element.querySelector('.e-groupdroparea').classList.add('e-hover');
        } else {
          gObj.element.querySelector('.e-groupdroparea').classList.remove('e-hover');
        }
      }
    };
    Group.prototype.columnDragStart = function (e) {
      if (e.target.classList.contains('e-stackedheadercell')) {
        return;
      }
      var dropArea = this.parent.element.querySelector('.e-groupdroparea');
      if (dropArea) {
        dropArea.setAttribute('aria-dropeffect', 'copy');
      }
      var element = e.target.classList.contains('e-headercell') ? e.target : parentsUntil(e.target, 'e-headercell');
      if (sf.base.isNullOrUndefined(element)) {
        element = e.event.target.classList.contains('e-headercell') ? e.event.target : parentsUntil(e.event.target, 'e-headercell');
      }
      element.setAttribute('aria-grabbed', 'true');
    };
    Group.prototype.columnDrop = function (e) {
      var gObj = this.parent;
      if (e.droppedElement.getAttribute('action') === 'grouping') {
        var column = gObj.getColumnByUid(e.droppedElement.getAttribute('e-mappinguid'));
        if (sf.base.isNullOrUndefined(column) || column.allowGrouping === false || parentsUntil(gObj.getColumnHeaderByUid(column.uid), 'e-grid').getAttribute('id') !== gObj.element.getAttribute('id')) {
          return;
        }
        gObj.dotNetRef.invokeMethodAsync('GroupColumn', column.field, 'Ungroup');
      }
    };
    // private keyPressHandler(e: KeyboardEventArgs): void {
    //     let gObj: SfGrid = this.parent;
    //     if (e.action !== 'ctrlSpace' && (!this.groupSettings.columns.length ||
    //         ['altDownArrow', 'altUpArrow', 'ctrlDownArrow', 'ctrlUpArrow', 'enter'].indexOf(e.action) === -1)) {
    //         return;
    //     }
    //     e.preventDefault();
    //     switch (e.action) {
    //         case 'altDownArrow':
    //         case 'altUpArrow':
    //             let selected: number[] = gObj.allowSelection ? gObj.getSelectedRowIndexes() : [];
    //             if (selected.length) {
    //                 let rows: HTMLCollection = gObj.getContentTable().querySelector('tbody').children;
    //                 let dataRow: HTMLTableRowElement = gObj.getDataRows()[selected[selected.length - 1]] as HTMLTableRowElement;
    //                 let grpRow: Element;
    //                 for (let i: number = dataRow.rowIndex; i >= 0; i--) {
    //                     if (!rows[i].classList.contains('e-row') && !rows[i].classList.contains('e-detailrow')) {
    //                         grpRow = rows[i];
    //                         break;
    //                     }
    //                 }
    //                 this.expandCollapseRows(grpRow.querySelector(e.action === 'altUpArrow' ?
    //                     '.e-recordplusexpand' : '.e-recordpluscollapse'));
    //             }
    //             break;
    //         case 'ctrlDownArrow':
    //             this.expandAll();
    //             break;
    //         case 'ctrlUpArrow':
    //             this.collapseAll();
    //             break;
    //         case 'enter':
    //             if (this.parent.isEdit || (closest(e.target as Element, '#' + this.parent.element.id + '_searchbar') !== null)) { return; }
    //             let element: HTMLElement = this.focus.getFocusedElement();
    //             let row: Element = element ? element.parentElement.querySelector('[class^="e-record"]') : null;
    //             if (!row) { break; }
    //             this.expandCollapseRows(row);
    //             break;
    //         case 'ctrlSpace':
    //             let elem: HTMLElement = gObj.focusModule.currentInfo.element;
    //             if (elem && elem.classList.contains('e-headercell')) {
    //                 let column: Column = gObj.getColumnByUid(elem.firstElementChild.getAttribute('e-mappinguid'));
    //                 column.field && gObj.groupSettings.columns.indexOf(column.field) < 0 ?
    //                     this.groupColumn(column.field) : this.ungroupColumn(column.field);
    //             }
    //             break;
    //     }
    // }
    Group.prototype.initDragAndDrop = function () {
      this.initializeGHeaderDrop();
      this.initializeGHeaderDrag();
    };
    Group.prototype.initializeGHeaderDrag = function () {
      var ele = this.parent.element.querySelector('.e-groupdroparea');
      if (!ele) {
        return;
      }
      new sf.base.Draggable(ele, {
        dragTarget: this.parent.options.groupReordering ? '.e-drag' : '.e-groupheadercell',
        distance: this.parent.options.groupReordering ? -10 : 5,
        helper: this.helper,
        dragStart: this.dragStart,
        drag: this.drag,
        dragStop: this.dragStop,
        isPreventSelect: false
      });
    };
    Group.prototype.initializeGHeaderDrop = function () {
      var ele = this.parent.element.querySelector('.e-groupdroparea');
      if (!ele) {
        return;
      }
      new sf.base.Droppable(ele, {
        accept: '.e-dragclone',
        drop: this.drop
      });
    };
    /**
     *
     * For internal use only - Get the module name.
     *
     * @private
     * @returns {string} The name of the module.
     */
    Group.prototype.getModuleName = function () {
      return 'group';
    };
    Group.prototype.getGHeaderCell = function (field) {
      if (this.element && this.element.querySelector('[ej-mappingname="' + field + '"]')) {
        return this.element.querySelector('[ej-mappingname="' + field + '"]').parentElement;
      }
      return null;
    };
    return Group;
  }();

  /**
   * The `ColumnChooser` module is used to show or hide columns dynamically.
   */
  var ColumnChooser = /** @class */function () {
    function ColumnChooser(parent) {
      this.mediaCol = [];
      this.media = {};
      this.mediaBindInstance = {};
      this.mediaColVisibility = {};
      this.parent = parent;
    }
    /**
     * Get columnChooser Position.
     *
     * @returns {void}
     * @hidden
     */
    ColumnChooser.prototype.renderColumnChooser = function () {
      var dlgelement = this.parent.element.querySelector('#' + this.parent.element.id + '_ccdlg');
      var stickyHeader = this.parent.getHeaderContent().parentElement.classList.contains('e-sticky');
      if (stickyHeader) {
        dlgelement.classList.add('e-sticky');
      }
    };
    ColumnChooser.prototype.setMediaColumns = function (isResetPersistData) {
      var columns = this.parent.getColumns();
      var isPersistEnabled = this.parent.options.enablePersistence;
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      var persistData = isPersistEnabled ? JSON.parse(window.localStorage.getItem('grid' + this.parent.element.id)) : null;
      if (!sf.base.isNullOrUndefined(columns)) {
        for (var index = 0; index < columns.length; index++) {
          var column = columns[index];
          var isVisible = sf.base.isNullOrUndefined(column.visible) || column.visible || isResetPersistData;
          if (column.hideAtMedia !== '' && isVisible) {
            this.pushMediaColumn(column, index);
            if (isPersistEnabled && !sf.base.isNullOrUndefined(persistData) && !sf.base.isNullOrUndefined(this.mediaColVisibility[column.uid])) {
              persistData.columns[index].visible = this.mediaColVisibility[column.uid];
            }
          }
        }
        if (isPersistEnabled && !sf.base.isNullOrUndefined(persistData)) {
          window.localStorage.setItem('grid' + this.parent.element.id, JSON.stringify(persistData));
        }
        this.parent.dotNetRef.invokeMethodAsync('SetMediaColumnVisibility', {
          mediaColVisibility: this.mediaColVisibility
        });
        this.mediaColVisibility = {};
      }
    };
    ColumnChooser.prototype.windowResized = function () {
      // eslint-disable-next-line @typescript-eslint/no-this-alias
      var _this = this;
      setTimeout(function () {
        if (!sf.base.isNullOrUndefined(_this.mediaColVisibility) && Object.keys(_this.mediaColVisibility).length > 0) {
          _this.parent.dotNetRef.invokeMethodAsync('SetMediaColumnVisibility', {
            mediaColVisibility: _this.mediaColVisibility,
            invokedByMedia: true
          });
          _this.mediaColVisibility = {};
        }
      }, 100);
    };
    ColumnChooser.prototype.pushMediaColumn = function (col, index) {
      this.mediaCol.push(col);
      this.media[col.uid] = window.matchMedia(col.hideAtMedia);
      this.mediaQueryUpdate(index, this.media[col.uid]);
      this.mediaBindInstance[parseInt(index.toString(), 10)] = this.mediaQueryUpdate.bind(this, index);
      this.media[col.uid].addListener(this.mediaBindInstance[parseInt(index.toString(), 10)]);
    };
    ColumnChooser.prototype.mediaQueryUpdate = function (columnIndex, e) {
      var col = this.parent.getColumns()[parseInt(columnIndex.toString(), 10)];
      if (this.mediaCol.some(function (mediaColumn) {
        return mediaColumn.uid === col.uid;
      })) {
        this.mediaColVisibility[col.uid] = e.matches;
      }
    };
    ColumnChooser.prototype.updateMediaColumns = function (mediaColumnsUid) {
      var keys = Object.keys(mediaColumnsUid);
      var _loop_1 = function _loop_1(i) {
        var idxToSplice = -1;
        if (this_1.mediaCol.some(function (mCol) {
          idxToSplice++;
          return mCol.uid === keys[parseInt(i.toString(), 10)];
        })) {
          this_1.mediaCol.splice(idxToSplice, 1);
        } else {
          this_1.pushMediaColumn(this_1.parent.getColumnByUid(keys[parseInt(i.toString(), 10)]), this_1.parent.getColumnIndexByUid(keys[parseInt(i.toString(), 10)]));
        }
      };
      var this_1 = this;
      for (var i = 0; i < keys.length; i++) {
        _loop_1(i);
      }
    };
    ColumnChooser.prototype.removeMediaListener = function () {
      for (var i = 0; i < this.mediaCol.length; i++) {
        this.media[this.mediaCol[parseInt(i.toString(), 10)].uid].removeListener(this.mediaBindInstance[this.mediaCol[parseInt(i.toString(), 10)].index]);
      }
    };
    return ColumnChooser;
  }();

  /**
   * The `ColumnMenu` module is used to show or hide columns dynamically.
   */
  var ColumnMenu = /** @class */function () {
    function ColumnMenu(parent) {
      this.key = null;
      this.uid = null;
      this.parent = parent;
    }
    /**
     * Get columnMenu Position.
     *
     * @param {string} uid - The UID of the columnMenu
     * @param {boolean} isFilter - Whether the columnMenu is used for filtering.
     * @param {string} key - The key associated with the columnMenu.
     * @returns {void}
     * @hidden
     */
    ColumnMenu.prototype.renderColumnMenu = function (uid, isFilter, key) {
      this.key = key;
      this.uid = uid;
      var e = this.parent.getColumnHeaderByUid(uid).querySelector('.e-columnmenu');
      var columnMenuElement = document.getElementsByClassName("e-".concat(this.parent.element.id, "-column-menu"))[0];
      columnMenuElement.style.position = 'absolute';
      var element = columnMenuElement.getElementsByTagName('ul')[0];
      if (!sf.base.isNullOrUndefined(element)) {
        var pos = {
          top: 0,
          left: 0
        };
        element.style.visibility = 'hidden';
        columnMenuElement.style.display = 'block';
        var elePos = element.getBoundingClientRect();
        element.classList.add('e-transparent');
        element.style.visibility = '';
        columnMenuElement.style.display = '';
        var headerCell = this.getHeaderCell(e);
        if (this.parent.options.enableRtl) {
          pos = sf.popups.calculatePosition(headerCell, 'left', 'bottom');
        } else {
          pos = sf.popups.calculatePosition(headerCell, 'right', 'bottom');
          pos.left -= elePos.width;
        }
        pos.left -= scrollX;
        pos.top -= scrollY;
        if (isFilter) {
          sf.base.EventHandler.add(element, 'mouseover', this.appendFilter, this);
          sf.base.EventHandler.add(element, 'keydown', this.appendFilter, this);
        }
        return {
          Left: Math.sign(pos.left) !== -1 ? Math.ceil(pos.left) : 0,
          Top: Math.ceil(pos.top)
        };
      } else {
        return {
          Left: 1,
          Top: 1
        };
      }
    };
    ColumnMenu.prototype.setPosition = function () {
      var columnMenuElement = document.getElementsByClassName("e-".concat(this.parent.element.id, "-column-menu"))[0];
      var element = !sf.base.isNullOrUndefined(columnMenuElement) ? columnMenuElement.getElementsByTagName('ul')[0] : null;
      if (!sf.base.isNullOrUndefined(element) && !sf.base.isNullOrUndefined(this.uid) && !sf.base.isNullOrUndefined(this.parent.getColumnHeaderByUid(this.uid)) && !sf.base.Browser.isDevice) {
        var e = this.parent.getColumnHeaderByUid(this.uid).querySelector('.e-columnmenu');
        var headerCell = this.getHeaderCell(e);
        var btnOffset = headerCell.getBoundingClientRect();
        var left = btnOffset.left + scrollX;
        var top_1 = btnOffset.bottom + scrollY;
        var popupOffset = element.getBoundingClientRect();
        var docElement = document.documentElement;
        if (btnOffset.bottom + popupOffset.height > docElement.clientHeight) {
          if (top_1 - btnOffset.height - popupOffset.height > docElement.clientTop) {
            top_1 = top_1 - btnOffset.height - popupOffset.height;
          }
        }
        if (btnOffset.left + popupOffset.width > docElement.clientWidth) {
          if (btnOffset.right - popupOffset.width > docElement.clientLeft) {
            left = left + btnOffset.width - popupOffset.width;
          }
        }
        left = left - element.getBoundingClientRect().width + btnOffset.width;
        columnMenuElement.style.left = Math.ceil(left + 1) + 'px';
        columnMenuElement.style.top = Math.ceil(top_1 + 1) + 'px';
      }
    };
    ColumnMenu.prototype.appendFilter = function (e) {
      var _this = this;
      var showdialog = false;
      setTimeout(function () {
        if (sf.base.closest(e.target, '#' + _this.key) && (_this.parent.element.querySelector('.e-filter-popup') || !_this.parent.element.querySelector('.e-filter-popup')) || !sf.base.isNullOrUndefined(e.target.parentElement) && e.target.parentElement.id === _this.key && (_this.parent.element.querySelector('.e-filter-popup') || !_this.parent.element.querySelector('.e-filter-popup'))) {
          showdialog = true;
        } else if (!sf.base.closest(e.target, '#' + _this.key) && _this.parent.element.querySelector('.e-filter-popup')) {
          showdialog = false;
        }
        _this.parent.dotNetRef.invokeMethodAsync('FilterMouseOverHandler', _this.uid, showdialog);
      }, 10);
    };
    ColumnMenu.prototype.getHeaderCell = function (e) {
      return sf.base.closest(e, 'th.e-headercell');
    };
    return ColumnMenu;
  }();

  /**
   * The `Filter` module is used to set the Filter Dialog position dynamically.
   */
  var Filter = /** @class */function () {
    function Filter(parent) {
      this.parent = parent;
    }
    /**
     *
     * Get Filter Popup Position.
     *
     * @param {string} dlgID - The dialog ID.
     * @param {string} ColUid - The column UID.
     * @param {string} type - The type of filtering.
     * @param {boolean} isColumnMenu - Indicates if it's a column menu.
     * @returns {void}
     * @hidden
     */
    Filter.prototype.filterPopupRender = function (dlgID, ColUid, type, isColumnMenu) {
      var dlgelement = this.parent.element.querySelector('#' + dlgID);
      var leftValue = 0;
      var topValue = 0;
      if (!sf.base.isNullOrUndefined(dlgelement)) {
        if (isColumnMenu) {
          sf.base.EventHandler.add(dlgelement, 'mousedown', this.mouseDownHandler, this);
          dlgelement.style.maxHeight = type === 'excel' ? '800px' : '350px';
          var element = document.getElementsByClassName("e-".concat(this.parent.element.id, "-column-menu"))[0].getElementsByTagName('ul')[0];
          var li = sf.base.isNullOrUndefined(element.querySelector('.' + 'e-icon-filter')) ? element.getElementsByClassName('e-menu-item e-focused')[0] : element.querySelector('.' + 'e-icon-filter').parentElement;
          var ul = this.parent.element.querySelector('.' + 'e-filter-popup');
          var gridPos = this.parent.element.getBoundingClientRect();
          var liPos = li.getBoundingClientRect();
          var left = liPos.left - gridPos.left;
          var top_1 = liPos.top - gridPos.top;
          var elementVisible = dlgelement.style.display;
          dlgelement.style.display = 'block';
          if (gridPos.height < top_1) {
            top_1 = top_1 - ul.offsetHeight + liPos.height;
          } else if (gridPos.height < top_1 + ul.offsetHeight) {
            top_1 = gridPos.height - ul.offsetHeight;
          }
          if (gridPos.height < ul.offsetHeight) {
            top_1 = ul.offsetHeight - gridPos.height;
          }
          if (window.innerHeight < ul.offsetHeight + top_1 + gridPos.top) {
            top_1 = window.innerHeight - ul.offsetHeight - gridPos.top;
          }
          left += this.parent.options.enableRtl ? -ul.offsetWidth : liPos.width;
          if (gridPos.width <= left + ul.offsetWidth) {
            left -= liPos.width + ul.offsetWidth;
          } else if (left < 0) {
            left += ul.offsetWidth + liPos.width;
          }
          dlgelement.style.display = elementVisible;
          leftValue = left.toString();
          topValue = top_1.toString();
        }
      }
      return [leftValue, topValue];
    };
    Filter.prototype.mouseDownHandler = function (args) {
      if ((args && sf.base.closest(args.target, '.e-filter-popup') || args.currentTarget && args.currentTarget.activeElement && parentsUntil(args.currentTarget.activeElement, 'e-filter-popup') || parentsUntil(args.target, 'e-popup') || parentsUntil(args.target, 'e-popup-wrapper')) && !sf.base.Browser.isDevice) {
        this.parent.dotNetRef.invokeMethodAsync('PreventColumnMenuClose', true);
      }
    };
    return Filter;
  }();

  /**
   * Editing
   */
  var Edit = /** @class */function () {
    function Edit(parent) {
      this.parent = parent;
    }
    Edit.prototype.createTooltip = function (results, isAdd) {
      var _this = this;
      var toolTipPos = {};
      var arrowPosition;
      var element;
      var isHeader = false;
      var _loop_1 = function _loop_1(i) {
        var gcontent = this_1.parent.getContent();
        var name_1 = results["".concat(i)]['fieldName'];
        var uid = results["".concat(i)]['uid'];
        var message = results["".concat(i)]['message'];
        name_1 = name_1.replace(/[.]/g, '___');
        if (this_1.parent.options.hasTemplateInEditSettings || sf.base.isNullOrUndefined(uid)) {
          element = this_1.parent.element.querySelector("#".concat(name_1)) || document.querySelector("#".concat(name_1));
          if (this_1.parent.options.isRenderedFromTreeGrid) {
            if (this_1.parent.element.querySelector("#DataItem___".concat(name_1)) != null || document.querySelector("#DataItem___".concat(name_1)) != null) {
              element = this_1.parent.element.querySelector("#DataItem___".concat(name_1)) || document.querySelector("#DataItem___".concat(name_1));
            } else {
              element = this_1.parent.element.querySelector("#".concat(name_1)) || document.querySelector("#".concat(name_1));
            }
          }
        } else {
          if (document.querySelectorAll('[e-mappinguid=' + uid + '_Dialog' + ']')[0]) {
            element = document.querySelectorAll('[e-mappinguid=' + uid + '_Dialog' + ']')[0];
          } else if ((this_1.parent.options.enableVirtualization || this_1.parent.options.frozenRows || this_1.parent.options.enableInfiniteScrolling) && this_1.parent.options.showAddNewRow) {
            var tdElements = this_1.parent.element.querySelectorAll('[e-mappinguid=' + uid + '].e-rowcell');
            var activeTdElement = Array.from(tdElements).filter(function (tdElement) {
              var formElement = parentsUntil(tdElement, 'e-gridform');
              return formElement && formElement.querySelector('.e-griderror');
            });
            if (this_1.parent.options.frozenRows) {
              if (parentsUntil(activeTdElement[0], 'e-gridheader')) {
                isHeader = true;
                element = activeTdElement[0];
              } else {
                isHeader = false;
              }
            } else if ((this_1.parent.options.enableVirtualization || this_1.parent.options.enableInfiniteScrolling) && parentsUntil(activeTdElement[0], 'e-gridheader')) {
              element = activeTdElement[0];
              isHeader = true;
            } else {
              element = this_1.parent.getContent().querySelectorAll('[e-mappinguid=' + uid + ']')[0];
              isHeader = false;
            }
          } else {
            var elements = this_1.parent.getContent().querySelectorAll('[e-mappinguid=' + uid + ']');
            for (var j = 0; j < elements.length; j++) {
              if (!elements[parseInt(j.toString(), 10)].querySelector('.e-disabled')) {
                element = elements[parseInt(j.toString(), 10)];
              }
            }
          }
        }
        if (sf.base.isNullOrUndefined(element)) {
          var column = this_1.parent.columnModel.filter(function (e) {
            return e.field.split(name_1).length > 1;
          });
          if (!sf.base.isNullOrUndefined(column) && column.length !== 0) {
            name_1 = column[0].field.replace(/[.]/g, '___');
            element = this_1.parent.getContent().querySelectorAll('[e-mappinguid=' + uid + ']')[0] || document.querySelectorAll('[e-mappinguid=' + uid + '_Dialog' + ']')[0];
          }
        }
        var isScroll = isHeader ? false : gcontent.scrollHeight > gcontent.clientHeight || gcontent.scrollWidth > gcontent.clientWidth;
        var isInline = this_1.parent.options.editMode !== 'Dialog';
        var isAdaptive = this_1.parent.options.enableAdaptiveUI;
        var dialogWrapperText = isAdaptive ? '_adaptive_dialogEdit_wrapper' : '_dialogEdit_wrapper';
        if (!element) {
          return {
            value: void 0
          };
        }
        var td = sf.base.closest(element, '.e-rowcell');
        var row = sf.base.closest(element, '.e-row');
        var validationForBottomRowPos = void 0;
        var isBatchModeLastRow = false;
        var viewPortRowCount = Math.round(this_1.parent.getContent().clientHeight / this_1.parent.getRowHeight()) - 1;
        var rows = [].slice.call(this_1.parent.getContent().querySelectorAll('.e-row'));
        if (this_1.parent.options.enableVirtualization && this_1.parent.options.allowGrouping && this_1.parent.options.groupCount > 0 && isAdd) {
          rows = [].slice.call(this_1.parent.getContent().querySelectorAll('tr'));
          rows.pop();
        }
        if (this_1.parent.options.editMode === 'Batch') {
          if (viewPortRowCount > 1 && rows.length >= viewPortRowCount && parseInt(rows[rows.length - 1].getAttribute('aria-rowindex'), 10) - 1 === parseInt(row.getAttribute('aria-rowindex'), 10) - 1) {
            isBatchModeLastRow = true;
          }
        }
        if (isInline) {
          // Cast row to HTMLElement before accessing offsetTop and offsetHeight
          var htmlRow = row;
          if (this_1.parent.options.frozenRows) ;
          if (viewPortRowCount > 1 && htmlRow.offsetTop + htmlRow.offsetHeight >= this_1.parent.getContent().clientHeight && (this_1.parent.options.newRowPosition === 'Bottom' && isAdd || !sf.base.isNullOrUndefined(td) && td.classList.contains('e-lastrowcell') && !row.classList.contains('e-addedrow')) || isBatchModeLastRow) {
            validationForBottomRowPos = true;
          } else if (isHeader && !sf.base.isNullOrUndefined(row)) {
            var rowIndexAttribute = row.getAttribute('aria-rowindex');
            if (rowIndexAttribute !== null) {
              var rowIndex = parseInt(rowIndexAttribute, 10) - 1;
              if (rowIndex + 1 === this_1.parent.options.frozenRows) {
                validationForBottomRowPos = true; // last frozen rows
              }
            }
          }
        }
        var table = isInline ? isHeader ? this_1.parent.getHeaderTable() : this_1.parent.getContentTable() : document.querySelector('#' + this_1.parent.element.id + dialogWrapperText).querySelector('.e-dlg-content');
        var client = table.getBoundingClientRect();
        var left = isInline ? this_1.parent.element.getBoundingClientRect().left : client.left;
        var input = sf.base.closest(element, 'td');
        var inputClient = input ? input.getBoundingClientRect() : element.parentElement.getBoundingClientRect();
        var divUid = uid + '_Error';
        var div = void 0;
        if (this_1.parent.options.hasTemplateInEditSettings || sf.base.isNullOrUndefined(uid)) {
          div = this_1.parent.element.querySelector("#".concat(name_1, "_Error")) || document.querySelector("#".concat(name_1, "_Error"));
          if (this_1.parent.options.isRenderedFromTreeGrid) {
            div = this_1.parent.element.querySelector("#DataItem___".concat(name_1, "_Error")) || document.querySelector("#DataItem___".concat(name_1, "_Error")) || div;
          }
        } else if ((this_1.parent.options.enableVirtualization || this_1.parent.options.frozenRows) && this_1.parent.options.showAddNewRow) {
          if (isHeader) {
            div = this_1.parent.getHeaderContent().querySelectorAll('[e-mappinguid=' + divUid + ']')[0];
          } else {
            div = this_1.parent.getContent().querySelectorAll('[e-mappinguid=' + divUid + ']')[0] || document.querySelectorAll('[e-mappinguid=' + divUid + ']')[0];
          }
        } else {
          div = this_1.parent.getContent().querySelectorAll('[e-mappinguid=' + divUid + ']')[0] || document.querySelectorAll('[e-mappinguid=' + divUid + ']')[0];
        }
        if (sf.base.isNullOrUndefined(div)) {
          return {
            value: void 0
          };
        }
        div.style.top = (isHeader ? inputClient.top + inputClient.height : inputClient.bottom - client.top) + table.scrollTop + 9 + 'px';
        div.style.left = inputClient.left - left + table.scrollLeft + inputClient.width / 2 + 'px';
        div.style.maxWidth = 'auto';
        if (isInline && client.left < left) {
          div.style.left = parseInt(div.style.left, 10) - client.left + left + 'px';
        }
        var arrow = void 0;
        if (validationForBottomRowPos) {
          arrow = div.querySelector('.e-tip-bottom');
        } else {
          arrow = div.querySelector('.e-tip-top');
        }
        if ((this_1.parent.options.frozenColumns || this_1.parent.options.frozenRows) && this_1.parent.options.editMode !== 'Dialog') {
          var getEditCell = this_1.parent.options.editMode === 'Normal' ? sf.base.closest(element, '.e-editcell') : sf.base.closest(element, '.e-table');
          getEditCell.style.position = 'relative';
          div.style.position = 'absolute';
        }
        div.style.display = 'block';
        var errorLabel = div.querySelector('.e-error');
        var tempLabelElement = null;
        if (!sf.base.isNullOrUndefined(errorLabel)) {
          errorLabel.innerText = message;
        } else {
          tempLabelElement = document.createElement('label');
          tempLabelElement.className = 'e-error';
          tempLabelElement.innerText = message;
          var tipContent = div.querySelector('.e-tip-content');
          if (!sf.base.isNullOrUndefined(tipContent)) {
            tipContent.appendChild(tempLabelElement);
          }
        }
        if (!validationForBottomRowPos && isInline && gcontent.getBoundingClientRect().bottom < inputClient.bottom + inputClient.height) {
          gcontent.scrollTop = gcontent.scrollTop + div.offsetHeight + arrow.scrollHeight;
        }
        var lineHeight = parseInt(document.defaultView.getComputedStyle(div, null).getPropertyValue('font-size'), 10);
        var labelElement = div.querySelector('label');
        if (div.getBoundingClientRect().width < inputClient.width && labelElement && labelElement.getBoundingClientRect().height / (lineHeight * 1.2) >= 2 && this_1.parent.options.editMode !== 'Dialog') {
          div.style.width = inputClient.width - 4 + 'px';
        }
        if ((this_1.parent.options.frozenColumns || this_1.parent.options.frozenRows) && (this_1.parent.options.editMode === 'Normal' || this_1.parent.options.editMode === 'Batch')) {
          div.style.left = input.offsetLeft + (input.offsetWidth / 2 - div.offsetWidth / 2) + 'px';
        } else {
          div.style.left = parseInt(div.style.left, 10) - div.offsetWidth / 2 + 'px';
        }
        if (isInline && !isScroll && !this_1.parent.options.allowPaging || this_1.parent.options.frozenColumns || this_1.parent.options.frozenRows) {
          if (!this_1.parent.options.showAddNewRow && !this_1.parent.options.enableAutoFill && !this_1.parent.options.enableVirtualMaskRow && !this_1.parent.options.enableVirtualization) {
            gcontent.style.position = 'static';
          }
          var pos = sf.popups.calculateRelativeBasedPosition(input, div);
          div.style.top = pos.top + inputClient.height + 9 + 'px';
        } else if (gcontent.style.position !== '') {
          gcontent.style.position = '';
        }
        if (validationForBottomRowPos) {
          if (isScroll && !this_1.parent.options.frozenColumns && this_1.parent.options.height !== 'auto' && !this_1.parent.options.frozenRows && !this_1.parent.options.enableVirtualization) {
            var scrollWidth = gcontent.scrollWidth > gcontent.offsetWidth ? getScrollBarWidth() : 0;
            var gHeight = this_1.parent.options.height.toString().indexOf('%') === -1 ? parseInt(this_1.parent.options.height, 10) : gcontent.offsetHeight;
            div.style.bottom = gHeight - gcontent.querySelector('table').offsetHeight - scrollWidth + inputClient.height + 9 + 'px';
          } else {
            div.style.bottom = inputClient.height + 9 + 'px';
          }
          //TODO: NEW LINES ADDED SHOULD CHECK
          // if (rows.length < viewPortRowCount && this.parent.editSettings.newRowPosition === 'Bottom' && (this.editModule.args
          //     && this.editModule.args.requestType === 'add')) {
          //     let rowsCount: number = this.parent.frozenRows ? this.parent.frozenRows + (rows.length - 1) : rows.length - 1;
          //     let rowsHeight: number = rowsCount * this.parent.getRowHeight();
          //     let position: number = this.parent.getContent().clientHeight - rowsHeight;
          //     div.style.bottom = position + 9 + 'px';
          // }
          div.style.top = null;
        }
        // div.style.display = 'none';
        if (input && this_1.parent.options.enableRtl && !this_1.parent.options.enableVirtualization) {
          var inputElement = input.querySelector('.e-input');
          var inputRight = inputElement.getBoundingClientRect().right;
          var elemRight = div.getBoundingClientRect().right;
          if (elemRight > inputRight) {
            var offSet = elemRight - inputRight;
            div.style.left = div.offsetLeft - offSet + 'px';
          }
        }
        arrowPosition = validationForBottomRowPos ? 'bottom' : 'top';
        if (name_1.includes('___') && sf.base.isNullOrUndefined(uid)) {
          name_1 = name_1.replace('___', '.');
        }
        var columnUid = function () {
          if (!sf.base.isNullOrUndefined(uid)) {
            return uid;
          }
          var col = _this.parent.getColumnByField(name_1);
          if (col !== null && col !== undefined && col.uid !== null && col.uid !== undefined) {
            return col.uid;
          }
          if (_this.parent.options.isRenderedFromTreeGrid) {
            var treeCol = _this.parent.getColumnByField('DataItem.' + name_1);
            if (treeCol !== null && treeCol !== undefined && treeCol.uid !== null && treeCol.uid !== undefined) {
              return treeCol.uid;
            }
          }
          return uid;
        }();
        toolTipPos["".concat(columnUid)] = "top: ".concat(div.style.top, "; bottom: ").concat(div.style.bottom, "; left: ").concat(div.style.left, "; \n            max-width: ").concat(div.style.maxWidth, "; width: ").concat(div.style.width, "; text-align: center; position: ").concat(div.style.position, ";");
        if (!sf.base.isNullOrUndefined(tempLabelElement)) {
          tempLabelElement.remove();
        }
      };
      var this_1 = this;
      for (var i = 0; i < results.length; i++) {
        var state_1 = _loop_1(i);
        if (_typeof(state_1) === "object") return state_1.value;
      }
      this.parent.dotNetRef.invokeMethodAsync('ShowValidationPopup', toolTipPos, arrowPosition);
    };
    return Edit;
  }();

  /**
   * The `Clipboard` module is used to handle clipboard copy action.
   */
  var Clipboard = /** @class */function () {
    function Clipboard(parent) {
      this.copyContent = '';
      this.isSelect = false;
      this.parent = parent;
      this.clipBoardTextArea = sf.base.createElement('textarea', {
        className: 'e-clipboard',
        styles: 'opacity: 0',
        attrs: {
          tabindex: '-1',
          'aria-label': 'clipboard'
        }
      });
      this.parent.element.appendChild(this.clipBoardTextArea);
    }
    Clipboard.prototype.pasteHandler = function () {
      var _this = this;
      var grid = this.parent;
      var target = sf.base.closest(document.activeElement, '.e-rowcell');
      if (!target || !grid.options.allowEditing || grid.options.editMode !== 'Batch' || grid.options.selectionMode !== 'Cell' || grid.options.cellSelectionMode === 'Flow' || target.classList.contains('e-editedbatchcell') && grid.options.cellSelectionMode === 'Box') {
        return;
      }
      this.activeElement = document.activeElement;
      this.clipBoardTextArea.value = '';
      var x = window.scrollX;
      var y = window.scrollY;
      this.clipBoardTextArea.focus();
      setTimeout(function () {
        _this.activeElement.focus();
        window.scrollTo(x, y);
        var name = 'Paste';
        grid.dotNetRef.invokeMethodAsync('InvokeCopyPasteAction', {
          clipboardText: _this.clipBoardTextArea.value
        }, name);
      }, 10);
    };
    Clipboard.prototype.pasteAction = function (data, rowIndex, colIndex, cancel) {
      if (!cancel && data !== '') {
        this.paste(data, rowIndex, colIndex);
      }
    };
    Clipboard.prototype.paste = function (data, rowIndex, colIndex) {
      var grid = this.parent;
      var cIdx = colIndex;
      var rIdx = rowIndex;
      var col;
      var value;
      var isAvail;
      if (!grid.options.allowEditing || grid.options.editMode !== 'Batch' || grid.options.selectionMode !== 'Cell' || grid.options.cellSelectionMode === 'Flow') {
        return;
      }
      // Check if the data represents a single cell with multiline content
      var isSingleCellMultiline = data.trim().startsWith('"') && data.trim().endsWith('"');
      if (isSingleCellMultiline) {
        // Handle single cell multiline paste
        var pasteData = data.trim().slice(1, -1).replace(/\\n/g, '\n'); // Remove quotes and decode newlines
        var column = grid.getColumnByIndex(cIdx);
        this.invokePasteAction(rIdx, cIdx, pasteData, column);
        return;
      }
      var rows = data.split('\n');
      var cols;
      var dataRows = grid.getDataRows();
      var mRows;
      var isFrozen = this.parent.options.frozenColumns;
      if (isFrozen) {
        mRows = grid.getMovableDataRows();
      }
      for (var r = 0; r < rows.length; r++) {
        cols = rows[parseInt(r.toString(), 10)].split('\t');
        cIdx = colIndex;
        if (r === rows.length - 1 && rows[parseInt(r.toString(), 10)] === '' || sf.base.isUndefined(grid.getRowByIndex(rIdx))) {
          cIdx++;
          break;
        }
        for (var c = 0; c < cols.length; c++) {
          isAvail = grid.getCellFromIndex(rIdx, cIdx);
          if (isFrozen) {
            var fTr = dataRows[parseInt(rIdx.toString(), 10)];
            var mTr = mRows[parseInt(rIdx.toString(), 10)];
            isAvail = !fTr.querySelector('[aria-colindex="' + (cIdx + 1) + '"]') ? mTr.querySelector('[aria-colindex="' + (cIdx + 1) + '"]') : true;
          }
          if (!isAvail) {
            cIdx++;
            break;
          }
          col = grid.getColumnByIndex(cIdx);
          value = cols[parseInt(c.toString(), 10)];
          this.invokePasteAction(rIdx, cIdx, value, col);
          cIdx++;
        }
        rIdx++;
      }
    };
    Clipboard.prototype.invokePasteAction = function (rowIndex, columnIndex, pasteValue, column) {
      var grid = this.parent;
      if (column.allowEditing && !column.isPrimaryKey && !column.template) {
        grid.dotNetRef.invokeMethodAsync('InvokePasteAction', {
          cellValue: pasteValue
        }, rowIndex, columnIndex, column.field);
      }
    };
    Clipboard.prototype.pasteData = function (rowIndex, columnField, value, columnIndex, cancel) {
      if (!cancel) {
        if (this.parent.editModule) {
          {
            this.parent.dotNetRef.invokeMethodAsync('UpdateCell', rowIndex, columnField, value);
          }
        }
        var cell = this.parent.getCellFromIndex(rowIndex, columnIndex);
        if (cell) {
          sf.base.classList(cell, ['e-focus', 'e-focused'], []);
        }
      }
    };
    Clipboard.prototype.setCopyData = function (withHeader) {
      var _this = this;
      if (window.getSelection().toString() === '') {
        this.clipBoardTextArea.value = this.copyContent = '';
        var rows = this.parent.getRows();
        if (this.parent.options.selectionMode !== 'Cell') {
          //let selectedIndexes: Object[] = this.parent.getSelectedRowIndexes().sort((a: number, b: number) => { return a - b; });
          var selectedIndexes = this.parent.getSelectedRowIndexes(this.parent.options.enableVirtualization);
          if (withHeader) {
            var headerTextArray = [];
            for (var i = 0; i < this.parent.getVisibleColumns().length; i++) {
              headerTextArray[parseInt(i.toString(), 10)] = this.parent.getVisibleColumns()[parseInt(i.toString(), 10)].headerText;
            }
            this.getCopyData(headerTextArray, false, '\t', withHeader);
            this.copyContent += '\n';
          }
          for (var i = 0; i < selectedIndexes.length; i++) {
            if (i > 0) {
              this.copyContent += '\n';
            }
            if (parseInt(rows[0].getAttribute('aria-rowindex'), 10) !== 1 && this.parent.options.selectionMode !== 'Both') {
              selectedIndexes[parseInt(i.toString(), 10)] = selectedIndexes[parseInt(i.toString(), 10)] - parseInt(rows[0].getAttribute('aria-rowindex'), 10) + 1;
            }
            var cells = [].slice.call(rows[selectedIndexes[parseInt(i.toString(), 10)]].querySelectorAll('.e-rowcell:not(.e-hide)'));
            this.getCopyData(cells, false, '\t', withHeader);
          }
        } else {
          var obj = this.checkBoxSelection();
          if (obj.status) {
            if (withHeader) {
              var headers = [];
              for (var i = 0; i < obj.colIndexes.length; i++) {
                headers.push(this.parent.getColumnHeaderByIndex(obj.colIndexes[parseInt(i.toString(), 10)]));
              }
              this.getCopyData(headers, false, '\t', withHeader);
              this.copyContent += '\n';
            }
            for (var i = 0; i < obj.rowIndexes.length; i++) {
              if (i > 0) {
                this.copyContent += '\n';
              }
              var cells = [].slice.call(rows[obj.rowIndexes[parseInt(i.toString(), 10)]].querySelectorAll('.e-cellselectionbackground'));
              this.getCopyData(cells, false, '\t', withHeader);
            }
          } else {
            this.getCopyData([].slice.call(this.parent.element.querySelectorAll('.e-cellselectionbackground')), true, '\n', withHeader);
          }
        }
        var name_1 = 'Copy';
        if (this.parent.options.isClipboardEventBinded) {
          this.parent.dotNetRef.invokeMethodAsync('InvokeCopyPasteAction', {
            clipboardText: this.copyContent
          }, name_1);
        } else {
          this.clipBoardData(false, this.copyContent);
        }
      } else if (parentsUntil(document.activeElement, 'e-grid')) {
        var clipboardFocus_1 = document.activeElement;
        var selectedText = window.getSelection().toString();
        if (selectedText) {
          navigator.clipboard.writeText(selectedText).then(function () {
            _this.clipBoardTextArea.blur();
            clipboardFocus_1.focus();
          });
        }
      }
    };
    Clipboard.prototype.clipBoardData = function (cancel, data) {
      var _this = this;
      var clipboardFocus = document.activeElement;
      this.copyContent = data !== '' ? data : this.copyContent;
      if (!cancel) {
        this.clipBoardTextArea.value = this.copyContent;
        if (!sf.base.Browser.userAgent.match(/ipad|ipod|iphone/i)) {
          this.clipBoardTextArea.select();
        } else {
          this.clipBoardTextArea.setSelectionRange(0, this.clipBoardTextArea.value.length);
        }
        this.isSelect = true;
        if (navigator.clipboard) {
          navigator.clipboard.writeText(this.copyContent).then(function () {
            _this.clipBoardTextArea.blur();
            clipboardFocus.focus({
              preventScroll: true
            });
          });
        }
        if (this.isSelect) {
          window.getSelection().removeAllRanges();
          this.isSelect = false;
        }
      }
    };
    Clipboard.prototype.getCopyData = function (cells, isCell, splitKey, withHeader) {
      var isElement = typeof cells[0] !== 'string';
      for (var j = 0; j < cells.length; j++) {
        if (withHeader && isCell) {
          this.copyContent += this.parent.getColumns()[parseInt(cells[parseInt(j.toString(), 10)].getAttribute('aria-colindex'), 10) - 1].headerText + '\n';
        }
        if (isElement) {
          if (!cells[parseInt(j.toString(), 10)].classList.contains('e-hide')) {
            if (!cells[parseInt(j.toString(), 10)].classList.contains('e-gridchkbox') && Object.keys(cells[parseInt(j.toString(), 10)].querySelectorAll('.e-check')).length) {
              this.copyContent += true;
            } else if (!cells[parseInt(j.toString(), 10)].classList.contains('e-gridchkbox') && Object.keys(cells[parseInt(j.toString(), 10)].querySelectorAll('.e-uncheck')).length) {
              this.copyContent += false;
            } else {
              this.copyContent += cells[parseInt(j.toString(), 10)].innerText;
            }
          }
        } else {
          this.copyContent += cells[parseInt(j.toString(), 10)];
        }
        if (j < cells.length - 1) {
          this.copyContent += splitKey;
        }
      }
    };
    Clipboard.prototype.copy = function (withHeader) {
      if (navigator.clipboard) {
        this.setCopyData(withHeader);
      }
    };
    Clipboard.prototype.getSelectedRowCellIndexes = function () {
      var gridObj = this.parent;
      var rowCellIndxes = [];
      var rows = gridObj.getRows();
      var mrows;
      if (gridObj.options.frozenColumns) {
        mrows = gridObj.getMovableDataRows();
      }
      var _loop_1 = function _loop_1(i) {
        var tempCells = rows[parseInt(i.toString(), 10)].querySelectorAll('.e-cellselectionbackground');
        if (gridObj.options.frozenColumns && !tempCells.length) {
          tempCells = mrows[parseInt(i.toString(), 10)].querySelectorAll('.e-cellselectionbackground');
        }
        if (tempCells.length) {
          var cellIndexes_1 = [];
          tempCells.forEach(function (element) {
            cellIndexes_1.push(parseInt(element.getAttribute('aria-colindex'), 10) - 1);
          });
          rowCellIndxes.push({
            rowIndex: i,
            cellIndexes: cellIndexes_1
          });
        }
      };
      for (var i = 0; i < rows.length; i++) {
        _loop_1(i);
      }
      return rowCellIndxes;
    };
    Clipboard.prototype.checkBoxSelection = function () {
      var gridObj = this.parent;
      var rowCellIndxes;
      var obj = {
        status: false
      };
      if (gridObj.options.selectionMode === 'Cell') {
        rowCellIndxes = this.getSelectedRowCellIndexes();
        var str = void 0;
        var rowIndexes = [];
        var i = void 0;
        for (i = 0; i < rowCellIndxes.length; i++) {
          if (rowCellIndxes[parseInt(i.toString(), 10)].cellIndexes.length) {
            rowIndexes.push(rowCellIndxes[parseInt(i.toString(), 10)].rowIndex);
          }
          if (rowCellIndxes[parseInt(i.toString(), 10)].cellIndexes.length) {
            if (!str) {
              str = JSON.stringify(rowCellIndxes[parseInt(i.toString(), 10)].cellIndexes.sort());
            }
            if (str !== JSON.stringify(rowCellIndxes[parseInt(i.toString(), 10)].cellIndexes.sort())) {
              break;
            }
          }
        }
        rowIndexes.sort(function (a, b) {
          return a - b;
        });
        if (i > 0 && i === rowCellIndxes.length) {
          obj = {
            status: true,
            rowIndexes: rowIndexes,
            colIndexes: rowCellIndxes[0].cellIndexes
          };
        }
      }
      return obj;
    };
    return Clipboard;
  }();
  var CustomToolTip = /** @class */function () {
    function CustomToolTip(parent) {
      this.parent = parent;
      if (this.isEllipsisTooltip()) {
        this.wireEvents();
      }
    }
    CustomToolTip.prototype.wireEvents = function () {
      sf.base.EventHandler.add(this.parent.getContent(), 'scroll', this.scrollHandler, this);
      sf.base.EventHandler.add(this.parent.element, 'mousemove', this.mouseMoveHandler, this);
      sf.base.EventHandler.add(this.parent.element, 'mouseout', this.mouseMoveHandler, this);
      sf.base.EventHandler.add(this.parent.element, 'keydown', this.onKeyPressed, this);
    };
    CustomToolTip.prototype.unWireevents = function () {
      sf.base.EventHandler.remove(this.parent.getContent(), 'scroll', this.scrollHandler);
      sf.base.EventHandler.remove(this.parent.element, 'mousemove', this.mouseMoveHandler);
      sf.base.EventHandler.remove(this.parent.element, 'mouseout', this.mouseMoveHandler);
      sf.base.EventHandler.remove(this.parent.element, 'keydown', this.onKeyPressed);
    };
    CustomToolTip.prototype.updateEvents = function () {
      if (this.isEllipsisTooltip()) {
        this.unWireevents();
        this.wireEvents();
      } else {
        this.unWireevents();
      }
    };
    CustomToolTip.prototype.open = function (target) {
      this.close();
      this.ctrlId = sf.base.getUniqueID(this.parent.element.getAttribute('id'));
      if (sf.base.isNullOrUndefined(this.toolTipElement)) {
        this.toolTipElement = sf.base.createElement('div', {
          className: 'e-tooltip-wrap e-popup e-lib e-control e-popup-open',
          styles: 'width: "auto", height: "auto", position: "absolute"',
          attrs: {
            role: 'tooltip',
            'aria-hidden': 'false',
            'id': this.ctrlId + '_content'
          }
        });
      }
      sf.base.attributes(target, {
        'aria-describedby': this.ctrlId + '_content',
        'data-tooltip-id': this.ctrlId + '_content'
      });
      this.renderToolTip();
      this.setPosition(target);
    };
    CustomToolTip.prototype.renderToolTip = function () {
      var content = sf.base.createElement('div', {
        className: 'e-tip-content'
      });
      content.textContent = this.content;
      this.toolTipElement.appendChild(content);
      var arrow = sf.base.createElement('div', {
        className: 'e-arrow-tip e-tip-bottom',
        styles: 'top: 99.9%'
      });
      arrow.appendChild(sf.base.createElement('div', {
        className: 'e-arrow-tip-outer e-tip-bottom'
      }));
      arrow.appendChild(sf.base.createElement('div', {
        className: 'e-arrow-tip-inner e-tip-bottom',
        styles: 'top: -6px'
      }));
      this.toolTipElement.appendChild(arrow);
      document.body.appendChild(this.toolTipElement);
    };
    CustomToolTip.prototype.setPosition = function (target) {
      var tooltipPostion = {
        top: 0,
        left: 0
      };
      var arrow = this.toolTipElement.querySelector('.e-arrow-tip');
      var popUpPosition = sf.popups.calculatePosition(target, 'Center', 'Top');
      tooltipPostion.top -= this.toolTipElement.offsetHeight + arrow.offsetHeight;
      tooltipPostion.left -= this.toolTipElement.offsetWidth / 2;
      this.toolTipElement.style.top = popUpPosition.top + tooltipPostion.top + 'px';
      this.toolTipElement.style.left = popUpPosition.left + tooltipPostion.left + 'px';
      var dialogElement = parentsUntil(this.parent.element, 'e-dialog') || parentsUntil(this.parent.element, 'e-multicolumn-list');
      if (dialogElement) {
        this.toolTipElement.style.zIndex = (parseInt(dialogElement.style.zIndex, 10) + 1).toString();
      }
    };
    CustomToolTip.prototype.close = function () {
      if (this.toolTipElement) {
        var prevTarget = this.parent.element.querySelector('[aria-describedby="' + this.ctrlId + '_content' + '"]');
        if (!sf.base.isNullOrUndefined(prevTarget)) {
          prevTarget.removeAttribute('aria-describedby');
          prevTarget.removeAttribute('data-tooltip-id');
          this.toolTipElement = null;
        } else if (!sf.base.isNullOrUndefined(this.parent.element.querySelector('form'))) {
          if (!sf.base.isNullOrUndefined(document.getElementById(this.ctrlId + '_content'))) {
            document.getElementById(this.ctrlId + '_content').remove();
          }
          this.toolTipElement = null;
        }
        if (!sf.base.isNullOrUndefined(document.getElementById(this.ctrlId + '_content'))) {
          document.getElementById(this.ctrlId + '_content').remove();
          this.toolTipElement = null;
        }
      }
    };
    CustomToolTip.prototype.getTooltipStatus = function (element) {
      var headerTable = this.parent.getHeaderTable();
      var contentTable = this.parent.getContentTable();
      var headerDivTag = 'e-gridheader';
      var contentDivTag = 'e-gridcontent';
      var htable = this.createTable(headerTable, headerDivTag, 'header');
      var ctable = this.createTable(contentTable, contentDivTag, 'content');
      //let td: HTMLElement = element;
      var table = element.classList.contains('e-headercell') ? htable : ctable;
      var ele = element.classList.contains('e-headercell') ? 'th' : 'tr';
      table.querySelector(ele).className = element.className;
      table.querySelector(ele).innerText = element.innerText;
      var width = table.querySelector(ele).getBoundingClientRect().width;
      document.body.removeChild(htable);
      document.body.removeChild(ctable);
      if (element.firstElementChild) {
        var childElement = element.firstElementChild;
        if (childElement.offsetWidth === childElement.scrollWidth) {
          var clonedElement = childElement.cloneNode(true);
          Object.assign(clonedElement.style, {
            overflow: 'visible',
            visibility: 'hidden',
            width: 'fit-content',
            position: 'absolute',
            left: '-9999px'
          });
          element.appendChild(clonedElement);
          var clonedElementWidth = clonedElement.getBoundingClientRect().width;
          element.removeChild(clonedElement);
          return childElement.offsetWidth < clonedElementWidth;
        }
        return width > element.getBoundingClientRect().width || childElement.offsetWidth < childElement.scrollWidth;
      } else {
        return width > element.getBoundingClientRect().width || element.offsetWidth < element.scrollWidth;
      }
    };
    CustomToolTip.prototype.mouseMoveHandler = function (e) {
      if (this.isEllipsisTooltip()) {
        if (this.parent.options.allowTextWrap) {
          var wrapMode = this.parent.options.wrapMode;
          var hideTooltip = wrapMode === 'Header' && parentsUntil(e.target, 'e-gridheader') || wrapMode === 'Content' && parentsUntil(e.target, 'e-gridcontent') || wrapMode === 'Both';
          if (hideTooltip) {
            return;
          }
        }
        var element = parentsUntil(e.target, 'e-ellipsistooltip');
        if (this.prevElement !== element || e.type === 'mouseout') {
          this.close();
        }
        var tagName = e.target.tagName;
        var elemNames = ['A', 'BUTTON', 'INPUT'];
        if (element && e.type !== 'mouseout' && !(sf.base.Browser.isDevice && elemNames.indexOf(tagName) !== -1)) {
          if (element.getAttribute('data-tooltip-id')) {
            return;
          }
          if (this.getTooltipStatus(element)) {
            if (element.getElementsByClassName('e-headertext').length) {
              var column = this.parent.getColumnByUid(element.querySelector('.e-headercelldiv').getAttribute('e-mappinguid'));
              this.content = !sf.base.isNullOrUndefined(column.description) ? column.description : element.getElementsByClassName('e-headertext')[0].innerText;
            } else {
              this.content = element.innerText;
            }
            this.prevElement = element;
            this.open(element);
          }
        }
      }
      this.hoverFrozenRows(e);
    };
    CustomToolTip.prototype.hoverFrozenRows = function (e) {
      if (this.parent.options.frozenColumns) {
        var row = parentsUntil(e.target, 'e-row');
        var frozenHover = [].slice.call(this.parent.element.querySelectorAll('.e-frozenhover'));
        if (frozenHover.length && e.type === 'mouseout') {
          for (var i = 0; i < frozenHover.length; i++) {
            frozenHover[parseInt(i.toString(), 10)].classList.remove('e-frozenhover');
          }
        } else if (row) {
          var rows = [].slice.call(this.parent.element.querySelectorAll('tr[aria-rowindex="' + (parseInt(row.getAttribute('aria-rowindex'), 10) - 1) + '"]'));
          rows.splice(rows.indexOf(row), 1);
          if (row.getAttribute('aria-selected') !== 'true') {
            for (var i = 0; i < rows.length; i++) {
              rows[parseInt(i.toString(), 10)].classList.add('e-frozenhover');
            }
          } else {
            for (var i = 0; i < rows.length; i++) {
              rows[parseInt(i.toString(), 10)].classList.remove('e-frozenhover');
            }
          }
        }
      }
    };
    CustomToolTip.prototype.isEllipsisTooltip = function () {
      var cols = this.parent.getColumns();
      if (this.parent.options.clipMode === 'EllipsisWithTooltip') {
        return true;
      }
      for (var i = 0; i < cols.length; i++) {
        if (cols[parseInt(i.toString(), 10)].clipMode === 'EllipsisWithTooltip') {
          return true;
        }
      }
      return false;
    };
    CustomToolTip.prototype.scrollHandler = function () {
      if (this.isEllipsisTooltip()) {
        this.close();
      }
    };
    /**
     * To create table for ellipsiswithtooltip
     *
     * @param {Element} table - The table element to be used.
     * @param {string} tag - The tag name for the sub-div element.
     * @param {string} type - The type indicating whether it's 'header' or not.
     * @returns {HTMLDivElement} The created div element containing the table.
     *
     * @hidden
     */
    CustomToolTip.prototype.createTable = function (table, tag, type) {
      var myTableDiv = sf.base.createElement('div');
      myTableDiv.className = this.parent.element.className;
      myTableDiv.style.cssText = 'display: inline-block;visibility:hidden;position:absolute';
      var mySubDiv = sf.base.createElement('div');
      mySubDiv.className = tag;
      var myTable = sf.base.createElement('table');
      myTable.className = table.className;
      myTable.style.cssText = 'table-layout: auto;width: auto';
      var ele = type === 'header' ? 'th' : 'td';
      var myTr = sf.base.createElement('tr');
      var mytd = sf.base.createElement(ele);
      myTr.appendChild(mytd);
      myTable.appendChild(myTr);
      mySubDiv.appendChild(myTable);
      myTableDiv.appendChild(mySubDiv);
      document.body.appendChild(myTableDiv);
      return myTableDiv;
    };
    CustomToolTip.prototype.onKeyPressed = function (e) {
      if (e.key === 'Tab' || e.key === 'ShiftTab') {
        this.close();
      }
    };
    CustomToolTip.prototype.destroy = function () {
      this.close();
      this.unWireevents();
    };
    return CustomToolTip;
  }();

  // eslint-disable-next-line valid-jsdoc, jsdoc/require-param, jsdoc/require-returns
  /**
   *
   * Reorder module is used to handle row reordering.
   *
   * @hidden
   */
  var RowDD = /** @class */function () {
    /**
     * Constructor for the Grid print module
     *
     * @param {SfGrid} [parent] - Optional parent grid instance.
     * @hidden
     */
    function RowDD(parent) {
      var _this = this;
      this.isOverflowBorder = true;
      this.istargetGrid = false;
      this.dataRowElements = [];
      this.showAddNewRowDisable = false;
      this.cloneElementGrid = null;
      this.previousDragTarget = null;
      /**
       * Helper function to handle drag start event.
       *
       * @param {Object} e - The event object containing the sender property with MouseEventArgs.
       */
      /* tslint:disable-next-line:max-line-length */
      // tslint:disable-next-line:max-func-body-length
      this.helper = function (e) {
        var gObj = _this.parent;
        var target = _this.draggable.currentStateTarget;
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        _this.draggable.queryPositionInfo = function (value) {
          if (gObj.options.enableRtl && sf.base.isNullOrUndefined(gObj.options.rowDropTarget)) {
            value.left = this.position.left - (this.parentClientRect.left + this.borderWidth.left) - document.querySelector('.e-cloneproperties').clientWidth + gObj.element.querySelector('.e-rowdragdrop').clientWidth / 2 + 'px';
          }
          return value;
        };
        var visualElement = sf.base.createElement('div', {
          className: 'e-cloneproperties e-draganddrop e-grid e-dragclone',
          styles: 'height:"auto", z-index:2, width:' + gObj.element.offsetWidth
        });
        var table = sf.base.createElement('table', {
          styles: 'width:' + gObj.element.offsetWidth
        });
        var tbody = sf.base.createElement('tbody');
        var senderElement = e.sender.target;
        var senderTargetIsNotCell = (sf.base.isNullOrUndefined(parentsUntil(senderElement, 'e-rowcell')) || parentsUntil(target, 'e-emptyrow')) && !senderElement.classList.contains('e-selectionbackground');
        if (document.getElementsByClassName('e-griddragarea').length || senderTargetIsNotCell && gObj.options.selectionType !== 'Single' || !gObj.options.rowDropTarget && !parentsUntil(target, 'e-rowdragdrop') && sf.base.Browser.isDevice || gObj.options.selectionType === 'Single' && target.parentElement.getAttribute('aria-rowindex') === null || _this.isTargetInEditMode(target)) {
          if (sf.base.isNullOrUndefined(parentsUntil(target, 'e-rowdragdrop'))) {
            return false;
          }
        }
        if (gObj.options.rowDropTarget && gObj.options.selectionMode === 'Row' && gObj.options.selectionType === 'Single' && _this.draggable.currentStateTarget.parentElement.getAttribute('aria-rowindex') !== null) {
          gObj.dotNetRef.invokeMethodAsync('SelectRow', parseInt(_this.draggable.currentStateTarget.parentElement.getAttribute('aria-rowindex'), 10) - 1, false, -1);
        }
        var targetRow = sf.base.closest(target, 'tr');
        if (!targetRow) {
          return false;
        }
        _this.startedRow = targetRow.cloneNode(true);
        if (!sf.base.isNullOrUndefined(_this.startedRow.querySelector('.e-rowcell.e-focus'))) {
          sf.base.removeClass([_this.startedRow.querySelector('.e-rowcell.e-focus')], ['e-focus', 'e-focused']);
        }
        var selectedRows = gObj.getSelectedRows();
        var targetRowIsDetailRow = parentsUntil(target, 'e-detailrow');
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        var rowsSelected = selectedRows.filter(function (row) {
          var rowIsDetailRow = parentsUntil(row, 'e-detailrow');
          return sf.base.isNullOrUndefined(rowIsDetailRow) || !sf.base.isNullOrUndefined(targetRowIsDetailRow) && !sf.base.isNullOrUndefined(rowIsDetailRow);
        });
        var dragUid = _this.startedRow.getAttribute('data-uid') ? _this.startedRow.getAttribute('data-uid') : null;
        var isDragRow = selectedRows.some(function (row) {
          return row.getAttribute('data-uid') === dragUid;
        });
        removeElement(_this.startedRow, '.e-indentcell');
        removeElement(_this.startedRow, '.e-detailrowcollapse');
        removeElement(_this.startedRow, '.e-detailrowexpand');
        _this.removeCell(_this.startedRow, 'e-gridchkbox');
        var exp = new RegExp('e-active', 'g'); //high contrast issue
        _this.startedRow.innerHTML = _this.startedRow.innerHTML.replace(exp, '');
        tbody.appendChild(_this.startedRow);
        if (isDragRow && !sf.base.isNullOrUndefined(rowsSelected) && rowsSelected.length > 1 && gObj.getSelectedRows().length > 1 && _this.startedRow.hasAttribute('aria-selected')) {
          var dropCountEle = sf.base.createElement('span', {
            className: 'e-dropitemscount',
            innerHTML: '' + rowsSelected.length
          });
          visualElement.appendChild(dropCountEle);
        }
        var ele = sf.base.closest(target, 'tr').querySelector('.e-icon-rowdragicon');
        if (ele && parentsUntil(target, 'e-rowdragdrop')) {
          ele.classList.add('e-dragstartrow');
        }
        table.appendChild(tbody);
        visualElement.appendChild(table);
        var gridIsDetailRow = parentsUntil(gObj.element, 'e-detailrow');
        if (gridIsDetailRow) {
          _this.cloneElementGrid = gObj.element;
          var mainParent = getRootElement(gObj.element);
          mainParent.appendChild(visualElement);
        } else {
          gObj.element.appendChild(visualElement);
        }
        return visualElement;
      };
      this.dragStart = function (e) {
        var gObj = _this.parent;
        document.body.classList.add('e-prevent-select');
        if (document.getElementsByClassName('e-griddragarea').length) {
          return;
        }
        var spanCssEle = _this.parent.element.querySelector('.e-dropitemscount');
        if (_this.parent.getSelectedRows().length > 1 && spanCssEle) {
          spanCssEle.style.left = document.querySelector('.e-cloneproperties table').offsetWidth - 5 + 'px';
        }
        var fromIdx = parseInt(_this.startedRow.getAttribute('aria-rowindex'), 10) - 1;
        var dragUid = !sf.base.isNullOrUndefined(_this.startedRow.getAttribute('data-uid')) ? _this.startedRow.getAttribute('data-uid') : null;
        _this.parent.dotNetRef.invokeMethodAsync('RowDragStartEvent', fromIdx, dragUid);
        e.bindEvents(e.dragElement);
        _this.dragStartData = _this.rowData;
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        var dropElem = document.getElementById(gObj.options.rowDropTarget);
        if (gObj.options.rowDropTarget && dropElem && dropElem.blazor__instance && typeof dropElem.blazor__instance.getModuleName === 'function' && dropElem.blazor__instance.getModuleName() === 'grid') {
          if (sf.base.isNullOrUndefined(gObj.element.querySelector('.e-dragstartrow'))) {
            dropElem.blazor__instance.getContent().classList.add('e-allowRowDrop');
          }
        }
      };
      this.drag = function (e) {
        var gObj = _this.parent;
        _this.istargetGrid = false;
        _this.destinationGrid = _this.parent;
        _this.showAddNewRowDisable = false;
        var cloneElement = document.querySelector('.e-cloneproperties');
        var target = _this.getElementFromPosition(cloneElement, e.event);
        _this.previousDragTarget = target;
        var cloneElementGrid = !sf.base.isNullOrUndefined(_this.cloneElementGrid) ? _this.cloneElementGrid : parentsUntil(cloneElement, 'e-grid').parentElement || null;
        var cloneElementDragRow = !sf.base.isNullOrUndefined(cloneElementGrid) ? cloneElementGrid.querySelector('.e-dragstartrow') : null;
        var targetElementGrid = parentsUntil(e.target, 'e-grid');
        if (_this.parent.options.rowDropTarget) {
          var dropElement = document.getElementById(gObj.options.rowDropTarget);
          if (!sf.base.isNullOrUndefined(cloneElementGrid) && !sf.base.isNullOrUndefined(targetElementGrid) && cloneElementGrid.id !== targetElementGrid.id) {
            _this.destinationGrid = sf.base.isNullOrUndefined(dropElement) ||
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            sf.base.isNullOrUndefined(dropElement.blazor__instance) ?
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            _this.parent : dropElement.blazor__instance;
          }
          if (parentsUntil(e.target, 'e-grid')) {
            _this.istargetGrid = _this.parent.options.rowDropTarget === parentsUntil(e.target, 'e-grid').id;
          }
        }
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        var instance = !sf.base.isNullOrUndefined(targetElementGrid) && !sf.base.isNullOrUndefined(targetElementGrid.blazor__instance) ? targetElementGrid.blazor__instance : null;
        if (!sf.base.isNullOrUndefined(instance) && !sf.base.isNullOrUndefined(instance.options) && instance.options.showAddNewRow && !_this.showAddNewRowDisable) {
          gObj.dotNetRef.invokeMethodAsync('DisableShowAddForm', 'RowDragStart', false, _this.destinationGrid.dotNetRef);
          _this.showAddNewRowDisable = true;
        }
        sf.base.classList(cloneElement, ['e-defaultcur'], ['e-notallowedcur', 'e-movecur']);
        _this.isOverflowBorder = true;
        var trElement = parentsUntil(target, 'e-grid') ? sf.base.closest(e.target, 'tr') : null;
        var cloneIsDetailRow = parentsUntil(cloneElementGrid, 'e-detailrow');
        var targetIsDetailRow = parentsUntil(targetElementGrid, 'e-detailrow');
        var targetRow = parentsUntil(target, 'e-row');
        var targetIsLastRowDragBorder = !sf.base.isNullOrUndefined(target) && target.classList.contains('e-lastrow-dragborder');
        if (!e.target) {
          return;
        }
        _this.stopTimer();
        gObj.element.classList.add('e-rowdrag');
        _this.dragTarget = trElement && parentsUntil(target, 'e-grid').id === (sf.base.isNullOrUndefined(cloneElementGrid) ? cloneElement.parentElement.id : cloneElementGrid.id) ? gObj.options.groupCount > 0 || _this.parent.options.showAddNewRow ? parseInt(trElement.getAttribute('aria-rowindex'), 10) - 1 : trElement.rowIndex : parseInt(_this.startedRow.getAttribute('aria-rowindex'), 10) - 1;
        if (gObj.options.rowDropTarget && !sf.base.isNullOrUndefined(targetElementGrid) && cloneElementGrid.id !== targetElementGrid.id && !sf.base.isNullOrUndefined(cloneElementDragRow)) {
          _this.dragTarget = trElement && parentsUntil(target, 'e-grid').id !== (!sf.base.isNullOrUndefined(_this.cloneElementGrid) ? cloneElementGrid.id : cloneElement.parentElement.id) ? trElement.rowIndex : parseInt(_this.startedRow.getAttribute('aria-rowindex'), 10) - 1;
        }
        if (gObj.options.rowDropTarget) {
          var dropElement = document.getElementById(gObj.options.rowDropTarget);
          var gridRow = parentsUntil(e.target, 'e-row');
          var targetISGrid = parentsUntil(target, 'e-grid');
          if (parentsUntil(target, 'e-gridcontent')) {
            if (cloneElementGrid.id === parentsUntil(target, 'e-grid').id && !sf.base.isNullOrUndefined(targetRow) && targetRow.getAttribute('aria-selected') === 'true' || !gObj.options.allowEmptyAreaDrop && sf.base.isNullOrUndefined(gridRow) && !sf.base.isNullOrUndefined(targetElementGrid) && sf.base.isNullOrUndefined(targetElementGrid.querySelector('.e-emptyrow')) || !sf.base.isNullOrUndefined(dropElement) && dropElement.classList.contains('e-grid') && !sf.base.isNullOrUndefined(targetElementGrid) && _this.destinationGrid.element.id !== targetElementGrid.id || targetIsLastRowDragBorder) {
              sf.base.classList(cloneElement, ['e-notallowedcur'], ['e-defaultcur']);
            } else if (parentsUntil(trElement, 'e-showAddNewRow') || !sf.base.isNullOrUndefined(targetISGrid) && targetISGrid.parentElement.classList.contains('e-treegrid')) {
              sf.base.classList(cloneElement, ['e-notallowedcur'], ['e-defaultcur']);
            } else {
              sf.base.classList(cloneElement, ['e-defaultcur'], ['e-notallowedcur']);
            }
          } else if (parentsUntil(target, 'e-droppable:not(.e-headercontent)')) {
            sf.base.classList(cloneElement, ['e-defaultcur'], ['e-notallowedcur']);
          } else if (!sf.base.isNullOrUndefined(dropElement) && !sf.base.isNullOrUndefined(target) && (parentsUntil(target, 'e-grid') || !dropElement.contains(target))) {
            sf.base.classList(cloneElement, ['e-notallowedcur'], ['e-defaultcur']);
          }
        } else {
          var elem = parentsUntil(target, 'e-grid');
          if (elem && elem.id === (!sf.base.isNullOrUndefined(_this.cloneElementGrid) ? cloneElementGrid.id : cloneElement.parentElement.id) && !sf.base.isNullOrUndefined(targetRow) && (targetRow.getAttribute('aria-selected') === 'false' || sf.base.isNullOrUndefined(targetRow.getAttribute('aria-selected')))) {
            sf.base.classList(cloneElement, ['e-movecur'], ['e-defaultcur']);
          } else if (targetIsLastRowDragBorder) {
            sf.base.classList(cloneElement, ['e-notallowedcur'], ['e-defaultcur']);
          } else if (gObj.options.allowEmptyAreaDrop && target && target.classList.contains('e-content') && !(gObj.options.groupCount > 0)) {
            sf.base.classList(cloneElement, ['e-movecur'], ['e-defaultcur']);
          } else {
            sf.base.classList(cloneElement, ['e-notallowedcur'], ['e-movecur']);
          }
        }
        var stackedColumn = gObj.reorderModule.processStackedColumns(gObj.reorderModule.getColumnsModel(gObj.options.columns)).length;
        var stackedHeader = gObj.getStackedColumns(gObj.reorderModule.getColumnsModel(gObj.options.columns)).length;
        if (!sf.base.isNullOrUndefined(targetElementGrid) && gObj.options.allowGrouping && (targetElementGrid.blazor__instance.options.groupCount === gObj.options.columns.length || stackedColumn - stackedHeader === gObj.options.groupCount) && !gObj.options.showGroupedColumn) {
          sf.base.classList(cloneElement, ['e-notallowedcur'], ['e-defaultcur']);
        }
        if (gObj.options.allowRowDragAndDrop || !gObj.options.rowDropTarget && e.target.classList.contains('e-selectionbackground')) {
          if (parentsUntil(target, 'e-grid')) {
            var treeGridElement = !sf.base.isNullOrUndefined(cloneElementGrid) ? parentsUntil(cloneElementGrid, 'e-treegrid') : null;
            if (!sf.base.isNullOrUndefined(targetElementGrid) && cloneElementGrid === targetElementGrid || treeGridElement) {
              if (!sf.base.isNullOrUndefined(trElement) && !parentsUntil(trElement, 'e-content')) {
                // eslint-disable-next-line @typescript-eslint/no-explicit-any
                _this.removeTargetGridBorder(cloneElementGrid.blazor__instance);
                // eslint-disable-next-line @typescript-eslint/no-explicit-any
                _this.removeTargetGridBorder(targetElementGrid.blazor__instance);
              }
              _this.updateScrollPostion(e.event, targetRow);
            }
          }
          if (!sf.base.isNullOrUndefined(document.querySelector('.e-lastrow-dragborder'))) {
            document.querySelector('.e-lastrow-dragborder').remove();
          }
          if (parentsUntil(trElement, 'e-columnheader')) {
            var detailRow = parentsUntil(trElement, 'e-detailrow');
            if (!sf.base.isNullOrUndefined(detailRow) && detailRow.getElementsByClassName('e-emptyrow')) {
              sf.base.classList(cloneElement, ['e-notallowedcur'], ['e-defaultcur']);
              return;
            }
          }
          var isNotEmptyGrid = !sf.base.isNullOrUndefined(trElement) && !trElement.classList.contains('e-emptyrow');
          var isFrozenRowsAndColumn = _this.parent.options.frozenRows && _this.parent.options.frozenColumns && !sf.base.isNullOrUndefined(trElement) && parseInt(_this.startedRow.getAttribute('aria-rowindex'), 10) - 1 !== parseInt(trElement.getAttribute('aria-rowindex'), 10) - 1;
          if (_this.isOverflowBorder && (parseInt(_this.startedRow.getAttribute('aria-rowindex'), 10) - 1 !== _this.dragTarget || isFrozenRowsAndColumn) && isNotEmptyGrid) {
            _this.moveDragRows(e, _this.startedRow, trElement);
          } else {
            var rows = _this.parent.getRows();
            var isLastRow = !sf.base.isNullOrUndefined(trElement) && _this.startedRow.getAttribute('data-uid') !== rows[rows.length - 1].getAttribute('data-uid');
            if (trElement && !sf.base.isNullOrUndefined(_this.parent.getRowByIndex(rows.length - 1)) && _this.parent.getRowByIndex(rows.length - 1).getAttribute('data-uid') === trElement.getAttribute('data-uid') && isLastRow && !gObj.options.groupCount) {
              var bottomborder = sf.base.createElement('div', {
                className: 'e-lastrow-dragborder'
              });
              var gridcontentEle = _this.parent.getContent();
              if (!sf.base.isNullOrUndefined(gridcontentEle) && !sf.base.isNullOrUndefined(parentsUntil(trElement, 'e-row'))) {
                var isHorizontalScrollAlone = gridcontentEle.scrollWidth > gridcontentEle.clientWidth && gridcontentEle.scrollHeight <= gridcontentEle.clientHeight;
                bottomborder.style.width = isHorizontalScrollAlone ? _this.parent.getContent().offsetWidth + 'px' : _this.parent.getContent().offsetWidth - _this.getScrollWidth() + 'px';
                _this.borderIndex = Number(parentsUntil(trElement, 'e-row').getAttribute('aria-rowindex')) - 1;
                if (!gridcontentEle.parentElement.querySelectorAll('.e-lastrow-dragborder').length) {
                  gridcontentEle.classList.add('e-grid-relative');
                  gridcontentEle.parentElement.appendChild(bottomborder);
                  _this.setBottomBorderPosition(gObj, bottomborder);
                }
              }
            } else if (!sf.base.isNullOrUndefined(targetElementGrid) && targetElementGrid.querySelector('.e-emptyrow') && cloneElementGrid.id !== targetElementGrid.id && targetElementGrid.querySelector('.e-content').getElementsByClassName('e-emptyrow').length > 0 && !cloneElement.classList.contains('e-notallowedcur')) {
              // eslint-disable-next-line @typescript-eslint/no-explicit-any
              if (targetElementGrid.blazor__instance.getRows().length === 0 || !sf.base.isNullOrUndefined(trElement) && parentsUntil(trElement, 'e-detailrow') && trElement.getElementsByClassName('e-emptyrow').length > 0) {
                if (!sf.base.isNullOrUndefined(cloneIsDetailRow) || sf.base.isNullOrUndefined(cloneIsDetailRow) && sf.base.isNullOrUndefined(targetIsDetailRow)) {
                  var bottomborder = sf.base.createElement('div', {
                    className: 'e-lastrow-dragborder'
                  });
                  var gridcontentEle = targetElementGrid.querySelector('.e-content');
                  if (!sf.base.isNullOrUndefined(gridcontentEle)) {
                    var isHorizontalScrollAlone = gridcontentEle.scrollWidth > gridcontentEle.clientWidth && gridcontentEle.scrollHeight <= gridcontentEle.clientHeight;
                    bottomborder.style.width = isHorizontalScrollAlone ? _this.destinationGrid.getContent().offsetWidth + 'px' : _this.destinationGrid.getContent().offsetWidth - _this.getScrollWidth() + 'px';
                    var isDetailRow = !sf.base.isNullOrUndefined(trElement) ? trElement.classList.contains('e-detailrow') : false;
                    gridcontentEle = isDetailRow ? trElement.getElementsByClassName('e-grid')[0].querySelector('.e-content') : gridcontentEle;
                    bottomborder.style.position = isDetailRow ? 'relative' : bottomborder.style.position;
                    if (!gridcontentEle.parentElement.querySelectorAll('.e-lastrow-dragborder').length) {
                      gridcontentEle.classList.add('e-grid-relative');
                      gridcontentEle.parentElement.appendChild(bottomborder);
                      bottomborder.style.bottom = _this.destinationGrid.options.allowPaging ? _this.destinationGrid.element.querySelector('.e-pager').offsetHeight + _this.getScrollWidth() + 'px' : _this.getScrollWidth() + 'px';
                    }
                  }
                }
              }
            } else if (!sf.base.isNullOrUndefined(targetElementGrid) && gObj.options.allowEmptyAreaDrop && targetElementGrid.blazor__instance.options.groupCount === 0) {
              var gridContentEle = targetElementGrid.querySelector('.e-content');
              var bottomborder = sf.base.createElement('div', {
                className: 'e-lastrow-dragborder'
              });
              var dataRows = gridContentEle.querySelectorAll('tr.e-row:not(.e-emptyrow)');
              var lastDataRow = dataRows[dataRows.length - 1];
              if (lastDataRow && !cloneElement.classList.contains('e-notallowedcur')) {
                var lastRowRect = lastDataRow.getBoundingClientRect();
                var contentRect = gridContentEle.getBoundingClientRect();
                if (!gridContentEle.querySelector('.e-lastrow-dragborder')) {
                  bottomborder.style.position = 'absolute';
                  bottomborder.style.left = '0px';
                  var isHorizontalScrollAlone = gridContentEle.scrollWidth > gridContentEle.clientWidth && gridContentEle.scrollHeight <= gridContentEle.clientHeight;
                  bottomborder.style.width = isHorizontalScrollAlone ? _this.destinationGrid.getContent().offsetWidth + 'px' : _this.destinationGrid.getContent().offsetWidth - _this.getScrollWidth() + 'px';
                  var scrollOffset = gridContentEle.scrollTop;
                  var offsetTop = lastRowRect.bottom - contentRect.top + scrollOffset;
                  bottomborder.style.top = offsetTop + 'px';
                  gridContentEle.classList.add('e-grid-relative');
                  gridContentEle.appendChild(bottomborder);
                }
              }
            }
            _this.removeBorder(trElement);
          }
          if (gObj.options.allowGrouping && gObj.options.groupCount) {
            if (!sf.base.isNullOrUndefined(trElement) && !trElement.querySelector('td.e-groupcaption') && _this.startedRow.getAttribute('caption-uid') !== trElement.getAttribute('caption-uid') && gObj.options.groupCount) {
              _this.addorRemoveDashedBorder(e, false, _this.dataRowElements);
              _this.dataRowElements = _this.parent.getDataRows().filter(function (row) {
                return row.getAttribute('caption-uid') === trElement.getAttribute('caption-uid');
              });
              _this.addorRemoveDashedBorder(e, true, _this.dataRowElements);
            } else {
              _this.addorRemoveDashedBorder(e, false, _this.dataRowElements);
            }
            var stackedColumn_1 = gObj.reorderModule.processStackedColumns(gObj.reorderModule.getColumnsModel(gObj.options.columns)).length;
            var stackedHeader_1 = gObj.getStackedColumns(gObj.reorderModule.getColumnsModel(gObj.options.columns)).length;
            if ((gObj.options.groupCount === gObj.options.columns.length || stackedColumn_1 - stackedHeader_1 === gObj.options.groupCount) && !gObj.options.showGroupedColumn) {
              _this.addorRemoveDashedBorder(e, false, _this.dataRowElements);
            }
          }
          if (!_this.isOverflowBorder) {
            _this.addorRemoveDashedBorder(e, false, _this.dataRowElements);
          }
        }
        if (gObj.options.rowDropTarget && _this.istargetGrid) {
          if (cloneElementGrid !== targetElementGrid) {
            _this.updateScrollPostion(e.event, targetRow);
            _this.moveDragRows(e, _this.startedRow, trElement);
            if (!sf.base.isNullOrUndefined(trElement)) {
              _this.lastRowBorderBetweenGrids(trElement, e.event);
            }
          }
        }
      };
      this.lastRowBorderBetweenGrids = function (trElement, event) {
        var gObj = _this.destinationGrid;
        var rowElements = gObj.getRows();
        var isLastRow = rowElements.length > 0 && !sf.base.isNullOrUndefined(trElement) && gObj.getContent().querySelector('tr:last-child') === trElement;
        //Get the mouse position relative to the target row
        var mouseY = getPosition(event).y - trElement.getBoundingClientRect().top;
        //Calculate the midpoint of the target row
        var targetMidpoint = trElement.offsetHeight / 2;
        //Calculate the combined height of the rows
        var rowsHeight = Array.from(rowElements).reduce(function (totalHeight, row) {
          return totalHeight + row.offsetHeight;
        }, 0);
        //Check if there is empty space after the last row
        var checkEmptySpace = rowsHeight >= gObj.getContent().clientHeight;
        if (rowElements.length > 0 && mouseY > targetMidpoint && checkEmptySpace && trElement && !sf.base.isNullOrUndefined(gObj.getRowByIndex(rowElements.length - 1)) && gObj.getRowByIndex(rowElements.length - 1).getAttribute('data-uid') === trElement.getAttribute('data-uid') && isLastRow && !gObj.options.groupCount) {
          var bottomborder = sf.base.createElement('div', {
            className: 'e-lastrow-dragborder'
          });
          var gridcontentEle = gObj.getContent();
          if (!sf.base.isNullOrUndefined(gridcontentEle)) {
            var isHorizontalScrollAlone = gridcontentEle.scrollWidth > gridcontentEle.clientWidth && gridcontentEle.scrollHeight <= gridcontentEle.clientHeight;
            bottomborder.style.width = isHorizontalScrollAlone ? gridcontentEle.offsetWidth + 'px' : gridcontentEle.offsetWidth - _this.getScrollWidth() + 'px';
            _this.borderIndex = Number(parentsUntil(trElement, 'e-row').getAttribute('aria-rowindex')) - 1;
            if (!gridcontentEle.parentElement.querySelectorAll('.e-lastrow-dragborder').length) {
              gridcontentEle.classList.add('e-grid-relative');
              gridcontentEle.parentElement.appendChild(bottomborder);
              _this.setBottomBorderPosition(gObj, bottomborder);
            }
          }
        }
      };
      this.setBottomBorderPosition = function (gObj, bottomBorder) {
        // Retrieve relevant DOM elements
        var aggregateFooterElement = gObj.element.querySelector('.e-gridfooter');
        var pagerElement = gObj.element.querySelector('.e-pager');
        // Determine heights of the footer and pager, setting default to 0 if elements are not found
        var footerOffsetHeight = aggregateFooterElement ? aggregateFooterElement.offsetHeight : 0;
        var pagerOffsetHeight = _this.parent.options.allowPaging && pagerElement ? pagerElement.offsetHeight : 0;
        // Calculate the total height for the bottom border position
        var scrollWidth = _this.getScrollWidth();
        var bottomOffset = footerOffsetHeight + pagerOffsetHeight + scrollWidth;
        // Apply calculated position to the bottom border style
        bottomBorder.style.bottom = "".concat(bottomOffset, "px");
      };
      this.dragStop = function (e) {
        document.body.classList.remove('e-prevent-select');
        _this.processDragStop(e);
      };
      this.emptyTargertInGrid = function (target) {
        var gObj = _this.parent;
        var contentDragBorder = gObj.getContent().getElementsByClassName('e-dragborder');
        var headerDragBorder = gObj.getHeaderContent().getElementsByClassName('e-firstrow-dragborder');
        var lastRowDragBorder = gObj.getContent().parentElement.getElementsByClassName('e-lastrow-dragborder');
        var iconTarget = !target.classList.contains('e-rowdragdrop') && !target.classList.contains('e-icon-rowdragicon');
        var emptyTarget = _this.destinationGrid.getRows().length > 0 && !sf.base.isNullOrUndefined(gObj.element.querySelector('.e-dragstartrow')) && !sf.base.isNullOrUndefined(contentDragBorder) && !sf.base.isNullOrUndefined(headerDragBorder) && contentDragBorder.length === 0 && headerDragBorder.length === 0 && sf.base.isNullOrUndefined(parentsUntil(target, 'e-rowcell')) && iconTarget;
        if (!_this.parent.options.rowDropTarget && !emptyTarget && !_this.isOverflowBorder && !sf.base.isNullOrUndefined(headerDragBorder) && !sf.base.isNullOrUndefined(lastRowDragBorder) && headerDragBorder.length === 0 && lastRowDragBorder.length === 0) {
          return true;
        }
        return emptyTarget;
      };
      this.processDragStop = function (e) {
        var gObj = _this.parent;
        var targetEle = _this.getElementFromPosition(e.helper, e.event);
        var target = targetEle && !targetEle.classList.contains('e-dlg-overlay') ? targetEle : e.target;
        gObj.element.classList.remove('e-rowdrag');
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        var dropElement = document.getElementById(gObj.options.rowDropTarget);
        var dragStartRow = gObj.element.querySelector('.e-dragstartrow');
        var cloneElement = document.querySelector('.e-cloneproperties');
        var cloneElementGrid = !sf.base.isNullOrUndefined(_this.cloneElementGrid) ? _this.cloneElementGrid : parentsUntil(cloneElement, 'e-grid').parentElement;
        var targetElementGrid = parentsUntil(e.target, 'e-grid');
        var targetIsNotGrid = !sf.base.isNullOrUndefined(dropElement) && !dropElement.classList.contains('e-grid') && !sf.base.isNullOrUndefined(targetElementGrid) && cloneElementGrid.id !== targetElementGrid.id;
        var whiteSpaceInEmptyGrid = !sf.base.isNullOrUndefined(targetElementGrid) && !sf.base.isNullOrUndefined(targetElementGrid.querySelector('.e-emptyrow')); //When a row is dropped into an empty grid that displays 'no records to display,' and the target is a content element, then the variable becomes true.
        if (_this.parent.options.allowRowDragAndDrop && _this.parent.options.rowDropTarget && (!parentsUntil(target, 'e-grid') && dropElement.contains(e.target) || targetIsNotGrid && (parentsUntil(e.target, 'e-row') || whiteSpaceInEmptyGrid))) {
          var toIdx = 0;
          var targetClass = _this.getElementXPath(target);
          var targetID = target.id;
          var fromIdx = parseInt(_this.startedRow.getAttribute('aria-rowindex'), 10) - 1;
          var positions = target.getBoundingClientRect();
          gObj.dotNetRef.invokeMethodAsync('ReorderRows', fromIdx, toIdx, 'add', false, targetClass, targetID, positions, null, true, false, null, null, false, getPosition(e.event).x, getPosition(e.event).y);
        }
        if (gObj.options.rowDropTarget && dropElement && dropElement.blazor__instance && typeof dropElement.blazor__instance.getModuleName === 'function' && dropElement.blazor__instance.getModuleName() === 'grid') {
          dropElement.blazor__instance.getContent().classList.remove('e-allowRowDrop');
        }
        //To handle the target differences during dragging and dropping caused by the added lastRowBorder, we allow the row to be dropped in this scenario.
        if (gObj.options.rowDropTarget && _this.previousDragTarget !== e.target && target.classList.contains('e-lastrow-dragborder') && !sf.base.isNullOrUndefined(targetElementGrid) && cloneElementGrid.id !== targetElementGrid.id) {
          _this.stopTimer();
          _this.parent.getContent().classList.remove('e-grid-relative');
          _this.removeBorder(targetEle);
          if (gObj.options.groupCount) {
            _this.addorRemoveDashedBorder(e, false, _this.dataRowElements);
          }
          _this.drop(e);
          return;
        }
        if (!parentsUntil(target, 'e-gridcontent') || cloneElement.classList.contains('e-notallowedcur') || _this.emptyTargertInGrid(target) && !gObj.options.allowEmptyAreaDrop || parentsUntil(e.target, 'e-columnheader')) {
          _this.dragTarget = null;
          sf.base.remove(e.helper);
          _this.stopTimer();
          _this.removeBorder(targetEle);
          if (_this.parent.options.rowDropTarget && !sf.base.isNullOrUndefined(targetElementGrid)) {
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            _this.removeTargetGridBorder(targetElementGrid.blazor__instance);
          }
          if (gObj.options.groupCount) {
            _this.addorRemoveDashedBorder(e, false, _this.dataRowElements);
          }
          if (dragStartRow) {
            dragStartRow.classList.remove('e-dragstartrow');
          }
          if (gObj.options.showAddNewRow && _this.showAddNewRowDisable) {
            gObj.dotNetRef.invokeMethodAsync('DisableShowAddForm', 'RowDragStop', true, null);
            _this.showAddNewRowDisable = false;
          }
          var fromIdx = parseInt(_this.startedRow.getAttribute('aria-rowindex'), 10) - 1;
          if (!sf.base.isNullOrUndefined(cloneElement) && cloneElement.classList.contains('e-notallowedcur')) {
            gObj.dotNetRef.invokeMethodAsync('ReorderRows', fromIdx, 0, 'add', false, '', target.id, null, null, true, false, null, null, false, getPosition(e.event).x, getPosition(e.event).y);
          }
          return;
        }
        if (_this.parent.options.allowRowDragAndDrop) {
          _this.stopTimer();
          _this.parent.getContent().classList.remove('e-grid-relative');
          _this.removeBorder(targetEle);
          if (gObj.options.groupCount) {
            _this.addorRemoveDashedBorder(e, false, _this.dataRowElements);
          }
          if (dragStartRow && _this.parent.options.rowDropTarget && !sf.base.isNullOrUndefined(targetElementGrid) && cloneElementGrid.id !== targetElementGrid.id) {
            return;
          }
          if (dragStartRow && !sf.base.isNullOrUndefined(targetElementGrid) && cloneElementGrid.id === targetElementGrid.id) {
            dragStartRow.classList.remove('e-dragstartrow');
          }
          var targetClass_1 = _this.getElementXPath(e.target);
          var targetID_1 = target.id;
          var fromIdx_1 = parseInt(_this.startedRow.getAttribute('aria-rowindex'), 10) - 1;
          var fromUid_1 = !sf.base.isNullOrUndefined(_this.startedRow.getAttribute('data-uid')) ? _this.startedRow.getAttribute('data-uid') : null;
          var toUid_1 = !sf.base.isNullOrUndefined(sf.base.closest(e.target, 'tr')) ? sf.base.closest(e.target, 'tr').getAttribute('data-uid') : null;
          var toIdx_1 = _this.parent.options.enableVirtualization ? fromIdx_1 === _this.dragTarget ? _this.dragTarget : fromIdx_1 < _this.borderIndex ? _this.borderIndex : _this.borderIndex + 1 : _this.parent.options.frozenRows ? fromIdx_1 < _this.borderIndex ? _this.borderIndex : _this.borderIndex + 1 : _this.dragTarget;
          if (e.target && e.target.classList.contains('e-content') && gObj.options.allowEmptyAreaDrop) {
            var lastrow = gObj.getContentTable().querySelector('tr:last-child');
            if (lastrow && !sf.base.isNullOrUndefined(cloneElementGrid) && !sf.base.isNullOrUndefined(targetElementGrid) && cloneElementGrid.id === targetElementGrid.id) {
              toIdx_1 = parseInt(lastrow.getAttribute('aria-rowindex'), 10) - 1;
              toUid_1 = lastrow.getAttribute('data-uid');
            }
          }
          if (Number.isNaN(toIdx_1) || sf.base.isNullOrUndefined(toIdx_1)) {
            if (gObj.options.showAddNewRow && _this.showAddNewRowDisable) {
              gObj.dotNetRef.invokeMethodAsync('DisableShowAddForm', 'RowDragStop', true, null);
              _this.showAddNewRowDisable = false;
            }
            return;
          }
          setTimeout(function () {
            if (fromIdx_1 !== toIdx_1) {
              gObj.dotNetRef.invokeMethodAsync('ReorderRows', fromIdx_1, toIdx_1, 'delete', true, targetClass_1, targetID_1, null, null, false, false, fromUid_1, toUid_1, false, getPosition(e.event).x, getPosition(e.event).y);
            }
          }, 10);
          _this.dragTarget = null;
        }
        if (gObj.options.showAddNewRow && _this.showAddNewRowDisable) {
          gObj.dotNetRef.invokeMethodAsync('DisableShowAddForm', 'RowDragStop', false, null);
          _this.showAddNewRowDisable = false;
        }
      };
      this.removeCell = function (targetRow, className) {
        return [].slice.call(targetRow.querySelectorAll('td')).filter(function (cell) {
          if (cell.classList.contains(className)) {
            targetRow.deleteCell(cell.cellIndex);
          }
        });
      };
      this.drop = function (e) {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        var droppedElement = e.droppedElement || e.helper;
        _this.columnDrop({
          target: e.target,
          droppedElement: droppedElement,
          mouseEvent: e.event
        });
        sf.base.remove(droppedElement);
      };
      this.parent = parent;
      if (this.parent.options.allowRowDragAndDrop) {
        this.initializeDrag();
      }
    }
    RowDD.prototype.stopTimer = function () {
      window.clearInterval(this.timer);
    };
    RowDD.prototype.initializeDrag = function () {
      var gObj = this.parent;
      this.draggable = new sf.base.Draggable(gObj.getContent(), {
        dragTarget: '.e-rowcelldrag, .e-rowdragdrop, .e-rowcell',
        distance: 5,
        helper: this.helper,
        dragStart: this.dragStart,
        drag: this.drag,
        dragStop: this.dragStop,
        isPreventSelect: false
      });
      this.droppable = new sf.base.Droppable(gObj.getContent(), {
        accept: '.e-dragclone',
        drop: this.drop
      });
    };
    RowDD.prototype.updateScrollPostion = function (e, targetRow) {
      var _this = this;
      var y = getPosition(e).y;
      var scrollElement = this.destinationGrid.getContent();
      var clientRect = scrollElement.getBoundingClientRect();
      var rowHeight = this.destinationGrid.getRowHeight() - 15;
      if (clientRect.top + rowHeight >= y || clientRect.top + scrollElement.clientHeight - rowHeight - 20 <= y) {
        var scrollPixel_1 = clientRect.top + rowHeight >= y ? -this.destinationGrid.getRowHeight() : this.destinationGrid.getRowHeight();
        this.isOverflowBorder = false;
        this.stopTimer();
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        this.timer = window.setInterval(function () {
          _this.setScrollDown(scrollElement, scrollPixel_1, targetRow);
        }, 200);
      }
    };
    RowDD.prototype.setScrollDown = function (scrollElement, scrollPixel, targetRow) {
      scrollElement.scrollTop = scrollElement.scrollTop + scrollPixel;
      if (!sf.base.isNullOrUndefined(targetRow)) {
        var targetRowIndex = parseInt(targetRow.getAttribute('aria-rowindex'), 10) - 1;
        var lastRowIndex = scrollElement.querySelectorAll('tr.e-row:not(.e-emptyrow)').length - 1;
        var rowElement = [].slice.call(scrollElement.querySelectorAll('.e-dragborder'));
        if (targetRowIndex !== lastRowIndex && rowElement.length > 0) {
          addRemoveActiveClasses(rowElement, false, 'e-dragborder');
        }
      }
    };
    RowDD.prototype.moveDragRows = function (e, startedRow, targetRow) {
      var cloneElement = document.querySelector('.e-cloneproperties');
      var element = sf.base.closest(e.target, 'tr');
      if (parentsUntil(element, 'e-gridcontent') && (!sf.base.isNullOrUndefined(cloneElement) && (!sf.base.isNullOrUndefined(this.cloneElementGrid) ? this.cloneElementGrid.id : parentsUntil(cloneElement.parentElement, 'e-grid').id) === parentsUntil(element, 'e-grid').id || this.istargetGrid)) {
        var targetElement = element ? element : this.startedRow;
        this.setBorder(targetElement, e.event, startedRow, targetRow);
      }
    };
    RowDD.prototype.setBorder = function (element, event, startedRow, targetRow) {
      var node = this.parent.element;
      if (this.istargetGrid) {
        node = this.destinationGrid.element;
      }
      var cloneElement = document.querySelector('.e-cloneproperties');
      if (this.parent.element.id !== this.destinationGrid.element.id && (this.parent.element.getElementsByClassName('e-firstrow-dragborder').length > 0 || this.parent.element.getElementsByClassName('e-lastrow-dragborder').length > 0)) {
        this.removeTargetGridBorder(this.parent);
      }
      if (this.parent.options.groupCount) {
        this.removeBorder(element);
      } else {
        this.removeFirstRowBorder(element);
        this.removeLastRowBorder(element);
      }
      this.parent.element.querySelector('.e-dragstartrow');
      // Get the mouse position relative to the target row
      var mouseY = getPosition(event).y - targetRow.getBoundingClientRect().top;
      // Calculate the midpoint of the target row
      var targetMidpoint = targetRow.offsetHeight / 2;
      if (!sf.base.isNullOrUndefined(targetRow)) {
        var targetRowGrid = parentsUntil(targetRow, 'e-grid');
        if (!sf.base.isNullOrUndefined(targetRowGrid) && targetRowGrid.id === parentsUntil(startedRow, 'e-grid').parentElement.id && targetRow.getAttribute('aria-selected') === 'true') {
          this.removeBorder(element);
          return;
        }
      }
      if (parentsUntil(element, 'e-gridcontent') && ((!sf.base.isNullOrUndefined(this.cloneElementGrid) ? this.cloneElementGrid.id : parentsUntil(cloneElement.parentElement, 'e-grid').id) === parentsUntil(element, 'e-grid').id || this.istargetGrid)) {
        sf.base.removeClass(node.querySelectorAll('.e-rowcell,.e-rowdragdrop,.e-detailrowcollapse, .e-detailrowexpand'), ['e-dragborder']);
        var rowElement = [];
        var targetRowIndex = parseInt(targetRow.getAttribute('aria-rowindex'), 10) - 1;
        var lastRow = this.destinationGrid.getContentTable().querySelector('tr:last-child');
        var addNewRowElement = this.destinationGrid.element.querySelector('.e-showAddNewRow');
        if (targetRow && targetRowIndex === 0 && sf.base.isNullOrUndefined(addNewRowElement) && (this.parent.element.id !== this.destinationGrid.element.id && mouseY < targetMidpoint || this.parent.element.id === this.destinationGrid.element.id)) {
          if (!targetRow.classList.contains('e-emptyrow') && targetRow.classList.contains('e-row')) {
            if (this.parent.options.groupCount && !targetRow.classList.contains('e-groupcaption')) {
              element = targetRow;
              rowElement = [].slice.call(element.querySelectorAll('.e-groupcaption,.e-summarycell,.e-rowcell,.e-rowdragdrop,.e-detailrowcollapse, .e-detailrowexpand'));
            } else {
              var div = sf.base.createElement('div', {
                className: 'e-firstrow-dragborder'
              });
              var gridheaderEle = this.destinationGrid.getHeaderContent();
              gridheaderEle.classList.add('e-grid-relative');
              if (!sf.base.isNullOrUndefined(this.destinationGrid.getContent())) {
                var isHorizontalScrollAlone = this.destinationGrid.getContent().scrollWidth > this.destinationGrid.getContent().clientWidth && this.destinationGrid.getContent().scrollHeight <= this.destinationGrid.getContent().clientHeight;
                div.style.width = isHorizontalScrollAlone ? node.offsetWidth + 'px' : node.offsetWidth - this.getScrollWidth() + 'px';
              }
              if (!gridheaderEle.querySelectorAll('.e-firstrow-dragborder').length) {
                gridheaderEle.appendChild(div);
              }
            }
          }
          this.borderIndex = -1;
          if (this.destinationGrid.element.getElementsByClassName('e-lastrow-dragborder').length > 0) {
            this.destinationGrid.element.getElementsByClassName('e-lastrow-dragborder')[0].remove();
          } else if (document.getElementsByClassName('e-lastrow-dragborder').length > 0) {
            var lastRowBorder = document.getElementsByClassName('e-lastrow-dragborder')[0];
            if (!sf.base.isNullOrUndefined(parentsUntil(lastRowBorder, 'e-grid'))) {
              lastRowBorder.remove();
            }
          }
        } else if (targetRow && parseInt(startedRow.getAttribute('aria-rowindex'), 10) - 1 > targetRowIndex || this.parent.options.rowDropTarget && this.parent.element.id !== this.destinationGrid.element.id) {
          if (this.parent.options.groupCount && this.parent.options.enableVirtualization) {
            targetRowIndex = this.parent.getDataRows().indexOf(targetRow);
          }
          if (this.parent.options.groupCount && !targetRow.classList.contains('e-groupcaption')) {
            element = targetRow;
            if (!sf.base.isNullOrUndefined(element) && !element.classList.contains('e-detailrow')) {
              rowElement = [].slice.call(element.querySelectorAll('.e-rowcell,.e-rowdragdrop,.e-detailrowcollapse, .e-detailrowexpand'));
            }
          } else {
            if (targetRow === lastRow && mouseY > targetMidpoint) {
              element = this.destinationGrid.getRowByIndex(targetRowIndex);
            } else {
              element = this.destinationGrid.options.frozenRows ? this.destinationGrid.getRowByIndex(targetRowIndex) : targetRow.classList.contains('e-row') ? this.destinationGrid.getRowByIndex(targetRow.rowIndex - 1) : element;
            }
            if (!sf.base.isNullOrUndefined(element) && !element.classList.contains('e-detailrow')) {
              rowElement = [].slice.call(element.querySelectorAll('.e-rowcell,.e-rowdragdrop,.e-detailrowcollapse, .e-detailrowexpand'));
            }
          }
        } else {
          if (!sf.base.isNullOrUndefined(element) && !element.classList.contains('e-detailrow') && !cloneElement.classList.contains('e-notallowedcur')) {
            rowElement = [].slice.call(element.querySelectorAll('.e-rowcell,.e-rowdragdrop,.e-detailrowcollapse, .e-detailrowexpand'));
          }
        }
        if (rowElement.length > 0) {
          this.borderIndex = Number(parentsUntil(rowElement[0], 'e-row').getAttribute('aria-rowindex')) - 1;
          if (!sf.base.isNullOrUndefined(parentsUntil(targetRow, 'e-editedrow'))) {
            sf.base.classList(cloneElement, ['e-notallowedcur'], ['e-defaultcur']);
          }
          if (!this.parent.options.groupCount) {
            if (sf.base.isNullOrUndefined(parentsUntil(targetRow, 'e-editedrow'))) {
              addRemoveActiveClasses(rowElement, true, 'e-dragborder');
            }
            if (this.destinationGrid.element.getElementsByClassName('e-lastrow-dragborder').length > 0) {
              this.destinationGrid.element.getElementsByClassName('e-lastrow-dragborder')[0].remove();
            } else if (document.getElementsByClassName('e-lastrow-dragborder').length > 0) {
              var lastRowBorder = document.getElementsByClassName('e-lastrow-dragborder')[0];
              if (!sf.base.isNullOrUndefined(parentsUntil(lastRowBorder, 'e-grid'))) {
                lastRowBorder.remove();
              }
            }
            if (this.destinationGrid.element.getElementsByClassName('e-firstrow-dragborder').length > 0) {
              this.destinationGrid.element.getElementsByClassName('e-firstrow-dragborder')[0].remove();
            } else if (document.getElementsByClassName('e-firstrow-dragborder').length > 0) {
              var lastRowBorder = document.getElementsByClassName('e-firstrow-dragborder')[0];
              if (!sf.base.isNullOrUndefined(parentsUntil(lastRowBorder, 'e-grid'))) {
                lastRowBorder.remove();
              }
            }
          }
        }
      }
    };
    RowDD.prototype.addorRemoveDashedBorder = function (e, add, dataRowElements) {
      if (dataRowElements.length <= 0) {
        return;
      }
      var firstDataRow = dataRowElements[0];
      var lastDataRow = dataRowElements[dataRowElements.length - 1];
      var firstDataRowCells = [];
      var lastDataRowCells = [];
      firstDataRowCells = [].slice.call(firstDataRow.querySelectorAll('.e-rowcell:not(.e-hide),.e-rowdragdrop,.e-detailrowcollapse, .e-detailrowexpand'));
      lastDataRowCells = [].slice.call(lastDataRow.querySelectorAll('.e-rowcell:not(.e-hide),.e-rowdragdrop,.e-detailrowcollapse, .e-detailrowexpand'));
      addRemoveActiveClasses(firstDataRowCells, add, 'e-dragtop');
      addRemoveActiveClasses(lastDataRowCells, add, 'e-dragbottom');
      this.updateDragClasses(add, dataRowElements);
    };
    RowDD.prototype.updateDragClasses = function (add, dataRowElements) {
      for (var i = 0; i < dataRowElements.length; i++) {
        var rowElementCells = [];
        rowElementCells = [].slice.call(dataRowElements[parseInt(i.toString(), 10)].querySelectorAll('.e-rowcell:not(.e-hide),.e-rowdragdrop,.e-detailrowcollapse, .e-detailrowexpand'));
        if (rowElementCells.length) {
          if (add) {
            rowElementCells[0].classList.add('e-dragleft');
            rowElementCells[rowElementCells.length - 1].classList.add('e-dragright');
          } else {
            rowElementCells[0].classList.remove('e-dragleft');
            rowElementCells[rowElementCells.length - 1].classList.remove('e-dragright');
          }
        }
      }
    };
    RowDD.prototype.getScrollWidth = function () {
      var scrollElem = !sf.base.isNullOrUndefined(this.destinationGrid) && this.destinationGrid.element.id !== this.parent.element.id ? this.destinationGrid.getContent() : this.parent.getContent();
      return scrollElem.scrollWidth > scrollElem.offsetWidth ? getScrollBarWidth() : 0;
    };
    RowDD.prototype.removeFirstRowBorder = function (element) {
      if (this.destinationGrid.element.getElementsByClassName('e-firstrow-dragborder').length > 0 && element && element.rowIndex !== 0) {
        this.destinationGrid.element.getElementsByClassName('e-firstrow-dragborder')[0].remove();
      }
      var cloneElement = document.querySelector('.e-cloneproperties');
      var dropTarget = document.getElementById(this.parent.options.rowDropTarget);
      if (sf.base.isNullOrUndefined(element) && document.querySelectorAll('.e-firstrow-dragborder').length > 0 && !sf.base.isNullOrUndefined(cloneElement) && cloneElement.classList.contains('e-notallowedcur') && !sf.base.isNullOrUndefined(dropTarget)) {
        if (dropTarget.querySelectorAll('.e-firstrow-dragborder').length > 0) {
          dropTarget.querySelectorAll('.e-firstrow-dragborder')[0].remove();
        }
      }
    };
    RowDD.prototype.removeLastRowBorder = function (element) {
      var islastRowIndex = element && !sf.base.isNullOrUndefined(this.destinationGrid.getRowByIndex(this.destinationGrid.getRows().length - 1)) && this.destinationGrid.getRowByIndex(this.destinationGrid.getRows().length - 1).getAttribute('data-uid') !== element.getAttribute('data-uid');
      if (this.destinationGrid.element.getElementsByClassName('e-lastrow-dragborder').length > 0 && element && islastRowIndex) {
        this.destinationGrid.element.getElementsByClassName('e-lastrow-dragborder')[0].remove();
      }
    };
    RowDD.prototype.removeBorder = function (element) {
      this.removeFirstRowBorder(element);
      this.removeLastRowBorder(element);
      var dropTarget = document.getElementById(this.parent.options.rowDropTarget);
      var cloneElement = document.querySelector('.e-cloneproperties');
      element = this.destinationGrid.getRows().filter(function (row) {
        return row.querySelector('td.e-dragborder');
      })[0];
      if (element) {
        var rowElement = [].slice.call(element.querySelectorAll('.e-dragborder'));
        addRemoveActiveClasses(rowElement, false, 'e-dragborder');
      } else if (sf.base.isNullOrUndefined(element) && !sf.base.isNullOrUndefined(dropTarget) && !sf.base.isNullOrUndefined(cloneElement) && cloneElement.classList.contains('e-notallowedcur')) {
        var dragBorders = [].slice.call(dropTarget.querySelectorAll('.e-dragborder'));
        addRemoveActiveClasses(dragBorders, false, 'e-dragborder');
      }
    };
    RowDD.prototype.getElementFromPosition = function (element, event) {
      var position = getPosition(event);
      element.style.display = 'none';
      var target = document.elementFromPoint(position.x, position.y);
      element.style.display = '';
      return target;
    };
    RowDD.prototype.getElementXPath = function (element) {
      if (!element) {
        return null;
      }
      if (element.id) {
        return "//[@id=".concat(element.id, "]") + (element.className !== '' ? '.' + element.className.toLowerCase() : '');
      } else if (element.tagName === 'BODY') {
        return '/html/body';
      } else {
        if (sf.base.isNullOrUndefined(element) || sf.base.isNullOrUndefined(element.parentElement) || sf.base.isNullOrUndefined(element.parentElement.childNodes)) {
          return null;
        }
        var sameTagSiblings = [].slice.call(element.parentElement.childNodes).filter(function (e) {
          return e.nodeName === element.nodeName;
        });
        var idx = sameTagSiblings.indexOf(element);
        return this.getElementXPath(element.parentNode) + '/' + element.tagName.toLowerCase() + (element.className !== '' ? '.' + element.className.toLowerCase() : '') + (sameTagSiblings.length > 1 ? "[".concat(idx + 1, "]") : '');
      }
    };
    RowDD.prototype.getTargetIdx = function (targetRow) {
      return targetRow ? parseInt(targetRow.getAttribute('aria-rowindex'), 10) - 1 : 0;
    };
    RowDD.prototype.columnDrop = function (e) {
      var gObj = this.parent;
      var rowDragTargetId = null;
      var cloneElement = document.querySelector('.e-cloneproperties');
      if (e.target.classList.contains('e-lastrow-dragborder') && !sf.base.isNullOrUndefined(cloneElement) && !cloneElement.classList.contains('e-notallowedcur')) {
        var targetElementGrid = parentsUntil(e.target, 'e-grid');
        if (!sf.base.isNullOrUndefined(targetElementGrid)) {
          // eslint-disable-next-line @typescript-eslint/no-explicit-any
          gObj = targetElementGrid.blazor__instance;
        }
      }
      if (parentsUntil(gObj.element, 'e-detailrow') && (sf.base.isNullOrUndefined(this.cloneElementGrid) || !sf.base.isNullOrUndefined(e.droppedElement) && !sf.base.isNullOrUndefined(e.droppedElement.parentElement) && e.droppedElement.parentElement.id !== gObj.element.id) && !sf.base.isNullOrUndefined(gObj.options.rowDropTarget)) {
        this.cloneElementGrid = document.getElementById(gObj.options.rowDropTarget);
        if (!sf.base.isNullOrUndefined(this.cloneElementGrid)) {
          rowDragTargetId = this.cloneElementGrid.id;
        }
      }
      var targetGridId = rowDragTargetId ? rowDragTargetId : e.droppedElement.parentElement.id;
      if (e.droppedElement.getAttribute('action') !== 'grouping') {
        var targetRow = sf.base.closest(e.target, 'tr');
        var srcControl = void 0;
        if (e.droppedElement.querySelector('tr').getAttribute('single-dragrow') !== 'true' && targetGridId === gObj.element.id || e.droppedElement.querySelector('tr').getAttribute('single-dragrow') === 'true' && targetGridId !== gObj.element.id) {
          this.removeTargetGridBorder(this.parent);
          this.removeTargetGridBorder(this.destinationGrid);
          return;
        }
        if (targetGridId !== gObj.element.id) {
          // eslint-disable-next-line @typescript-eslint/no-explicit-any
          srcControl = this.cloneElementGrid && rowDragTargetId ? this.cloneElementGrid.blazor__instance : e.droppedElement.parentElement.blazor__instance;
        }
        var dragStartRow = srcControl.content.querySelector('.e-dragstartrow');
        if (srcControl.element.id !== gObj.element.id && srcControl.options.rowDropTarget !== gObj.element.id) {
          if (!sf.base.isNullOrUndefined(dragStartRow)) {
            dragStartRow.classList.remove('e-dragstartrow');
            if (gObj.element.getElementsByClassName('e-lastrow-dragborder').length > 0) {
              this.removeTargetGridBorder(gObj);
            }
          }
          return;
        }
        var targetIndex = this.getTargetIdx(targetRow);
        var lastRow = gObj.getContentTable().querySelector('tr:last-child');
        //const currentIndex = targetIndex;
        if (!sf.base.isNullOrUndefined(targetRow)) {
          // Get the mouse position relative to the target row
          var mouseY = getPosition(e.mouseEvent).y - targetRow.getBoundingClientRect().top;
          // Calculate the midpoint of the target row
          var targetMidpoint = targetRow.offsetHeight / 2;
          if (e.target && !lastRow.classList.contains('e-emptyrow')) {
            if (lastRow && targetRow === lastRow && mouseY > targetMidpoint) {
              targetIndex = parseInt(lastRow.getAttribute('aria-rowindex'), 10);
            }
          }
        }
        if (e.target && e.target.classList.contains('e-content') && gObj.options.allowEmptyAreaDrop) {
          if (lastRow) {
            targetIndex = parseInt(lastRow.getAttribute('aria-rowindex'), 10);
          }
        }
        if (isNaN(targetIndex)) {
          targetIndex = 0;
        }
        if (gObj.options.allowPaging) {
          targetIndex = targetIndex + gObj.options.currentPage * gObj.options.pageSize - gObj.options.pageSize;
        }
        var targetClass = this.getElementXPath(e.target);
        var offsetParent = parentsUntil(e.target, 'e-grid');
        var targetID = !sf.base.isNullOrUndefined(offsetParent) ? offsetParent.id : '';
        var positions = e.target.getBoundingClientRect();
        this.removeTargetGridBorder(this.parent);
        var dragBetweenGrid = false;
        var isClonedRowNotSelected = false;
        var clonedRowElement = parentsUntil(dragStartRow, 'e-row');
        if (!sf.base.isNullOrUndefined(clonedRowElement)) {
          var ariaSelected = clonedRowElement.getAttribute('aria-selected');
          isClonedRowNotSelected = ariaSelected === 'false' || sf.base.isNullOrUndefined(ariaSelected);
        }
        var fromIndex = isClonedRowNotSelected ? parseInt(clonedRowElement.getAttribute('aria-rowindex'), 10) - 1 : 0;
        if (gObj.options.tValue === srcControl.options.tValue) {
          if (!sf.base.isNullOrUndefined(dragStartRow)) {
            dragBetweenGrid = isClonedRowNotSelected;
            dragStartRow.classList.remove('e-dragstartrow');
          }
          gObj.dotNetRef.invokeMethodAsync('ReorderRows', fromIndex, targetIndex, 'add', false, targetClass, targetID, positions, srcControl.dotNetRef, false, false, null, null, dragBetweenGrid, getPosition(e.mouseEvent).x, getPosition(e.mouseEvent).y);
          srcControl.dotNetRef.invokeMethodAsync('ReorderRows', fromIndex, targetIndex, 'delete', false, targetClass, targetID, positions, null, false, false, null, null, dragBetweenGrid, getPosition(e.mouseEvent).x, getPosition(e.mouseEvent).y);
        } else {
          if (!sf.base.isNullOrUndefined(dragStartRow)) {
            dragStartRow.classList.remove('e-dragstartrow');
          }
          srcControl.dotNetRef.invokeMethodAsync('ReorderRows', fromIndex, targetIndex, 'delete', false, targetClass, targetID, positions, null, false, true, null, null, dragBetweenGrid, getPosition(e.mouseEvent).x, getPosition(e.mouseEvent).y);
        }
      }
    };
    RowDD.prototype.removeTargetGridBorder = function (grid) {
      if (!sf.base.isNullOrUndefined(grid)) {
        if (grid.element.getElementsByClassName('e-firstrow-dragborder').length > 0) {
          grid.element.getElementsByClassName('e-firstrow-dragborder')[0].remove();
        }
        if (grid.element.getElementsByClassName('e-lastrow-dragborder').length > 0) {
          grid.element.getElementsByClassName('e-lastrow-dragborder')[0].remove();
        } else if (document.getElementsByClassName('e-lastrow-dragborder').length > 0) {
          var lastRowBorder = document.getElementsByClassName('e-lastrow-dragborder')[0];
          if (!sf.base.isNullOrUndefined(parentsUntil(lastRowBorder, 'e-grid'))) {
            lastRowBorder.remove();
          }
        }
        sf.base.removeClass(grid.element.querySelectorAll('.e-rowcell.e-dragborder,.e-detailrowcollapse.e-dragborder, .e-rowdragdrop.e-dragborder, e-detailrowexpand.e-dragborder'), ['e-dragborder']);
      }
    };
    RowDD.prototype.isTargetInEditMode = function (targetElement) {
      if (!sf.base.isNullOrUndefined(parentsUntil(targetElement, 'e-editedrow')) || !sf.base.isNullOrUndefined(parentsUntil(targetElement, 'e-editedbatchcell')) || !sf.base.isNullOrUndefined(parentsUntil(targetElement, 'e-addedrow')) || !sf.base.isNullOrUndefined(parentsUntil(targetElement, 'e-showAddNewRow'))) {
        return true;
      }
      return false;
    };
    /**
     * To destroy the print
     *
     * @returns {void}
     * @hidden
     */
    RowDD.prototype.destroy = function () {
      var gridElement = this.parent.element;
      if (!gridElement || !gridElement.querySelector('.e-gridheader') && !gridElement.querySelector('.e-gridcontent')) {
        return;
      }
      if (!sf.base.isNullOrUndefined(this.draggable)) {
        this.draggable.destroy();
      }
      if (!sf.base.isNullOrUndefined(this.droppable)) {
        this.droppable.destroy();
      }
    };
    return RowDD;
  }();
  var __spreadArray$1 = undefined && undefined.__spreadArray || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar) ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
  };
  var Selection = /** @class */function () {
    function Selection(parent) {
      this.parent = parent;
      this.addEventListener();
    }
    Selection.prototype.addEventListener = function () {
      sf.base.EventHandler.add(this.parent.getContent().parentElement, 'mousedown', this.mouseDownHandler, this);
      if (this.parent.options.allowDragSelection) {
        sf.base.EventHandler.add(this.parent.getContent().parentElement, 'touchstart', this.mouseDownHandler, this);
      }
    };
    Selection.prototype.removeEventListener = function () {
      sf.base.EventHandler.remove(this.parent.getContent().parentElement, 'mousedown', this.mouseDownHandler);
      if (this.parent.options.allowDragSelection) {
        sf.base.EventHandler.remove(this.parent.getContent().parentElement, 'touchstart', this.mouseDownHandler);
      }
    };
    Selection.prototype.mouseDownHandler = function (e) {
      var target = e.target;
      var gObj = this.parent;
      var isDrag;
      var gridElement = parentsUntil(target, 'e-grid');
      if (gObj.options.isRenderedFromGantt && !gObj.options.allowDragSelection) {
        return;
      }
      if (gObj.rowDragAndDropModule.isTargetInEditMode(target)) {
        return;
      }
      if (gridElement && gridElement.id !== gObj.element.id || parentsUntil(target, 'e-headercontent') && !this.parent.options.frozenRows) {
        return;
      }
      if (e.shiftKey || e.ctrlKey) {
        e.preventDefault();
      }
      // Ensure focus on template cell during multi-select (Ctrl/Shift + left mouse button click)
      if (gObj.options.selectionType === 'Multiple' && (e.ctrlKey || e.shiftKey) && e.button === 0) {
        var templateCell = parentsUntil(e.target, 'e-templatecell');
        if (templateCell) {
          templateCell.focus();
        }
      }
      if (parentsUntil(target, 'e-rowcell') && !e.shiftKey && !e.ctrlKey) {
        if ((gObj.options.cellSelectionMode.indexOf('Box') > -1 || (gObj.options.allowDragSelection && !gObj.options.enableAutoFill && gObj.options.cellSelectionMode.indexOf('Flow')) > -1) && e.button !== 2 && !this.isRowType() && !this.isSingleSel()) {
          this.isCellDrag = true;
          isDrag = true;
        } else {
          this.isCellDrag = false;
        }
        if ((gObj.options.allowRowDragAndDrop || gObj.options.allowDragSelection && !gObj.options.enableAutoFill) && !gObj.options.isEdit && e.button !== 2) {
          if (!gObj.options.allowDragSelection && (!this.isRowType() || sf.base.closest(target, 'td').classList.contains('e-selectionbackground')) || this.isSingleSel()) {
            if (gObj.options.allowDragSelection) {
              document.body.classList.add('e-disableuserselect');
            }
            this.isDragged = false;
            return;
          }
          isDrag = true;
          if (sf.base.isNullOrUndefined(gridElement.querySelector('.e-griddragarea'))) {
            this.element = sf.base.createElement('div', {
              className: 'e-griddragarea'
            });
            this.element.style.left = 0 + 'px';
            this.element.style.top = 0 + 'px';
            this.element.style.zIndex = '10';
            gObj.getContent().appendChild(this.element);
          }
        }
        if (isDrag) {
          this.isAutoFillSel = false;
          this.enableDrag(e, true);
        }
      }
      this.updateStartEndCells();
      if (target.classList.contains('e-autofill') || target.classList.contains('e-xlsel')) {
        this.isCellDrag = true;
        this.isAutoFillSel = true;
        this.enableDrag(e);
        document.body.style.cursor = 'crosshair';
      }
    };
    Selection.prototype.mouseUpHandler = function (e) {
      document.body.classList.remove('e-disableuserselect');
      if (!sf.base.isNullOrUndefined(this.element) && !sf.base.isNullOrUndefined(this.element.parentNode)) {
        sf.base.remove(this.element);
      }
      var targetGrid = parentsUntil(e.target, 'e-grid');
      var targetGridId = targetGrid ? targetGrid.id : null;
      if (!this.isCellDrag && !sf.base.isNullOrUndefined(this.prevStartDIndex) && !sf.base.isNullOrUndefined(this.prevEndIndex) && !isNaN(this.prevStartDIndex) && !isNaN(this.prevEndIndex)) {
        var targetElement = sf.base.closest(e.target, 'td');
        var endDragCellIndex = targetElement ? parseInt(targetElement.getAttribute('aria-colindex'), 10) - 1 : NaN;
        if (!isNaN(this.startDragCellIndex) && !isNaN(endDragCellIndex) && !sf.base.isNullOrUndefined(this.startDragCellIndex) && !sf.base.isNullOrUndefined(endDragCellIndex)) {
          this.parent.dotNetRef.invokeMethodAsync('DragSelection', this.prevStartDIndex, this.prevEndIndex, false, targetGridId, this.startDragCellIndex, endDragCellIndex);
        } else {
          this.parent.dotNetRef.invokeMethodAsync('DragSelection', this.prevStartDIndex, this.prevEndIndex, false, targetGridId, 0, 0);
        }
      } else if (this.parent.options.allowDragSelection && !this.parent.options.enableAutoFill && this.isDragged && !this.isAutoFillSel && !sf.base.isNullOrUndefined(this.startDragIndex) && !sf.base.isNullOrUndefined(this.endDragIndex) && !sf.base.isNullOrUndefined(this.endDragCellIndex)) {
        this.isDragged = false;
        this.parent.dotNetRef.invokeMethodAsync('DragCellSelection', this.startDragIndex, this.startDragCellIndex, this.endDragIndex, this.endDragCellIndex, false, targetGridId);
      }
      this.isDragged = false;
      document.body.style.cursor = 'default';
      this.prevStartDIndex = undefined;
      this.prevEndIndex = undefined;
      this.startDragIndex = undefined;
      this.startDragCellIndex = undefined;
      this.endDragIndex = undefined;
      this.endDragCellIndex = undefined;
      if (this.parent.options.editMode === 'Batch' && this.parent.options.enableAutoFill) {
        if (!sf.base.isNullOrUndefined(this.endRowIndex) && !sf.base.isNullOrUndefined(this.endColIndex) && !this.isAutoFillSel && this.isInitialSelect) {
          this.parent.dotNetRef.invokeMethodAsync('ClearSelection');
          var updateAFPos = this.updateAutofillPosition(this.endColIndex, this.endRowIndex, true);
          this.parent.dotNetRef.invokeMethodAsync('UpdateAutofillPositions', updateAFPos, 'UpdateAutofillBox');
          this.assignCells();
          this.selectCellByRow();
          this.isInitialSelect = false;
        }
        if (this.isAutoFillSel) {
          // eslint-disable-next-line @typescript-eslint/no-this-alias
          var _this_1 = this;
          this.assignCells();
          setTimeout(function () {
            _this_1.selectCellByRow();
          }, 10);
          this.expandAFBorder(e, true);
          var updateAFBor = this.createBorder(this.startRowIndex, this.startColIndex, this.endRowIndex, this.endColIndex, true);
          this.parent.dotNetRef.invokeMethodAsync('UpdateAutofillPositions', updateAFBor, 'UpdateAutofillBorder');
          var updateAFPos = this.updateAutofillPosition(this.endColIndex, this.endRowIndex, true);
          this.parent.dotNetRef.invokeMethodAsync('UpdateAutofillPositions', updateAFPos, 'UpdateAutofillBox');
        }
      }
      sf.base.EventHandler.remove(this.parent.getContent(), 'mousemove', this.mouseMoveHandler);
      if (this.parent.options.allowDragSelection) {
        sf.base.EventHandler.remove(this.parent.getContent(), 'touchmove', this.mouseMoveHandler);
      }
      if (this.parent.options.frozenRows) {
        sf.base.EventHandler.remove(this.parent.getHeaderContent(), 'mousemove', this.mouseMoveHandler);
        if (this.parent.options.allowDragSelection) {
          sf.base.EventHandler.remove(this.parent.getHeaderContent(), 'touchmove', this.mouseMoveHandler);
        }
      }
      sf.base.EventHandler.remove(document.body, 'mouseup', this.mouseUpHandler);
      if (this.parent.options.allowDragSelection) {
        sf.base.EventHandler.remove(document.body, 'touchend', this.mouseUpHandler);
      }
    };
    Selection.prototype.enableDrag = function (e, isUpdate) {
      var gObj = this.parent;
      if (isUpdate) {
        var tr = sf.base.closest(e.target, 'tr');
        this.startDragIndex = parseInt(tr.getAttribute('aria-rowindex'), 10) - 1;
        this.startDragCellIndex = parseInt(parentsUntil(e.target, 'e-rowcell').getAttribute('aria-colindex'), 10) - 1;
      }
      document.body.classList.add('e-disableuserselect');
      var gBRect = gObj.element.getBoundingClientRect();
      var postion = getPosition(e);
      this.x = postion.x - gBRect.left;
      this.y = postion.y - gBRect.top;
      sf.base.EventHandler.add(gObj.getContent(), 'mousemove', this.mouseMoveHandler, this);
      if (this.parent.options.allowDragSelection) {
        sf.base.EventHandler.add(gObj.getContent(), 'touchmove', this.mouseMoveHandler, this);
      }
      if (this.parent.options.frozenRows) {
        sf.base.EventHandler.add(gObj.getHeaderContent(), 'mousemove', this.mouseMoveHandler, this);
        if (this.parent.options.allowDragSelection) {
          sf.base.EventHandler.add(gObj.getHeaderContent(), 'touchmove', this.mouseMoveHandler, this);
        }
      }
      sf.base.EventHandler.add(document.body, 'mouseup', this.mouseUpHandler, this);
      if (this.parent.options.allowDragSelection) {
        sf.base.EventHandler.add(document.body, 'touchend', this.mouseUpHandler, this);
      }
      if (!sf.base.isNullOrUndefined(this.startDragIndex) && !sf.base.isNullOrUndefined(this.startDragCellIndex) && !isNaN(this.startDragIndex) && !isNaN(this.startDragCellIndex)) {
        this.parent.dotNetRef.invokeMethodAsync('DragSelectionStarted', this.startDragIndex, this.startDragCellIndex);
      }
    };
    Selection.prototype.mouseMoveHandler = function (e) {
      if (!this.parent.options.allowDragSelection && e.type !== 'touchmove') {
        e.preventDefault();
      }
      var gBRect = this.parent.element.getBoundingClientRect();
      var x1 = this.x;
      var y1 = this.y;
      var position = getPosition(e);
      var x2 = position.x - gBRect.left;
      var y2 = position.y - gBRect.top;
      var xPos = e.type === 'touchmove' ? e.touches[0].pageX : e.pageX;
      var yPos = e.type === 'touchmove' ? e.touches[0].pageY : e.pageY;
      var eleLocation = yPos + 2;
      var tmp;
      var target;
      if (!sf.base.isNullOrUndefined(e.touches) && !sf.base.isNullOrUndefined(document.elementFromPoint(e.touches[0].clientX, e.touches[0].clientY))) {
        target = sf.base.closest(document.elementFromPoint(e.touches[0].clientX, e.touches[0].clientY), 'tr');
      } else {
        target = sf.base.closest(e.target, 'tr');
      }
      this.isDragged = true;
      if (!this.isCellDrag || this.parent.options.allowDragSelection && !this.parent.options.enableAutoFill && this.isCellDrag) {
        if (!target) {
          target = sf.base.closest(document.elementFromPoint(this.parent.element.offsetLeft + 2, !sf.base.isNullOrUndefined(e.touches) ? e.touches[0].clientY : e.clientY), 'tr');
        }
        if (x1 > x2) {
          tmp = x2;
          x2 = x1;
          x1 = tmp;
        }
        if (y1 > y2) {
          tmp = y2;
          y2 = y1;
          y1 = tmp;
          eleLocation = yPos - 2;
        }
        var classList = ['.e-gridheader', '.e-groupdroparea', '.e-toolbar'];
        var siblingHeight = 0;
        for (var i = 0; i < classList.length; i++) {
          var sibling = this.parent.element.querySelector(classList[parseInt(i.toString(), 10)]);
          if (sibling) {
            siblingHeight += sibling.offsetHeight;
          }
        }
        var topHeight = this.parent.element.querySelector('.e-yscroll');
        var scrollTopHeight = 0;
        if (topHeight) {
          scrollTopHeight += topHeight.scrollTop;
        }
        var Content = this.parent.element.querySelector('.e-content');
        this.element.style.left = x1 + Content.scrollLeft + 2 + 'px';
        this.element.style.top = y1 - siblingHeight + scrollTopHeight + 'px';
        this.element.style.width = x2 - x1 + 'px';
        this.element.style.height = y2 - y1 + 'px';
        this.element.style.pointerEvents = 'none';
      }
      if (target && !e.ctrlKey && !e.shiftKey) {
        var rowIndex = parseInt(target.getAttribute('aria-rowindex'), 10) - 1;
        if (!this.isCellDrag && (sf.base.isNullOrUndefined(this.prevStartDIndex) || this.prevStartDIndex !== this.startDragIndex || this.prevEndIndex !== rowIndex)) {
          //Below calculation is to perform ClearSelection in server side
          var clearIndex = -1;
          var isInvokedFirst = false;
          var selectedIndexes = this.parent.getSelectedRowIndexes(this.parent.options.enableVirtualization);
          if (sf.base.isNullOrUndefined(this.prevStartDIndex)) {
            clearIndex = -1;
            isInvokedFirst = true;
          } else if (rowIndex >= this.prevStartDIndex && selectedIndexes.indexOf(rowIndex) >= 0) {
            clearIndex = this.prevEndIndex;
          } else if (this.prevStartDIndex > rowIndex && selectedIndexes.indexOf(this.startDragIndex) >= 0) {
            clearIndex = this.prevEndIndex;
          }
          this.prevStartDIndex = this.startDragIndex;
          this.prevEndIndex = rowIndex;
          if (isInvokedFirst && !isNaN(rowIndex)) {
            this.parent.dotNetRef.invokeMethodAsync('DragSelection', this.startDragIndex, rowIndex, true, null, 0, 0);
          } else {
            this.performDragSelection(this.startDragIndex, rowIndex, clearIndex);
          }
        } else if (this.parent.options.allowDragSelection && !this.parent.options.enableAutoFill && this.isCellDrag) {
          var target_1;
          var isInvokedFirst = false;
          if (!sf.base.isNullOrUndefined(e.touches) && !sf.base.isNullOrUndefined(document.elementFromPoint(e.touches[0].clientX, e.touches[0].clientY))) {
            target_1 = sf.base.closest(document.elementFromPoint(e.touches[0].clientX, e.touches[0].clientY), 'td');
          } else {
            target_1 = sf.base.closest(e.target, 'td');
          }
          if (!target_1) {
            target_1 = document.elementFromPoint(xPos, eleLocation);
          }
          if (target_1) {
            if (sf.base.isNullOrUndefined(this.prevStartDIndex)) {
              isInvokedFirst = true;
            }
            this.prevStartDIndex = this.startDragIndex;
            this.endDragIndex = rowIndex;
            this.endDragCellIndex = parseInt(target_1.getAttribute('aria-colindex'), 10) - 1;
            if (!this.isAutoFillSel) {
              if (isInvokedFirst && !isNaN(rowIndex) && !isNaN(parseInt(target_1.getAttribute('aria-colindex'), 10) - 1)) {
                this.parent.dotNetRef.invokeMethodAsync('DragCellSelection', this.startDragIndex, this.startDragCellIndex, rowIndex, parseInt(target_1.getAttribute('aria-colindex'), 10) - 1, true, null);
              } else {
                this.performDragCellSelection(this.startDragIndex, this.startDragCellIndex, rowIndex, parseInt(target_1.getAttribute('aria-colindex'), 10) - 1);
              }
            }
          }
        } else if (this.parent.options.editMode === 'Batch' && this.parent.options.enableAutoFill) {
          if (this.startCell) {
            var td = parentsUntil(e.target, 'e-rowcell');
            if (td && !td.classList.contains('e-editedbatchcell')) {
              this.startAFCell = this.startCell;
              this.endAFCell = td;
              this.endCell = td;
              if (this.isAutoFillSel) {
                this.expandAFBorder(e, false);
              } else {
                this.assignCells();
                var updateAFBor = this.createBorder(this.startRowIndex, this.startColIndex, this.endRowIndex, this.endColIndex, true);
                this.parent.dotNetRef.invokeMethodAsync('UpdateAutofillPositions', updateAFBor, 'UpdateAutofillBorder');
                this.isInitialSelect = true;
              }
            }
          }
        }
      }
    };
    Selection.prototype.performDragCellSelection = function (startIndex, startCellIndex, rowIndex, endCellIndex) {
      var sIndex = startIndex;
      var eIndex = rowIndex;
      if (startIndex > rowIndex) {
        sIndex = rowIndex;
        eIndex = startIndex;
      }
      this.selectCellsByRange(sIndex, eIndex, startCellIndex, endCellIndex);
    };
    Selection.prototype.selectCellsByRange = function (sIndex, eIndex, startCellIndex, endCellIndex) {
      var HeaderRows = this.parent.getHeaderContent().querySelectorAll('tr.e-row[data-uid]');
      var ContentRows = this.parent.getContent().querySelectorAll('tr.e-row[data-uid]');
      var rows = Array.from(HeaderRows).concat(Array.from(ContentRows));
      for (var i = 0; i < rows.length; i++) {
        var cells_1 = [].slice.call(rows[parseInt(i.toString(), 10)].querySelectorAll('.e-rowcell'));
        rows[parseInt(i.toString(), 10)].removeAttribute('aria-selected');
        addRemoveActiveClasses.apply(void 0, __spreadArray$1([cells_1, false], ['e-aria-selected', 'e-active'], false));
      }
      var min;
      var max;
      var cells = [];
      if (sIndex > eIndex) {
        var temp = sIndex;
        sIndex = eIndex;
        eIndex = temp;
      }
      if (startCellIndex > endCellIndex) {
        var Celltemp = startCellIndex;
        startCellIndex = endCellIndex;
        endCellIndex = Celltemp;
      }
      for (var i = sIndex; i <= eIndex; i++) {
        if (this.parent.options.cellSelectionMode.indexOf('Box') < 0) {
          min = i === sIndex ? startCellIndex : 0;
          max = i === eIndex ? endCellIndex : this.getLastColIndex(i);
        } else {
          min = startCellIndex;
          max = endCellIndex;
        }
        for (var j = min < max ? min : max, len = min > max ? min : max; j <= len; j++) {
          cells.push(this.getCellIndex(i, j));
        }
        addRemoveActiveClasses.apply(void 0, __spreadArray$1([cells, true], ['e-aria-selected', 'e-active'], false));
      }
    };
    Selection.prototype.getCellIndex = function (rowIndex, cellIndex) {
      var HeaderRows = this.parent.getHeaderContent().querySelectorAll('tr.e-row[data-uid]');
      var ContentRows = this.parent.getContent().querySelectorAll('tr.e-row[data-uid]');
      var rows = Array.from(HeaderRows).concat(Array.from(ContentRows));
      return rows[parseInt(rowIndex.toString(), 10)] && rows[parseInt(rowIndex.toString(), 10)].querySelectorAll('.e-rowcell')[parseInt(cellIndex.toString(), 10)];
    };
    Selection.prototype.getLastColIndex = function (rowIndex) {
      var cells = this.parent.getDataRows()[parseInt(rowIndex.toString(), 10)].querySelectorAll('td.e-rowcell');
      return parseInt(cells[cells.length - 1].getAttribute('aria-colindex'), 10) - 1;
    };
    /**
     * Update the position of the autofill handle.
     *
     * @param {number} cellindex - The column index of the cell.
     * @param {number} index - The row index of the cell.
     * @param {boolean} [newSelect=false] - Whether the selection is new or not.
     * @returns {object} - The updated position information of the autofill handle.
     * @hidden
     */
    Selection.prototype.updateAutofillPosition = function (cellindex, index, newSelect) {
      if (newSelect === void 0) {
        newSelect = false;
      }
      var row = this.parent.getRowByIndex(index);
      var cell = row.querySelector('[aria-colindex="' + (cellindex + 1) + '"]');
      var selectedCells = [].slice.call(this.parent.element.querySelectorAll('.e-cellselectionbackground'));
      var autoFillBoxLeft = '';
      var autoFillBoxRight = '';
      var autoFillBoxTop = '';
      if (selectedCells && !newSelect) {
        cell = selectedCells[selectedCells.length - 1];
      }
      if (cell && cell.offsetParent) {
        var clientRect = cell.getBoundingClientRect();
        var parentOff = cell.offsetParent.getBoundingClientRect();
        var colWidth = this.isLastCell(cell) ? 4 : 0;
        var rowHeight = this.isLastRow(cell) ? 3 : 0;
        if (!this.parent.options.enableRtl) {
          autoFillBoxLeft = clientRect.left - parentOff.left + clientRect.width - 4 - colWidth + 'px';
        } else {
          autoFillBoxRight = parentOff.right - clientRect.right + clientRect.width - 4 - colWidth + 'px';
        }
        autoFillBoxTop = clientRect.top - parentOff.top + clientRect.height - 5 - rowHeight + 'px';
      }
      return {
        Left: autoFillBoxLeft,
        Right: autoFillBoxRight,
        Top: autoFillBoxTop
      };
    };
    /**
     * Creates a border around the specified cell range.
     *
     * @param {number} startRowIndex - The starting row index of the cell range.
     * @param {number} startColIndex - The starting column index of the cell range.
     * @param {number} [endRowIndex=null] - The ending row index of the cell range.
     * @param {number} [endColIndex=null] - The ending column index of the cell range.
     * @param {boolean} [newSelect=false] - Whether the selection is new or not.
     * @returns {object} - The updated border information.
     * @hidden
     */
    Selection.prototype.createBorder = function (startRowIndex, startColIndex, endRowIndex, endColIndex, newSelect) {
      if (endRowIndex === void 0) {
        endRowIndex = null;
      }
      if (endColIndex === void 0) {
        endColIndex = null;
      }
      if (newSelect === void 0) {
        newSelect = false;
      }
      var selectedCells = [].slice.call(this.parent.element.querySelectorAll('.e-cellselectionbackground'));
      var rowstart = this.parent.getRowByIndex(startRowIndex);
      var cellStart = rowstart.querySelector('[aria-colindex="' + (startColIndex + 1) + '"]');
      var cellsStart = [].slice.call(cellStart.parentElement.querySelectorAll('[aria-colindex="' + (startColIndex + 1) + '"]'));
      var rowEnd;
      var cellEnd;
      var cellsEnd;
      var autoFillBorderRight = '';
      var autoFillBorderLeft = '';
      var autoFillBordersWidth = '';
      var autoFillBorderWidth = '';
      var autoFillBorderHeight = '';
      var autoFillBorderTop = '';
      if (endRowIndex != null && endColIndex != null) {
        rowEnd = this.parent.getRowByIndex(endRowIndex);
        cellEnd = rowEnd.querySelector('[aria-colindex="' + (endColIndex + 1) + '"]');
        cellsEnd = [].slice.call(cellEnd.parentElement.querySelectorAll('[aria-colindex="' + (endColIndex + 1) + '"]'));
      } else {
        rowEnd = rowstart;
        cellEnd = cellStart;
        cellsEnd = cellsStart;
      }
      if (selectedCells && !newSelect) {
        cellsStart = [].slice.call(selectedCells[0].parentElement.querySelectorAll('[aria-colindex="' + (selectedCells[0].cellIndex - this.parent.getIndentCount() + 1) + '"]'));
        cellsEnd = [].slice.call(selectedCells[selectedCells.length - 1].parentElement.querySelectorAll('[aria-colindex="' + (selectedCells[selectedCells.length - 1].cellIndex - this.parent.getIndentCount() + 1) + '"]'));
      }
      if (!this.startCell) {
        this.startCell = cellsStart[0];
      }
      this.endCells = cellsEnd[0];
      var start = cellsStart[0];
      var end = cellsEnd[0];
      var stOff = start.getBoundingClientRect();
      var endOff = end.getBoundingClientRect();
      var parentOff = start.offsetParent.getBoundingClientRect();
      var rowHeight = this.isLastRow(end) ? 2 : 0;
      var topOffSet = this.parent.options.frozenRows && this.isFirstRow(start) ? 1.5 : 0;
      var leftOffset = this.parent.options.frozenColumns && this.isFirstCell(start) ? 1 : 0;
      if (this.parent.options.enableRtl) {
        autoFillBorderRight = parentOff.right - stOff.right - leftOffset + 'px';
        autoFillBorderWidth = stOff.right - endOff.left + leftOffset + 1 + 'px';
      } else {
        autoFillBorderLeft = stOff.left - parentOff.left - leftOffset + 'px';
        autoFillBorderWidth = endOff.right - stOff.left + leftOffset + 1 + 'px';
      }
      autoFillBorderTop = stOff.top - parentOff.top - topOffSet + 'px';
      autoFillBorderHeight = endOff.top - stOff.top > 0 ? endOff.top - parentOff.top + endOff.height + 1 - (stOff.top - parentOff.top) - rowHeight + topOffSet + 'px' : endOff.height + topOffSet - rowHeight + 1 + 'px';
      autoFillBordersWidth = '2px';
      return {
        Right: autoFillBorderRight,
        Width: autoFillBorderWidth,
        BorderWidth: autoFillBordersWidth,
        Left: autoFillBorderLeft,
        Height: autoFillBorderHeight,
        Top: autoFillBorderTop
      };
    };
    Selection.prototype.expandAFBorder = function (e, isApply) {
      var selectedCells = [].slice.call(this.parent.element.querySelectorAll('.e-cellselectionbackground'));
      var startrowIdx = parseInt(parentsUntil(this.startCell, 'e-row').getAttribute('aria-rowindex'), 10) - 1;
      var startCellIdx = parseInt(this.startCell.getAttribute('aria-colindex'), 10) - 1;
      var endrowIdx = parseInt(parentsUntil(this.endCell, 'e-row').getAttribute('aria-rowindex'), 10) - 1;
      var endCellIdx = parseInt(this.endCell.getAttribute('aria-colindex'), 10) - 1;
      var rowLen = parseInt(parentsUntil(selectedCells[selectedCells.length - 1], 'e-row').getAttribute('aria-rowindex'), 10) - 1 - (parseInt(parentsUntil(selectedCells[0], 'e-row').getAttribute('aria-rowindex'), 10) - 1);
      var rowIdx = parseInt(parentsUntil(selectedCells[0], 'e-row').getAttribute('aria-rowindex'), 10) - 1;
      var row = this.parent.getRowByIndex(rowIdx);
      var colLen = 0;
      for (var i = 0, cellLen = row.cells.length; i < cellLen; i++) {
        if (row.cells[parseInt(i.toString(), 10)].classList.contains('e-cellselectionbackground')) {
          colLen++;
        }
      }
      colLen = colLen - 1;
      colLen = colLen >= 0 ? colLen : 0;
      switch (true) {
        case !isApply && this.endAFCell.classList.contains('e-cellselectionbackground') && !!parentsUntil(e.target, 'e-rowcell'):
          this.startAFCell = this.parent.getCellFromIndex(startrowIdx, startCellIdx);
          this.endAFCell = this.parent.getCellFromIndex(startrowIdx + rowLen, startCellIdx + colLen);
          this.drawAFBorders();
          break;
        case startCellIdx + colLen < endCellIdx && endCellIdx - startCellIdx - colLen + 1 > endrowIdx - startrowIdx - rowLen && endCellIdx - startCellIdx - colLen + 1 > startrowIdx - endrowIdx:
          this.endAFCell = this.parent.getCellFromIndex(startrowIdx + rowLen, endCellIdx);
          endrowIdx = parseInt(parentsUntil(this.endAFCell, 'e-row').getAttribute('aria-rowindex'), 10) - 1;
          endCellIdx = parseInt(this.endAFCell.getAttribute('aria-colindex'), 10) - 1;
          if (!isApply) {
            this.drawAFBorders();
          } else {
            var cellIdx = parseInt(this.endCells.getAttribute('aria-colindex'), 10) - 1;
            for (var i = startrowIdx; i <= endrowIdx; i++) {
              var cells = this.getAutoFillCells(i);
              var c = 0;
              for (var j = cellIdx + 1; j <= endCellIdx; j++) {
                if (c > colLen) {
                  c = 0;
                }
                this.updateValue(i, j, cells[parseInt(c.toString(), 10)]);
                c++;
              }
            }
          }
          break;
        case startCellIdx > endCellIdx && startCellIdx - endCellIdx + 1 > endrowIdx - startrowIdx - rowLen && startCellIdx - endCellIdx + 1 > startrowIdx - endrowIdx:
          this.startAFCell = this.parent.getCellFromIndex(startrowIdx, endCellIdx);
          this.endAFCell = this.endCells;
          if (!isApply) {
            this.drawAFBorders();
          } else {
            for (var i = startrowIdx; i <= startrowIdx + rowLen; i++) {
              var cells = this.getAutoFillCells(i);
              cells.reverse();
              var c = 0;
              for (var j = this.startCellIndex - 1; j >= endCellIdx; j--) {
                if (c > colLen) {
                  c = 0;
                }
                this.updateValue(i, j, cells[parseInt(c.toString(), 10)]);
                c++;
              }
            }
          }
          break;
        case startrowIdx > endrowIdx:
          this.startAFCell = this.parent.getCellFromIndex(endrowIdx, startCellIdx);
          this.endAFCell = this.endCells;
          if (!isApply) {
            this.drawAFBorders();
          } else {
            var trIdx = parseInt(this.endCells.parentElement.getAttribute('aria-rowindex'), 10) - 1;
            var r = trIdx;
            for (var i = startrowIdx - 1; i >= endrowIdx; i--) {
              if (r === this.startIndex - 1) {
                r = trIdx;
              }
              var cells = this.getAutoFillCells(r);
              var c = 0;
              r--;
              for (var j = this.startCellIndex; j <= this.startCellIndex + colLen; j++) {
                this.updateValue(i, j, cells[parseInt(c.toString(), 10)]);
                c++;
              }
            }
          }
          break;
        default:
          this.endAFCell = this.parent.getCellFromIndex(endrowIdx, startCellIdx + colLen);
          if (!isApply) {
            this.drawAFBorders();
          } else {
            var trIdx = parseInt(this.endCells.parentElement.getAttribute('aria-rowindex'), 10) - 1;
            var r = this.startIndex;
            for (var i = trIdx + 1; i <= endrowIdx; i++) {
              if (r === trIdx + 1) {
                r = this.startIndex;
              }
              var cells = this.getAutoFillCells(r);
              r++;
              var c = 0;
              for (var m = this.startCellIndex; m <= this.startCellIndex + colLen; m++) {
                this.updateValue(i, m, cells[parseInt(c.toString(), 10)]);
                c++;
              }
            }
          }
          break;
      }
    };
    Selection.prototype.drawAFBorders = function () {
      if (!this.startCell) {
        return;
      }
      var stOff = this.startAFCell.getBoundingClientRect();
      var endOff = this.endAFCell.getBoundingClientRect();
      var top = endOff.top - stOff.top > 0 ? 1 : 0;
      var firstCellTop = endOff.top - stOff.top >= 0 && (parentsUntil(this.startAFCell, 'e-movablecontent') || parentsUntil(this.startAFCell, 'e-frozencontent')) && this.isFirstRow(this.startAFCell) ? 1.5 : 0;
      var firstCellLeft = (parentsUntil(this.startAFCell, 'e-movablecontent') || parentsUntil(this.startAFCell, 'e-movableheader')) && this.isFirstCell(this.startAFCell) ? 1 : 0;
      var rowHeight = this.isLastRow(this.endAFCell) && (parentsUntil(this.endAFCell, 'e-movablecontent') || parentsUntil(this.endAFCell, 'e-frozencontent')) ? 2 : 1;
      var parentOff = this.startAFCell.offsetParent.getBoundingClientRect();
      var parentRect = this.parent.element.getBoundingClientRect();
      var sTop = this.startAFCell.offsetParent.parentElement.scrollTop;
      var sLeft = this.startAFCell.offsetParent.parentElement.scrollLeft;
      var scrollTop = sTop - this.startAFCell.offsetTop;
      var scrollLeft = sLeft - this.startAFCell.offsetLeft;
      scrollTop = scrollTop > 0 ? Math.floor(scrollTop) - 1 : 0;
      scrollLeft = scrollLeft > 0 ? scrollLeft : 0;
      var left = stOff.left - parentRect.left;
      var bdrAFLeftLeft = '';
      var bdrAFLeftHeight = '';
      var bdrAFLeftTop = '';
      var bdrAFLeftRight = '';
      var bdrAFRightLeft = '';
      var bdrAFRightHeight = '';
      var bdrAFRightRight = '';
      var bdrAFRightTop = '';
      var bdrAFTopLeft = '';
      var bdrAFTopTop = '';
      var bdrAFTopWidth = '';
      var bdrAFBottomLeft = '';
      var bdrAFBottomTop = '';
      var bdrAFBottomWidth = '';
      if (!this.parent.options.enableRtl) {
        bdrAFLeftLeft = left - firstCellLeft + scrollLeft - 1 + sLeft + 'px';
        bdrAFRightLeft = endOff.left - parentRect.left - 2 + endOff.width + sLeft + 'px';
        bdrAFTopLeft = left + scrollLeft - 0.5 + sLeft + 'px';
        bdrAFTopWidth = parseInt(bdrAFRightLeft, 10) - parseInt(bdrAFLeftLeft, 10) - firstCellLeft + 1 + 'px';
      } else {
        var scrolloffSet = parentsUntil(this.startAFCell, 'e-movablecontent') || parentsUntil(this.startAFCell, 'e-movableheader') ? stOff.right - this.startAFCell.offsetParent.parentElement.getBoundingClientRect().width - parentRect.left : 0;
        bdrAFLeftRight = parentRect.right - endOff.right - 2 + endOff.width + 'px';
        bdrAFRightRight = parentRect.right - stOff.right - firstCellLeft + scrolloffSet - 1 + 'px';
        bdrAFTopLeft = endOff.left - parentRect.left - 0.5 + 'px';
        bdrAFTopWidth = parseInt(bdrAFLeftRight, 10) - parseInt(bdrAFRightRight, 10) - firstCellLeft + 1 + 'px';
      }
      bdrAFLeftTop = stOff.top - parentOff.top - firstCellTop + 'px';
      bdrAFLeftHeight = endOff.top - stOff.top > 0 ? endOff.top - parentOff.top + endOff.height + 1 - (stOff.top - parentOff.top) + firstCellTop - rowHeight - scrollTop + 'px' : endOff.height + firstCellTop - rowHeight - scrollTop + 'px';
      bdrAFRightTop = bdrAFLeftTop;
      bdrAFRightHeight = parseInt(bdrAFLeftHeight, 10) + 'px';
      bdrAFTopTop = bdrAFRightTop;
      bdrAFBottomLeft = bdrAFTopLeft;
      bdrAFBottomTop = parseFloat(bdrAFLeftTop) + parseFloat(bdrAFLeftHeight) - top - 1 + 'px';
      bdrAFBottomWidth = bdrAFTopWidth;
      var positionAF = {
        BorderLeftAutofillLeft: bdrAFLeftLeft,
        BorderLeftAutofillTop: bdrAFLeftTop,
        BorderLeftAutofillHeight: bdrAFLeftHeight,
        BorderLeftAutofillRight: bdrAFLeftRight,
        BorderRightAutofillLeft: bdrAFRightLeft,
        BorderRightAutofillHeight: bdrAFRightHeight,
        BorderRightAutofillRight: bdrAFRightRight,
        BorderRightAutofillTop: bdrAFRightTop,
        BorderTopAutofillLeft: bdrAFTopLeft,
        BorderTopAutofillTop: bdrAFTopTop,
        BorderTopAutofillWidth: bdrAFTopWidth,
        BorderBottomAutofillLeft: bdrAFBottomLeft,
        BorderBottomAutofillTop: bdrAFBottomTop,
        BorderBottomAutofillWidth: bdrAFBottomWidth
      };
      this.parent.dotNetRef.invokeMethodAsync('UpdateAutofillPositions', positionAF, 'UpdateAutofillPosition');
    };
    Selection.prototype.updateValue = function (rowIndex, colIndex, cell) {
      var col = this.parent.getColumnByIndex(colIndex);
      var valueIndex = parseInt(parentsUntil(cell, 'e-row').getAttribute('aria-rowindex'), 10) - 1;
      var column = this.parent.getColumnByIndex(cell.cellIndex - this.parent.getIndentCount());
      var value = cell.innerText;
      this.parent.dotNetRef.invokeMethodAsync('UpdateAutofillCell', rowIndex, col.field, column.field, valueIndex, value);
    };
    Selection.prototype.getAutoFillCells = function (rowIndex) {
      var cells = [].slice.call(this.parent.getDataRows()[parseInt(rowIndex.toString(), 10)].querySelectorAll('.e-cellselectionbackground'));
      return cells;
    };
    Selection.prototype.updateStartEndCells = function () {
      var cells = [].slice.call(this.parent.element.querySelectorAll('.e-cellselectionbackground'));
      this.startCell = cells[0];
      this.endCell = cells[cells.length - 1];
      if (this.startCell) {
        this.startIndex = parseInt(this.startCell.parentElement.getAttribute('aria-rowindex'), 10) - 1;
        this.startCellIndex = parseInt(parentsUntil(this.startCell, 'e-rowcell').getAttribute('aria-colindex'), 10) - 1;
      }
    };
    Selection.prototype.assignCells = function () {
      this.startRowIndex = parseInt(this.startAFCell.parentElement.getAttribute('aria-rowindex'), 10) - 1;
      this.endRowIndex = parseInt(this.endAFCell.parentElement.getAttribute('aria-rowindex'), 10) - 1;
      this.startColIndex = parseInt(this.startAFCell.getAttribute('aria-colindex'), 10) - 1;
      this.endColIndex = parseInt(this.endAFCell.getAttribute('aria-colindex'), 10) - 1;
      if (this.startRowIndex > this.endRowIndex) {
        this.startRowIndex = this.endRowIndex;
        this.endRowIndex = parseInt(this.startAFCell.parentElement.getAttribute('aria-rowindex'), 10) - 1;
      }
      if (this.endColIndex < this.startColIndex) {
        this.startColIndex = this.endColIndex;
        this.endColIndex = parseInt(this.startAFCell.getAttribute('aria-colindex'), 10) - 1;
      }
    };
    Selection.prototype.selectCellByRow = function () {
      for (var i = this.startRowIndex; i <= this.endRowIndex; i++) {
        for (var j = this.startColIndex; j <= this.endColIndex; j++) {
          this.parent.dotNetRef.invokeMethodAsync('SelectCellByRow', i, j);
        }
      }
    };
    Selection.prototype.isLastCell = function (cell) {
      var LastCell = [].slice.call(cell.parentElement.querySelectorAll('.e-rowcell:not(.e-hide)'));
      return LastCell[LastCell.length - 1] === cell;
    };
    Selection.prototype.isLastRow = function (cell) {
      var LastRow = [].slice.call(sf.base.closest(cell, 'tbody').querySelectorAll('.e-row:not(.e-hiddenrow)'));
      return LastRow[LastRow.length - 1] === cell.parentElement;
    };
    Selection.prototype.isFirstRow = function (cell) {
      var rows = [].slice.call(sf.base.closest(cell, 'tbody').querySelectorAll('.e-row:not(.e-hiddenrow)'));
      return cell.parentElement === rows[0];
    };
    Selection.prototype.isFirstCell = function (cell) {
      var cells = [].slice.call(cell.parentElement.querySelectorAll('.e-rowcell:not(.e-hide)'));
      return cells[0] === cell;
    };
    Selection.prototype.performDragSelection = function (startIndex, endIndex, clearIndex) {
      var sIndex = startIndex;
      var eIndex = endIndex;
      if (startIndex > endIndex) {
        sIndex = endIndex;
        eIndex = startIndex;
      }
      if (clearIndex !== -1) {
        this.clearSelectionExceptDragIndexes(sIndex, eIndex);
      }
      this.selectRangeOfRows(sIndex, eIndex);
    };
    Selection.prototype.selectRangeOfRows = function (startIndex, endIndex) {
      var HeaderRows = this.parent.getHeaderContent().querySelectorAll('tr.e-row[data-uid]');
      var ContentRows = this.parent.getContent().querySelectorAll('tr.e-row[data-uid]');
      var rows = Array.from(HeaderRows).concat(Array.from(ContentRows));
      var _loop_1 = function _loop_1(i) {
        var row = this_1.parent.options.enableVirtualization ? rows.filter(function (_) {
          return parseInt(_.getAttribute('aria-rowindex'), 10) - 1 === i;
        })[0] : rows[parseInt(i.toString(), 10)];
        this_1.selectRow(row, false);
      };
      var this_1 = this;
      for (var i = startIndex; i <= endIndex; i++) {
        _loop_1(i);
      }
    };
    Selection.prototype.selectRow = function (row, isMovableRow) {
      if (!sf.base.isNullOrUndefined(row)) {
        row.setAttribute('aria-selected', 'true');
        var cells = [].slice.call(row.querySelectorAll('.e-rowcell'));
        if (this.parent.options.allowRowDragAndDrop) {
          cells = [].slice.call(row.querySelectorAll('.e-rowdragdrop')).concat(cells);
        }
        addRemoveActiveClasses.apply(void 0, __spreadArray$1([cells, true], ['e-aria-selected', 'e-active'], false));
        if (!isMovableRow) {
          var checkboxCell = cells.filter(function (cell) {
            return cell.classList.contains('e-gridchkbox');
          })[0];
          if (!sf.base.isNullOrUndefined(checkboxCell) && checkboxCell.classList.contains('e-gridchkbox')) {
            checkboxCell.querySelector('.e-frame').classList.replace('e-uncheck', 'e-check');
          }
        }
      }
    };
    Selection.prototype.clearSelectionByRow = function (row) {
      var cells = [].slice.call(row.querySelectorAll('.e-rowcell'));
      if (this.parent.options.allowRowDragAndDrop) {
        cells = [].slice.call(row.querySelectorAll('.e-rowdragdrop')).concat(cells);
      }
      row.removeAttribute('aria-selected');
      addRemoveActiveClasses.apply(void 0, __spreadArray$1([cells, false], ['e-aria-selected', 'e-active'], false));
      var checkboxCell = cells.filter(function (cell) {
        return cell.classList.contains('e-gridchkbox');
      })[0];
      if (!sf.base.isNullOrUndefined(checkboxCell) && checkboxCell.classList.contains('e-gridchkbox')) {
        checkboxCell.querySelector('.e-frame').classList.replace('e-check', 'e-uncheck');
      }
    };
    Selection.prototype.clearSelectionExceptDragIndexes = function (startIndex, endIndex) {
      var rows = this.parent.getRows();
      var _loop_2 = function _loop_2(i) {
        var j = this_2.parent.options.enableVirtualization ? parseInt(rows[parseInt(i.toString(), 10)].getAttribute('aria-rowindex'), 10) - 1 : i;
        if (j < startIndex || j > endIndex) {
          var row = this_2.parent.options.enableVirtualization ? rows.filter(function (_) {
            return parseInt(_.getAttribute('aria-rowindex'), 10) - 1 === j;
          })[0] : rows[parseInt(j.toString(), 10)];
          this_2.clearSelectionByRow(row);
        }
      };
      var this_2 = this;
      for (var i = 0; i < rows.length; i++) {
        _loop_2(i);
      }
    };
    Selection.prototype.isRowType = function () {
      return this.parent.options.selectionMode === 'Row' || this.parent.options.selectionMode === 'Both';
    };
    Selection.prototype.isSingleSel = function () {
      return this.parent.options.selectionType === 'Single';
    };
    return Selection;
  }();

  /**
   * InterSectionObserver - class watch whether it enters the viewport.
   *
   * @hidden
   */
  var InterSectionObserver = /** @class */function () {
    function InterSectionObserver(element, options) {
      var _this = this;
      this.fromWheel = false;
      this.touchMove = false;
      /** @hidden */
      this.options = {};
      this.isWheelScrolling = false;
      this.isTouchScrolling = false;
      this.sentinelInfo = {
        'up': {
          check: function check(rect, info) {
            var top = rect.top - _this.containerRect.top;
            var bottom = _this.containerRect.bottom > rect.bottom ? _this.containerRect.bottom - rect.bottom : 0;
            info.entered = top >= 0;
            return top + _this.options.pageHeight / 2 >= 0 || bottom > 0 && rect.bottom > 0;
          },
          axis: 'Y'
        },
        'down': {
          check: function check(rect, info) {
            var bottom = rect.bottom;
            info.entered = rect.bottom <= _this.containerRect.bottom;
            return bottom - _this.containerRect.top - _this.options.pageHeight / 2 <= _this.options.pageHeight / 2;
          },
          axis: 'Y'
        },
        'right': {
          check: function check(rect, info) {
            var right = rect.right;
            info.entered = right < _this.containerRect.right;
            return right - _this.containerRect.width <= _this.containerRect.right;
          },
          axis: 'X'
        },
        'left': {
          check: function check(rect, info) {
            var left = rect.left;
            info.entered = left > 0;
            return left + _this.containerRect.width >= _this.containerRect.left;
          },
          axis: 'X'
        }
      };
      /**
       * Constants for scroll behavior configuration
       */
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      this.SCROLL_CONSTANTS = {
        DEBOUNCE_DELAY_CHROME: 200,
        DEBOUNCE_DELAY_DEFAULT: 100,
        DEBOUNCE_DELAY_HORIZONTAL: 50,
        LIGHT_SCROLL_THRESHOLD: 20
      };
      this.element = element;
      this.options = options;
    }
    InterSectionObserver.prototype.observe = function (callback, onEnterCallback) {
      var _this = this;
      this.options.virtualScrollHandler = this.virtualScrollHandler(callback, onEnterCallback);
      this.containerRect = this.options.container.getBoundingClientRect();
      sf.base.EventHandler.add(this.options.container, 'wheel', function () {
        _this.fromWheel = true;
        _this.isWheelScrolling = true;
        _this.isTouchScrolling = false;
        // Add your additional property here if needed
        return true;
      }, this);
      sf.base.EventHandler.add(this.options.container, 'touchstart', function () {
        _this.isTouchScrolling = true;
        _this.isWheelScrolling = false;
        // Add your additional property here if needed
        return true;
      }, this);
      sf.base.EventHandler.add(this.options.container, 'scroll', this.options.virtualScrollHandler, this);
      if (!sf.base.isNullOrUndefined(parentsUntil(this.element, 'e-gridcontent')) && !sf.base.isNullOrUndefined(parentsUntil(this.element, 'e-gridcontent').querySelector('.e-movablescrollbar'))) {
        sf.base.EventHandler.add(parentsUntil(this.element, 'e-gridcontent').querySelector('.e-movablescrollbar'), 'scroll', this.options.virtualScrollHandler, this);
      }
    };
    InterSectionObserver.prototype.disconnect = function () {
      var _this = this;
      this.containerRect = this.options.container.getBoundingClientRect();
      sf.base.EventHandler.remove(this.options.container, 'wheel', function () {
        _this.fromWheel = true;
        _this.isWheelScrolling = true;
        _this.isTouchScrolling = false;
        // Add your additional property here if needed
        return true;
      });
      sf.base.EventHandler.add(this.options.container, 'touchstart', function () {
        _this.isTouchScrolling = true;
        _this.isWheelScrolling = false;
        // Add your additional property here if needed
        return true;
      }, this);
      sf.base.EventHandler.remove(this.options.container, 'scroll', this.options.virtualScrollHandler);
      if (!sf.base.isNullOrUndefined(parentsUntil(this.element, 'e-gridcontent')) && !sf.base.isNullOrUndefined(parentsUntil(this.element, 'e-gridcontent').querySelector('.e-movablescrollbar'))) {
        sf.base.EventHandler.remove(parentsUntil(this.element, 'e-gridcontent').querySelector('.e-movablescrollbar'), 'scroll', this.options.virtualScrollHandler);
      }
    };
    InterSectionObserver.prototype.check = function (direction) {
      var info = this.sentinelInfo["".concat(direction)];
      return info.check(this.element.getBoundingClientRect(), info);
    };
    /**
     * Creates debounced callback functions with appropriate delays
     *
     * @param {Function} callback - Original callback to debounce
     * @returns {{standard: Function, horizontal: Function}} Object containing debounced callbacks
     */
    InterSectionObserver.prototype.createDebouncedCallbacks = function (callback) {
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      var delay = sf.base.Browser.info.name === 'chrome' ? this.SCROLL_CONSTANTS.DEBOUNCE_DELAY_CHROME : this.SCROLL_CONSTANTS.DEBOUNCE_DELAY_DEFAULT;
      return {
        standard: sf.base.debounce(callback, delay),
        horizontal: sf.base.debounce(callback, this.SCROLL_CONSTANTS.DEBOUNCE_DELAY_HORIZONTAL)
      };
    };
    /**
     * Retrieves and normalizes scroll offset from target element
     * Handles Safari on macOS negative scroll bug
     *
     * @param {HTMLElement} target - Scroll container element
     * @returns {ScrollOffset} Normalized scroll offset
     */
    InterSectionObserver.prototype.getScrollOffset = function (target) {
      var top = target.scrollTop;
      var left = target.scrollLeft;
      // Fix Safari on macOS negative scroll position bug
      if (this.isSafariOnMacOS() && top < 0) {
        top = target.scrollTop = 0;
      }
      return {
        top: top,
        left: left
      };
    };
    /**
     * Detects if browser is Safari running on macOS
     *
     * @returns {boolean} True if Safari on macOS, false otherwise
     */
    InterSectionObserver.prototype.isSafariOnMacOS = function () {
      var userAgent = navigator.userAgent;
      var isMacOS = userAgent.indexOf('Mac OS') !== -1;
      var isSafari = /^((?!chrome|android).)*safari/i.test(userAgent);
      return isMacOS && isSafari;
    };
    /**
     * Calculates scroll direction based on current and previous positions
     *
     * @param {ScrollOffset} offset - Current scroll offset
     * @param {HTMLElement} target - Scroll container element
     * @returns {ScrollDirection} Calculated scroll direction
     */
    InterSectionObserver.prototype.calculateScrollDirection = function (offset, target) {
      var isHorizontalScrollbar = target.classList.contains('e-movablescrollbar');
      if (isHorizontalScrollbar) {
        return this.getHorizontalDirection(offset.left);
      }
      // Determine primary direction (vertical or horizontal)
      var hasVerticalScroll = this.options.previousTop !== offset.top;
      var hasHorizontalScroll = this.options.previousLeft !== offset.left;
      if (hasVerticalScroll) {
        return this.getVerticalDirection(offset.top);
      } else if (hasHorizontalScroll) {
        return this.getHorizontalDirection(offset.left);
      }
      // Default to vertical direction if no scroll detected
      return this.getVerticalDirection(offset.top);
    };
    /**
     * Determines vertical scroll direction
     *
     * @param {number}currentTop - Current vertical scroll position
     * @returns {ScrollDirection} 'up' or 'down' direction
     */
    InterSectionObserver.prototype.getVerticalDirection = function (currentTop) {
      return this.options.previousTop < currentTop ? 'down' : 'up';
    };
    /**
     * Determines horizontal scroll direction
     *
     * @param {number}currentLeft - Current horizontal scroll position
     * @returns {ScrollDirection} 'left' or 'right' direction
     */
    InterSectionObserver.prototype.getHorizontalDirection = function (currentLeft) {
      return this.options.previousLeft < currentLeft ? 'right' : 'left';
    };
    /**
     * Updates internal scroll tracking state
     *
     * @param {ScrollOffset} offset - Current scroll offset
     * @param {ScrollDirection} direction - Current scroll direction
     * @returns {void}
     */
    InterSectionObserver.prototype.updateScrollTracking = function (offset, direction) {
      var isHorizontalMove = direction === 'left' || direction === 'right';
      // Update PreventAdjustTable for scroll optimization
      this.PreventAdjustTable = this.calculatePreventAdjustTable(offset, direction);
      // Update previous scroll positions
      if (!isHorizontalMove) {
        this.options.previousTop = offset.top;
      }
      this.options.previousLeft = offset.left;
    };
    /**
     * Calculates PreventAdjustTable value for scroll optimization
     *
     * @param {ScrollOffset} offset - Current scroll offset
     * @param {ScrollDirection} direction - Current scroll direction
     * @returns {string} 'horizontal' or direction value
     */
    InterSectionObserver.prototype.calculatePreventAdjustTable = function (offset, direction) {
      var scrollDirection = direction;
      var isHorizontalMove = direction === 'left' || direction === 'right';
      scrollDirection = isHorizontalMove ? 'horizontal' : this.options.previousTop === offset.top && offset.left === this.options.previousLeft ? 'horizontal' : direction;
      return scrollDirection;
    };
    /**
     * Checks if the given axis is being tracked
     *
     * @param {string} axis - Axis to check ('X' or 'Y')
     * @returns {boolean} True if axis is tracked, false otherwise
     */
    InterSectionObserver.prototype.isAxisTracked = function (axis) {
      return this.options.axes.indexOf(axis) !== -1;
    };
    /**
     * Checks if mask rows are present in the container
     *
     * @param {string} axis - Axis to check ('X' or 'Y')
     * @returns {boolean} True if mask rows exist for non-horizontal scroll
     */
    InterSectionObserver.prototype.hasMaskRows = function (axis) {
      return axis !== 'X' && this.options.container.querySelectorAll('.e-masked-row').length > 0;
    };
    /**
     * Checks if overscan is enabled for the given axis
     *
     * @param {string} axis - Axis to check ('X' or 'Y')
     * @returns {boolean} True if overscan enabled for non-horizontal scroll
     */
    InterSectionObserver.prototype.hasOverscan = function (axis) {
      return axis !== 'X' && this.options.overscanCount > 0;
    };
    /**
     * Retrieves scroll threshold configuration
     *
     * @returns {ScrollThresholdConfig} Configuration object with scroll parameters
     */
    InterSectionObserver.prototype.getScrollThresholdConfig = function () {
      return {
        rowHeight: this.options.rowHeight,
        pageSize: this.options.pageSize,
        overscanCount: this.options.overscanCount,
        height: this.options.height
      };
    };
    /**
     * Gets view information with default fallback
     *
     * @returns {VirtualInfo} Virtual info with at minimum startIndex
     */
    InterSectionObserver.prototype.getViewInfo = function () {
      var viewInfo = this.options.viewInfo;
      if (sf.base.isNullOrUndefined(viewInfo)) {
        viewInfo = {
          startIndex: 0
        };
      }
      return viewInfo;
    };
    /**
     * Determines if current scroll is a light scroll (doesn't require full re-render)
     * Light scrolls occur when viewport movement is within acceptable thresholds
     *
     * @param {ScrollDirection} direction - Current scroll direction
     * @param {ScrollOffset} offset - Current scroll offset
     * @returns {boolean} True if light scroll, false if full re-render needed
     */
    InterSectionObserver.prototype.determineScrollType = function (direction, offset) {
      switch (direction) {
        case 'down':
          return this.isLightScrollDown(offset.top);
        case 'up':
          return this.isLightScrollUp(offset.top);
        case 'right':
          return this.isLightScrollHorizontal(offset.left, direction);
        case 'left':
          return this.isLightScrollHorizontal(offset.left, direction);
        default:
          return true;
      }
    };
    /**
     * Determines if downward scroll is light (within threshold)
     *
     * @param {number} scrollTop - Current vertical scroll position
     * @returns {boolean} True if light scroll, false if full re-render needed
     */
    InterSectionObserver.prototype.isLightScrollDown = function (scrollTop) {
      if (!this.options.maskRowEnabled) {
        return true;
      }
      var config = this.getScrollThresholdConfig();
      var viewInfo = this.getViewInfo();
      var exactTopIndex = scrollTop / config.rowHeight;
      var inViewIndexCount = config.height / config.rowHeight;
      var exactEndIndex = exactTopIndex + inViewIndexCount;
      var pageSizeTotal = config.pageSize + config.overscanCount * 2;
      var pageSizeBy2 = pageSizeTotal / 2;
      var pageSizeBy4 = pageSizeTotal / 4;
      var overscanRows = viewInfo.startIndex === 0 ? 0 : config.overscanCount;
      var threshold = viewInfo.startIndex - overscanRows + Math.round(pageSizeBy2 + pageSizeBy4);
      // Check if scroll exceeds threshold requiring full re-render
      if (exactEndIndex > threshold) {
        inViewIndexCount = Math.ceil(inViewIndexCount) - 1;
        var rowIndexDifference = Math.ceil(exactTopIndex) - (viewInfo.startIndex - overscanRows);
        // Full re-render needed if difference exceeds visible rows
        return rowIndexDifference < inViewIndexCount;
      }
      return true;
    };
    /**
     * Determines if upward scroll is light (within threshold)
     *
     * @param {number} scrollTop - Current vertical scroll position
     * @returns {boolean} True if light scroll, false if full re-render needed
     */
    InterSectionObserver.prototype.isLightScrollUp = function (scrollTop) {
      if (!this.options.maskRowEnabled) {
        return true;
      }
      var config = this.getScrollThresholdConfig();
      var viewInfo = this.getViewInfo();
      var exactTopIndex = scrollTop / config.rowHeight;
      config.height / config.rowHeight;
      var pageSizeTotal = config.pageSize + config.overscanCount * 2;
      var pageSizeBy2 = pageSizeTotal / 2;
      var pageSizeBy4 = pageSizeTotal / 4;
      var overscanRows = viewInfo.startIndex === 0 ? 0 : config.overscanCount;
      var threshold = viewInfo.endIndex - overscanRows - Math.round(pageSizeBy2 + pageSizeBy4);
      // Check if scroll is above threshold requiring full re-render
      if (exactTopIndex < threshold) {
        var loadAtIndex = Math.round((viewInfo.startIndex * config.rowHeight + pageSizeBy4 * config.rowHeight) / config.rowHeight);
        // Full re-render needed if scrolled beyond load point
        return exactTopIndex >= loadAtIndex && Math.ceil(exactTopIndex) <= viewInfo.startIndex;
      }
      return true;
    };
    /**
     * Determines if horizontal scroll is light (within threshold)
     *
     * @param {number} scrollLeft - Current horizontal scroll position
     * @param {ScrollDirection} direction - Scroll direction ('left' or 'right')
     * @returns {boolean} True if light scroll, false otherwise
     */
    InterSectionObserver.prototype.isLightScrollHorizontal = function (scrollLeft, direction) {
      var scrollDelta = direction === 'right' ? scrollLeft - this.options.previousLeft : this.options.previousLeft - scrollLeft;
      return scrollDelta < this.SCROLL_CONSTANTS.LIGHT_SCROLL_THRESHOLD;
    };
    InterSectionObserver.prototype.virtualScrollHandler = function (callback, onEnterCallback) {
      var _this = this;
      // prepare debounced callbacks once per handler creation
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      var callbacks = this.createDebouncedCallbacks(callback);
      return function (e) {
        var target = e.target;
        var scrollOffset = _this.getScrollOffset(target);
        var scrollDirection = _this.calculateScrollDirection(scrollOffset, target);
        // Update scroll tracking properties
        _this.updateScrollTracking(scrollOffset, scrollDirection);
        // We are using the same below structure of if conditions in the getInfoFromView method when we enabled showVirtualMaskRow property.
        // Based on this structure only, we will decide whether we have to change the start and end index. So if we have scenario(while
        // scrolling) to change the start and end index(in getInfoFromView method), then here we have to set isLightScroll = false and
        // based on this isLightScroll value, we will adjust the table(in onEntered method) to show maskrow(if isLightScroll is false
        // then we show maskrow other we set the previously rendered translate itself so datarows will be shown(by using _this.translateMaskY value).
        // Determine if this is a light scroll (doesn't require full re-render)
        var isLightScroll = _this.determineScrollType(scrollDirection, scrollOffset);
        // Get sentinel information for the current direction
        // eslint-disable-next-line security/detect-object-injection
        var currentSentinel = _this.sentinelInfo[scrollDirection];
        // Early return if axis is not being tracked
        if (!_this.isAxisTracked(currentSentinel.axis)) {
          return;
        }
        // Check viewport conditions
        var check = _this.check(scrollDirection);
        var hasMaskRows = _this.hasMaskRows(currentSentinel.axis);
        var hasOverscan = _this.hasOverscan(currentSentinel.axis);
        if (currentSentinel.entered || (sf.base.isNullOrUndefined(_this.blazorActiveKey) || _this.blazorActiveKey === '') && !_this.fromWheel && hasMaskRows) {
          onEnterCallback(currentSentinel, isLightScroll, scrollDirection, {
            top: scrollOffset.top,
            left: scrollOffset.left
          }, _this.fromWheel, check);
        }
        var top = scrollOffset.top,
          left = scrollOffset.left;
        if (check || hasMaskRows || hasOverscan) {
          var fn = currentSentinel.axis === 'X' ? callbacks.horizontal : callbacks.standard;
          fn({
            scrollDirection: scrollDirection,
            sentinel: currentSentinel,
            offset: {
              top: top,
              left: left
            },
            focusElement: document.activeElement,
            isWheelScroll: _this.fromWheel
          });
        }
        if (!hasMaskRows) {
          _this.fromWheel = false;
        }
      };
    };
    InterSectionObserver.prototype.setPageHeight = function (value) {
      this.options.pageHeight = value;
    };
    return InterSectionObserver;
  }();

  /**
   * VirtualContentRenderer
   *
   * @returns {void} This method does not return a value.
   * @hidden
   */
  var VirtualContentRenderer = /** @class */function () {
    function VirtualContentRenderer(parent) {
      var _this_1 = this;
      this.preStartIndex = 0;
      this.preventEvent = false;
      this.actions = ['filtering', 'clearfiltering', 'searching', 'grouping', 'ungrouping', 'Filtering', 'ClearFiltering', 'Searching', 'Grouping', 'Ungrouping', 'UnGrouping'];
      this.offsets = {};
      this.tmpOffsets = {};
      this.offsetKeys = [];
      this.currentInfo = {};
      this.nextRowToNavigate = 0;
      /** @hidden */
      this.startIndex = 0;
      /** @hidden */
      this.selectedCellNavigation = -1;
      /** @hidden */
      this.selectedRowNavigation = -1;
      /** @hidden */
      this.selectedRowIndex = -1;
      /** @hidden */
      this.focusColumnIndex = -1;
      /** @hidden */
      this.isScrollIntoview = false;
      /** @hidden */
      this.scrollInfo = {};
      /** @hidden */
      this.frozenMidScroll = false;
      /** @hidden */
      this.focusFromPager = false;
      /** @hidden */
      this.keyCombination = '';
      /**
       * Binds the scroll event observer to monitor and handle virtual scrolling.
       * This method sets up the intersection observer to track scroll position changes
       * and triggers appropriate rendering callbacks. Also restores persisted scroll
       * position if persistence is enabled.
       *
       * @public
       * @returns {void}
       */
      this.bindScrollEvent = function () {
        // --- SECTION 1: Disconnect existing observer to prevent memory leaks ---
        if (!sf.base.isNullOrUndefined(_this_1.observer)) {
          _this_1.observer.disconnect();
        }
        var gridObject = _this_1.parent;
        var _a = gridObject.options,
          enableVirtualization = _a.enableVirtualization,
          enableColumnVirtualization = _a.enableColumnVirtualization,
          enablePersistence = _a.enablePersistence;
        var scrollCallback = function scrollCallback(scrollArgs) {
          // Cache parent options to avoid repeated property access
          if (enableVirtualization || enableColumnVirtualization) {
            _this_1.scrollListener(scrollArgs);
          }
        };
        // Attach observer with scroll callback and sentinel enter callback
        _this_1.observer.observe(scrollCallback, _this_1.onEntered());
        // --- SECTION 2: Handle persisted scroll position restoration ---
        if (enablePersistence && gridObject.scrollPosition) {
          _this_1.content.scrollTop = gridObject.scrollPosition.top;
          var restoredScrollArgs = {
            scrollDirection: 'down',
            sentinel: _this_1.observer.sentinelInfo.down,
            offset: gridObject.scrollPosition,
            focusElement: gridObject.element,
            isWheelScroll: _this_1.observer.options.isWheelScroll
          };
          _this_1.scrollListener(restoredScrollArgs);
          if (enableColumnVirtualization) {
            _this_1.content.scrollLeft = gridObject.scrollPosition.left;
          }
        }
      };
      this.parent = parent;
      this.contentPanel = this.parent.element.querySelector('.e-gridcontent');
      this.vHelper = new VirtualHelper(parent);
      this.virtualEle = new VirtualElementHandler(parent);
      this.addEventListener();
    }
    /**
     * Retrieves the header content panel element of the grid.
     *
     * @returns {Element} The header content panel element.
     */
    VirtualContentRenderer.prototype.getPanel = function () {
      return this.contentPanel;
    };
    /**
     * Retrieves the header table element within the grid.
     *
     * @returns {Element} The header table element.
     */
    VirtualContentRenderer.prototype.getTable = function () {
      return this.contentPanel.querySelector('.e-table');
    };
    /**
     * Renders and initializes the virtual table with intersection observer
     * Sets up scroll event handling and virtual element configuration
     *
     * @returns {number} The calculated row height for the grid
     * @throws {Error} If required elements are not found in DOM
     */
    VirtualContentRenderer.prototype.renderTable = function () {
      this.header = this.parent.virtualHeaderModule;
      this.virtualEle.table = this.getTable();
      this.virtualEle.content = this.content = this.getPanel().querySelector('.e-content');
      // Render virtual wrapper with parent height
      var gridHeight = Number(this.parent.options.height);
      this.virtualEle.renderWrapper(gridHeight);
      this.virtualEle.renderPlaceHolder();
      var content = this.content;
      // Cache options to avoid repeated property access and improve readability
      var _a = this.parent.options,
        enableColumnVirtualization = _a.enableColumnVirtualization,
        totalItemCount = _a.totalItemCount,
        overscanCount = _a.overscanCount,
        enableVirtualMaskRow = _a.enableVirtualMaskRow,
        height = _a.height,
        pageSize = _a.pageSize;
      var rowHeight = this.parent.getRowHeight();
      var observerConfig = {
        container: content,
        pageHeight: this.getBlockHeight() * 2,
        debounceEvent: true,
        axes: enableColumnVirtualization ? ['X', 'Y'] : ['Y'],
        totalItems: totalItemCount,
        overscanCount: overscanCount,
        pageSize: pageSize,
        rowHeight: rowHeight,
        maskRowEnabled: enableVirtualMaskRow,
        height: height.toString().indexOf('%') < 0 ? this.content.getBoundingClientRect().height : this.parent.element.getBoundingClientRect().height,
        previousTop: 0,
        previousLeft: 0
      };
      this.observer = new InterSectionObserver(this.virtualEle.wrapper, observerConfig);
      this.bindScrollEvent();
      return rowHeight;
    };
    VirtualContentRenderer.prototype.addEventListener = function () {
      var firstElementChild = this.getPanel().firstElementChild;
      firstElementChild.scrollTop = 0;
      firstElementChild.scrollLeft = 0;
      sf.base.EventHandler.add(this.parent.element, 'keydown', this.keyDownHandler, this);
    };
    VirtualContentRenderer.prototype.removeEventListener = function () {
      if (!sf.base.isNullOrUndefined(this.observer)) {
        this.observer.disconnect();
      }
      sf.base.EventHandler.remove(this.parent.element, 'keydown', this.keyDownHandler);
    };
    VirtualContentRenderer.prototype.handleScrollEnd = function (virtualRefreshArgs, translatey, isWheelScroll, isScrollByNavigation) {
      if (!isWheelScroll && !isScrollByNavigation) {
        this.parent.dotNetRef.invokeMethodAsync('VirtualRefresh', virtualRefreshArgs, translatey, this.selectedRowIndex, this.isScrollIntoview, this.focusColumnIndex, this.frozenMidScroll, this.focusFromPager, false);
      }
      if (isScrollByNavigation) {
        this.isScrollByNavigation = false;
      }
    };
    VirtualContentRenderer.prototype.ensurePageSize = function () {
      var rowHeight = this.parent.getRowHeight();
      // Cache options to avoid repeated property access and improve readability
      var _a = this.parent.options,
        overscanCount = _a.overscanCount,
        height = _a.height,
        pageSize = _a.pageSize;
      var viewportHeight = height.toString().indexOf('%') < 0 ? this.content.getBoundingClientRect().height : this.parent.element.getBoundingClientRect().height;
      // Calculate visible rows and buffer size for smooth scrolling
      var visibleRowCount = Math.floor(viewportHeight / rowHeight);
      var bufferRowCount = visibleRowCount * 2;
      // Determine optimal page size
      var totalSizeWithOverscan = pageSize + overscanCount;
      var optimalPageSize = totalSizeWithOverscan < bufferRowCount ? bufferRowCount : pageSize;
      // Calculate virtual table width for column range
      var startOffset = this.getColumnOffset(this.startColIndex - 1);
      var endOffset = this.getColumnOffset(this.endColIndex);
      var virtualTableWidth = endOffset - startOffset;
      // Build configuration payload for .NET interop
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      var pageConfiguration = {
        pageSize: optimalPageSize,
        startColumnIndex: this.startColIndex,
        endColumnIndex: this.endColIndex,
        VTableWidth: virtualTableWidth.toString()
      };
      this.parent.dotNetRef.invokeMethodAsync('SetPageSizeAndCIndex', pageConfiguration);
      this.parent.options.pageSize = optimalPageSize;
      this.observer.options.pageHeight = this.getBlockHeight() * 2;
    };
    /**
     * Prevents focus changes during virtual scrolling by redirecting focus to the grid content container.
     * This method is called when wheel or touch scrolling is detected to avoid focus jumping
     * between individual cells during rapid scroll operations.
     *
     * When a row cell is focused during scroll, focus is transferred to the parent grid content
     * element with preventScroll option to maintain smooth scrolling behavior.
     *
     * @private
     * @returns {void}
     */
    VirtualContentRenderer.prototype.preventFocusWhileScroll = function () {
      var activeElement = document.activeElement;
      if (sf.base.isNullOrUndefined(activeElement)) {
        return;
      }
      // Only proceed if the focused element is a row cell
      if (!activeElement.classList.contains('e-rowcell')) {
        return;
      }
      var gridContent = parentsUntil(activeElement, 'e-content');
      if (!gridContent) {
        return;
      }
      // Verify we're within a valid grid element structure
      var gridElement = parentsUntil(activeElement, 'e-grid');
      if (!gridElement) {
        return;
      }
      // Transfer focus to grid content container without triggering scroll
      // This prevents individual cell focus during rapid scroll operations
      gridContent.focus({
        preventScroll: true
      });
    };
    /**
     * Applies auto-fit to virtualized columns that have autoFit enabled
     *
     * @private
     * @param {Column[]} columns - Array of virtualized columns
     * @returns {void}
     */
    VirtualContentRenderer.prototype.applyAutoFitToVirtualizedColumns = function (columns) {
      if (!columns || columns.length === 0) {
        return;
      }
      for (var _i = 0, columns_1 = columns; _i < columns_1.length; _i++) {
        var column = columns_1[_i];
        if (column.autoFit) {
          this.parent.resizeModule.autoFitColumns(column.field);
        }
      }
    };
    VirtualContentRenderer.prototype.scrollListener = function (scrollArgs) {
      // Constants for timeout delays
      var SCROLL_DEBOUNCE_DELAY = 100;
      var IMMEDIATE_EXECUTION_DELAY = 0;
      // eslint-disable-next-line @typescript-eslint/no-this-alias
      var _this = this;
      var _a = _this.parent.options,
        enablePersistence = _a.enablePersistence,
        enableColumnVirtualization = _a.enableColumnVirtualization,
        enableVirtualMaskRow = _a.enableVirtualMaskRow,
        enableVirtualization = _a.enableVirtualization,
        enableRtl = _a.enableRtl,
        frozenColumns = _a.frozenColumns,
        rowHeight = _a.rowHeight,
        virtualizedColumns = _a.virtualizedColumns,
        pageSize = _a.pageSize,
        groupCount = _a.groupCount;
      var parentRowHeight = _this.parent.getRowHeight();
      if (enablePersistence) {
        this.parent.scrollPosition = scrollArgs.offset;
      }
      if (this.preventEvent) {
        this.preventEvent = false;
      }
      var activeElement = document.activeElement;
      if (activeElement.classList.contains('e-grid') && this.isScrollByFocus && enableColumnVirtualization) {
        this.isScrollByFocus = false;
      }
      var info = scrollArgs.sentinel;
      var previousStartIndex = this.preStartIndex;
      var previousColumnIndexes = this.parent.getColumnIndexesInView();
      var viewInfo = this.observer.options.viewInfo = this.currentInfo = this.getInfoFromView(scrollArgs.scrollDirection, info, scrollArgs.offset);
      var horizontalTranslateValue = enableColumnVirtualization && enableRtl ? -1 * this.getColumnOffset(viewInfo.columnIndexes[0] - 1) : this.getColumnOffset(viewInfo.columnIndexes[0] - 1);
      var isPreventFocusScroll = false;
      var hasColumnIndexChanged = this.parent.options.enableColumnVirtualization && JSON.stringify(previousColumnIndexes) !== JSON.stringify(viewInfo.columnIndexes);
      //Horizontal Scroll Handling
      if (enableColumnVirtualization && hasColumnIndexChanged) {
        // Apply auto-fit for virtualized columns if needed
        this.applyAutoFitToVirtualizedColumns(virtualizedColumns);
        var columnTranslateX = enableColumnVirtualization && enableRtl ? -1 * this.getColumnOffset(this.startColIndex - 1) : this.getColumnOffset(this.startColIndex - 1);
        var endOffset = this.getColumnOffset(this.endColIndex);
        var tableWidth = enableColumnVirtualization && enableRtl ? -1 * endOffset + columnTranslateX : endOffset - columnTranslateX;
        var verticalTranslateY_1 = !sf.base.isNullOrUndefined(viewInfo.endIndex) ? (viewInfo.endIndex - pageSize) * (rowHeight ? rowHeight : parentRowHeight) : 0;
        this.movableTranslateY = !sf.base.isNullOrUndefined(this.movableTranslateY) ? this.movableTranslateY : 0;
        // Calculate translateY for virtual mask rows
        var maskTranslateY = enableVirtualMaskRow && enableVirtualization ? frozenColumns ? this.movableTranslateY : this.translateMaskY + this.movableTranslateY : 0;
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        var virtualRefreshArgs_1 = {
          requestType: 'virtualscroll',
          isHeaderNavigated: _this.isHeaderNavigated,
          selectedRowNavigation: _this.selectedRowNavigation,
          selectedCellNavigation: _this.selectedCellNavigation,
          isScrollByFocus: _this.isScrollByFocus,
          startColumnIndex: viewInfo.columnIndexes[0],
          endColumnIndex: viewInfo.columnIndexes[viewInfo.columnIndexes.length - 1],
          axis: 'X',
          VTablewidth: tableWidth.toString(),
          translateX: horizontalTranslateValue,
          translateY: maskTranslateY
        };
        clearTimeout(this.scrollTimer);
        this.scrollTimer = setTimeout(function () {
          _this.handleScrollEnd(virtualRefreshArgs_1, verticalTranslateY_1, scrollArgs.isWheelScroll, false);
        }, SCROLL_DEBOUNCE_DELAY);
        setTimeout(function () {
          _this.parent.dotNetRef.invokeMethodAsync('RemoveValidationPopup');
          if (scrollArgs.isWheelScroll) {
            _this.parent.dotNetRef.invokeMethodAsync('VirtualRefresh', virtualRefreshArgs_1, verticalTranslateY_1, _this.selectedRowIndex, _this.isScrollIntoview, _this.focusColumnIndex, false, false, false);
          }
          _this.isScrollByFocus = false;
        }, IMMEDIATE_EXECUTION_DELAY);
      } else {
        // Handle horizontal mask row adjustment when column indexes haven't changed
        var isHorizontalScroll = this.currentInfo.direction === 'left' || this.currentInfo.direction === 'right';
        if (isHorizontalScroll && enableVirtualMaskRow) {
          this.virtualEle.adjustTable(horizontalTranslateValue, this.translateMaskY, this.currentInfo.direction);
        }
      }
      var columnIndexesInView = enableColumnVirtualization ? viewInfo.columnIndexes : [];
      this.parent.setColumnIndexesInView(columnIndexesInView);
      if (this.isScrollByNavigation) {
        clearTimeout(this.scrollTimer);
        this.scrollTimer = setTimeout(function () {
          _this.handleScrollEnd(null, 0, null, true);
        }, SCROLL_DEBOUNCE_DELAY);
      } else {
        this.nextRowToNavigate = 0;
      }
      //Vertical Scroll Handling
      var hasRowIndexChanged = this.preStartIndex !== previousStartIndex;
      if (hasRowIndexChanged && enableVirtualization) {
        if (groupCount === 0 && (this.observer.isWheelScrolling || this.observer.isTouchScrolling)) {
          this.preventFocusWhileScroll();
          isPreventFocusScroll = true;
        }
        _this.parent.options.currentPage = viewInfo.currentPage;
        var isCalledFromScrollIntoView_1 = _this.isScrollIntoview;
        var nextRowToFocus = this.nextRowToNavigate;
        var startIndex = viewInfo.endIndex - pageSize;
        var translateY_1 = startIndex * (rowHeight ? rowHeight : parentRowHeight);
        // Build vertical refresh arguments
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        var verticalRefreshArgs_1 = {
          requestType: 'virtualscroll',
          nextRowToNavigate: nextRowToFocus,
          virtualStartIndex: startIndex,
          virtualEndIndex: viewInfo.endIndex,
          axis: 'Y',
          RHeight: parentRowHeight
        };
        setTimeout(function () {
          _this.parent.dotNetRef.invokeMethodAsync('VirtualRefresh', verticalRefreshArgs_1, translateY_1, _this.selectedRowIndex, isCalledFromScrollIntoView_1, _this.focusColumnIndex, false, false, isPreventFocusScroll);
        }, IMMEDIATE_EXECUTION_DELAY);
        _this.isScrollIntoview = false;
      } else if (this.shouldHandleRowSelection(previousStartIndex)) {
        _this.parent.dotNetRef.invokeMethodAsync('SelectRow', _this.selectedRowIndex, _this.isScrollIntoview, _this.focusColumnIndex);
        _this.selectedRowIndex = -1;
        _this.isScrollIntoview = false;
      }
      //Update previous info for next scroll event
      this.prevInfo = viewInfo;
    };
    /**
     * Determines if row selection should be handled for scroll into view scenario
     *
     * @private
     * @param {number} previousStartIndex - Previous start index before scroll
     * @returns {boolean} True if row selection should be handled
     */
    VirtualContentRenderer.prototype.shouldHandleRowSelection = function (previousStartIndex) {
      var isValidRowIndex = this.selectedRowIndex >= 0;
      var hasIndexNotChanged = this.preStartIndex === previousStartIndex;
      var isScrollIntoView = this.isScrollIntoview;
      var isVerticalScroll = this.currentInfo.direction === 'up' || this.currentInfo.direction === 'down';
      return isValidRowIndex && hasIndexNotChanged && isScrollIntoView && isVerticalScroll;
    };
    /**
     * Sets the virtual table width and applies translation transforms for column virtualization.
     * This method adjusts both header and content virtual elements when column indexes change
     * or when explicitly refreshed. Handles frozen columns and RTL scenarios.
     *
     * @public
     * @param {object} [args] - Optional configuration object
     * @param {boolean} [args.refresh] - Whether to force refresh regardless of column index changes
     * @param {string} [args.axis] - The scroll axis ('X' or 'Y')
     * @param {string} [args.direction] - The scroll direction (e.g., 'left', 'right')
     * @returns {void}
     */
    VirtualContentRenderer.prototype.setColVTableWidthAndTranslate = function (args) {
      var _a = this.parent.options,
        enableColumnVirtualization = _a.enableColumnVirtualization,
        frozenColumns = _a.frozenColumns,
        enableVirtualization = _a.enableVirtualization;
      // Determine if column indexes have changed or refresh is forced
      var hasColumnIndexChanged = enableColumnVirtualization && this.prevInfo && JSON.stringify(this.currentInfo.columnIndexes) !== JSON.stringify(this.prevInfo.columnIndexes);
      var shouldRefresh = args && args.refresh;
      if (hasColumnIndexChanged || shouldRefresh) {
        var translateX = this.getColumnOffset(this.startColIndex - 1);
        var virtualTableWidth = this.getColumnOffset(this.endColIndex) - translateX;
        var tableWidth = virtualTableWidth.toString();
        if (frozenColumns === 0) {
          this.header.virtualEle.setWrapperWidth(tableWidth);
          this.virtualEle.setWrapperWidth(tableWidth);
          this.parent.getContentTable().parentElement.style.width = tableWidth + 'px';
        }
        // Apply translation to header with direction
        var headerDirection = args && args.direction ? args.direction : '';
        this.header.virtualEle.adjustTable(this.movableTranslateX, 0, headerDirection);
        if (enableColumnVirtualization && args && args.axis === 'X') {
          if (!enableVirtualization) {
            this.invokeAdjustTable(this.movableTranslateX, 0, args.direction);
          } else {
            var existingTranslateY = this.virtualEle.extractTranslateY(this.virtualEle.wrapper.style.transform);
            var translateXValue = parseInt(existingTranslateY.replace('px', ''), 10);
            this.invokeAdjustTable(this.movableTranslateX, translateXValue, args.direction);
          }
        }
      }
    };
    VirtualContentRenderer.prototype.invokeAdjustTable = function (xValue, yValue, direction) {
      this.virtualEle.adjustTable(xValue, yValue, direction);
    };
    VirtualContentRenderer.prototype.refreshOnDataChange = function () {
      var panelFirstElementChild = this.getPanel().firstElementChild;
      panelFirstElementChild.scrollTop = 0;
      panelFirstElementChild.scrollLeft = 0;
      if (this.parent.options.enableColumnVirtualization) {
        this.header.virtualEle.adjustTable(0, 0);
      }
      this.virtualEle.adjustTable(0, 0);
      this.refreshOffsets();
      this.refreshVirtualElement();
    };
    /**
     * Handles keyboard events for virtual scrolling navigation.
     * Tracks arrow key presses for scroll optimization and handles Ctrl+Home/End navigation.
     *
     * This method:
     * - Sets the active arrow key (ArrowUp/ArrowDown) on the observer for scroll optimization
     * - Detects Ctrl+Home and Ctrl+End key combinations for quick navigation
     * - Prevents default browser behavior for Ctrl+Home/End to enable custom handling
     *
     * @private
     * @param {KeyboardEventArgs} keyboardEvent - The keyboard event arguments
     * @returns {void}
     */
    VirtualContentRenderer.prototype.keyDownHandler = function (keyboardEvent) {
      var ARROW_DOWN = 'ArrowDown';
      var ARROW_UP = 'ArrowUp';
      var END_KEY = 'End';
      var HOME_KEY = 'Home';
      var CTRL_END = 'ctrlEnd';
      var CTRL_HOME = 'ctrlHome';
      var EMPTY_KEY = '';
      if (sf.base.isNullOrUndefined(this.observer)) {
        return;
      }
      // Cache the pressed key to avoid repeated property access
      var pressedKey = keyboardEvent.key;
      // Track arrow key presses for scroll optimization
      var isArrowDown = pressedKey === ARROW_DOWN;
      var isArrowUp = pressedKey === ARROW_UP;
      var isArrowKey = isArrowDown || isArrowUp;
      this.observer.blazorActiveKey = isArrowKey ? pressedKey : EMPTY_KEY;
      // Handle Ctrl+Home and Ctrl+End navigation keys
      if (keyboardEvent.ctrlKey) {
        var isEndKey = pressedKey === END_KEY;
        var isHomeKey = pressedKey === HOME_KEY;
        var isNavigationKey = isEndKey || isHomeKey;
        if (isNavigationKey) {
          this.keyCombination = isEndKey ? CTRL_END : CTRL_HOME;
          keyboardEvent.preventDefault();
        }
      }
    };
    VirtualContentRenderer.prototype.columnVirtualizationKeyDownHandler = function (keyboardEvent) {
      // Constants for keys and CSS classes
      var TAB_KEY = 'Tab';
      var LEFT_FREEZE_CLASS = 'e-leftfreeze';
      var RIGHT_FREEZE_CLASS = 'e-rightfreeze';
      var ROW_CELL_CLASS = 'e-rowcell';
      var HEADER_CELL_CLASS = 'e-headercell';
      this.focusFromPager = false;
      // --- SECTION 1: Cache parent options and extract event target information ---
      var _a = this.parent.options,
        frozenColumns = _a.frozenColumns,
        columns = _a.columns;
      var targetElement = keyboardEvent.target;
      var targetCell = parentsUntil(targetElement, ROW_CELL_CLASS);
      var targetHeader = parentsUntil(targetElement, HEADER_CELL_CLASS);
      // --- SECTION 2: Calculate column index and determine column set ---
      var columnIndex = targetCell ? parseInt(targetCell.getAttribute('aria-colindex'), 10) - 1 : -1;
      var orderedFrozenColumns = this.parent.getOrderedFrozenColumns();
      var gridColumns = frozenColumns === 0 ? columns : orderedFrozenColumns;
      // --- SECTION 3: Check frozen column state and scroll requirements ---
      var scrollValue = Math.round(this.content.scrollLeft + this.content.clientWidth);
      var isFrozenElement = targetElement.classList.contains(LEFT_FREEZE_CLASS) || targetElement.classList.contains(RIGHT_FREEZE_CLASS);
      var updateScrollLeft = false;
      if (frozenColumns > 0 && isFrozenElement) {
        var currentCellIsLastFreeze = this.checkNextElementHasFreeze(targetElement);
        updateScrollLeft = currentCellIsLastFreeze;
      }
      // --- SECTION 4: Delegate to appropriate handler based on Tab direction ---
      var isForwardTab = keyboardEvent.key === TAB_KEY && !keyboardEvent.shiftKey;
      var isBackwardTab = keyboardEvent.key === TAB_KEY && keyboardEvent.shiftKey;
      if (isForwardTab) {
        this.handleForwardTabNavigation(targetElement, targetHeader, columnIndex, gridColumns, updateScrollLeft, scrollValue);
      } else if (isBackwardTab) {
        this.handleBackwardTabNavigation(targetElement, columnIndex, orderedFrozenColumns, columns, frozenColumns, updateScrollLeft, scrollValue);
      }
    };
    /**
     * Handles forward Tab key navigation for column virtualization.
     * Resets scroll position to start when reaching last column or navigating from frozen columns.
     *
     * @private
     * @param {HTMLElement} targetElement - The currently focused element
     * @param {Element} targetHeader - The header cell element if target is in header
     * @param {number} columnIndex - The column index of the target element
     * @param {Column[]} gridColumns - Array of grid columns
     * @param {boolean} updateScrollLeft - Whether to update scroll left position
     * @param {number} scrollValue - Current scroll position value
     * @returns {void}
     */
    VirtualContentRenderer.prototype.handleForwardTabNavigation = function (targetElement, targetHeader, columnIndex, gridColumns, updateScrollLeft, scrollValue) {
      var _this_1 = this;
      var LEFT_FREEZE_CLASS = 'e-leftfreeze';
      var ROW_CLASS = 'e-row';
      var MAPPING_UID_ATTR = 'e-mappinguid';
      var ARIA_COL_INDEX_ATTR = 'aria-colindex';
      var ARIA_ROW_INDEX_ATTR = 'aria-rowindex';
      // Check if target is last header cell
      var lastHeaderCell = false;
      if (!sf.base.isNullOrUndefined(targetHeader)) {
        var headerDiv = targetHeader.querySelector("[".concat(MAPPING_UID_ATTR, "]"));
        if (!sf.base.isNullOrUndefined(headerDiv)) {
          var headerMappingUid = headerDiv.getAttribute(MAPPING_UID_ATTR);
          var lastColumnUid = gridColumns[gridColumns.length - 1].uid;
          lastHeaderCell = headerMappingUid === lastColumnUid;
        }
      }
      // Check if next element after target is pager
      var nextTargetIsPager = this.parent.options.allowPaging && targetElement.parentElement.nextElementSibling == null;
      // Determine if current column is last in grid
      var isLastColumn = columnIndex === gridColumns.length - 1 && !nextTargetIsPager;
      var gridContent = this.parent.getContent();
      // Check if scroll update is needed for frozen mid-scroll scenario
      var isScrollableFrozenScenario = updateScrollLeft && scrollValue <= gridContent.scrollWidth && gridContent.scrollLeft !== 0 && targetElement.classList.contains(LEFT_FREEZE_CLASS);
      var needsScrollUpdate = this.frozenMidScroll = isScrollableFrozenScenario;
      // Apply scroll and navigation state updates after DOM settles
      setTimeout(function () {
        var shouldResetScroll = isLastColumn || lastHeaderCell || needsScrollUpdate;
        if (shouldResetScroll) {
          // Reset scroll to start for wraparound or frozen column navigation
          gridContent.scrollLeft = 0;
          // Store navigation state for frozen mid-scroll scenario
          if (needsScrollUpdate && !sf.base.isNullOrUndefined(targetElement)) {
            var targetColIndex = parseInt(targetElement.getAttribute(ARIA_COL_INDEX_ATTR), 10);
            _this_1.selectedCellNavigation = targetColIndex;
            var parentRow = parentsUntil(targetElement, ROW_CLASS);
            if (!sf.base.isNullOrUndefined(parentRow)) {
              var rowIndex = parseInt(parentRow.getAttribute(ARIA_ROW_INDEX_ATTR), 10) - 1;
              _this_1.selectedRowNavigation = rowIndex;
            }
          }
        }
      }, 30);
    };
    /**
     * Handles backward Tab (Shift+Tab) key navigation for column virtualization.
     * Scrolls to end when reaching first column or navigating to frozen columns.
     *
     * @private
     * @param {HTMLElement} targetElement - The currently focused element
     * @param {number} columnIndex - The column index of the target element
     * @param {Column[]} orderedFrozenColumns - Array of frozen columns in order
     * @param {Column[]} columns - Array of all grid columns
     * @param {number} frozenColumns - Count of frozen columns in the grid
     * @param {boolean} updateScrollLeft - Whether to update scroll left position
     * @param {number} scrollValue - Current scroll position value
     * @returns {void}
     */
    VirtualContentRenderer.prototype.handleBackwardTabNavigation = function (targetElement, columnIndex, orderedFrozenColumns, columns, frozenColumns, updateScrollLeft, scrollValue) {
      var _this_1 = this;
      // Constants
      var MOVABLE_SCROLLBAR_CLASS = 'e-movablescrollbar';
      var ROW_CLASS = 'e-row';
      var HEADER_CELL_DIV_CLASS = 'e-headercelldiv';
      var MAPPING_UID_ATTR = 'e-mappinguid';
      var ARIA_COL_INDEX_ATTR = 'aria-colindex';
      var ARIA_ROW_INDEX_ATTR = 'aria-rowindex';
      // Check if target is movable scrollbar (special pager case)
      var targetIsMovableScrollBar = this.parent.options.allowPaging && frozenColumns > 0 && targetElement.classList.contains(MOVABLE_SCROLLBAR_CLASS);
      var gridContent = this.parent.getContent();
      // Determine if current column is first in virtualized range
      var isFirstVirtualColumn = columnIndex === 0 || targetIsMovableScrollBar;
      // Check if scroll update is needed for frozen mid-scroll scenario
      var isScrollableFrozenScenario = updateScrollLeft && scrollValue < gridContent.scrollWidth;
      var needsScrollUpdate = this.frozenMidScroll = isScrollableFrozenScenario;
      // Apply scroll and navigation state updates after DOM settles
      setTimeout(function () {
        var shouldScrollToEnd = isFirstVirtualColumn || needsScrollUpdate;
        if (shouldScrollToEnd) {
          // Set focus tracking flags for scroll restoration
          _this_1.isScrollByFocus = true;
          _this_1.isLastCell = true;
          _this_1.selectedRowNavigation = 0;
          _this_1.focusFromPager = targetIsMovableScrollBar;
          // Calculate last column index based on frozen column state
          var hasFrozenColumns = frozenColumns > 0;
          var selectedCellNavigation = hasFrozenColumns ? orderedFrozenColumns.length - 1 : columns.length - 1;
          // Scroll to end to reveal last virtualized column
          gridContent.scrollLeft = gridContent.scrollWidth;
          // Update navigation state for frozen mid-scroll scenario
          if (needsScrollUpdate && !sf.base.isNullOrUndefined(targetElement)) {
            // Handle header cell scenario
            var headerCellDiv = targetElement.querySelector(".".concat(HEADER_CELL_DIV_CLASS));
            if (!sf.base.isNullOrUndefined(headerCellDiv)) {
              var mappingUid_1 = headerCellDiv.getAttribute(MAPPING_UID_ATTR);
              var currentColIndex = orderedFrozenColumns.findIndex(function (col) {
                return col.uid === mappingUid_1;
              });
              selectedCellNavigation = currentColIndex;
            }
            // Handle data cell scenario
            var parentRow = parentsUntil(targetElement, ROW_CLASS);
            if (!sf.base.isNullOrUndefined(parentRow)) {
              var cellColIndex = parseInt(targetElement.getAttribute(ARIA_COL_INDEX_ATTR), 10) - 1;
              selectedCellNavigation = cellColIndex;
              var rowIndex = parseInt(parentRow.getAttribute(ARIA_ROW_INDEX_ATTR), 10) - 1;
              _this_1.selectedRowNavigation = rowIndex;
            }
          }
          // Store final cell navigation state
          _this_1.selectedCellNavigation = selectedCellNavigation;
        }
      }, 30);
    };
    VirtualContentRenderer.prototype.checkNextElementHasFreeze = function (element) {
      // Get the next sibling element
      var nextElement = element.nextElementSibling;
      // Check if the next sibling exists and has 'e-leftfreeze' in its class
      if (nextElement && nextElement.classList.contains('e-leftfreeze')) {
        return false;
      }
      return true;
    };
    VirtualContentRenderer.prototype.handleCellFocusAndNavigation = function (cell, action, keyCombination) {
      // Navigation action constants
      var MOVE_RIGHT_CELL = 'MoveRightCell';
      var MOVE_LEFT_CELL = 'MoveLeftCell';
      var MOVE_DOWN_CELL = 'MoveDownCell';
      var MOVE_UP_CELL = 'MoveUpCell';
      // Key combination constants
      var ALT_W = 'AltW';
      var CTRL_HOME = 'CtrlHome';
      var CTRL_END = 'CtrlEnd';
      var HOME_KEY = 'Home';
      var END_KEY = 'End';
      // CSS class constants
      var HEADER_CELL_DIV_CLASS = 'e-headercelldiv';
      // Attribute constants
      var ARIA_COL_INDEX_ATTR = 'aria-colindex';
      var DATA_UID_ATTR = 'data-uid';
      var MAPPING_UID_ATTR = 'e-mappinguid';
      // Input validation
      if (!cell) {
        return;
      }
      var rowHeight = this.parent.getRowHeight();
      var content = this.parent.getContent();
      var cellDOMRect = cell.getBoundingClientRect();
      var contentDOMRect = content.getBoundingClientRect();
      // Reset scroll tracking flags on observer
      if (this.observer) {
        this.observer.isWheelScrolling = false;
        this.observer.isTouchScrolling = false;
      }
      var _a = this.parent.options,
        enableColumnVirtualization = _a.enableColumnVirtualization,
        columns = _a.columns,
        frozenColumns = _a.frozenColumns;
      // Boolean flags for navigation actions
      var isMoveRight = action === MOVE_RIGHT_CELL;
      var isMoveLeft = action === MOVE_LEFT_CELL;
      var isMoveDown = action === MOVE_DOWN_CELL;
      var isMoveUp = action === MOVE_UP_CELL;
      var isHorizontalNavigation = isMoveRight || isMoveLeft;
      var isVerticalNavigation = isMoveDown || isMoveUp;
      // --- SECTION 1: Handle cell navigation tracking for non-virtualized columns ---
      if (!enableColumnVirtualization && isHorizontalNavigation) {
        this.trackNonVirtualizedColumnNavigation(cell, isMoveRight, isMoveLeft, ARIA_COL_INDEX_ATTR);
      }
      // --- SECTION 2: Handle cell navigation tracking for virtualized columns ---
      if (enableColumnVirtualization && isHorizontalNavigation) {
        this.trackVirtualizedColumnNavigation(cell, frozenColumns, DATA_UID_ATTR, MAPPING_UID_ATTR, ARIA_COL_INDEX_ATTR, HEADER_CELL_DIV_CLASS);
      }
      // --- SECTION 3: Apply focus behavior based on navigation direction ---
      if (isVerticalNavigation) {
        // Vertical navigation: prevent scroll during focus to avoid browser auto-scroll
        cell.focus({
          preventScroll: true
        });
      } else if (isHorizontalNavigation) {
        this.applyHorizontalNavigationFocus(cell, keyCombination, enableColumnVirtualization, columns);
      }
      // --- SECTION 4: Handle viewport scroll adjustments ---
      var scrollBarWidth = getScrollBarWidth();
      var visibleBottomEdge = contentDOMRect.top + contentDOMRect.height - scrollBarWidth;
      var visibleTopEdge = contentDOMRect.top + rowHeight;
      // Scroll down when cell is below viewport
      if (isMoveDown && cellDOMRect.bottom > visibleBottomEdge) {
        this.isScrollByNavigation = true;
        content.scrollTop += rowHeight;
      }
      // Scroll up when cell is above viewport
      else if (isMoveUp && cellDOMRect.bottom < visibleTopEdge) {
        this.isScrollFromFocus = true;
        this.isScrollByNavigation = true;
        content.scrollTop -= rowHeight;
      }
      // --- SECTION 5: Handle special key combinations ---
      var isCancelAction = keyCombination == null && action == null;
      var requiresSpecialHandling = keyCombination === ALT_W || keyCombination === CTRL_HOME || keyCombination === CTRL_END || keyCombination === HOME_KEY || keyCombination === END_KEY || isCancelAction;
      if (requiresSpecialHandling) {
        //keyCombination == null && action == null -> We get this when clicking Cancel in Add form
        var requiresScrollIntoView = keyCombination === ALT_W || keyCombination === CTRL_HOME || keyCombination === CTRL_END;
        if (requiresScrollIntoView) {
          cell.focus({
            preventScroll: true
          });
          cell.scrollIntoView({
            behavior: 'auto',
            block: 'nearest',
            inline: 'nearest'
          });
        } else {
          // Handle Home, End keys or cancel action
          cell.focus();
        }
      }
    };
    /**
     * Tracks cell navigation for non-virtualized columns
     *
     * @private
     * @param {HTMLElement} cell - The cell element being navigated
     * @param {boolean} isMoveRight - Whether navigation is moving right
     * @param {boolean} isMoveLeft - Whether navigation is moving left
     * @param {string} ariaColIndexAttr - The aria-colindex attribute name
     * @returns {void}
     */
    VirtualContentRenderer.prototype.trackNonVirtualizedColumnNavigation = function (cell, isMoveRight, isMoveLeft, ariaColIndexAttr) {
      var cellColIndex = Number(cell.getAttribute(ariaColIndexAttr)) - 1;
      if (this.selectedCellNavigation === -1) {
        this.selectedCellNavigation = cellColIndex;
      } else if (isMoveRight && this.selectedCellNavigation + 1 === cellColIndex) {
        this.selectedCellNavigation++;
      } else if (isMoveLeft && this.selectedCellNavigation - 1 === cellColIndex) {
        this.selectedCellNavigation--;
      }
    };
    /**
     * Tracks cell navigation for virtualized columns
     *
     * @param {HTMLElement} cell - The cell element being navigated
     * @param {number} frozenColumns - Count of frozen columns in the grid
     * @param {string} dataUidAttr - The data-uid attribute name
     * @param {string} mappingUidAttr - The e-mappinguid attribute name
     * @param {string} ariaColIndexAttr - The aria-colindex attribute name
     * @param {string} headerCellDivClass - The header cell div class name
     * @private
     * @returns {void}
     */
    VirtualContentRenderer.prototype.trackVirtualizedColumnNavigation = function (cell, frozenColumns, dataUidAttr, mappingUidAttr, ariaColIndexAttr, headerCellDivClass) {
      var ROW_CLASS = 'e-row';
      var COLUMN_HEADER_CLASS = 'e-columnheader';
      var isHeaderCell = false;
      var parentRow = parentsUntil(cell, ROW_CLASS);
      if (sf.base.isNullOrUndefined(parentRow) && parentsUntil(cell, COLUMN_HEADER_CLASS)) {
        parentRow = parentsUntil(cell, COLUMN_HEADER_CLASS);
        isHeaderCell = true;
        this.selectedRowNavigation = 0;
      }
      var currentColIndex = 0;
      var gridColumns = frozenColumns === 0 ? this.parent.getColumns() : this.parent.getOrderedFrozenColumns();
      // Get column index for header cell
      if (isHeaderCell) {
        var headerCellDiv = cell.querySelector(headerCellDivClass);
        if (!sf.base.isNullOrUndefined(cell) && !sf.base.isNullOrUndefined(headerCellDiv)) {
          var mappingUid_2 = headerCellDiv.getAttribute(mappingUidAttr) || cell.getAttribute(dataUidAttr);
          if (!sf.base.isNullOrUndefined(mappingUid_2) && mappingUid_2 !== '') {
            var foundIndex = gridColumns.findIndex(function (col) {
              return col.uid === mappingUid_2;
            });
            currentColIndex = foundIndex >= 0 ? foundIndex : 0;
          }
        }
      } else {
        // Get column index for body cell
        var colIndexAttr = cell.getAttribute(ariaColIndexAttr);
        if (!sf.base.isNullOrUndefined(cell)) {
          if (!sf.base.isNullOrUndefined(colIndexAttr)) {
            currentColIndex = Number(colIndexAttr) - 1;
          }
        }
      }
      this.selectedCellNavigation = currentColIndex;
    };
    /**
     * Applies focus for horizontal navigation (left/right)
     *
     * @private
     * @param {HTMLElement} cell - The cell element to apply focus to
     * @param {string} keyCombination - The key combination pressed (e.g., 'ShiftTab')
     * @param {boolean} enableColumnVirtualization - Whether column virtualization is enabled
     * @param {Column[]} columns - Array of grid columns
     * @returns {void}
     */
    VirtualContentRenderer.prototype.applyHorizontalNavigationFocus = function (cell, keyCombination, enableColumnVirtualization, columns) {
      var parentRow = parentsUntil(cell, 'e-row');
      if (!sf.base.isNullOrUndefined(parentRow)) {
        this.isHeaderNavigated = false;
        var rowIndexAttr = parentRow.getAttribute('aria-rowindex');
        this.selectedRowNavigation = rowIndexAttr ? Number(rowIndexAttr) - 1 : 0;
      } else {
        this.isHeaderNavigated = true;
      }
      // Handle ShiftTab navigation on last cell in header
      if (enableColumnVirtualization && keyCombination === 'ShiftTab' && this.isLastCell && this.isHeaderNavigated) {
        this.selectedRowNavigation++;
        this.selectedCellNavigation = columns.length - 1;
        this.isLastCell = false;
      }
      this.isScrollByFocus = true;
      cell.focus();
    };
    VirtualContentRenderer.prototype.getInfoFromView = function (direction, info, offsets) {
      var virtualInfo = {
        direction: direction,
        sentinelInfo: info,
        offsets: offsets,
        startIndex: this.preStartIndex,
        endIndex: this.preEndIndex
      };
      // Cache parent options to avoid repeated property access
      var _a = this.parent.options,
        pageSize = _a.pageSize,
        allowGrouping = _a.allowGrouping,
        overscanCount = _a.overscanCount,
        groupCount = _a.groupCount,
        enableLazyLoading = _a.enableLazyLoading,
        height = _a.height,
        enableVirtualMaskRow = _a.enableVirtualMaskRow,
        totalItemCount = _a.totalItemCount,
        isRenderedFromTreeGrid = _a.isRenderedFromTreeGrid;
      var viewportHeight = height.toString().indexOf('%') < 0 ? this.content.getBoundingClientRect().height : this.parent.element.getBoundingClientRect().height;
      virtualInfo.page = this.getPageFromTop(offsets.top + viewportHeight, virtualInfo);
      virtualInfo.blockIndexes = this.vHelper.getBlockIndexes(virtualInfo.page);
      virtualInfo.columnIndexes = info.axis === 'X' ? this.vHelper.getColumnIndexes() : this.parent.getColumnIndexesInView();
      // --- Row Start and End Index Calculation ---
      var rowHeight = this.parent.getRowHeight();
      var exactTopIndex = offsets.top / rowHeight;
      var noOfInViewIndexes = viewportHeight / rowHeight;
      var exactEndIndex = exactTopIndex + noOfInViewIndexes;
      // Calculate quarter and half page sizes (used for threshold calculations)
      var quarterPageSize = pageSize / 4;
      var halfPageSize = pageSize / 2;
      // Adjust for overscan in grouped scenarios
      var hasGroupingWithOverscan = overscanCount > 0 && allowGrouping && groupCount > 0 && !enableLazyLoading && overscanCount < this.parent.options.pageSize;
      if (hasGroupingWithOverscan) {
        var adjustedPageSize = pageSize + overscanCount * 2;
        quarterPageSize = adjustedPageSize / 4;
        halfPageSize = adjustedPageSize / 2;
      }
      var totalCount = groupCount ? this.getVisibleGroupedRowCount() : this.count;
      // --- Handle Downward Scrolling ---
      if (virtualInfo.direction === 'down' && !this.isScrollFromFocus) {
        this.handleDownwardScroll(virtualInfo, exactTopIndex, exactEndIndex, noOfInViewIndexes, quarterPageSize, halfPageSize, totalCount, pageSize, overscanCount, totalItemCount, enableVirtualMaskRow);
      }
      // --- Handle Upward Scrolling ---
      else if (virtualInfo.direction === 'up') {
        this.handleUpwardScroll(virtualInfo, exactTopIndex, exactEndIndex, noOfInViewIndexes, quarterPageSize, halfPageSize, totalCount, rowHeight, pageSize, totalItemCount, enableVirtualMaskRow, isRenderedFromTreeGrid);
      }
      if (!enableVirtualMaskRow) {
        this.isScrollFromFocus = false;
        this.preStartIndex = this.startIndex = virtualInfo.startIndex;
        this.preEndIndex = virtualInfo.endIndex;
      }
      return virtualInfo;
    };
    /**
     * Handles downward scroll calculations and updates virtual info.
     * Modifies the virtualInfo object in place by calculating and updating:
     * - startIndex and endIndex based on scroll position
     * - currentPage based on end index
     * - Updates internal state (preStartIndex, preEndIndex, isScrollFromFocus)
     * - Updates navigation tracking (nextRowToNavigate)
     *
     * @private
     * @param {VirtualInfo} virtualInfo - Virtual information object to be updated with scroll calculations
     * @param {number} exactTopIndex - The exact row index at the top of the viewport
     * @param {number} exactEndIndex - The exact row index at the bottom of the viewport
     * @param {number} visibleRowCount - Number of rows visible in the current viewport
     * @param {number} quarterPageSize - One quarter of the page size for threshold calculations
     * @param {number} halfPageSize - Half of the page size for threshold calculations
     * @param {number} totalRowCount - Total number of rows in the grid
     * @param {number} pageSize - Number of rows per page
     * @param {number} overscanCount - Number of additional rows to render outside viewport
     * @param {number} totalItemCount - Total number of items in the data source
     * @param {boolean} enableVirtualMaskRow - Whether virtual mask row rendering is enabled
     * @returns {void}
     */
    VirtualContentRenderer.prototype.handleDownwardScroll = function (virtualInfo, exactTopIndex, exactEndIndex, visibleRowCount, quarterPageSize, halfPageSize, totalRowCount, pageSize, overscanCount, totalItemCount, enableVirtualMaskRow) {
      var suggestedStartIndex = Math.round(exactEndIndex) - Math.round(quarterPageSize);
      var overscanRowsRendered = this.preStartIndex === 0 ? 0 : overscanCount;
      // Handle virtual mask row scenario
      var thresholdForMaskRow = virtualInfo.startIndex - overscanRowsRendered + Math.round(halfPageSize + quarterPageSize);
      if (enableVirtualMaskRow && exactEndIndex > thresholdForMaskRow) {
        visibleRowCount = Math.ceil(visibleRowCount) - 1;
        var rowIndexDifference = Math.ceil(exactTopIndex) - (this.preStartIndex - overscanRowsRendered);
        if (rowIndexDifference >= visibleRowCount) {
          // Calculate new start and end indexes
          var calculatedStartIndex = Math.floor(exactTopIndex);
          virtualInfo.startIndex = calculatedStartIndex >= 0 ? calculatedStartIndex : 0;
          var calculatedEndIndex = virtualInfo.startIndex + pageSize;
          virtualInfo.endIndex = calculatedEndIndex < totalRowCount ? calculatedEndIndex : totalRowCount;
          // Adjust start index if end reached
          if (calculatedEndIndex >= totalRowCount) {
            var adjustedStartIndex = virtualInfo.endIndex - pageSize;
            virtualInfo.startIndex = adjustedStartIndex < 0 ? 0 : adjustedStartIndex;
          }
          virtualInfo.currentPage = Math.ceil(virtualInfo.endIndex / pageSize);
          this.preStartIndex = this.startIndex = virtualInfo.startIndex;
          this.preEndIndex = virtualInfo.endIndex;
        }
        this.isScrollFromFocus = false;
      }
      // Handle non-virtual-mask-row scenario
      var shouldUpdateIndexes = !enableVirtualMaskRow && sf.base.isNullOrUndefined(virtualInfo.startIndex);
      var thresholdExceeded = exactEndIndex > virtualInfo.startIndex + Math.round(halfPageSize + quarterPageSize);
      var notAtEnd = virtualInfo.endIndex !== totalRowCount;
      if (shouldUpdateIndexes || thresholdExceeded && notAtEnd) {
        virtualInfo.startIndex = suggestedStartIndex >= 0 ? Math.round(suggestedStartIndex) : 0;
        // Check if overscan end is reached
        var isOverscanEndReached = overscanCount > 0 && overscanCount > pageSize && virtualInfo.startIndex + pageSize >= totalItemCount;
        // Adjust start index based on exact top position
        if (virtualInfo.startIndex > exactTopIndex && !isOverscanEndReached) {
          virtualInfo.startIndex = Math.floor(exactTopIndex);
        }
        var calculatedEndIndex = virtualInfo.startIndex + pageSize;
        // Adjust start index if calculated end exceeds visible end
        if (calculatedEndIndex < exactEndIndex) {
          virtualInfo.startIndex = Math.ceil(exactEndIndex) - pageSize;
        }
        virtualInfo.endIndex = calculatedEndIndex < totalRowCount ? calculatedEndIndex : totalRowCount;
        // Final adjustment if at end
        if (calculatedEndIndex >= totalRowCount) {
          var adjustedStartIndex = virtualInfo.endIndex - pageSize;
          virtualInfo.startIndex = adjustedStartIndex > 0 ? adjustedStartIndex : 0;
        }
        virtualInfo.currentPage = Math.ceil(virtualInfo.endIndex / pageSize);
      }
      // Update navigation tracking
      var selectedIndexes = this.parent.getSelectedRowIndexes(true);
      this.nextRowToNavigate = selectedIndexes.length > 0 ? selectedIndexes[selectedIndexes.length - 1] - 1 : -1;
    };
    /**
     * Handles upward scroll calculations and updates virtual info.
     * Modifies the virtualInfo object in place by calculating and updating
     *
     * @private
     * @param {VirtualInfo} virtualInfo - Virtual information object to be updated with scroll calculations
     * @param {number} exactTopIndex - The exact row index at the top of the viewport
     * @param {number} exactEndIndex - The exact row index at the bottom of the viewport
     * @param {number} visibleRowCount - Number of rows visible in the current viewport
     * @param {number} quarterPageSize - One quarter of the page size for threshold calculations
     * @param {number} halfPageSize - Half of the page size for threshold calculations
     * @param {number} totalRowCount - Total number of rows in the grid
     * @param {number} rowHeight - Height of a single row in pixels
     * @param {number} pageSize - Number of rows per page
     * @param {number} totalItemCount - Total number of items in the data source
     * @param {boolean} enableVirtualMaskRow - Whether virtual mask row rendering is enabled
     * @param {boolean} isRenderedFromTreeGrid - Whether the grid is rendered as a tree grid
     * @returns {void}
     */
    VirtualContentRenderer.prototype.handleUpwardScroll = function (virtualInfo, exactTopIndex, exactEndIndex, visibleRowCount, quarterPageSize, halfPageSize, totalRowCount, rowHeight, pageSize, totalItemCount, enableVirtualMaskRow, isRenderedFromTreeGrid) {
      var hasIndexes = Boolean(virtualInfo.startIndex && virtualInfo.endIndex);
      if (!hasIndexes && !enableVirtualMaskRow) {
        return;
      }
      // Calculate load threshold
      var loadAtIndex = Math.round((virtualInfo.startIndex * rowHeight + quarterPageSize * rowHeight) / rowHeight);
      // Handle virtual mask row scenario
      if (enableVirtualMaskRow) {
        visibleRowCount = Math.ceil(visibleRowCount);
        var shouldRecalculate = exactTopIndex < loadAtIndex || Math.ceil(exactTopIndex) > this.preStartIndex;
        if (shouldRecalculate) {
          var ceiledTopIndex = Math.ceil(exactTopIndex);
          var calculatedStartIndex = ceiledTopIndex > 0 ? ceiledTopIndex : 0;
          // Calculate custom thresholds
          var customStartThreshold = totalRowCount - pageSize - halfPageSize;
          var endThreshold = totalRowCount - pageSize;
          var isNearEnd = endThreshold <= exactEndIndex && exactEndIndex <= totalRowCount;
          // Determine start index based on position
          if (exactTopIndex < totalRowCount && customStartThreshold <= exactTopIndex && !isNearEnd) {
            virtualInfo.startIndex = calculatedStartIndex > 0 ? calculatedStartIndex - halfPageSize : 0;
          } else {
            var potentialStartIndex = calculatedStartIndex + pageSize;
            virtualInfo.startIndex = calculatedStartIndex > 0 ? potentialStartIndex > totalRowCount ? totalRowCount - pageSize : calculatedStartIndex : 0;
          }
          // Adjust for tree grid scenario
          if (virtualInfo.startIndex > 0 && isRenderedFromTreeGrid && virtualInfo.endIndex >= totalRowCount) ;else if (virtualInfo.startIndex > 0) {
            virtualInfo.startIndex -= 1;
          }
          var calculatedEndIndex = virtualInfo.startIndex + pageSize;
          if (virtualInfo.startIndex <= 0) {
            virtualInfo.endIndex = pageSize;
          } else {
            virtualInfo.endIndex = calculatedEndIndex < totalRowCount ? calculatedEndIndex : totalRowCount;
          }
          // Adjust if one away from end
          if (virtualInfo.endIndex + 1 === totalRowCount) {
            virtualInfo.endIndex = totalRowCount;
          }
          // Reset start index if negative or total items fit in page
          if (virtualInfo.startIndex < 0 || totalItemCount <= pageSize) {
            virtualInfo.startIndex = 0;
          }
          // Adjust start index if at end
          if (virtualInfo.endIndex === totalRowCount) {
            virtualInfo.startIndex = virtualInfo.endIndex - pageSize;
          }
          virtualInfo.currentPage = Math.ceil(virtualInfo.startIndex / pageSize);
          this.preStartIndex = this.startIndex = virtualInfo.startIndex;
          this.preEndIndex = virtualInfo.endIndex;
        }
        this.isScrollFromFocus = false;
      }
      // Handle non-virtual-mask-row scenario
      if (exactTopIndex < loadAtIndex && !enableVirtualMaskRow) {
        var indexAdjustment = quarterPageSize > visibleRowCount ? quarterPageSize : visibleRowCount + visibleRowCount / 4;
        var calculatedEndIndex = Math.round(exactTopIndex + indexAdjustment);
        virtualInfo.endIndex = calculatedEndIndex < totalRowCount ? calculatedEndIndex : totalRowCount;
        var calculatedStartIndex = virtualInfo.endIndex - pageSize;
        virtualInfo.startIndex = calculatedStartIndex > 0 ? calculatedStartIndex : 0;
        // Adjust end index if start is negative
        if (calculatedStartIndex < 0) {
          virtualInfo.endIndex = pageSize;
        }
        virtualInfo.currentPage = Math.ceil(virtualInfo.startIndex / pageSize);
      }
      // Update navigation tracking
      var selectedRowIndexes = this.parent.getSelectedRowIndexes(true);
      var LAST_SELECTED_OFFSET = -1;
      this.nextRowToNavigate = selectedRowIndexes.length > 0 ? selectedRowIndexes[0] + 1 : 1 * LAST_SELECTED_OFFSET;
    };
    VirtualContentRenderer.prototype.onDataReady = function () {
      var _this_1 = this;
      // eslint-disable-next-line @typescript-eslint/no-this-alias
      var _this = this;
      var _a = this.parent.options,
        totalItemCount = _a.totalItemCount,
        requestType = _a.requestType,
        pageSize = _a.pageSize,
        isAdd = _a.isAdd,
        editMode = _a.editMode,
        newRowPosition = _a.newRowPosition,
        enableVirtualMaskRow = _a.enableVirtualMaskRow,
        enableVirtualization = _a.enableVirtualization,
        rowHeight = _a.rowHeight,
        enableLazyLoading = _a.enableLazyLoading,
        overscanCount = _a.overscanCount,
        customizedOverScan = _a.customizedOverScan,
        frozenColumns = _a.frozenColumns,
        enableColumnVirtualization = _a.enableColumnVirtualization;
      // --- SECTION 2: Update observer and basic state ---
      this.observer.options.totalItems = totalItemCount;
      this.count = totalItemCount;
      this.maxPage = Math.ceil(this.count / pageSize);
      // Rebind scroll event if not a virtual scroll request
      if (requestType !== 'virtualscroll') {
        this.bindScrollEvent();
      }
      // --- SECTION 3: Handle offset refresh for specific request types ---
      // Constants for request types that require offset refresh
      var REFRESH_TRIGGER_TYPES = ['Refresh', 'Filtering', 'ClearFiltering', 'Searching', 'Grouping', 'UnGrouping', 'Reorder', 'RowDragAndDrop', 'refresh', 'filtering', 'clearfiltering', 'searching', 'grouping', 'ungrouping', 'reorder', 'GroupExpandCollapse', 'InfiniteScrolling'];
      if (REFRESH_TRIGGER_TYPES.indexOf(requestType) !== -1 || requestType == null) {
        this.refreshOffsets();
      }
      // --- SECTION 4: Update virtual height and scroll module ---
      this.setVirtualHeight();
      this.parent.scrollModule.refresh();
      this.resetScrollPosition(requestType);
      this.setColVTableWidthAndTranslate();
      // --- SECTION 5: Handle bottom row addition in normal edit mode ---
      var isBottomNormalAdd = isAdd && editMode === 'Normal' && newRowPosition === 'Bottom';
      if (isBottomNormalAdd && enableVirtualization) {
        this.updateTransform(null, null, false, true);
      }
      // --- SECTION 6: Handle virtual mask row transformation ---
      var hasVirtualMaskWithVirtualization = enableVirtualMaskRow && enableVirtualization;
      var isNotGroupExpandCollapse = requestType !== 'GroupExpandCollapse';
      var shouldApplyMaskTransform = (hasVirtualMaskWithVirtualization || enableLazyLoading) && isNotGroupExpandCollapse;
      if (shouldApplyMaskTransform) {
        // Calculate grid-specific values
        var gridRowHeight = rowHeight;
        var gridPageSize = isBottomNormalAdd ? pageSize + 1 : pageSize;
        // Calculate start index with safety check
        var rawStartIndex = this.currentInfo.endIndex - gridPageSize;
        var startIndex = Math.max(0, Number.isFinite(Number(rawStartIndex)) ? Number(rawStartIndex) : 0);
        // Calculate Y translation value
        var effectiveRowHeight = gridRowHeight || this.parent.getRowHeight();
        var yValue_1 = startIndex * effectiveRowHeight;
        // Reset Y value for specific conditions (NaN check or scroll at top with specific request types)
        var RESET_REQUEST_TYPES = ['Filtering', 'filtering', 'clearfiltering', 'ClearFiltering', 'Refresh'];
        var shouldResetYValue = isNaN(yValue_1) || this.content.scrollTop === 0 && RESET_REQUEST_TYPES.indexOf(requestType) !== -1;
        if (shouldResetYValue) {
          yValue_1 = 0;
        }
        // Calculate overscan count based on current viewport state
        var gridOverScanCount = 0;
        if (overscanCount > 0) {
          var actualStartIndex = this.currentInfo.startIndex || 0;
          var viewportFirstDataRow = this.content.querySelectorAll('.e-row:not(.e-masked-row)')[0];
          if (viewportFirstDataRow) {
            var viewportStartIndex = parseInt(viewportFirstDataRow.getAttribute('aria-rowindex'), 10) - 1;
            gridOverScanCount = this.isDefaultGrouping() ? customizedOverScan : actualStartIndex - viewportStartIndex;
          }
        }
        // Apply overscan adjustment
        var overscanRowHeight = gridRowHeight || this.parent.getRowHeight(startIndex !== 0);
        yValue_1 -= (pageSize + gridOverScanCount) * overscanRowHeight;
        // Reset if no data
        if (this.count === 0) {
          yValue_1 = 0;
        }
        // Handle horizontal translation for scroll into view
        if (this.isScrollIntoview && this.currentInfo.direction === 'down' && enableColumnVirtualization) {
          _this.translateMaskX = _this.translateMaskX === 0 ? this.movableTranslateX : _this.translateMaskX;
        }
        // Determine if transform should be applied now
        var isNotAtEnd = totalItemCount !== this.currentInfo.endIndex;
        var isScrollingUp = this.currentInfo.direction === 'up';
        var shouldApplyTransformNow = overscanCount === 0 || isNotAtEnd || isScrollingUp || isBottomNormalAdd;
        // Apply transform if conditions are met
        if (shouldApplyTransformNow) {
          setTimeout(function () {
            _this.translateMaskY = yValue_1;
            _this.translateMaskX = sf.base.isNullOrUndefined(_this.translateMaskX) ? 0 : _this.translateMaskX;
            var xTranslate = frozenColumns === 0 ? _this_1.translateMaskX : 0;
            var transformValue = "translate(".concat(xTranslate, "px, ").concat(yValue_1, "px)");
            _this_1.virtualEle.wrapper.style.transform = transformValue;
          }, 0);
        }
      }
      // --- SECTION 7: Ensure previous info is initialized ---
      this.prevInfo = this.prevInfo || this.vHelper.getData();
    };
    /**
     * Set the virtual height for internal use.
     *
     * @hidden
     * @returns {void}
     */
    VirtualContentRenderer.prototype.setVirtualHeight = function () {
      var _a = this.parent.options,
        columns = _a.columns,
        enableColumnVirtualization = _a.enableColumnVirtualization,
        totalItemCount = _a.totalItemCount,
        pageSize = _a.pageSize,
        groupCount = _a.groupCount,
        frozenColumns = _a.frozenColumns,
        enableVirtualMaskRow = _a.enableVirtualMaskRow,
        visibleGroupedRowsCount = _a.visibleGroupedRowsCount,
        enableVirtualization = _a.enableVirtualization;
      // Cache row height
      var baseRowHeight = this.parent.getRowHeight();
      // Determine width for virtual track; guard if no visible columns
      var visibleColumns = (columns || []).filter(function (c) {
        return c && c.visible;
      });
      var width = enableColumnVirtualization && visibleColumns.length > 0 ? this.getColumnOffset(visibleColumns.length - 1) + 'px' : '100%';
      var virtualHeight = 0;
      var totalItemRowHeight = totalItemCount * baseRowHeight;
      var pageSizeRowHeight = pageSize * baseRowHeight;
      if (enableVirtualMaskRow) {
        if (frozenColumns > 0 && enableColumnVirtualization && groupCount === 0) {
          virtualHeight = totalItemRowHeight - 2 * pageSizeRowHeight;
        } else if (this.isDefaultGrouping()) {
          virtualHeight = visibleGroupedRowsCount * baseRowHeight;
        } else {
          virtualHeight = totalItemRowHeight;
        }
      } else if (groupCount) {
        virtualHeight = visibleGroupedRowsCount * this.parent.getRowHeight();
      } else if (enableVirtualization) {
        virtualHeight = frozenColumns > 0 && enableColumnVirtualization ? totalItemRowHeight - pageSizeRowHeight : totalItemRowHeight;
      }
      this.virtualEle.setVirtualHeight(virtualHeight, width);
      if (enableColumnVirtualization) {
        this.header.virtualEle.setVirtualHeight(1, width);
      }
    };
    /**
     * Calculates the virtual page number based on the scroll position from the top.
     * Uses binary search-like logic through offset keys to determine which page corresponds
     * to the current scroll position, accounting for grouped and non-grouped scenarios.
     *
     * @private
     * @param {number} sTop - The scroll top position in pixels
     * @param {VirtualInfo} info - Virtual information object to be updated with block index
     * @returns {number} The calculated page number (1-indexed, clamped between 1 and maxPage)
     */
    VirtualContentRenderer.prototype.getPageFromTop = function (sTop, info) {
      var _this_1 = this;
      // Determine total blocks based on grouping state
      var isGroupedAdaptive = isGroupAdaptive(this.parent);
      var totalBlocks = isGroupedAdaptive ? this.getGroupedTotalBlocks() : this.getTotalBlocks();
      // Early return for edge cases
      if (!this.offsetKeys || this.offsetKeys.length === 0 || sTop < 0) {
        info.block = 0;
        return 1;
      }
      // Find matching offset block
      var calculatedPage = 0;
      var matchFound = this.offsetKeys.some(function (offsetKey) {
        // Parse offset key to get block index
        var blockIndex = Number(offsetKey);
        var blockOffset = _this_1.offsets[parseInt(offsetKey, 10)];
        // Determine if scroll position matches this block
        var isWithinBlock = sTop <= blockOffset;
        var isLastBlockExceeded = blockIndex === totalBlocks && sTop > blockOffset;
        var isMatchingBlock = isWithinBlock || isLastBlockExceeded;
        if (isMatchingBlock) {
          // Set block type: even blocks = 1, odd blocks = 0
          info.block = blockIndex % 2 === 0 ? 1 : 0;
          // Calculate and clamp page number to valid range [1, maxPage]
          var rawPage = _this_1.vHelper.getPage(blockIndex);
          calculatedPage = Math.max(1, Math.min(rawPage, _this_1.maxPage));
        }
        return isMatchingBlock;
      });
      // Fallback: if no match found, return first page
      return matchFound ? calculatedPage : 1;
    };
    /**
     * Calculates the Y-axis translation value for virtual scrolling based on scroll position and viewport height.
     * This method determines the optimal vertical offset to render content blocks efficiently during scrolling.
     *
     * @protected
     * @param {number} sTop - The scroll top position in pixels
     * @param {number} cHeight - The content/viewport height in pixels
     * @param {VirtualInfo} [info] - Optional virtual information object containing page and block indexes
     * @param {boolean} [isOnenter] - Optional flag indicating if called from onEntered sentinel callback
     * @returns {number} The calculated Y-axis translation value in pixels for virtual content positioning
     */
    VirtualContentRenderer.prototype.getTranslateY = function (sTop, cHeight, info, isOnenter) {
      if (info === undefined) {
        info = {
          page: this.getPageFromTop(sTop + cHeight, {})
        };
        info.blockIndexes = this.vHelper.getBlockIndexes(info.page);
      }
      var block = (info.blockIndexes[0] || 1) - 1;
      var translate = this.getOffset(block);
      var endTranslate = this.getOffset(info.blockIndexes[info.blockIndexes.length - 1]);
      if (isOnenter) {
        info = this.prevInfo;
      }
      var result = translate > sTop ? this.getOffset(block - 1) : endTranslate < sTop + cHeight ? this.getOffset(block + 1) : translate;
      var blockHeight = this.offsets[info.blockIndexes[info.blockIndexes.length - 1]] - this.tmpOffsets[info.blockIndexes[0]];
      if (result + blockHeight > this.offsets[isGroupAdaptive(this.parent) ? this.getGroupedTotalBlocks() : this.getTotalBlocks()] && this.parent.options.groupCount === 0) {
        result -= result + blockHeight - this.offsets[this.getTotalBlocks()];
      }
      return result;
    };
    VirtualContentRenderer.prototype.getOffset = function (block) {
      return Math.min(this.offsets[parseInt(block.toString(), 10)] | 0, this.offsets[this.maxBlock] | 0);
    };
    VirtualContentRenderer.prototype.onEntered = function () {
      var _this_1 = this;
      return function (current, isLightScroll, direction, e, isWheel, check) {
        // eslint-disable-next-line @typescript-eslint/no-this-alias
        var _this = _this_1;
        _this_1.observer.options.isWheelScroll = isWheel;
        if (sf.base.Browser.isIE && !isWheel && check && !_this_1.preventEvent) ;
        // --- SECTION 1: Extract and cache parent options ---
        var _a = _this_1.parent.options,
          enableRtl = _a.enableRtl,
          enableColumnVirtualization = _a.enableColumnVirtualization,
          frozenColumns = _a.frozenColumns,
          enableVirtualMaskRow = _a.enableVirtualMaskRow,
          pageSize = _a.pageSize,
          rowHeight = _a.rowHeight,
          overscanCount = _a.overscanCount,
          totalItemCount = _a.totalItemCount,
          allowGrouping = _a.allowGrouping,
          groupCount = _a.groupCount,
          height = _a.height;
        // --- SECTION 2: Determine scroll axis and calculate base offsets ---
        var isHorizontalScroll = current.axis === 'X';
        var previousScrollTop = _this_1.prevInfo.offsets ? _this_1.prevInfo.offsets.top : null;
        var contentHeight = _this_1.content.getBoundingClientRect().height;
        // Calculate horizontal offset based on axis and column indexes
        var columnIndexForOffset = isHorizontalScroll ? _this_1.vHelper.getColumnIndexes()[0] - 1 : _this_1.prevInfo.columnIndexes[0] - 1;
        var horizontalTranslate = _this_1.getColumnOffset(columnIndexForOffset);
        // Apply RTL adjustment if needed
        if (enableColumnVirtualization && enableRtl) {
          horizontalTranslate = -1 * horizontalTranslate;
        }
        // Calculate vertical offset using translateY method
        var usesPreviousInfo = isHorizontalScroll && previousScrollTop === e.top;
        var verticalTranslate = _this_1.getTranslateY(e.top, contentHeight, usesPreviousInfo ? _this_1.prevInfo : undefined, true);
        // Adjust Y for horizontal scroll with current info
        if (_this_1.currentInfo && _this_1.currentInfo.startIndex && isHorizontalScroll) {
          var currentEndIndex = sf.base.isNullOrUndefined(_this_1.currentInfo.endIndex) ? 0 : _this_1.currentInfo.endIndex;
          verticalTranslate = (currentEndIndex - pageSize) * _this_1.parent.getRowHeight();
        }
        // --- SECTION 3: Calculate movable translate values ---
        // Determine X translation for movable content
        var hasColumnOffset = Boolean(_this_1.vHelper.cOffsets[_this_1.startColIndex - 1]);
        _this_1.movableTranslateX = hasColumnOffset ? _this_1.vHelper.cOffsets[_this_1.startColIndex - 1] : horizontalTranslate;
        // Apply RTL adjustment to movable X
        if (enableColumnVirtualization && enableRtl) {
          _this_1.movableTranslateX = -1 * _this_1.movableTranslateX;
        }
        // Override with frozen column offset if applicable
        var hasFrozenColumnOffset = enableColumnVirtualization && frozenColumns > 0 && Boolean(_this_1.vHelper.mOffsets[_this_1.startColIndex - 1]);
        if (hasFrozenColumnOffset) {
          _this_1.movableTranslateX = _this_1.vHelper.mOffsets[_this_1.startColIndex - 1];
        }
        // Calculate Y translation for movable content
        var effectiveRowHeight = rowHeight > 0 ? rowHeight : _this_1.parent.getRowHeight();
        _this_1.movableTranslateY = pageSize * effectiveRowHeight;
        // --- SECTION 4: Handle non-virtual-mask-row scenarios ---
        if (!enableVirtualMaskRow) {
          // Only process when overscan is disabled
          if (overscanCount === 0) {
            // Handle end-of-data downward scroll
            var isAtEndScrollingDown = _this_1.currentInfo.endIndex === totalItemCount && direction === 'down';
            if (isAtEndScrollingDown) {
              // Recalculate verticalTranslate based on start index
              verticalTranslate = _this_1.currentInfo.startIndex * effectiveRowHeight;
              // Cache options to avoid repeated property access and improve readability
              var _b = _this_1.parent.options,
                requestType = _b.requestType,
                isAdd = _b.isAdd,
                newRowPosition = _b.newRowPosition,
                editMode = _b.editMode;
              var isBottomSaveAdd = (requestType === 'Save' || isAdd) && newRowPosition === 'Bottom' && editMode === 'Normal';
              // Prevent scroll jump for bottom add
              if (isBottomSaveAdd) {
                verticalTranslate = _this_1.preventScrollJump(verticalTranslate);
              }
              // Apply table adjustment with bounds checking
              var maxVerticalOffset = _this_1.offsets[_this_1.maxBlock];
              var finalYTranslate = isBottomSaveAdd ? verticalTranslate : Math.min(verticalTranslate, maxVerticalOffset);
              _this_1.virtualEle.adjustTable(_this_1.movableTranslateX, finalYTranslate);
            }
            // Handle upward scroll or vertical axis with no horizontal prevention
            else {
              var shouldAdjustTable = (!isHorizontalScroll || direction === 'up') && _this_1.observer.PreventAdjustTable !== 'horizontal';
              if (shouldAdjustTable) {
                var maxVerticalOffset = _this_1.offsets[_this_1.maxBlock];
                _this_1.virtualEle.adjustTable(_this_1.movableTranslateX, Math.min(verticalTranslate, maxVerticalOffset));
              }
            }
          }
        }
        // --- SECTION 5: Handle virtual-mask-row scenarios ---
        else {
          // Determine total item count based on grouping
          var isNotAtEnd = false;
          var isDefaultGrouping = _this_1.isDefaultGrouping();
          var totalCount = isDefaultGrouping ? _this_1.getVisibleGroupedRowCount() : totalItemCount;
          // Check if verticalTranslate is within bounds and not at end
          var isWithinBounds = _this_1.offsets[_this_1.maxBlock] >= verticalTranslate;
          //Overscan with normal grouping scenario
          if (_this_1.parent.options.overscanCount > 0 && isDefaultGrouping) {
            var calculatedEndIndex = _this_1.currentInfo.endIndex + _this_1.parent.options.overscanCount;
            isNotAtEnd = calculatedEndIndex < _this_1.getVisibleGroupedRowCount();
          } else {
            isNotAtEnd = _this_1.currentInfo.endIndex !== totalCount;
          }
          if (isWithinBounds && isNotAtEnd) {
            var isHorizontalDirection = direction === 'right' || direction === 'left';
            if (isHorizontalDirection) {
              var previousColumnIndexes = _this_1.parent.getColumnIndexesInView();
              var currentViewInfo = _this_1.getInfoFromView(direction, current, e);
              // Check if column indexes have changed
              var columnIndexesChanged = JSON.stringify(previousColumnIndexes) !== JSON.stringify(currentViewInfo.columnIndexes);
              if (columnIndexesChanged) {
                // Adjust table for horizontal scroll with mask row offset
                var maskRowYOffset = _this_1.translateMaskY + _this_1.movableTranslateY;
                _this_1.virtualEle.adjustTable(_this_1.movableTranslateX, maskRowYOffset);
              }
            } else {
              // Handle vertical scrolling (up/down)
              var blockDifference_1 = enableColumnVirtualization ? enableVirtualMaskRow ? 4 : 2 : 0;
              setTimeout(function () {
                var downwardScrollY = verticalTranslate;
                var gridRowHeight = _this.parent.options.rowHeight !== 0 ? _this.parent.options.rowHeight : _this.parent.getRowHeight(true);
                var rowsInDOM = 0;
                // Handle overscan-disabled scenario
                if (enableVirtualMaskRow && overscanCount === 0) {
                  // Calculate Y for downward scroll based on scroll position
                  downwardScrollY = _this.content.scrollTop - 2 * pageSize * gridRowHeight;
                  // Get actual row count in DOM
                  rowsInDOM = allowGrouping && groupCount > 0 ? _this.parent.getContent().querySelectorAll('tr').length : _this.parent.element.querySelectorAll('.e-row').length;
                }
                // Handle overscan-enabled scenario
                else {
                  var actualStartIndex = _this.currentInfo.startIndex;
                  var viewportFirstDatarow = _this.content.querySelectorAll('.e-row:not(.e-masked-row)')[0];
                  var viewportStartIndex = viewportFirstDatarow ? parseInt(viewportFirstDatarow.getAttribute('aria-rowindex'), 10) : 0;
                  var calculatedOverscan = actualStartIndex - viewportStartIndex;
                  // Adjust overscan for non-percentage heights
                  if (height !== '100%') {
                    var isInvalidOverscan = isNaN(calculatedOverscan) || calculatedOverscan < 0;
                    calculatedOverscan = isInvalidOverscan ? 2 * calculatedOverscan : calculatedOverscan;
                  }
                  // Calculate Y for downward scroll with overscan
                  downwardScrollY = _this.content.scrollTop - 2 * (pageSize + calculatedOverscan) * gridRowHeight;
                  rowsInDOM = _this.parent.element.querySelectorAll('.e-row').length;
                }
                // Prevent scroll jump for both directions
                //sets downwardScrollY when we scroll downwards.
                downwardScrollY = _this.preventScrollJump(downwardScrollY, rowsInDOM);
                //sets verticalTranslate when we scroll upwards.
                verticalTranslate = _this.preventScrollJump(verticalTranslate, rowsInDOM);
                //If isLightScroll is true, then we update _this.translateMaskY which is the translate set in the onDataReady based on the previous data rendering.
                // Determine minimum Y value based on scroll type and direction
                var baseYForTranslate = isLightScroll ? _this.translateMaskY : direction === 'down' ? downwardScrollY : verticalTranslate;
                // Calculate final translate Y with bounds checking
                var finalTranslateY = Math.min(baseYForTranslate, _this.offsets[_this.maxBlock - blockDifference_1]);
                // Handle special case: upward scroll to top
                var isUpwardScrollToTop = direction === 'up' && finalTranslateY === 0 && verticalTranslate === 0;
                var hasNoStartIndex = sf.base.isNullOrUndefined(_this.prevInfo.startIndex) && sf.base.isNullOrUndefined(_this.currentInfo.startIndex);
                var hasBothZeroStartIndex = _this.prevInfo.startIndex === 0 && _this.currentInfo.startIndex === 0;
                var needsTopAdjustment = isUpwardScrollToTop && (hasNoStartIndex || hasBothZeroStartIndex || _this.parent.isMacOS());
                if (needsTopAdjustment) {
                  var startIndex = _this.currentInfo.startIndex === 0 || sf.base.isNullOrUndefined(_this.currentInfo.startIndex) ? 0 : _this.currentInfo.startIndex;
                  finalTranslateY = startIndex - pageSize * gridRowHeight;
                }
                // Apply final table adjustment
                _this.virtualEle.adjustTable(horizontalTranslate, finalTranslateY);
              }, 0);
            }
          }
        }
        // --- SECTION 6  : Handle horizontal axis refresh ---
        if (isHorizontalScroll) {
          _this_1.setColVTableWidthAndTranslate({
            refresh: true,
            axis: 'X',
            direction: direction
          });
        }
      };
    };
    VirtualContentRenderer.prototype.preventScrollJump = function (translate, rowsInDOM) {
      rowsInDOM = sf.base.isNullOrUndefined(rowsInDOM) ? this.parent.element.querySelectorAll('.e-row').length : rowsInDOM;
      var _a = this.parent.options,
        rowHeight = _a.rowHeight,
        groupCount = _a.groupCount,
        allowGrouping = _a.allowGrouping,
        totalItemCount = _a.totalItemCount,
        enableVirtualMaskRow = _a.enableVirtualMaskRow;
      var gridRowHeight = rowHeight || this.parent.getRowHeight(true);
      var totalCount = allowGrouping && groupCount > 0 && enableVirtualMaskRow ? this.getVisibleGroupedRowCount() : totalItemCount;
      var virtualTableHeight = totalCount * gridRowHeight;
      //The below condition will return true when the scrollHeight for the grid content elements increase above the virtual table height.
      //If it increases, then we will face the scroll jump issue. To avoid the scroll jump issue, we are preventing the scrollheight
      //to be less than or equal to the virtual table height by using below calculation.
      if (translate + rowsInDOM * gridRowHeight > virtualTableHeight) {
        translate = virtualTableHeight - rowsInDOM * gridRowHeight;
      }
      return translate;
    };
    VirtualContentRenderer.prototype.getBlockSize = function () {
      return this.parent.options.pageSize >> 1;
    };
    VirtualContentRenderer.prototype.getBlockHeight = function () {
      return this.getBlockSize() * this.parent.getRowHeight();
    };
    VirtualContentRenderer.prototype.getGroupedTotalBlocks = function () {
      if (this.parent.options.enableLazyLoading && this.parent.options.enableVirtualization) {
        return this.getTotalBlocks();
      }
      var visibleGroupedRowCount = this.getVisibleGroupedRowCount();
      // Get the block size (number of rows per block)
      var blockSize = this.getBlockSize();
      // Guard against division by zero
      if (blockSize === 0) {
        return 1;
      }
      var calculatedBlocks = visibleGroupedRowCount / blockSize;
      var isLessThanOneBlock = calculatedBlocks < 1;
      var totalBlocks = isLessThanOneBlock ? 1 : Math.floor(calculatedBlocks);
      return totalBlocks;
    };
    VirtualContentRenderer.prototype.getVisibleGroupedRowCount = function () {
      var placeHolderHeight = this.virtualEle.placeholder.style.height;
      var virtualTrackHeight = placeHolderHeight.substring(0, placeHolderHeight.indexOf('p'));
      var visibleRowCount = Number(virtualTrackHeight) / this.parent.getRowHeight();
      return Math.round(visibleRowCount);
    };
    VirtualContentRenderer.prototype.getTotalBlocks = function () {
      return Math.ceil(!sf.base.isNullOrUndefined(this.count) ? this.count / this.getBlockSize() : 0);
    };
    VirtualContentRenderer.prototype.getColumnOffset = function (block) {
      var _a = this.vHelper,
        mOffsets = _a.mOffsets,
        cOffsets = _a.cOffsets;
      if (sf.base.isNullOrUndefined(block)) {
        return 0;
      }
      if (this.parent.options.frozenColumns > 0) {
        return block > Object.keys(mOffsets).length - 1 ? cOffsets[parseInt(block.toString(), 10)] | 0 : mOffsets[parseInt(block.toString(), 10)] | 0;
      }
      return cOffsets[parseInt(block.toString(), 10)] | 0;
    };
    VirtualContentRenderer.prototype.resetScrollPosition = function (action) {
      var lowercaseAction = !sf.base.isNullOrUndefined(action) ? action.toLowerCase() : action;
      var isActionInList = this.actions.some(function (value) {
        return value === action;
      });
      var hasInitialGrouping = !sf.base.isNullOrUndefined(this.parent.options.initGroupingField) && this.parent.options.initGroupingField.length > 0;
      var isGroupingRelatedAction = !sf.base.isNullOrUndefined(lowercaseAction) && (lowercaseAction === 'sorting' || lowercaseAction === 'save' || lowercaseAction === 'delete');
      var requiresGroupingReset = hasInitialGrouping && isGroupingRelatedAction;
      // Determine if scroll should be reset
      var shouldResetScroll = isActionInList || requiresGroupingReset;
      if (shouldResetScroll) {
        var contentElement = this.content;
        this.preventEvent = contentElement.scrollTop !== 0;
        contentElement.scrollTop = 0;
        this.observer.options.previousTop = 0;
        this.currentInfo.startIndex = this.currentInfo.startIndex !== 0 ? 0 : this.currentInfo.startIndex;
        this.currentInfo.endIndex = this.currentInfo.startIndex === 0 ? this.currentInfo.startIndex + this.parent.options.pageSize : this.currentInfo.endIndex;
      }
    };
    /**
     * Refresh offsets for some functionality.
     *
     * @returns {void}
     */
    VirtualContentRenderer.prototype.refreshOffsets = function () {
      //const gObj: SfGrid = this.parent;
      var row = 0;
      var blockSize = this.getBlockSize();
      var totalBlocks = isGroupAdaptive(this.parent) ? this.getGroupedTotalBlocks() : this.getTotalBlocks();
      this.maxBlock = totalBlocks % 2 === 0 ? totalBlocks - 2 : totalBlocks - 1;
      this.offsets = {};
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      var vcRows = [];
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      var cache = {};
      //Row offset update
      var gridRowHeight = this.parent.getRowHeight();
      // --- SECTION 2: Calculate row offsets for each virtual block ---
      var blocks = Array(totalBlocks).fill(null).map(function () {
        return ++row;
      });
      for (var i = 0; i < blocks.length; i++) {
        var tmp = (cache[blocks[parseInt(i.toString(), 10)]] || []).length;
        var isGroupAdaptiveGrid = isGroupAdaptive(this.parent);
        var rem = !isGroupAdaptiveGrid ? this.count % blockSize : vcRows.length % blockSize;
        var size = !isGroupAdaptiveGrid && blocks[parseInt(i.toString(), 10)] in cache ? tmp * gridRowHeight : rem && blocks[parseInt(i.toString(), 10)] === totalBlocks ? rem * gridRowHeight : this.getBlockHeight();
        this.offsets[parseInt(blocks[parseInt(i.toString(), 10)].toString(), 10)] = (this.offsets[parseInt((blocks[parseInt(i.toString(), 10)] - 1).toString(), 10)] | 0) + size;
        this.tmpOffsets[parseInt(blocks[parseInt(i.toString(), 10)].toString(), 10)] = this.offsets[parseInt((blocks[parseInt(i.toString(), 10)] - 1).toString(), 10)] | 0;
      }
      this.offsetKeys = Object.keys(this.offsets);
      //Column offset update
      if (this.parent.options.enableColumnVirtualization) {
        this.vHelper.refreshColOffsets();
      }
    };
    VirtualContentRenderer.prototype.updateTransform = function (x, y, isOverscan, isBottomAdd) {
      // eslint-disable-next-line @typescript-eslint/no-this-alias
      var _this = this;
      // --- SECTION 1: Cache grid options to avoid repeated property access ---
      var _a = _this.parent.options,
        rowHeight = _a.rowHeight,
        enableVirtualMaskRow = _a.enableVirtualMaskRow,
        enableLazyLoading = _a.enableLazyLoading,
        requestType = _a.requestType,
        pageSize = _a.pageSize,
        overscanCount = _a.overscanCount,
        enableVirtualization = _a.enableVirtualization,
        totalItemCount = _a.totalItemCount,
        editMode = _a.editMode,
        newRowPosition = _a.newRowPosition;
      var gridRowHeight = rowHeight || this.parent.getRowHeight();
      // --- SECTION 2: Handle regular virtual scrolling (non-bottom-add scenario) ---
      if (!isBottomAdd) {
        var verticalTranslation_1 = 0;
        // Check if virtual mask row mode is active
        var hasVirtualMaskOrLazyLoad = enableVirtualMaskRow && enableVirtualization || enableLazyLoading;
        var isNotGroupExpandCollapse = requestType !== 'GroupExpandCollapse';
        var shouldCalculateMaskTranslation = hasVirtualMaskOrLazyLoad && isNotGroupExpandCollapse;
        if (shouldCalculateMaskTranslation) {
          // Calculate base start index for current viewport
          var startIndex_1 = _this.currentInfo.endIndex - pageSize;
          startIndex_1 = startIndex_1 > 0 ? startIndex_1 : 0;
          // Calculate base vertical translation
          verticalTranslation_1 = startIndex_1 * gridRowHeight;
          // Guard against NaN values
          verticalTranslation_1 = isNaN(verticalTranslation_1) ? 0 : verticalTranslation_1;
          // Calculate overscan offset if applicable
          var overscanRowCount = 0;
          if (overscanCount > 0) {
            var actualStartIndex = _this.currentInfo.startIndex || 0;
            var viewportFirstDatarow = _this.content.querySelectorAll('.e-row:not(.e-masked-row)')[0];
            var viewportStartIndex = viewportFirstDatarow ? parseInt(viewportFirstDatarow.getAttribute('aria-rowindex'), 10) - 1 : 0;
            overscanRowCount = actualStartIndex - viewportStartIndex;
          }
          // Adjust translation to account for page size and overscan
          var totalRowOffset = pageSize + overscanRowCount;
          verticalTranslation_1 = verticalTranslation_1 - totalRowOffset * gridRowHeight;
        }
        setTimeout(function () {
          _this.translateMaskX = x;
          if (isOverscan) {
            _this.translateMaskY = _this.preventScrollJump(verticalTranslation_1);
          }
          _this.virtualEle.adjustTable(x, _this.translateMaskY);
        }, 500);
        return;
      }
      // --- SECTION 3: Handle bottom row addition scenario ---
      // Calculate effective page size including overscan
      var effectivePageSize = overscanCount ? pageSize + overscanCount * 2 : pageSize;
      // Clamp page size to total item count
      effectivePageSize = totalItemCount < effectivePageSize ? totalItemCount : effectivePageSize;
      // Update current info to reflect bottom position
      _this.currentInfo.endIndex = totalItemCount;
      var startIndex = _this.currentInfo.startIndex = _this.currentInfo.endIndex - effectivePageSize;
      var verticalTranslation = startIndex * gridRowHeight;
      if (enableVirtualMaskRow) {
        verticalTranslation = verticalTranslation - pageSize * gridRowHeight;
        _this.preventScrollJump(verticalTranslation);
      }
      var finalTranslation = startIndex > 0 ? _this.preventScrollJump(verticalTranslation) : verticalTranslation;
      this.virtualEle.wrapper.style.transform = "translate(0px, ".concat(finalTranslation, "px)");
      // Handle special case: Normal edit mode with bottom row save
      var isNormalEditBottomSave = editMode === 'Normal' && requestType === 'Save' && newRowPosition === 'Bottom';
      if (isNormalEditBottomSave) {
        this.parent.getContent().scrollTop += gridRowHeight;
      }
    };
    VirtualContentRenderer.prototype.refreshColumnIndexes = function () {
      this.vHelper.refreshColOffsets();
      var colIndexes = this.vHelper.getColumnIndexes();
      this.parent.setColumnIndexesInView(colIndexes);
      this.parent.dotNetRef.invokeMethodAsync('SetColumnIndexes', colIndexes[0], colIndexes[colIndexes.length - 1]);
    };
    VirtualContentRenderer.prototype.refreshVirtualElement = function () {
      this.vHelper.refreshColOffsets();
      this.setVirtualHeight();
    };
    VirtualContentRenderer.prototype.isDefaultGrouping = function () {
      var _a = this.parent.options,
        allowGrouping = _a.allowGrouping,
        enableLazyLoading = _a.enableLazyLoading,
        groupCount = _a.groupCount;
      return allowGrouping && !enableLazyLoading && groupCount > 0;
    };
    return VirtualContentRenderer;
  }();
  /**
   * @hidden
   */
  var VirtualHeaderRenderer = /** @class */function () {
    function VirtualHeaderRenderer(parent) {
      this.parent = parent;
      this.vHelper = new VirtualHelper(this.parent);
      this.virtualEle = new VirtualElementHandler(this.parent);
      this.headerPanel = this.parent.element.querySelector('.e-gridheader');
    }
    /**
     * Get the header content div element of grid
     *
     * @returns {void}
     */
    VirtualHeaderRenderer.prototype.getPanel = function () {
      return this.headerPanel;
    };
    /**
     * Get the header table element of grid
     *
     * @returns {void}
     */
    VirtualHeaderRenderer.prototype.getTable = function () {
      return this.headerPanel.querySelector('.e-table');
    };
    VirtualHeaderRenderer.prototype.renderTable = function () {
      this.vHelper.refreshColOffsets();
      this.parent.setColumnIndexesInView(this.vHelper.getColumnIndexes(this.getPanel().querySelector('.e-headercontent')));
      this.virtualEle.table = this.getTable();
      this.virtualEle.content = this.getPanel().querySelector('.e-headercontent');
      this.virtualEle.content.style.position = 'relative';
      this.virtualEle.renderWrapper();
      this.virtualEle.renderPlaceHolder();
    };
    return VirtualHeaderRenderer;
  }();
  /**
   * @hidden
   */
  var VirtualElementHandler = /** @class */function () {
    function VirtualElementHandler(parent) {
      this.parent = parent;
    }
    VirtualElementHandler.prototype.renderWrapper = function (height) {
      this.wrapper = this.content.querySelector('.e-virtualtable');
      this.wrapper.setAttribute('styles', "min-height:".concat(sf.base.formatUnit(height)));
      var gridContent = this.content.querySelector('.e-gridcontent');
      if (!sf.base.isNullOrUndefined(gridContent)) {
        this.movableHeaderWrapper = this.content.querySelector('.e-virtualtable');
        this.movableContentWrapper = this.content.querySelector('.e-virtualtable');
      }
    };
    VirtualElementHandler.prototype.renderPlaceHolder = function () {
      var contentLastChild = this.content.lastElementChild;
      this.placeholder = this.content && contentLastChild instanceof HTMLElement && contentLastChild.classList.contains('e-virtualtrack') ? contentLastChild : this.content ? this.content.querySelector('.e-virtualtrack') : null;
    };
    VirtualElementHandler.prototype.adjustTable = function (xValue, yValue, direction) {
      var _a = this.parent.options,
        enableVirtualization = _a.enableVirtualization,
        enableColumnVirtualization = _a.enableColumnVirtualization,
        frozenColumns = _a.frozenColumns;
      this.filterTranslateX = xValue;
      if (enableColumnVirtualization && !enableVirtualization && yValue > 0) {
        yValue = 0;
      }
      if (sf.base.isNullOrUndefined(this.content)) {
        return;
      }
      var frozenCellSelector = this.content.querySelectorAll('.e-leftfreeze,.e-rightfreeze,.e-fixedfreeze');
      var cells = [].slice.call(frozenCellSelector);
      var frzLeftWidth = 0;
      var frzRightWidth = 0;
      // Calculate frozen column widths if Fixed freeze columns exist
      var headerContent = this.parent.getHeaderContent();
      var hasFixedFreezeColumns = !sf.base.isNullOrUndefined(headerContent) ? headerContent.querySelectorAll('.e-fixedfreeze').length > 0 : false;
      if (hasFixedFreezeColumns) {
        frzLeftWidth = this.parent.leftrightColumnWidth('left');
        frzRightWidth = this.parent.leftrightColumnWidth('right');
      }
      if (cells.length > 0) {
        for (var i = 0; i < cells.length; i++) {
          var cell = cells[parseInt(i.toString(), 10)];
          var col = null;
          // Determine column object based on cell type
          var isRowCell = cell.classList.contains('e-rowcell');
          var isHeaderCell = cell.classList.contains('e-headercell');
          var isFilterBarCell = cell.classList.contains('e-filterbarcell');
          if (isRowCell) {
            // Handle row cell - get column by UID or index
            var mappingUidElement = cell.querySelector('[e-mappinguid]');
            var hasAriaColIndex = !sf.base.isNullOrUndefined(parseInt(cell.getAttribute('aria-colindex'), 10) - 1);
            var hasMappingUid = !sf.base.isNullOrUndefined(mappingUidElement);
            if (hasAriaColIndex && hasMappingUid) {
              var uid = mappingUidElement.getAttribute('e-mappinguid');
              col = this.parent.getColumnByUid(uid);
            } else if (hasAriaColIndex) {
              var idx = parseInt(cell.getAttribute('aria-colindex'), 10) - 1;
              col = this.parent.getColumnByIndex(parseInt(idx.toString(), 10), true);
            }
          } else {
            if (isHeaderCell || isFilterBarCell) {
              var uid = null;
              if (isFilterBarCell) {
                uid = cell.getAttribute('e-mappinguid');
              } else if (isHeaderCell) {
                var mappingUidElement = cell.querySelector('[e-mappinguid]');
                if (!sf.base.isNullOrUndefined(mappingUidElement)) {
                  uid = mappingUidElement.getAttribute('e-mappinguid');
                }
              }
              if (!sf.base.isNullOrUndefined(uid)) {
                col = this.parent.getColumnByUid(uid);
              }
            }
          }
          if (!sf.base.isNullOrUndefined(col)) {
            var freezeType = col.freeze;
            if (freezeType === 'Left') {
              cell.style.left = col.translateLeftRightValue - xValue + 'px';
            } else if (freezeType === 'Right') {
              cell.style.right = col.translateLeftRightValue + xValue + 'px';
            } else if (freezeType === 'Fixed') {
              cell.style.left = frzLeftWidth - xValue + 'px';
              cell.style.right = frzRightWidth + xValue + 'px';
            }
          }
        }
      }
      var trackWidth = this.placeholder.offsetWidth;
      var contentOffsetWidth = this.parent.getContentTable().offsetWidth;
      var wouldExceedTrackWidth = contentOffsetWidth + xValue > trackWidth;
      if (direction === 'right' && wouldExceedTrackWidth && !frozenColumns) {
        //The above condition statisfies only if we set this xValue to the virtualtable, then the content scrollWidth will exceed
        //the actual trackWidth of the content element. So horizontal scroll jump issue will occur. To avoid this jump issue,
        //we are recalculating the xValue in below line.
        xValue = trackWidth - contentOffsetWidth;
      }
      yValue = enableVirtualization ? yValue : 0;
      this.wrapper.style.transform = 'translate(' + xValue + 'px, ' + yValue + 'px)';
    };
    VirtualElementHandler.prototype.extractTranslateY = function (transformStyle) {
      var match = transformStyle.match(/translate\([^,]+,([^)]+)\)/);
      if (match && match[1]) {
        return match[1].trim();
      }
      return '0';
    };
    VirtualElementHandler.prototype.setWrapperWidth = function (width, full) {
      this.wrapper.style.width = width ? "".concat(width, "px") : full ? '100%' : '';
    };
    VirtualElementHandler.prototype.setVirtualHeight = function (height, width) {
      if (this.parent.options.enableVirtualization) {
        this.placeholder.style.height = "".concat(height, "px");
      }
      if (!sf.base.isNullOrUndefined(this.content) && this.content.classList.contains('e-content') && !this.parent.options.enableVirtualization && this.parent.isMacSafariBrowser()) {
        this.placeholder.style.height = '1px';
      }
      if (sf.base.isNullOrUndefined(this.placeholder)) {
        return;
      }
      this.placeholder.style.width = width;
    };
    return VirtualElementHandler;
  }();
  /**
   * Content module is used to render grid content
   */
  var VirtualHelper = /** @class */function () {
    function VirtualHelper(parent) {
      this.cOffsets = {};
      this.mOffsets = {};
      this.data = {};
      this.groups = {};
      this.parent = parent;
    }
    VirtualHelper.prototype.getBlockIndexes = function (page) {
      return [page + (page - 1), page * 2];
    };
    VirtualHelper.prototype.getPage = function (block) {
      return block % 2 === 0 ? block / 2 : (block + 1) / 2;
    };
    VirtualHelper.prototype.getData = function () {
      return {
        page: this.parent.options.currentPage,
        blockIndexes: this.getBlockIndexes(this.parent.options.currentPage),
        direction: 'down',
        columnIndexes: this.parent.getColumnIndexesInView()
      };
    };
    VirtualHelper.prototype.getColumnIndexes = function (content) {
      var _this_1 = this;
      if (content === void 0) {
        content = this.parent.getHeaderContent();
      }
      if (!sf.base.isNullOrUndefined(this.parent.getContent())) {
        content = this.parent.getContent();
      }
      var indexes = [];
      var sLeft = this.parent.options.enableColumnVirtualization && this.parent.options.enableRtl ? -1 * content.scrollLeft | 0 : content.scrollLeft | 0;
      var keys = Object.keys(this.cOffsets);
      var cWidth = this.parent.options.needClientAction ? this.parent.options.frozenColumns && this.parent.options.enableColumnVirtualization ? this.parent.getHeaderContent().offsetWidth : content.getBoundingClientRect().width : parseInt(this.parent.options.width, 10);
      sLeft = Math.min(this.cOffsets[keys.length - 1] - cWidth, sLeft);
      var calWidth = sf.base.Browser.isDevice ? 2 * cWidth : cWidth / 2;
      var left = sLeft + cWidth + (sLeft === 0 ? calWidth : 0);
      keys.some(function (offset) {
        var iOffset = Number(offset);
        var offsetVal = _this_1.cOffsets["".concat(offset)];
        var border = sLeft - calWidth <= offsetVal && left + calWidth >= offsetVal;
        if (border) {
          indexes.push(iOffset);
        }
        return left + calWidth < offsetVal;
      });
      if (!indexes.length && !this.parent.options.frozenColumns) {
        if (keys.length > 0) {
          var firstColOffset = this.cOffsets["".concat(keys[0])];
          if (!sf.base.isNullOrUndefined(firstColOffset)) {
            left = left + firstColOffset;
            keys.some(function (offset) {
              var iOffset = Number(offset);
              var offsetVal = _this_1.cOffsets["".concat(offset)];
              var border = sLeft - calWidth <= offsetVal && left + calWidth >= offsetVal;
              if (border) {
                indexes.push(iOffset);
              }
              return left + calWidth < offsetVal;
            });
          }
        }
      }
      if (indexes.length === 1 && !this.parent.options.frozenColumns) {
        this.parent.virtualContentModule.startColIndex = indexes[0];
        this.parent.virtualContentModule.endColIndex = indexes[0] + 1;
        indexes.push(indexes[0] + 1);
      } else {
        this.parent.virtualContentModule.startColIndex = indexes[0];
        this.parent.virtualContentModule.endColIndex = indexes[indexes.length - 1];
      }
      return indexes;
    };
    VirtualHelper.prototype.refreshColOffsets = function () {
      var col = 0;
      this.cOffsets = {};
      this.mOffsets = {};
      var _a = this.parent.options,
        groupCount = _a.groupCount,
        columns = _a.columns,
        actualFrozenColumns = _a.actualFrozenColumns,
        frozenColumns = _a.frozenColumns,
        enableColumnVirtualization = _a.enableColumnVirtualization;
      var gLen = groupCount;
      var cols = columns;
      if (enableColumnVirtualization) {
        cols = cols.filter(function (e) {
          return e.visible;
        });
      }
      var cLen = cols.length;
      var blocks = Array(cLen).fill(null).map(function () {
        return col++;
      });
      for (var j = 0; j < blocks.length; j++) {
        this.cOffsets[blocks[parseInt(j.toString(), 10)]] = (this.cOffsets[blocks[parseInt(j.toString(), 10)] - 1] | 0) + (cols[parseInt(j.toString(), 10)].visible ? parseInt(cols[parseInt(j.toString(), 10)].width.toString(), 10) : 0);
      }
      if (frozenColumns) {
        var mCol_1 = 0;
        var mCols = void 0;
        if (!actualFrozenColumns) {
          var fCols_1 = cols.filter(function (col) {
            return col.isFrozen && (col.freeze === 'Left' || col.freeze === 'Right');
          });
          mCols = cols.filter(function (col) {
            return !fCols_1.some(function (fCol) {
              return fCol === col;
            });
          });
        } else {
          var fColsLen = 0;
          for (var i = 0; i < cols.length; i++) {
            if (!cols[parseInt(i.toString(), 10)].isFrozen && cols[parseInt(i.toString(), 10)].index < frozenColumns) {
              fColsLen++;
            } else {
              break;
            }
          }
          mCols = cols.slice(fColsLen, cLen);
        }
        var mColLen = mCols.length;
        var blocks_1 = Array(mColLen).fill(null).map(function () {
          return mCol_1++;
        });
        for (var j = 0; j < blocks_1.length; j++) {
          blocks_1[parseInt(j.toString(), 10)] = blocks_1[parseInt(j.toString(), 10)] + gLen;
          this.mOffsets[blocks_1[parseInt(j.toString(), 10)]] = (this.mOffsets[blocks_1[parseInt(j.toString(), 10)] - 1] | 0) + (mCols[parseInt(j.toString(), 10)].visible ? parseInt(mCols[parseInt(j.toString(), 10)].width, 10) : 0);
        }
      }
    };
    return VirtualHelper;
  }();
  var frozenDragClassList = {
    header: 'th.e-headercell',
    rowcell: '.e-rowcell',
    filterbarcell: '.e-filterbarcell',
    helper: 'e-frozen-helper',
    cursor: 'e-frozen-cursor',
    ariaColumnIndex: 'aria-colindex'
  };
  /**
   *
   * Frozen column drag and drop handling.
   *
   * @hidden
   */
  var FrozenDD = /** @class */function () {
    function FrozenDD(parent) {
      this.currentBorderIndex = -1;
      this.originalBorderIndex = -1;
      this.visibleLtoROrderedColumns = [];
      this.visibleRtoLOrderedColumns = [];
      this.widthCollection = [];
      this.visibleFrozenColumns = [];
      this.visibleFrozenLeftColumns = [];
      this.domRtoLOrderedColumns = [];
      this.domLtoROrderedColumns = [];
      this.parent = parent;
      if (this.parent.options.allowFreezeLineMoving) {
        this.wireEvents();
      }
    }
    FrozenDD.prototype.wireEvents = function () {
      sf.base.EventHandler.add(this.parent.getContent(), sf.base.Browser.touchStartEvent, this.dragStart, this);
      sf.base.EventHandler.add(this.parent.getHeaderContent(), sf.base.Browser.touchStartEvent, this.dragStart, this);
    };
    FrozenDD.prototype.unwireEvents = function () {
      sf.base.EventHandler.remove(this.parent.getContent(), sf.base.Browser.touchStartEvent, this.dragStart);
      sf.base.EventHandler.remove(this.parent.getHeaderContent(), sf.base.Browser.touchStartEvent, this.dragStart);
    };
    FrozenDD.prototype.dragStart = function (e) {
      if (e.target.classList.contains(frozenDragClassList.cursor)) {
        this.currentCursorElement = e.target;
        this.parent.getContent().classList.add('e-freezeline-moving');
        this.isLeftCursorDragging = (this.currentCursorElement.classList.contains('e-frozen-left-cursor') || this.currentCursorElement.classList.contains('e-frozen-fixedleft-cursor') || this.currentCursorElement.classList.contains('e-frozen-fixedright-cursor')) && !this.currentCursorElement.classList.contains('e-frozen-default-cursor') || this.currentCursorElement.classList.contains('e-frozen-default-cursor') && this.currentCursorElement.classList.contains('e-frozen-right-cursor') ? true : false;
        this.isLeftFixedCursorDragging = this.currentCursorElement.classList.contains('e-frozen-fixedleft-cursor') ? true : false;
        this.isRightFixedCursorDragging = this.currentCursorElement.classList.contains('e-frozen-fixedright-cursor') ? true : false;
        this.appendHelper();
        var rowcell = sf.base.closest(this.currentCursorElement, frozenDragClassList.header);
        var uid_1;
        if (sf.base.isNullOrUndefined(rowcell)) {
          rowcell = sf.base.closest(this.currentCursorElement, frozenDragClassList.rowcell);
          if (sf.base.isNullOrUndefined(rowcell)) {
            uid_1 = sf.base.closest(this.currentCursorElement, frozenDragClassList.filterbarcell).getAttribute('e-mappinguid');
          } else {
            var colIndex = rowcell.getAttribute(frozenDragClassList.ariaColumnIndex);
            var headerElement = this.parent.getHeaderContent().querySelector('[' + frozenDragClassList.ariaColumnIndex + '="' + colIndex + '"]');
            var mappingElement = !sf.base.isNullOrUndefined(headerElement) ? headerElement.querySelector('[e-mappinguid]') : null;
            uid_1 = !sf.base.isNullOrUndefined(mappingElement) ? mappingElement.getAttribute('e-mappinguid') : '';
          }
        } else {
          var mappingElement = rowcell.querySelector('[e-mappinguid]');
          uid_1 = !sf.base.isNullOrUndefined(mappingElement) ? mappingElement.getAttribute('e-mappinguid') : '';
        }
        var startIndex = this.parent.getColumns().filter(function (col) {
          return col.uid === uid_1;
        })[0].index;
        this.setColumnsForBorderUpdate();
        sf.base.EventHandler.add(document, sf.base.Browser.touchEndEvent, this.dragEnd, this);
        this.parent.dotNetRef.invokeMethodAsync('InvokeFreezeLineMoving', {
          fromIndex: startIndex
        });
      }
    };
    FrozenDD.prototype.appendHelper = function () {
      this.helperElement = sf.base.createElement('div', {
        className: frozenDragClassList.helper
      });
      this.setHelperHeight();
      this.parent.element.appendChild(this.helperElement);
    };
    FrozenDD.prototype.setHelperHeight = function () {
      var height = this.parent.getContent().offsetHeight;
      var headercell = sf.base.closest(this.currentCursorElement, frozenDragClassList.header);
      if (sf.base.isNullOrUndefined(headercell)) {
        var rowcell = sf.base.closest(this.currentCursorElement, frozenDragClassList.rowcell);
        if (sf.base.isNullOrUndefined(rowcell)) {
          var filterbarcell = sf.base.closest(this.currentCursorElement, frozenDragClassList.filterbarcell);
          var uid = filterbarcell.getAttribute('e-mappinguid');
          var headerContent = this.parent.getHeaderContent();
          var mappingElement = headerContent.querySelector('[e-mappinguid=' + uid + ']');
          if (!sf.base.isNullOrUndefined(mappingElement) && !sf.base.isNullOrUndefined(mappingElement.parentElement)) {
            var parentHeaderElement = mappingElement.parentElement;
            headercell = parentHeaderElement.parentElement;
          }
        } else {
          var index = rowcell.getAttribute(frozenDragClassList.ariaColumnIndex);
          headercell = this.parent.getHeaderContent().querySelector('[' + frozenDragClassList.ariaColumnIndex + '="' + index + '"]');
        }
      }
      var tr = [].slice.call(this.parent.getHeaderContent().querySelectorAll('tr'));
      for (var i = tr.indexOf(headercell.parentElement); i < tr.length; i++) {
        height += tr[parseInt(i.toString(), 10)].offsetHeight;
      }
      if (this.parent.getContent().scrollWidth > this.parent.getContent().clientWidth) {
        height -= 16;
      }
      var pos = this.calcPos(headercell);
      var top = this.calcPos(this.parent.getHeaderContent()).top;
      if (!this.currentCursorElement.classList.contains('e-frozen-default-cursor')) {
        if (headercell.classList.contains('e-rightfreeze')) {
          pos.left += this.parent.options.enableRtl ? headercell.offsetWidth - 2 : 0 - 1;
        } else {
          pos.left += this.parent.options.enableRtl ? 0 - 1 : headercell.offsetWidth - 2;
        }
      } else {
        pos.left = this.isLeftCursorDragging ? pos.left : pos.left + headercell.offsetWidth - 2;
      }
      this.helperElement.style.cssText = 'height: ' + height + 'px; top: ' + top + 'px; left:' + Math.floor(pos.left) + 'px;';
    };
    FrozenDD.prototype.calcPos = function (elem) {
      var parentOffset = {
        top: 0,
        left: 0,
        right: 0
      };
      var offset = elem.getBoundingClientRect();
      var doc = elem.ownerDocument;
      var offsetParent = parentsUntil(elem, 'e-grid') || doc.documentElement;
      while (offsetParent && (offsetParent === doc.body || offsetParent === doc.documentElement) && offsetParent.style.position === 'static') {
        offsetParent = offsetParent.parentNode;
      }
      if (offsetParent && offsetParent !== elem && offsetParent.nodeType === 1) {
        parentOffset = offsetParent.getBoundingClientRect();
      }
      return {
        top: offset.top - parentOffset.top,
        left: offset.left - parentOffset.left,
        right: offset.right - parentOffset.right
      };
    };
    FrozenDD.prototype.preventFreezeLineMoving = function (isCancel) {
      if (isCancel) {
        this.cancelFreezeLineMoving();
      } else {
        sf.base.EventHandler.add(this.parent.element, sf.base.Browser.touchMoveEvent, this.freezeLineDragging, this);
      }
    };
    FrozenDD.prototype.cancelFreezeLineMoving = function () {
      this.unwireEvents();
      sf.base.detach(this.helperElement);
      this.setDefaultValue();
    };
    FrozenDD.prototype.setDefaultValue = function () {
      this.parent.getContent().classList.remove('e-freezeline-moving');
      this.addOrRemoveClasses('remove');
      this.helperElement = null;
      this.currentBorderIndex = -1;
      this.currentCursorElement = null;
      this.borderCells = null;
      this.widthCollection = [];
    };
    FrozenDD.prototype.freezeLineDragging = function (e) {
      if (!this.helperElement) {
        return;
      }
      this.scrollHorizontally();
      this.updateHelper(e);
      this.updateBorder();
    };
    FrozenDD.prototype.setColumnsForBorderUpdate = function () {
      var _this = this;
      var gridColumns = this.parent.getColumns();
      var frozenLeftColumns = gridColumns.filter(function (col) {
        return col.freeze === 'Left' && col.isFrozen;
      });
      this.visibleFrozenLeftColumns = frozenLeftColumns.filter(function (col) {
        return col.visible;
      });
      var frozenRightColumns = gridColumns.filter(function (col) {
        return col.freeze === 'Right' && col.isFrozen;
      });
      var visibleFrozenRightColumns = frozenRightColumns.filter(function (col) {
        return col.visible;
      });
      if (this.parent.options.actualFrozenColumns > 0) {
        //Only executes if the Grid's FrozenColumns property is set
        var frozenColumns = gridColumns.slice(0, this.parent.options.actualFrozenColumns);
        this.visibleFrozenColumns = frozenColumns.filter(function (col) {
          return col.visible;
        });
        var movableColumns = gridColumns.filter(function (col) {
          return col.index > _this.parent.options.actualFrozenColumns - 1 && (!col.isFrozen || col.isFrozen && col.freeze === 'Fixed');
        });
        var visibleMovableColumns = movableColumns.filter(function (col) {
          return col.visible;
        });
        if (this.isLeftCursorDragging) {
          this.visibleLtoROrderedColumns = [].concat(this.visibleFrozenColumns, this.visibleFrozenLeftColumns, visibleMovableColumns, visibleFrozenRightColumns);
          this.domLtoROrderedColumns = [].concat(frozenColumns, frozenLeftColumns, movableColumns, frozenRightColumns);
          this.widthCollection = this.pushColumnWidth(this.visibleLtoROrderedColumns, this.widthCollection);
        } else {
          this.visibleRtoLOrderedColumns = [].concat(visibleFrozenRightColumns.reverse(), visibleMovableColumns.reverse(), this.visibleFrozenLeftColumns.reverse(), this.visibleFrozenColumns.reverse());
          this.domRtoLOrderedColumns = [].concat(frozenRightColumns.reverse(), movableColumns.reverse(), frozenLeftColumns.reverse(), frozenColumns.reverse());
          this.widthCollection = this.pushColumnWidth(this.visibleRtoLOrderedColumns, this.widthCollection);
        }
      } else {
        var movableColumns = gridColumns.filter(function (col) {
          return !col.isFrozen || col.isFrozen && col.freeze === 'Fixed';
        });
        var visibleMovableColumns = movableColumns.filter(function (col) {
          return col.visible;
        });
        if (this.isLeftCursorDragging) {
          this.domLtoROrderedColumns = [].concat(frozenLeftColumns, movableColumns, frozenRightColumns);
          this.visibleLtoROrderedColumns = [].concat(this.visibleFrozenLeftColumns, visibleMovableColumns, visibleFrozenRightColumns);
          this.widthCollection = this.pushColumnWidth(this.visibleLtoROrderedColumns, this.widthCollection);
        } else {
          this.domRtoLOrderedColumns = [].concat(frozenRightColumns.reverse(), movableColumns.reverse(), frozenLeftColumns.reverse());
          this.visibleRtoLOrderedColumns = [].concat(visibleFrozenRightColumns.reverse(), visibleMovableColumns.reverse(), this.visibleFrozenLeftColumns.reverse());
          this.widthCollection = this.pushColumnWidth(this.visibleRtoLOrderedColumns, this.widthCollection);
        }
      }
    };
    FrozenDD.prototype.updateBorder = function () {
      var index;
      if (this.currentBorderIndex === -1) {
        if (!sf.base.isNullOrUndefined(parentsUntil(this.currentCursorElement, 'e-rowcell'))) {
          this.currentBorderIndex = Number(parentsUntil(this.currentCursorElement, 'e-rowcell').getAttribute(frozenDragClassList.ariaColumnIndex));
        } else if (!sf.base.isNullOrUndefined(parentsUntil(this.currentCursorElement, 'e-filterbarcell'))) {
          var uid = parentsUntil(this.currentCursorElement, 'e-filterbarcell').getAttribute('e-mappinguid');
          var headercell = this.parent.getHeaderContent().querySelectorAll('[e-mappinguid=' + uid + ']')[0].parentElement;
          if (!sf.base.isNullOrUndefined(headercell)) {
            var gridHeaderCell = headercell.parentElement;
            this.currentBorderIndex = Number(gridHeaderCell.getAttribute(frozenDragClassList.ariaColumnIndex)) - 1;
          }
        } else {
          this.currentBorderIndex = Number(parentsUntil(this.currentCursorElement, 'e-headercell').getAttribute(frozenDragClassList.ariaColumnIndex)) - 1;
        }
      }
      var helperPosition = this.isLeftFixedCursorDragging || this.isRightFixedCursorDragging ? this.convertWidthToNumber(this.helperElement.style.left) + this.parent.getContent().scrollLeft : this.isLeftCursorDragging ? this.convertWidthToNumber(this.helperElement.style.left) : -this.calcPos(this.helperElement).right;
      if (helperPosition < this.widthCollection[0] / 2) {
        index = 0;
        this.frozenDefaultBorderClass = this.isLeftCursorDragging ? 'e-frozen-left-border' : 'e-frozen-right-border';
      } else if (helperPosition > this.widthCollection[0] / 2 && helperPosition < this.widthCollection[0]) {
        index = 0;
        this.frozenDefaultBorderClass = this.isLeftCursorDragging ? 'e-frozen-right-border' : 'e-frozen-left-border';
      } else {
        this.frozenDefaultBorderClass = 'default';
        for (var i = 0; i < this.widthCollection.length && i + 1 !== this.widthCollection.length; i++) {
          var indexValue = parseInt(i.toString(), 10);
          if (helperPosition > this.widthCollection[parseInt(indexValue.toString(), 10)] && helperPosition < this.widthCollection[i + 1]) {
            var colwidthBy2 = (this.widthCollection[i + 1] - this.widthCollection[parseInt(indexValue.toString(), 10)]) / 2;
            if (helperPosition < this.widthCollection[parseInt(indexValue.toString(), 10)] + colwidthBy2) {
              index = i;
            } else {
              index = i + 1;
            }
            break;
          }
        }
      }
      if (!sf.base.isNullOrUndefined(index) && this.currentBorderIndex !== index || this.frozenDefaultBorderClass !== 'default' && index === this.currentBorderIndex) {
        this.addOrRemoveClasses('remove');
        this.currentBorderIndex = index;
        var borderColumnUID = this.isLeftCursorDragging ? this.visibleLtoROrderedColumns[this.currentBorderIndex].uid : this.visibleRtoLOrderedColumns[this.currentBorderIndex].uid;
        var headerCells = this.parent.getHeaderContent().querySelectorAll('[e-mappinguid="' + borderColumnUID + '"]');
        if (headerCells.length) {
          for (var i = 0; i < headerCells.length; i++) {
            var headerCellElement = headerCells[parseInt(i.toString(), 10)];
            if (!sf.base.isNullOrUndefined(headerCellElement) && !sf.base.isNullOrUndefined(headerCellElement.parentElement) && headerCellElement.classList.contains('e-headercelldiv')) {
              this.borderHeaderCell = headerCellElement.parentElement.parentElement;
            }
          }
        }
        if (this.parent.getHeaderContent().querySelector('.e-filterbar')) {
          var filterBarCells = this.parent.getHeaderContent().querySelectorAll('.e-filterbarcell');
          for (var i = 0; i < filterBarCells.length; i++) {
            if (filterBarCells[parseInt(i.toString(), 10)].getAttribute('e-mappinguid') === borderColumnUID) {
              this.filterBarCell = filterBarCells[parseInt(i.toString(), 10)];
            }
          }
        }
        //currentBorderIndex and originalBorderIndex will vary if we drag the right frozen handler
        this.originalBorderIndex = Number(this.borderHeaderCell.getAttribute(frozenDragClassList.ariaColumnIndex)) - 1;
        this.borderCells = this.parent.getContent().querySelectorAll('[' + frozenDragClassList.ariaColumnIndex + '="' + (this.originalBorderIndex + 1) + '"]');
        this.addOrRemoveClasses('add');
      }
    };
    FrozenDD.prototype.addOrRemoveClasses = function (action) {
      var classToAddOrRemove = this.isLeftCursorDragging ? 'e-frozen-right-border' : 'e-frozen-left-border';
      if (!sf.base.isNullOrUndefined(this.borderCells)) {
        if (action === 'add') {
          classToAddOrRemove = this.frozenDefaultBorderClass === 'default' ? classToAddOrRemove : this.frozenDefaultBorderClass;
          sf.base.addClass(this.borderCells, classToAddOrRemove);
          this.borderHeaderCell.classList.add(classToAddOrRemove);
          if (!sf.base.isNullOrUndefined(this.filterBarCell)) {
            this.filterBarCell.classList.add(classToAddOrRemove);
          }
        } else {
          if (this.frozenDefaultBorderClass !== 'default') {
            sf.base.removeClass(this.borderCells, ['e-frozen-left-border', 'e-frozen-right-border']);
            this.borderHeaderCell.classList.remove('e-frozen-right-border');
            this.borderHeaderCell.classList.remove('e-frozen-left-border');
            if (!sf.base.isNullOrUndefined(this.filterBarCell)) {
              this.filterBarCell.classList.remove('e-frozen-right-border');
              this.filterBarCell.classList.remove('e-frozen-left-border');
            }
          } else {
            sf.base.removeClass(this.borderCells, classToAddOrRemove);
            this.borderHeaderCell.classList.remove(classToAddOrRemove);
            if (!sf.base.isNullOrUndefined(this.filterBarCell)) {
              this.filterBarCell.classList.remove(classToAddOrRemove);
            }
          }
        }
      }
    };
    FrozenDD.prototype.convertWidthToNumber = function (widthInString) {
      var widthInNumber = Number(widthInString.toString().replace('px', ''));
      return widthInNumber;
    };
    FrozenDD.prototype.pushColumnWidth = function (columns, result) {
      var previousWidth = 0;
      for (var i = 0; i < columns.length; i++) {
        var colWidth = this.convertWidthToNumber(columns[parseInt(i.toString(), 10)].width);
        result.push(previousWidth + colWidth);
        previousWidth = result[result.length - 1];
      }
      return result;
    };
    FrozenDD.prototype.scrollHorizontally = function () {
      if (this.parent.options.frozenColumns !== 0) {
        var movableContent = this.parent.getContent();
        if (movableContent.scrollWidth > movableContent.clientWidth) {
          movableContent.scrollLeft = this.isLeftFixedCursorDragging || this.isRightFixedCursorDragging ? movableContent.scrollLeft : this.isLeftCursorDragging ? 0 : movableContent.offsetWidth;
        }
      }
    };
    FrozenDD.prototype.updateHelper = function (e) {
      var mouseX = this.getCurrentScreenX(e);
      var cursorLeft = this.calcPos(this.currentCursorElement).left;
      var gridLeft = this.parent.element.getBoundingClientRect().left;
      var mouseMove = mouseX - (gridLeft + cursorLeft);
      var gridOffSetWIdth = this.parent.getContent().offsetWidth;
      var newHelperLeft = cursorLeft + mouseMove;
      var movableElement = this.parent.getContent().querySelector('.e-movablecontent');
      if (!movableElement) {
        movableElement = this.parent.getContent();
      }
      if (this.isLeftCursorDragging && !this.isLeftFixedCursorDragging && !this.isRightFixedCursorDragging) {
        var movableRight = this.calcPos(movableElement).right;
        newHelperLeft = newHelperLeft > gridOffSetWIdth + movableRight ? gridOffSetWIdth + movableRight - 1 : newHelperLeft;
      } else if (!this.isLeftFixedCursorDragging && !this.isRightFixedCursorDragging) {
        var movableLeft = this.calcPos(movableElement).left;
        newHelperLeft = newHelperLeft < movableLeft ? movableLeft + 1 : newHelperLeft;
        newHelperLeft = newHelperLeft > gridOffSetWIdth - getScrollBarWidth() ? gridOffSetWIdth - getScrollBarWidth() - 1 : newHelperLeft;
      }
      this.helperElement.style.left = newHelperLeft + 'px';
      this.helperElement.style.zIndex = '10';
    };
    FrozenDD.prototype.getCurrentScreenX = function (e) {
      if (e.touches && e.touches.length) {
        return e.touches[0].clientX;
      } else {
        return e.clientX;
      }
    };
    FrozenDD.prototype.dragEnd = function () {
      if (!this.helperElement) {
        return;
      }
      sf.base.EventHandler.remove(this.parent.element, sf.base.Browser.touchMoveEvent, this.freezeLineDragging);
      sf.base.EventHandler.remove(document, sf.base.Browser.touchEndEvent, this.dragEnd);
      var params = this.getServerParams();
      var frozenColumns = this.parent.options.actualFrozenColumns;
      if (this.parent.options.actualFrozenColumns > 0 && !params.isFrozen) {
        for (var i = 0; i < params.frozenColumnsUidCollection.length; i++) {
          for (var j = 0; j < this.visibleFrozenColumns.length; j++) {
            if (this.visibleFrozenColumns[parseInt(j.toString(), 10)].uid === params.frozenColumnsUidCollection[parseInt(i.toString(), 10)]) {
              frozenColumns--;
            }
          }
        }
      }
      var hasGridStructureChanges = this.currentCursorElement.classList.contains('e-frozen-default-cursor') || this.isLeftCursorDragging && this.frozenDefaultBorderClass === 'e-frozen-left-border' || !this.isLeftCursorDragging && this.frozenDefaultBorderClass === 'e-frozen-right-border' ? true : false;
      sf.base.detach(this.helperElement);
      this.setDefaultValue();
      if (!this.checkForServerCall(params.isFrozen, params.frozenColumnsUidCollection, params.endIndex, params.freezeDirection, params.freezeLineMovingDirection)) {
        return;
      }
      this.parent.dotNetRef.invokeMethodAsync('InvokeFreezeLineMoved', {
        frozenColumnsUidCollection: params.frozenColumnsUidCollection,
        freezeDirection: params.freezeDirection,
        freezeLineMovingDirection: params.freezeLineMovingDirection,
        isFrozen: params.isFrozen,
        fromIndex: params.startIndex,
        toIndex: params.endIndex,
        frozenColumnsCount: frozenColumns,
        hasGridStructureChanges: hasGridStructureChanges
      });
    };
    FrozenDD.prototype.getServerParams = function () {
      var freezeDirection;
      var isFrozen;
      var frozenColumnsUidCollection = [];
      var freezeLineMovingDirection;
      var startIndex = -1;
      var endIndex = -1;
      if (!sf.base.isNullOrUndefined(this.borderCells)) {
        var helperLeft = this.convertWidthToNumber(this.helperElement.style.left);
        var cursorLeft = this.calcPos(this.currentCursorElement).left;
        var rowcell = sf.base.closest(this.currentCursorElement, frozenDragClassList.rowcell);
        if (sf.base.isNullOrUndefined(rowcell)) {
          rowcell = sf.base.closest(this.currentCursorElement, frozenDragClassList.header);
          if (sf.base.isNullOrUndefined(rowcell)) {
            var uid = parentsUntil(this.currentCursorElement, 'e-filterbarcell').getAttribute('e-mappinguid');
            rowcell = this.parent.getHeaderContent().querySelectorAll('[e-mappinguid="' + uid + '"]')[0].parentElement.parentElement;
          }
        }
        startIndex = Number(rowcell.getAttribute(frozenDragClassList.ariaColumnIndex)) - 1;
        endIndex = Number(this.borderCells[0].getAttribute(frozenDragClassList.ariaColumnIndex)) - 1;
        if (this.isLeftCursorDragging) {
          freezeDirection = 'Left';
          if (this.isLeftFixedCursorDragging || this.isRightFixedCursorDragging) {
            freezeDirection = 'Fixed';
          }
          if (helperLeft > cursorLeft) {
            //Frozen Left Handler moving towards right side to add frozen columns
            if (this.isLeftFixedCursorDragging) {
              isFrozen = false;
            } else {
              isFrozen = true;
            }
            freezeLineMovingDirection = 'Right';
            var iterationStartIndex = this.parent.options.frozenColumns === 0 || this.parent.options.actualFrozenColumns === 0 && this.visibleFrozenLeftColumns.length === 0 ? 0 : startIndex + 1;
            var iterationEndIndex = this.currentCursorElement.classList.contains('e-frozen-default-cursor') ? this.frozenDefaultBorderClass === 'e-frozen-left-border' ? -1 : endIndex : endIndex;
            for (var i = iterationStartIndex; i <= iterationEndIndex; i++) {
              if (this.domLtoROrderedColumns[parseInt(i.toString(), 10)].visible) {
                frozenColumnsUidCollection.push(this.domLtoROrderedColumns[parseInt(i.toString(), 10)].uid);
              }
            }
          } else {
            //Frozen Left Handler moving towards left side to remove frozen columns
            if (this.isLeftFixedCursorDragging) {
              isFrozen = true;
            } else {
              isFrozen = false;
            }
            freezeLineMovingDirection = 'Left';
            if (this.frozenDefaultBorderClass !== 'e-frozen-left-border') {
              for (var i = startIndex; i > endIndex; i--) {
                if (this.domLtoROrderedColumns[parseInt(i.toString(), 10)].visible) {
                  frozenColumnsUidCollection.push(this.domLtoROrderedColumns[parseInt(i.toString(), 10)].uid);
                }
              }
            } else {
              //Frozen Left Handler moving towards left side to remove all the frozen columns
              for (var i = startIndex; i !== -1; i--) {
                if (this.domLtoROrderedColumns[parseInt(i.toString(), 10)].visible) {
                  frozenColumnsUidCollection.push(this.domLtoROrderedColumns[parseInt(i.toString(), 10)].uid);
                }
              }
            }
          }
        } else {
          var reverseArray = this.domRtoLOrderedColumns.reverse();
          if (helperLeft < cursorLeft) {
            //Frozen Right Handler moving towards left side to add frozen columns
            isFrozen = true;
            freezeDirection = 'Right';
            freezeLineMovingDirection = 'Left';
            if (this.frozenDefaultBorderClass !== 'e-frozen-right-border') {
              startIndex = this.currentCursorElement.classList.contains('e-frozen-default-cursor') ? startIndex + 1 : startIndex;
              for (var i = endIndex; i < startIndex; i++) {
                if (reverseArray[parseInt(i.toString(), 10)].visible) {
                  frozenColumnsUidCollection.push(reverseArray[parseInt(i.toString(), 10)].uid);
                }
              }
            }
          } else {
            //Frozen Right Handler moving towards right side to remove frozen columns
            isFrozen = false;
            freezeDirection = 'Left';
            freezeLineMovingDirection = 'Right';
            if (this.frozenDefaultBorderClass === 'e-frozen-right-border') {
              //Frozen Right Handler moving towards right side to remove all the frozen columns
              for (var i = startIndex; i <= endIndex; i++) {
                if (reverseArray[parseInt(i.toString(), 10)].visible) {
                  frozenColumnsUidCollection.push(reverseArray[parseInt(i.toString(), 10)].uid);
                }
              }
            } else {
              for (var i = startIndex; i < endIndex; i++) {
                if (reverseArray[parseInt(i.toString(), 10)].visible) {
                  frozenColumnsUidCollection.push(reverseArray[parseInt(i.toString(), 10)].uid);
                }
              }
            }
          }
        }
      }
      return {
        startIndex: startIndex,
        endIndex: endIndex,
        freezeDirection: freezeDirection,
        isFrozen: isFrozen,
        freezeLineMovingDirection: freezeLineMovingDirection,
        frozenColumnsUidCollection: frozenColumnsUidCollection
      };
    };
    FrozenDD.prototype.checkForServerCall = function (isFrozen, frozenColumnsUidCollection, endIndex, freezeDirection, freezeLineMovingDirection) {
      var allowServerCall = true;
      var originalFrozenColumns = this.parent.columnModel.slice(0, this.parent.options.actualFrozenColumns);
      originalFrozenColumns = originalFrozenColumns.filter(function (col) {
        return col.visible;
      });
      originalFrozenColumns = originalFrozenColumns.concat(this.parent.columnModel.filter(function (col) {
        return col.isFrozen && col.visible;
      }));
      if (isFrozen) {
        for (var i = 0; i < this.parent.columnModel.length; i++) {
          for (var j = 0; j < frozenColumnsUidCollection.length; j++) {
            if (frozenColumnsUidCollection[parseInt(j.toString(), 10)] === this.parent.columnModel[parseInt(i.toString(), 10)].uid) {
              var addColumn = true;
              for (var k = 0; k < originalFrozenColumns.length; k++) {
                if (originalFrozenColumns[parseInt(k.toString(), 10)].uid === frozenColumnsUidCollection[parseInt(j.toString(), 10)]) {
                  addColumn = false;
                }
              }
              if (addColumn) {
                originalFrozenColumns.push(this.parent.columnModel[parseInt(i.toString(), 10)]);
              }
            }
          }
        }
      }
      var totalFrozenWidth = 0;
      for (var i = 0; i < originalFrozenColumns.length; i++) {
        totalFrozenWidth += +originalFrozenColumns[parseInt(i.toString(), 10)].width;
      }
      if (this.parent.element.offsetWidth <= totalFrozenWidth) {
        allowServerCall = false;
      }
      if (!sf.base.isNullOrUndefined(this.parent.getContent().querySelector('.e-movablecontent'))) {
        var movableRow = this.parent.getMovableDataRows()[0].getElementsByTagName('td');
        var frozenRow = this.parent.getFrozenDataRows()[0].getElementsByTagName('td');
        var targetIndex = endIndex.toString();
        if (freezeLineMovingDirection === 'Right' && (parseInt(movableRow[movableRow.length - 1].getAttribute('aria-colindex'), 10) - 1).toString() === targetIndex || freezeLineMovingDirection === 'Left' && (parseInt(movableRow[0].getAttribute('aria-colindex'), 10) - 1).toString() === targetIndex || (parseInt(frozenRow[0].getAttribute('aria-colindex'), 10) - 1).toString() === targetIndex && freezeDirection === 'Right' && freezeLineMovingDirection === 'Left') {
          allowServerCall = false;
        }
      }
      return allowServerCall;
    };
    return FrozenDD;
  }();

  /**
   * InfiniteScroll
   *
   * @returns {void} This method does not return a value.
   * @hidden
   */
  var InfiniteScroll = /** @class */function () {
    //private keyInteraction: boolean = false;
    function InfiniteScroll(parent) {
      this.infiniteScrollTop = 0;
      this.infiniteDataRequested = false;
      this.infiniteScrollDirection = '';
      this.maxPage = 0;
      this.infiniteInitialRender = true;
      this.rowElements = [];
      this.isLazyChildLoad = false;
      this.currentRowIndex = 0;
      this.parent = parent;
      this.addKeydownListener();
    }
    InfiniteScroll.prototype.destroy = function () {
      this.removekeydownListener();
    };
    /**
     * Adds a keydown event listener to the parent element.
     * This listener triggers the `keydownHandler` method when a keydown event occurs.
     *
     * @private
     * @returns {void}
     */
    InfiniteScroll.prototype.addKeydownListener = function () {
      if (!this.parent.options.enableInfiniteScrolling) {
        return;
      }
      sf.base.EventHandler.add(this.parent.element, 'keydown', this.keydownHandler, this);
    };
    /**
     * Removes the keydown event listener to the parent element.
     * This listener triggers the `keydownHandler` method when a keydown event occurs.
     *
     * @private
     * @returns {void}
     */
    InfiniteScroll.prototype.removekeydownListener = function () {
      sf.base.EventHandler.remove(this.parent.element, 'keydown', this.keydownHandler);
    };
    /**
     * Handles the keydown event for infinite scrolling in the grid.
     *
     * @param {KeyboardEventArgs} e - The keyboard event arguments.
     * @private
     * @returns {void}
     *
     * This method checks if the 'Tab' key is pressed and if the active element is the last cell in the grid.
     * If so, it triggers the infinite scroll handler to load more data.
     *
     * If the 'Tab' key is pressed along with the 'Shift' key and the active element is the first cell in the grid,
     * it also triggers the infinite scroll handler to load more data.
     */
    InfiniteScroll.prototype.keydownHandler = function (e) {
      var activeElement = document.activeElement;
      var scrollElement = this.parent.scrollModule.content;
      var previousLeft = this.parent.scrollModule.previousValues.left;
      if (e.key === 'Tab') {
        var lastCell = this.getLastCell();
        var firstCell = this.getFirstCell();
        // this.keyInteraction = true;
        if (activeElement === lastCell || this.parent.options.infiniteCacheMode && e.shiftKey && activeElement === firstCell) {
          //this.keyInteraction = true;
          this.infiniteDataRequested = this.infiniteDataRequested ? false : this.infiniteDataRequested;
          this.infiniteScrollHandler(scrollElement, previousLeft, true);
        }
      }
    };
    /**
     * Retrieves the first cell element from the first row of the grid.
     *
     * @returns {HTMLElement} The first cell element of the first row.
     */
    InfiniteScroll.prototype.getFirstCell = function () {
      var rows = this.parent.getRows();
      var firstRow = rows[0];
      return !sf.base.isNullOrUndefined(firstRow) ? firstRow.children[0] : null;
    };
    /**
     * Retrieves the last cell element from the last row of the grid.
     *
     * @returns {HTMLElement} The last cell element in the last row of the grid.
     */
    InfiniteScroll.prototype.getLastCell = function () {
      var rows = this.parent.getRows();
      var lastRow = rows[rows.length - 1];
      return !sf.base.isNullOrUndefined(lastRow) ? lastRow.cells[lastRow.cells.length - 1] : null;
    };
    /**
     * Handles the data readiness for infinite scrolling in the grid.
     * @returns {void}
     */
    InfiniteScroll.prototype.infiniteOnDataReady = function () {
      // eslint-disable-next-line @typescript-eslint/no-this-alias
      var _this = this;
      this.maxPage = Math.ceil(this.parent.options.totalItemCount / this.parent.options.pageSize);
      if (this.parent.options.allowGrouping && !this.parent.options.enableLazyLoading && this.parent.options.groupCount > 0) {
        this.maxPage = this.maxPage + 1;
      }
      var resetRequestTypes = new Set(['Refresh', 'Filtering', 'Sorting', 'sorting', 'Searching', 'Grouping', 'UnGrouping', 'Reorder', 'RowDragAndDrop', 'refresh', 'filtering', 'searching', 'grouping', 'ungrouping', 'reorder', 'GroupExpandCollapse', null]);
      if (resetRequestTypes.has(this.parent.options.requestType)) {
        _this.infiniteDataRequested = false;
      }
    };
    /**
     * Checks if the bottom of the scrollable element has been reached.
     *
     * @param {HTMLElement} scrollElement - The scrollable element to check.
     * @returns {boolean} - Returns `true` if the bottom of the scrollable element is reached, otherwise `false`.
     */
    InfiniteScroll.prototype.isBottomReached = function (scrollElement) {
      var offset = scrollElement.scrollHeight - scrollElement.scrollTop;
      var offsetRound = Math.round(offset);
      var offsetFloor = offset < scrollElement.clientHeight ? Math.ceil(offset) : Math.floor(offset);
      var noScrollBar = scrollElement.scrollHeight > scrollElement.clientHeight;
      if (offsetFloor > scrollElement.clientHeight) {
        offsetFloor = offsetFloor - 1;
      }
      return noScrollBar && (offsetFloor === scrollElement.clientHeight || offsetRound === scrollElement.clientHeight);
    };
    /**
     * Determines if the current page is a grouped page.
     *
     * @returns {boolean} - Returns `true` if grouping is allowed, lazy loading is disabled,
     *                      there are groups, the current page is greater than or equal to 1,
     *                      and it is not the initial render of infinite scrolling.
     */
    InfiniteScroll.prototype.isGroupCurrentPage = function () {
      return this.parent.options.allowGrouping && !this.parent.options.enableLazyLoading && this.parent.options.groupCount > 0 && this.parent.options.currentPage >= 1 && !this.infiniteInitialRender;
    };
    /**
     * Handles the infinite scroll event for the grid.
     *
     * @param {HTMLElement} scrollElement - The scrollable element of the grid.
     * @param {number} previousLeft - The previous horizontal scroll position.
     * @param {boolean} keyInteraction - Indicates if the scroll was triggered by keyboard interaction.
     * @private
     * @returns {void}
     */
    InfiniteScroll.prototype.infiniteScrollHandler = function (scrollElement, previousLeft, keyInteraction) {
      // eslint-disable-next-line @typescript-eslint/no-this-alias
      var _this = this;
      var isLeftRightScroll = scrollElement.scrollLeft !== previousLeft;
      var infiniteContent = scrollElement.classList.contains('e-content');
      var delay = sf.base.Browser.info.name === 'chrome' ? 200 : 100;
      //for less records calculation records less than equal to 90
      var isBlockSizeValid = this.parent.options.pageSize * this.parent.options.infiniteInitialBlock <= this.parent.options.totalItemCount; // Ensure block size does not exceed total item count
      if (infiniteContent && !isLeftRightScroll && isBlockSizeValid) {
        var isBottom_1 = this.isBottomReached(scrollElement);
        var scrollElementTop = Math.floor(scrollElement.scrollTop);
        var currentPage = this.parent.options.currentPage;
        if (!this.infiniteDataRequested) {
          if (this.parent.getRows().length / this.parent.options.pageSize === this.maxPage) {
            return;
          }
          if (isBottom_1 && !this.infiniteInitialRender && (currentPage <= this.maxPage - 1 || this.parent.options.totalItemCount !== this.rowElements.length && !this.infiniteInitialRender && !this.parent.options.infiniteCacheMode)) {
            var activeElement_1 = document.activeElement;
            setTimeout(function () {
              var isBottom1 = _this.isBottomReached(scrollElement);
              if (isBottom1 && (!keyInteraction || keyInteraction && activeElement_1 === _this.getLastCell())) {
                _this.parent.dotNetRef.invokeMethodAsync('LoadInfiniteData', {
                  requestType: 'InfiniteScrolling'
                }, isBottom_1, false, false, null, null, 0, keyInteraction);
              }
            }, delay);
            this.infiniteScrollTop = this.calculateScrollPosition('down');
            this.infiniteScrollDirection = 'down';
          } else if (scrollElementTop === 0 && (currentPage > 1 || this.isGroupCurrentPage()) && this.parent.options.infiniteCacheMode) {
            setTimeout(function () {
              var scrollElement = _this.parent.getContent();
              if (scrollElement.scrollHeight !== scrollElement.clientHeight) {
                _this.parent.dotNetRef.invokeMethodAsync('LoadInfiniteData', {
                  requestType: 'InfiniteScrolling'
                }, isBottom_1, true, false, null, null, 0, keyInteraction);
              }
            }, delay);
            this.infiniteScrollTop = this.calculateScrollPosition('up');
            this.infiniteScrollDirection = 'up';
          }
        }
        this.infiniteDataRequested = isBottom_1 && !this.infiniteInitialRender || scrollElementTop === 0;
        this.infiniteInitialRender = false;
      }
    };
    /**
     * Handles the infinite scroll functionality for lazy loading in the grid.
     *
     * @param {boolean} scrollDown - Indicates the direction of the scroll. `true` if scrolling down, `false` if scrolling up.
     *
     * This method checks the scroll direction and determines if new rows need to be loaded
     * based on the current scroll position. It identifies the rows that need to be loaded
     * and triggers the loading of data accordingly.
     *
     * - If scrolling down, it checks for rows with the class `e-lazyload-middle-down`.
     *
     * The method updates the `currentRowIndex` based on the identified rows and invokes
     * the `LoadInfiniteData` method to load the necessary data.
     *
     * @private
     * @returns {void}
     */
    InfiniteScroll.prototype.lazyLoadInfiniteScrollHandler = function (scrollDown) {
      // eslint-disable-next-line @typescript-eslint/no-this-alias
      var _this = this;
      var gridcontent = this.parent.getContent();
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      var downTrs = [].slice.call(gridcontent.getElementsByClassName('e-lazyload-middle-down'));
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      var endTrs = [].slice.call(gridcontent.getElementsByClassName('e-lazyload-last-down'));
      var lazyLoadDown = false;
      var lazyLoadEnd = false;
      var middleRowIndex = 0;
      var tr;
      var middleTr;
      var endTr;
      var prevRowIndex = this.currentRowIndex;
      var delay = sf.base.Browser.info.name === 'chrome' ? 200 : 100;
      if (scrollDown && downTrs.length > 0) {
        var result = this.findRowElementsInGrid(downTrs);
        lazyLoadDown = result.rowEntered;
        middleRowIndex = result.rowIndex;
        tr = middleTr = result.row;
        if (lazyLoadDown) {
          this.currentRowIndex = middleRowIndex;
        }
      }
      if (!scrollDown && endTrs.length > 0) {
        for (var i = 0; i < endTrs.length; i++) {
          var top_1 = endTrs[parseInt(i.toString(), 10)].getBoundingClientRect().top;
          var endRowIndex = endTrs[parseInt(i.toString(), 10)].rowIndex;
          var scrollHeight = gridcontent.parentElement.scrollHeight;
          this.isLazyChildLoad = false;
          if (top_1 > 0 && top_1 < scrollHeight) {
            tr = endTr = endTrs[parseInt(i.toString(), 10)];
            lazyLoadEnd = true;
            endRowIndex = tr.rowIndex;
            if (lazyLoadEnd) {
              this.currentRowIndex = endRowIndex;
            }
            break;
          }
        }
      }
      if ((scrollDown && lazyLoadDown || !scrollDown && lazyLoadEnd) && this.currentRowIndex !== prevRowIndex && !this.isLazyChildLoad) {
        var middleTrUid_1 = !sf.base.isNullOrUndefined(middleTr) ? middleTr.getAttribute('data-uid') : null;
        var endTrUid_1 = !sf.base.isNullOrUndefined(endTr) ? endTr.getAttribute('data-uid') : null;
        var childRequest_1 = lazyLoadDown || lazyLoadEnd;
        setTimeout(function () {
          _this.parent.dotNetRef.invokeMethodAsync('LoadInfiniteData', {
            requestType: 'InfiniteScrolling'
          }, false, false, childRequest_1, middleTrUid_1, endTrUid_1, middleRowIndex, false);
        }, delay);
        this.isLazyChildLoad = true;
      }
    };
    /**
     * Finds the first row element in the lazy load grouped grid that meets a specific condition.
     *
     * @param {HTMLElement[]} rows - An array of HTML row elements to search through.
     * @returns {Object} An object containing:
     *  - `rowEntered` {boolean}: Indicates if a row meeting the condition was found.
     *  - `rowIndex` {number}: The index of the row that meets the condition.
     *  - `row` {HTMLElement | null}: The row element that meets the condition, or null if none found.
     */
    InfiniteScroll.prototype.findRowElementsInGrid = function (rows) {
      var rowEntered = false;
      var row = null;
      var rowIndex = 0;
      for (var _i = 0, rows_1 = rows; _i < rows_1.length; _i++) {
        var currentRow = rows_1[_i];
        var currentRowIndex = currentRow.rowIndex;
        if (this.isRowEnteredInGrid(currentRowIndex)) {
          rowEntered = true;
          row = currentRow;
          rowIndex = currentRowIndex;
          break; // Exit the loop early
        }
      }
      return {
        rowEntered: rowEntered,
        rowIndex: rowIndex,
        row: row
      };
    };
    /**
     * Checks if a row with the given index is currently visible within the grid's viewport.
     *
     * @param {number} index - The index of the row to check.
     * @returns {boolean} - Returns `true` if the row is visible within the grid's viewport, otherwise `false`.
     */
    InfiniteScroll.prototype.isRowEnteredInGrid = function (index) {
      var content = this.parent.getContent();
      var rowHeight = this.parent.getRowHeight();
      var startIndex = content.scrollTop / rowHeight;
      var endIndex = startIndex + content.offsetHeight / rowHeight;
      return index > startIndex && index < endIndex;
    };
    /**
     * Calculates the scroll position based on the given direction.
     *
     * @param {string} direction - The direction of the scroll, either 'up' or 'down'.
     * @returns {number} - The calculated scroll position.
     */
    InfiniteScroll.prototype.calculateScrollPosition = function (direction) {
      var scrollTop = 0;
      var scrollElement = this.parent.getContent();
      var gridRowHeight = this.parent.getRowHeight();
      var gridPageSize = this.parent.options.pageSize;
      if (direction === 'down') {
        if (this.parent.options.allowGrouping && !sf.base.isNullOrUndefined(this.parent.options.initGroupingField) && this.parent.options.initGroupingField.length > 0 && !this.parent.options.infiniteCacheMode) {
          var groupRowHeight = 0;
          var contentTable = this.parent.getContentTable();
          var captionRows = contentTable.querySelectorAll('tr:not(.e-row)');
          groupRowHeight = captionRows.length * gridRowHeight;
          scrollTop += groupRowHeight;
        } else {
          var pageSizeMaxBlock = gridPageSize * (this.parent.options.infiniteMaxBlocks - 1);
          pageSizeMaxBlock = pageSizeMaxBlock === 0 ? gridPageSize : pageSizeMaxBlock;
          var currentViewRowCount = 0;
          var i = 0;
          while (currentViewRowCount < scrollElement.offsetHeight) {
            i++;
            currentViewRowCount = i * gridRowHeight;
          }
          i -= 1;
          scrollTop += (pageSizeMaxBlock - i) * gridRowHeight;
        }
      } else if (direction === 'up') {
        scrollTop += gridPageSize * gridRowHeight;
      }
      return scrollTop;
    };
    /**
     * Resets the infinite scroll positions for various grid actions.
     * @returns {void}
     */
    InfiniteScroll.prototype.resetInfniniteScrollPositions = function () {
      var scrollElement = this.parent.getContent();
      var gridOptions = this.parent.options;
      if (this.infiniteInitialRender) {
        this.maxPage = Math.ceil(gridOptions.totalItemCount / gridOptions.pageSize);
      }
      var requestTypes = new Set(['Refresh', 'Filtering', 'ClearFiltering', 'Sorting', 'sorting', 'Searching', 'Grouping', 'UnGrouping', 'Reorder', 'refresh', 'filtering', 'searching', 'grouping', 'ungrouping', 'reorder', 'GroupExpandCollapse', 'GroupExpandCollapseAll', null]);
      if (requestTypes.has(gridOptions.requestType)) {
        this.infiniteInitialRender = true;
        scrollElement.scrollTop = 0;
        this.infiniteScrollDirection = '';
      }
      if (gridOptions.requestType === 'Delete') {
        var reachedBottom = scrollElement.scrollTop + scrollElement.offsetHeight >= scrollElement.scrollHeight;
        this.infiniteInitialRender = reachedBottom ? true : this.infiniteInitialRender;
      }
      if (this.infiniteScrollDirection === 'down' || this.infiniteScrollDirection === 'up') {
        this.infiniteDataRequested = false;
        if ((gridOptions.currentPage <= this.maxPage - 1 || scrollElement.scrollTop === 0) && gridOptions.infiniteCacheMode && this.parent.options.requestType === 'InfiniteScrolling') {
          if (this.isMacOsSafariVersion('17.4')) {
            // eslint-disable-next-line @typescript-eslint/no-this-alias
            var _this_1 = this;
            setTimeout(function () {
              scrollElement.scrollTop = _this_1.infiniteScrollTop;
            }, 500);
          } else {
            scrollElement.scrollTop = this.infiniteScrollTop;
          }
        }
      }
      if (this.isLazyChildLoad) {
        this.isLazyChildLoad = false;
      }
    };
    /**
     * Checks if the current browser is Safari on macOS and if its version is greater than or equal to the specified minimum version.
     *
     * @param {string} minVersion - The minimum Safari version required, in the format "major.minor".
     * @returns {boolean} - Returns `true` if the browser is Safari on macOS and its version is greater than or equal to the specified minimum version, otherwise `false`.
     */
    InfiniteScroll.prototype.isMacOsSafariVersion = function (minVersion) {
      var userAgent = navigator.userAgent;
      var isMacOS = userAgent.indexOf('Mac OS') !== -1; // check whether it is on macOS
      var isSafariBrowser = /^((?!chrome|android).)*safari/i.test(userAgent); // Check the browser is Safari
      if (isMacOS && isSafariBrowser) {
        // Extract Safari version number from the user agent
        var safariVersionMatch = userAgent.match(/Version\/(\d+)\.(\d+)/);
        if (safariVersionMatch) {
          var currentMainVersion = parseInt(safariVersionMatch[1], 10);
          var currentSubVersion = parseInt(safariVersionMatch[2], 10);
          // Splitting the main version and the sub version
          var _a = minVersion.split('.').map(Number),
            minMainVersion = _a[0],
            minSubVersion = _a[1];
          // Comparing versions
          if (currentMainVersion > minMainVersion || currentMainVersion === minMainVersion && currentSubVersion >= minSubVersion) {
            return true;
          }
        }
      }
      return false;
    };
    return InfiniteScroll;
  }();
  var __spreadArray = undefined && undefined.__spreadArray || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar) ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
  };
  /**
   * Constructor for the SfGrid client component.
   *
   * @param {HTMLElement} element - The root HTML element representing the grid.
   * @param {BlazorDotnetObject} dotNetRef - The Blazor .NET object reference for interop.
   * @param {string} dataId - The identifier for grid data.
   * @param {IGridOptions} options - Options to configure the grid behavior.
   * @param {HTMLElement} header - The HTML element representing the grid header.
   * @param {HTMLElement} content - The HTML element representing the grid content area.
   * @param {HTMLElement} footer - The HTML element representing the grid footer.
   */
  var SfGrid = /** @class */function () {
    function SfGrid(dataId, element, options, dotnetRef) {
      var _this_1 = this;
      this.columnModel = [];
      this.frozenColumnModel = [];
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      window.sfBlazor = window.sfBlazor;
      this.editedCellIndex = null;
      this.firstFocusableTemplateElement = null;
      this.lastFocusableTemplateElement = null;
      this.inViewIndexes = [];
      this.isRendered = false;
      this.isGridFirstRender = false;
      this.isResetDataTriggered = false;
      this.nColumnOffsets = [];
      this.previousTarget = null;
      this.preventSaveCellOnDragRelease = false;
      this._mouseDownX = null;
      this._mouseDownY = null;
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      this.getFrozenLeftColumn = function () {
        var columns = [];
        var gridColumns = this.getColumns();
        gridColumns.filter(function (c) {
          return c.isFrozen && c.freeze === 'Left';
        }).map(function (c) {
          return columns.push(c);
        });
        return columns;
      };
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      this.getFrozenRightColumns = function () {
        var columns = [];
        var gridColumns = this.getColumns();
        gridColumns.filter(function (c) {
          return c.isFrozen && c.freeze === 'Right';
        }).map(function (c) {
          return columns.push(c);
        });
        return columns;
      };
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      this.tapEvent = function (e) {
        if (this.resizeModule.getUserAgent()) {
          if (!Global.timer) {
            Global.timer = setTimeout(function () {
              Global.timer = null;
            }, 300);
          } else {
            clearTimeout(Global.timer);
            Global.timer = null;
            var clickEvent = new MouseEvent('dblclick', {
              bubbles: true,
              cancelable: true
            });
            e.target.dispatchEvent(clickEvent);
          }
        }
      };
      this.element = element;
      if (sf.base.isNullOrUndefined(this.element)) {
        return;
      }
      this.dotNetRef = dotnetRef;
      this.dataId = dataId;
      this.options = options;
      this.header = this.element.querySelector('.e-headercontent');
      this.content = this.element.querySelector('.e-gridcontent .e-content');
      this.footer = this.element.querySelector('.e-summarycontent');
      if (this.element.offsetWidth <= 0) {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        var gridtimer_1 = setInterval(function () {
          if (_this_1.element.offsetWidth > 0) {
            _this_1.initModulesResults = _this_1.initModules();
            clearInterval(gridtimer_1);
          }
        }, 500);
      } else {
        this.initModulesResults = this.initModules();
      }
      this.addScrollEvents(true);
      if (!sf.base.isNullOrUndefined(this.element)) {
        // eslint-disable-next-line camelcase
        this.element.blazor__instance = this;
        window.sfBlazor.setCompInstance(this);
      }
    }
    SfGrid.prototype.getInitModulesResults = function () {
      if (!sf.base.isNullOrUndefined(this.initModulesResults)) {
        this.initModulesResults.isMacDevice = navigator.userAgent.indexOf('Mac OS') !== -1;
      }
      return this.initModulesResults;
    };
    SfGrid.prototype.initModules = function () {
      this.scrollModule = new Scroll(this);
      this.infiniteScrollModule = new InfiniteScroll(this);
      this.freezeModule = new Freeze(this);
      this.headerDragDrop = new HeaderDragDrop(this);
      this.contentDragDrop = new ContentDragDrop(this);
      this.reorderModule = new Reorder(this);
      this.groupModule = new Group(this);
      this.resizeModule = new Resize(this);
      this.frozenDragDropModule = new FrozenDD(this);
      this.editModule = new Edit(this);
      this.columnChooserModule = new ColumnChooser(this);
      this.clipboardModule = new Clipboard(this);
      this.columnMenuModule = new ColumnMenu(this);
      this.filterModule = new Filter(this);
      this.virtualContentModule = new VirtualContentRenderer(this);
      this.virtualHeaderModule = new VirtualHeaderRenderer(this);
      this.toolTipModule = new CustomToolTip(this);
      this.rowDragAndDropModule = new RowDD(this);
      this.selectionModule = new Selection(this);
      this.widthService = new ColumnWidthService(this);
      this.isRendered = this.options.isPrerendered;
      this.keyModule = new sf.base.KeyboardEvents(this.element, {
        keyAction: this.keyActionHandler.bind(this),
        keyConfigs: gridKeyConfigs,
        eventName: 'keydown'
      });
      var contentReadyResults;
      var virtualRowHeight = null;
      if (this.options.enableColumnVirtualization) {
        this.virtualHeaderModule.renderTable();
      }
      if (this.options.enableVirtualization || this.options.enableColumnVirtualization) {
        virtualRowHeight = this.virtualContentModule.renderTable();
      }
      if (this.options.allowResizing) {
        this.resizeModule.render();
      }
      if (this.options.isFreezeLineMoved) {
        this.freezeLineMovedAction();
      }
      // needClientAction should only be used for virtual scroll and hideAtMedia features
      if (!this.options.needClientAction) {
        contentReadyResults = this.contentReady();
      } else {
        contentReadyResults = this.clientActions();
      }
      this.lastRowBorderCheck();
      this.wireEvents();
      if (!this.options.enableColumnVirtualization) {
        this.updateColumnWidth(this.options.columns);
      }
      if (contentReadyResults != null) {
        contentReadyResults.rowHeight = virtualRowHeight;
      }
      return contentReadyResults;
    };
    SfGrid.prototype.getHeaderContent = function () {
      return this.header;
    };
    SfGrid.prototype.getHeaderTable = function () {
      return this.header.querySelector('.e-table');
    };
    SfGrid.prototype.getContent = function () {
      return this.content;
    };
    SfGrid.prototype.getContentTable = function () {
      return this.content.querySelector('.e-table');
    };
    SfGrid.prototype.getFooterContent = function () {
      return this.footer;
    };
    SfGrid.prototype.getColumns = function (autoFitVirtual) {
      if (autoFitVirtual === void 0) {
        autoFitVirtual = false;
      }
      // let inview: number[] = this.inViewIndexes.map((v: number) => v - this.groupSettings.columns.length).filter((v: number) => v > -1);
      // let vLen: number = inview.length;
      // if (!this.enableColumnVirtualization || isNullOrUndefined(this.columnModel) || this.columnModel.length === 0 || isRefresh) {
      //     this.columnModel = [];
      //     this.updateColumnModel(this.columns as Column[]);
      // }
      // let columns: Column[] = vLen === 0 ? this.columnModel :
      //     this.columnModel.slice(inview[0], inview[vLen - 1] + 1);
      this.columnModel = [];
      var columns = this.options.enableColumnVirtualization && autoFitVirtual ? this.options.virtualizedColumns : this.options.columns;
      if (autoFitVirtual && this.options.frozenColumns > 0) {
        columns = this.getOrderedFrozenColumns();
        this.columnModel = columns;
      } else {
        this.updateColumnModel(columns);
      }
      return this.columnModel;
    };
    SfGrid.prototype.getOrderedFrozenColumns = function () {
      var columns = [];
      var gridColumns = this.getColumns();
      gridColumns.filter(function (c) {
        return c.isFrozen && c.freeze === 'Left';
      }).map(function (c) {
        return columns.push(c);
      });
      gridColumns.filter(function (c) {
        return !c.isFrozen || c.isFrozen && c.freeze === 'Fixed';
      }).map(function (c) {
        return columns.push(c);
      });
      gridColumns.filter(function (c) {
        return c.isFrozen && c.freeze === 'Right';
      }).map(function (c) {
        return columns.push(c);
      });
      return columns;
    };
    SfGrid.prototype.autofitFrozenColumns = function (autofitAllColumns) {
      var columns = [];
      var gridColumns = this.getColumns();
      // eslint-disable-next-line @typescript-eslint/no-unused-vars
      gridColumns.filter(function (c) {
        return (autofitAllColumns || c.autoFit) && c.isFrozen && c.freeze === 'Left';
      }).map(function (c) {
        return columns.push(c.field || c.uid);
      });
      // eslint-disable-next-line @typescript-eslint/no-unused-vars
      gridColumns.filter(function (c) {
        return (autofitAllColumns || c.autoFit) && !c.isFrozen;
      }).map(function (c) {
        return columns.push(c.field || c.uid);
      });
      // eslint-disable-next-line @typescript-eslint/no-unused-vars
      gridColumns.filter(function (c) {
        return (autofitAllColumns || c.autoFit) && c.isFrozen && c.freeze === 'Right';
      }).map(function (c) {
        return columns.push(c.field || c.uid);
      });
      return columns;
    };
    SfGrid.prototype.freezeLineMovedAction = function () {
      this.options.isFreezeLineMoved = false;
      var movableContent = this.getContent().querySelector('.e-movablecontent');
      if (sf.base.isNullOrUndefined(movableContent)) {
        movableContent = this.getContent();
      }
      if (movableContent.querySelector('table').style.width !== '') {
        if (this.options.frozenLeftColumnsCount !== 0 || this.options.frozenRightColumnsCount !== 0) {
          this.updateColumnLevelFrozen();
        }
        var widthService = new ColumnWidthService(this);
        widthService.setWidthToTable();
      }
      movableContent.scrollLeft = 0;
      if (!sf.base.isNullOrUndefined(this.element.querySelector('.e-movablescrollbar'))) {
        this.element.querySelector('.e-movablescrollbar').scrollLeft = 0;
      }
    };
    SfGrid.prototype.addScrollEvents = function (add) {
      if (this.options.showColumnMenu) {
        var elements = sf.popups.getScrollableParent(this.element);
        for (var i = 0; i < elements.length; i++) {
          if (elements[parseInt(i.toString(), 10)] instanceof HTMLElement) {
            if (add) {
              sf.base.EventHandler.add(elements[parseInt(i.toString(), 10)], 'scroll', this.scrollHandler, this);
            } else {
              sf.base.EventHandler.remove(elements[parseInt(i.toString(), 10)], 'scroll', this.scrollHandler);
            }
          }
        }
        if (add) {
          sf.base.EventHandler.add(this.content, 'scroll', this.scrollHandler, this);
        } else {
          sf.base.EventHandler.remove(this.content, 'scroll', this.scrollHandler);
        }
      }
    };
    SfGrid.prototype.scrollHandler = function () {
      if (!sf.base.isNullOrUndefined(this.element) && !sf.base.isNullOrUndefined(this.element.blazor__instance)) {
        return this.columnMenuModule.setPosition();
      }
    };
    SfGrid.prototype.updateColumnLevelFrozen = function () {
      var cols = this.columnModel;
      if (this.options.enableColumnVirtualization) {
        cols = cols.filter(function (x) {
          return x.visible;
        });
      }
      var leftCols = [];
      var rightCols = [];
      var movableCols = [];
      if (this.options.frozenRightCount !== 0 || this.options.frozenLeftCount !== 0 || this.options.frozenColumns !== 0) {
        for (var i = 0, len = cols.length; i < len; i++) {
          var col = cols[parseInt(i.toString(), 10)];
          if (col.freeze === 'Left' && col.isFrozen || col.index < this.options.frozenColumns) {
            leftCols.push(col);
          } else if (col.freeze === 'Right' && col.isFrozen) {
            rightCols.push(col);
          } else {
            movableCols.push(col);
          }
        }
        this.frozenColumnModel = leftCols.concat(movableCols).concat(rightCols);
      }
    };
    SfGrid.prototype.updateColumnModel = function (columns) {
      if (!sf.base.isNullOrUndefined(columns)) {
        for (var i = 0, len = columns.length; i < len; i++) {
          if (columns[parseInt(i.toString(), 10)].columns != null && columns[parseInt(i.toString(), 10)].columns.length > 0) {
            this.updateColumnModel(columns[parseInt(i.toString(), 10)].columns);
          } else {
            this.columnModel.push(columns[parseInt(i.toString(), 10)]);
          }
        }
        this.updateFixedcolumns();
      }
    };
    SfGrid.prototype.updateColumnWidth = function (columns) {
      this.nColumnOffsets = [];
      var offset = 0;
      if (!this.options.enableColumnVirtualization) {
        for (var i = 0, len = columns.length; i < len; i++) {
          offset = parseInt(offset.toString(), 10) + (columns[parseInt(i.toString(), 10)].visible ? parseInt(columns[parseInt(i.toString(), 10)].width, 10) : 0);
          this.nColumnOffsets.push(offset);
        }
      }
    };
    SfGrid.prototype.getColumnByIndex = function (index, frozenCols) {
      if (frozenCols === void 0) {
        frozenCols = false;
      }
      var column;
      this.getColumns(frozenCols).some(function (col, i) {
        column = col;
        return i === index;
      });
      return column;
    };
    SfGrid.prototype.getDataRows = function () {
      if (sf.base.isNullOrUndefined(this.getContentTable().querySelector('tbody'))) {
        return [];
      }
      var rows = [].slice.call(this.getContentTable().querySelector('tbody').children);
      if (this.options.frozenRows) {
        var freezeRows = [].slice.call(this.getHeaderTable().querySelector('tbody').children);
        rows = this.addMovableRows(freezeRows, rows);
      }
      var dataRows = this.generateDataRows(rows);
      return dataRows;
    };
    SfGrid.prototype.addMovableRows = function (fRows, mrows) {
      for (var i = 0, len = mrows.length; i < len; i++) {
        fRows.push(mrows[parseInt(i.toString(), 10)]);
      }
      return fRows;
    };
    SfGrid.prototype.generateDataRows = function (rows) {
      var dRows = [];
      for (var i = 0, len = rows.length; i < len; i++) {
        if (rows[parseInt(i.toString(), 10)].classList.contains('e-row') && !rows[parseInt(i.toString(), 10)].classList.contains('e-hiddenrow')) {
          dRows.push(rows[parseInt(i.toString(), 10)]);
        }
      }
      return dRows;
    };
    SfGrid.prototype.getMovableDataRows = function () {
      var rows = [].slice.call(this.getContent().querySelector('tbody').children);
      if (this.options.frozenRows) {
        var freezeRows = [].slice.call(this.getHeaderContent().querySelector('tbody').children);
        rows = this.addMovableRows(freezeRows, rows);
      }
      var dataRows = this.generateDataRows(rows);
      return dataRows;
    };
    SfGrid.prototype.getFrozenDataRows = function () {
      var rows = [].slice.call(this.getContent().querySelector('.e-frozencontent').querySelector('tbody').children);
      if (this.options.frozenRows) {
        var freezeRows = [].slice.call(this.getHeaderContent().querySelector('.e-frozenheader').querySelector('tbody').children);
        rows = this.addMovableRows(freezeRows, rows);
      }
      var dataRows = this.generateDataRows(rows);
      return dataRows;
    };
    SfGrid.prototype.leftrightColumnWidth = function (position) {
      var cols = position === 'left' ? this.getFrozenLeftColumn() : position === 'right' ? this.getFrozenRightColumns() : [];
      var width = 0;
      cols.filter(function (col) {
        if (col.visible) {
          width += parseInt(col.width.toString(), 10);
        }
      });
      return width;
    };
    SfGrid.prototype.getFrozenRightDataRows = function () {
      var rows = [].slice.call(this.getContent().querySelector('.e-frozen-right-content').querySelector('tbody').children);
      if (this.options.frozenRows) {
        var freezeRows = [].slice.call(this.getHeaderContent().querySelector('.e-frozenheader').querySelector('tbody').children);
        rows = this.addMovableRows(freezeRows, rows);
      }
      return this.generateDataRows(rows);
    };
    SfGrid.prototype.getRowByIndex = function (index) {
      return this.getDataRows()[parseInt(index.toString(), 10)];
    };
    SfGrid.prototype.getCellFromIndex = function (rowIndex, columnIndex) {
      return this.getDataRows()[parseInt(rowIndex.toString(), 10)] && this.getDataRows()[parseInt(rowIndex.toString(), 10)].querySelectorAll('.e-rowcell')[parseInt(columnIndex.toString(), 10)];
    };
    SfGrid.prototype.isMovableGrid = function (index, frozenColumns) {
      var gridColumns = this.getColumns(frozenColumns);
      if (this.options.actualFrozenColumns > 0) {
        return index >= this.options.actualFrozenColumns;
      } else {
        var isFrozenColumnsIndexes_1 = [];
        gridColumns.forEach(function (col) {
          if (col.isFrozen) {
            isFrozenColumnsIndexes_1.push(col.index);
          }
        });
        return isFrozenColumnsIndexes_1.indexOf(index) === -1;
      }
    };
    SfGrid.prototype.getColumnHeaderByIndex = function (index) {
      return this.getHeaderTable().querySelectorAll('.e-headercell')[parseInt(index.toString(), 10)];
    };
    SfGrid.prototype.getRows = function () {
      var dataRows = [].slice.call(this.getContentTable().querySelectorAll('tr.e-row[data-uid]'));
      if (this.options.frozenRows) {
        var freezeRows = [].slice.call(this.getHeaderContent().querySelectorAll('tr.e-row[data-uid]'));
        dataRows = this.addMovableRows(freezeRows, dataRows);
      }
      return dataRows;
    };
    SfGrid.prototype.getSelectedRows = function () {
      return this.getRows().filter(function (row) {
        return row.getAttribute('aria-selected') === 'true';
      });
    };
    SfGrid.prototype.getSelectedRowIndexes = function (isVirtualScroll) {
      var selectedIndexes = [];
      var rows = this.getRows();
      for (var i = 0; i < rows.length; i++) {
        if (rows[parseInt(i.toString(), 10)].hasAttribute('aria-selected') && rows[parseInt(i.toString(), 10)].getAttribute('aria-selected') === 'true') {
          var rowIndex = this.options.allowDragSelection && this.options.enableVirtualization || isVirtualScroll ? parseInt(rows[parseInt(i.toString(), 10)].getAttribute('aria-rowindex'), 10) - 1 : i;
          selectedIndexes.push(rowIndex);
        }
      }
      return selectedIndexes;
    };
    SfGrid.prototype.getVisibleColumns = function () {
      var cols = [];
      for (var _i = 0, _a = this.columnModel; _i < _a.length; _i++) {
        var col = _a[_i];
        if (col.visible) {
          cols.push(col);
        }
      }
      return cols;
    };
    /**
     * Gets a Column by column name.
     *
     * @param  {string} field - Specifies the column name.
     * @returns {Column} The column object that matches the given field name.
     * @blazorType GridColumn
     */
    SfGrid.prototype.getColumnByField = function (field) {
      return iterateArrayOrObject(this.getColumns(), function (item) {
        if (item.field === field) {
          return item;
        }
        return undefined;
      })[0];
    };
    /**
     * Gets a column index by column name.
     *
     * @param  {string} field - Specifies the column name.
     * @param {boolean} [virtualAutoFit=false] - Specifies whether to consider virtual autofit.
     * @returns {number} The index of the column that matches the given field name, or -1 if not found.
     */
    SfGrid.prototype.getColumnIndexByField = function (field, virtualAutoFit) {
      if (virtualAutoFit === void 0) {
        virtualAutoFit = false;
      }
      var cols = this.getColumns(virtualAutoFit);
      for (var i = 0; i < cols.length; i++) {
        if (cols[parseInt(i.toString(), 10)].field === field) {
          return i;
        }
      }
      return -1;
    };
    /**
     * Gets a column by UID.
     *
     * @param  {string} uid - Specifies the column UID.
     * @returns {Column} The column object that matches the given UID.
     * @blazorType GridColumn
     */
    SfGrid.prototype.getColumnByUid = function (uid) {
      return iterateArrayOrObject(__spreadArray(__spreadArray([], this.getColumns(), true), this.getStackedColumns(this.options.columns), true), function (item) {
        if (item.uid === uid) {
          return item;
        }
        return undefined;
      })[0];
    };
    /**
     * @hidden
     * Recursively gets all stacked columns.
     *
     * @param {Column[]} columns - The array of columns to search through.
     * @param {Column[]} [stackedColumn=[]] - The array to store stacked columns.
     * @returns {Column[]} An array of stacked columns.
     */
    SfGrid.prototype.getStackedColumns = function (columns, stackedColumn) {
      if (stackedColumn === void 0) {
        stackedColumn = [];
      }
      for (var _i = 0, columns_1 = columns; _i < columns_1.length; _i++) {
        var column = columns_1[_i];
        if (column.columns) {
          stackedColumn.push(column);
          this.getStackedColumns(column.columns, stackedColumn);
        }
      }
      return stackedColumn;
    };
    /**
     * Gets a column index by UID.
     *
     * @param {string} uid - Specifies the column UID.
     * @param {boolean} [virtualAutoFit=false] - Specifies whether to consider virtual autofit.
     * @returns {number} The index of the column that matches the given UID, or -1 if not found.
     */
    SfGrid.prototype.getColumnIndexByUid = function (uid, virtualAutoFit) {
      if (virtualAutoFit === void 0) {
        virtualAutoFit = false;
      }
      var index = iterateArrayOrObject(this.getColumns(virtualAutoFit), function (item, index) {
        if (item.uid === uid) {
          return index;
        }
        return undefined;
      })[0];
      return !sf.base.isNullOrUndefined(index) ? index : -1;
    };
    /**
     * Retrieves the column header element by its UID.
     *
     * @param {string} uid - The UID of the column to retrieve.
     * @returns {Element | null} The column header element that matches the given UID, or null if not found.
     */
    SfGrid.prototype.getColumnHeaderByUid = function (uid) {
      var headerCell = this.getHeaderContent().querySelector('[e-mappinguid=' + uid + ']');
      var headerContainer = headerCell ? headerCell.parentElement : null;
      return headerContainer ? headerContainer.parentElement : null;
    };
    /**
     * Gets UID by column name.
     *
     * @param {string} field - Specifies the column name.
     * @param {boolean} [virtualAutoFit=false] - Specifies whether to consider virtual autofit.
     * @returns {string} The UID of the column that matches the given field name.
     */
    SfGrid.prototype.getUidByColumnField = function (field, virtualAutoFit) {
      if (virtualAutoFit === void 0) {
        virtualAutoFit = false;
      }
      return iterateArrayOrObject(this.getColumns(virtualAutoFit), function (item) {
        if (item.field === field) {
          return item.uid;
        }
        return undefined;
      })[0];
    };
    SfGrid.prototype.getStackedHeaderColumnByHeaderText = function (stackedHeader, col) {
      for (var i = 0; i < col.length; i++) {
        var individualColumn = col[parseInt(i.toString(), 10)];
        if (individualColumn.field === stackedHeader || individualColumn.headerText === stackedHeader) {
          this.stackedColumn = individualColumn;
          break;
        } else if (individualColumn.columns) {
          this.getStackedHeaderColumnByHeaderText(stackedHeader, individualColumn.columns);
        }
      }
      return this.stackedColumn;
    };
    /**
     * Gets TH index by column uid value.
     *
     * @private
     * @param  {string} uid - Specifies the column uid.
     * @param {boolean} [virtualAutoFit=false] - Specifies whether to consider virtual autofit.
     * @returns {number} he normalized column index.
     */
    SfGrid.prototype.getNormalizedColumnIndex = function (uid, virtualAutoFit) {
      if (virtualAutoFit === void 0) {
        virtualAutoFit = false;
      }
      var index = this.getColumnIndexByUid(uid, virtualAutoFit);
      return index + this.getIndentCount();
    };
    /**
     * Gets indent cell count.
     *
     * @private
     * @returns {number} The count of indent cells.
     */
    SfGrid.prototype.getIndentCount = function () {
      var index = 0;
      if (this.options.allowGrouping) {
        index += this.options.groupCount;
      }
      if (this.options.hasDetailTemplate) {
        index++;
      }
      if (this.options.allowRowDragAndDrop && !this.options.hasDropTarget) {
        index++;
      }
      /**
       * TODO: index normalization based on the stacked header, grouping and detailTemplate
       * and frozen should be handled here
       */
      return index;
    };
    SfGrid.prototype.isPercentageWidth = function () {
      var columns = this.getVisibleColumns();
      var percentageCol = 0;
      var undefinedWidthCol = 0;
      for (var i = 0; i < columns.length; i++) {
        if (sf.base.isNullOrUndefined(columns[parseInt(i.toString(), 10)].width)) {
          undefinedWidthCol++;
        } else if (columns[parseInt(i.toString(), 10)].width.toString().indexOf('%') !== -1) {
          percentageCol++;
        }
      }
      return percentageCol === columns.length && !undefinedWidthCol;
    };
    /**
     * Gets indent Cell Width
     *
     * @hidden
     * @returns {void}
     */
    SfGrid.prototype.recalcIndentWidth = function () {
      if (this.options.isRenderedFromTreeGrid && this.options.hasDetailTemplate && this.options.allowRowDragAndDrop || !this.isRendered || !this.getHeaderTable().querySelector('.e-emptycell')) {
        return {
          isRowDragCellEnable: null,
          indentWidthCalc: null
        };
      }
      var emptyCells = this.getHeaderTable().querySelectorAll('.e-emptycell');
      var detailIndentCell = Array.from(emptyCells).filter(function (cell) {
        return cell.parentElement.classList.contains('e-detailheadercell');
      });
      var isRowDragHeaderCell = Array.from(emptyCells).filter(function (cell) {
        return cell.parentElement.classList.contains('e-rowdragheader');
      });
      var groupingIndentCell = Array.from(emptyCells).filter(function (cell) {
        return cell.parentElement.classList.contains('e-grouptopleftcell');
      });
      var requestIsNotRefreshOrGrouping = !sf.base.isNullOrUndefined(this.options.requestType) && this.options.requestType !== 'Refresh' && this.options.requestType !== 'Grouping' && this.options.requestType !== 'UnGrouping';
      var detailIndentRefresh = 'false';
      var groupIndentRefresh = 'false';
      if (detailIndentCell.length > 0) {
        detailIndentRefresh = detailIndentCell[0].classList.contains('e-indentRefreshed').toString();
      }
      if (groupingIndentCell.length > 0) {
        groupIndentRefresh = groupingIndentCell[0].classList.contains('e-indentRefreshed').toString();
      }
      // Handle Detail and DragDrop
      if (!this.options.groupCount && !this.options.hasDetailTemplate && this.options.allowRowDragAndDrop && this.options.hasDropTarget || !this.getContentTable() || isRowDragHeaderCell.length === 0 && (detailIndentRefresh === 'true' || groupIndentRefresh === 'true')) {
        return {
          isRowDragCellEnable: null,
          indentWidthCalc: null
        };
      }
      if (requestIsNotRefreshOrGrouping && groupIndentRefresh === 'true' || isRowDragHeaderCell.length > 0 && isRowDragHeaderCell[0].classList.contains('e-indentRefreshed')) {
        return {
          isRowDragCellEnable: null,
          indentWidthCalc: null
        };
      }
      var indentWidth = this.getHeaderTable().querySelector('.e-emptycell').parentElement.offsetWidth;
      if (isRowDragHeaderCell.length > 0 && groupingIndentCell.length > 0 && groupIndentRefresh === 'false') {
        indentWidth = groupingIndentCell[0].parentElement.offsetWidth;
      }
      if (isRowDragHeaderCell.length > 0 && (groupingIndentCell.length === 0 || groupIndentRefresh === 'true')) {
        indentWidth = isRowDragHeaderCell[0].parentElement.offsetWidth;
      }
      var perPixel = indentWidth / 30;
      if (perPixel >= 1) {
        indentWidth = 30 / perPixel;
      }
      this.getHeaderTable().querySelector('.e-emptycell').classList.add('e-indentRefreshed');
      if (isRowDragHeaderCell.length > 0) {
        isRowDragHeaderCell[0].classList.add('e-indentRefreshed');
      }
      var isRowDragCell = Array.from(this.getHeaderTable().querySelectorAll('.e-emptycell')).some(function (x) {
        return x.parentElement.classList.contains('e-rowdragheader');
      });
      var calculatedIndentWidth = '';
      if (this.isPercentageWidth() && perPixel > 1) {
        var perPixel_1 = indentWidth / 30;
        if (perPixel_1 >= 1) {
          indentWidth = 30 / perPixel_1;
          indentWidth = indentWidth > 5 ? 3.5 : indentWidth;
        }
        calculatedIndentWidth = indentWidth + '%';
      } else {
        calculatedIndentWidth = indentWidth + 'px';
      }
      return {
        isRowDragCellEnable: isRowDragCell,
        indentWidthCalc: calculatedIndentWidth
      };
    };
    SfGrid.prototype.resetColumnWidth = function () {
      if ((this.options.width === 'auto' || typeof this.options.width === 'string') && this.getColumns().filter(function (col) {
        return (!col.width || col.width === 'auto') && col.minWidth;
      }).length > 0) {
        var tgridWidth = this.widthService.getTableWidth(this.getColumns());
        this.widthService.setMinwidthBycalculation(tgridWidth);
      }
    };
    SfGrid.prototype.getContentCell = function (element, top, left) {
      if (!sf.base.isNullOrUndefined(element)) {
        var cellElement = document.elementFromPoint(left, top);
        var mappingUidDiv = null;
        var mappingUid = null;
        var headerCellDiv = null;
        if (cellElement.getAttribute('aria-colIndex') === null) {
          var headerCell = parentsUntil(cellElement, 'e-headercell');
          var rowCell = parentsUntil(cellElement, 'e-rowcell');
          cellElement = headerCell !== null ? headerCell : rowCell !== null ? rowCell : cellElement;
        }
        if (!sf.base.isNullOrUndefined(cellElement)) {
          var rowCellIndex = parentsUntil(cellElement, 'e-row');
          var colIndex = parseInt(cellElement.getAttribute('aria-colindex'), 10) - 1;
          var rowIndex = rowCellIndex ? parseInt(rowCellIndex.getAttribute('aria-rowindex'), 10) - 1 : -1;
          if (sf.base.isNullOrUndefined(rowCellIndex)) {
            mappingUidDiv = cellElement.querySelector('div[e-mappinguid]');
            if (sf.base.isNullOrUndefined(mappingUidDiv)) {
              headerCellDiv = parentsUntil(cellElement, 'e-headercelldiv');
              if (!sf.base.isNullOrUndefined(headerCellDiv)) {
                cellElement = headerCellDiv;
                mappingUid = headerCellDiv.getAttribute('e-mappinguid');
              }
            } else {
              mappingUid = mappingUidDiv.getAttribute('e-mappinguid');
            }
          }
          return {
            ColumnIndex: colIndex,
            RowIndex: rowIndex,
            MappingUid: mappingUid
          };
        }
      }
    };
    SfGrid.prototype.contentReady = function (action, isResetData) {
      if (action === void 0) {
        action = null;
      }
      //To add 100% width for main HTML element, when grid width is 100% or auto
      var mainLayoutTag = document.getElementsByTagName('main')[0];
      var gridInitializeResults = {
        rowHeight: null,
        isMacDevice: null,
        indentWidth: null,
        isRowDragCell: null
      };
      if (!sf.base.isNullOrUndefined(mainLayoutTag) && mainLayoutTag.parentElement.classList.contains('page') && (this.element.style.width === '100%' || 'auto')) {
        mainLayoutTag.style.width = '100%';
      }
      if (this.getColumns().some(function (x) {
        return x.autoFit;
      })) {
        // Add a setTimeout function to auto-fit columns when using frozen columns with a custom adapter.
        // eslint-disable-next-line @typescript-eslint/no-this-alias
        var __this_1 = this;
        if (__this_1.element.querySelector('.e-emptyrow') && __this_1.options.frozenColumns) {
          setTimeout(function () {
            __this_1.resizeModule.autoFit();
          }, 100);
        } else {
          this.resizeModule.autoFit();
          this.freezeModule.updateFrozenColumnStyles();
        }
      }
      if (this.options.allowResizing && this.isGridFirstRender && this.options.isColumnResized) {
        var widthService = new ColumnWidthService(this);
        widthService.setWidthToTable();
        this.isGridFirstRender = false;
      }
      if (!this.isGridFirstRender && this.options.frozenColumns && (this.options.enablePersistence || isResetData)) {
        var widthService = new ColumnWidthService(this);
        widthService.setWidthToTable();
      }
      if (this.options.isColumnReordered && !sf.base.isNullOrUndefined(this.getContent().querySelector('.e-movablecontent')) && this.getContent().querySelector('.e-movablecontent').querySelector('table').style.width !== '') {
        var widthService = new ColumnWidthService(this);
        widthService.setWidthToTable();
      }
      if (this.options.frozenColumns && this.options.enableColumnVirtualization) {
        this.freezeModule.setFrozenHeight();
        if (this.options.aggregatesCount !== 0) {
          var rowSummary = this.element.querySelectorAll('.e-summaryrow');
          var height = 0;
          for (var i = 0; i < rowSummary.length; i++) {
            if (rowSummary[parseInt(i.toString(), 10)].querySelectorAll('.e-templatecell').length > 0) {
              height = rowSummary[parseInt(i.toString(), 10)].offsetHeight;
              break;
            }
          }
          for (var i = 0; i < rowSummary.length; i++) {
            rowSummary[parseInt(i.toString(), 10)].style.height = height + 'px';
          }
        }
      }
      if (this.options.enableVirtualization || this.options.enableColumnVirtualization) {
        this.virtualContentModule.onDataReady();
      }
      var indentResults = this.recalcIndentWidth();
      if (!sf.base.isNullOrUndefined(indentResults)) {
        gridInitializeResults.indentWidth = indentResults.indentWidthCalc;
        gridInitializeResults.isRowDragCell = indentResults.isRowDragCellEnable;
      }
      this.resetColumnWidth();
      this.lastRowBorderCheck();
      if (this.options.autoFit) {
        this.addTableBorderClass();
      }
      if (action === 'Paging') {
        //restore focus on paging.
        if (!parentsUntil(document.activeElement, 'e-grid')) {
          this.element.focus();
        }
      }
      if (this.options.enableStickyHeader) {
        this.scrollModule.addStickyListener(true);
        var groupElem = this.element.querySelector('.e-groupdroparea');
        if (!sf.base.isNullOrUndefined(groupElem) && groupElem.classList.contains('e-sticky') && groupElem.style.top === '') {
          groupElem.classList.remove('e-sticky');
        }
      }
      if (this.options.enableInfiniteScrolling) {
        this.infiniteScrollModule.infiniteOnDataReady();
        this.infiniteScrollModule.resetInfniniteScrollPositions();
      }
      if (!sf.base.isNullOrUndefined(this.toolTipModule) && !sf.base.isNullOrUndefined(this.toolTipModule.toolTipElement)) {
        this.toolTipModule.close();
      }
      var frozenRightColumns = this.getColumns().filter(function (a) {
        return a.isFrozen && a.freeze === 'Right';
      });
      if (frozenRightColumns.length > 0) {
        //Below if condition checks that the grid contains horizontal scrollbar
        if (this.getContent().scrollWidth > this.getContent().clientWidth) {
          this.element.classList.add('e-right-shadow');
        }
      }
      if (this.options.frozenRows > 0) {
        this.element.querySelector('.e-frozenrow-border').style.width = this.getContent().scrollHeight > this.getContent().offsetHeight ? this.element.offsetWidth - 17 + 'px' : this.element.offsetWidth + 'px';
      }
      if ((this.options.width === '100%' || 'auto') && this.options.frozenColumns > 0 && this.options.allowGrouping && this.options.groupCount > 0) {
        var width = this.getContent().offsetWidth;
        this.dotNetRef.invokeMethodAsync('CalculateOffSetWidth', width);
      }
      if (this.options.frozenColumns > 0 && this.options.allowGrouping && this.options.groupCount > 0) {
        if (!(this.getContent().scrollWidth > this.getContent().clientWidth)) {
          this.dotNetRef.invokeMethodAsync('PreventExtraFrozenCellRendering');
        }
      }
      // Call height adjustment in vertical mode when text wrap is enabled
      if (this.options.enableAdaptiveUI && this.options.rowRenderingMode === 'Vertical' && this.options.allowTextWrap && (this.options.wrapMode === 'Header' || this.options.wrapMode === 'Both')) {
        this.adjustHeightsForTextWrap();
      }
      if (this.options.enableVirtualization && this.virtualContentModule.keyCombination === 'ctrlEnd') {
        var rows = this.getContent().querySelectorAll('tr.e-row[data-uid]');
        if (!sf.base.isNullOrUndefined(rows) && rows.length > 0) {
          var firstRow = rows[0];
          var lastRow = rows[rows.length - 1];
          var firstRowFirstCell_1 = firstRow.querySelector('td[tabindex="0"]');
          var lastRowLastCell_1 = lastRow.querySelector('td[tabindex="0"]');
          var allFocusableCells = Array.from(this.getContent().querySelectorAll('td[tabindex="0"]'));
          if (!sf.base.isNullOrUndefined(allFocusableCells) && allFocusableCells.length > 0) {
            var cellsToFocus = allFocusableCells.filter(function (cell) {
              return cell !== firstRowFirstCell_1 && cell !== lastRowLastCell_1;
            });
            if (!sf.base.isNullOrUndefined(cellsToFocus) && cellsToFocus.length > 0) {
              cellsToFocus[0].focus({
                preventScroll: true
              });
            }
          }
        }
      }
      return gridInitializeResults;
    };
    SfGrid.prototype.adjustHeightsForTextWrap = function () {
      var contentTable = this.getContent().querySelector('.e-table');
      if (!contentTable) {
        return;
      }
      if (contentTable.querySelector('tr.e-emptyrow')) {
        return;
      }
      var cells = contentTable.querySelectorAll('td');
      if (!cells.length) {
        return;
      }
      for (var i = 0; i < cells.length; i++) {
        // eslint-disable-next-line security/detect-object-injection
        var cell = cells[i];
        var headerCellHeight = parseFloat(window.getComputedStyle(cell, '::before').getPropertyValue('height')) || 0;
        var actualCellHeight = cell.offsetHeight;
        if (headerCellHeight > actualCellHeight) {
          cell.style.height = "".concat(headerCellHeight, "px");
          cell.style.boxSizing = 'content-box';
        }
      }
    };
    SfGrid.prototype.lastRowBorderCheck = function () {
      if (!this.options.enableVirtualization) {
        if (this.getContent().querySelector('.e-table').scrollHeight < this.getContent().clientHeight) {
          this.dotNetRef.invokeMethodAsync('LastRowBorder', true);
        } else if (this.options.editMode === 'Batch' && this.options.height !== 'auto' && this.options.height !== '100%') {
          var maxRows = Math.floor(this.getContent().clientHeight / this.getRowHeight());
          if (this.options.frozenRows > 0) {
            maxRows += this.options.frozenRows;
          }
          this.dotNetRef.invokeMethodAsync('MaximumVisibleRows', maxRows);
        }
      }
    };
    SfGrid.prototype.addTableBorderClass = function () {
      var headerTable = this.getHeaderTable();
      var contentTable = this.getContentTable();
      var footerTable;
      if (!sf.base.isNullOrUndefined(this.getFooterContent())) {
        footerTable = this.getFooterContent().querySelector('.e-table');
      }
      var tableWidth = headerTable.offsetWidth;
      var contentwidth = this.getContent().scrollWidth;
      if (contentwidth > tableWidth) {
        headerTable.classList.add('e-tableborder');
        contentTable.classList.add('e-tableborder');
        if (!sf.base.isNullOrUndefined(footerTable)) {
          footerTable.classList.add('e-tableborder');
        }
      } else {
        headerTable.classList.remove('e-tableborder');
        contentTable.classList.remove('e-tableborder');
        if (!sf.base.isNullOrUndefined(footerTable)) {
          footerTable.classList.remove('e-tableborder');
        }
      }
    };
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    SfGrid.prototype.wireEvents = function () {
      sf.base.EventHandler.add(this.element, 'mousedown', this.mouseDownHandler, this);
      sf.base.EventHandler.add(this.element, 'focus', this.gridFocus, this);
      this.delegateClickHandler = this.documentClickHandler.bind(this);
      sf.base.EventHandler.add(document, 'click', this.delegateClickHandler, this);
      sf.base.EventHandler.add(this.element, 'keydown', this.gridKeyDownHandler, this);
      sf.base.EventHandler.add(this.element, 'keydown', this.keyDownHandler, this);
      this.delegateKeyDownHandler = this.documentKeyHandler.bind(this);
      sf.base.EventHandler.add(document.body, 'keydown', this.delegateKeyDownHandler, this);
      sf.base.EventHandler.add(this.getContent(), 'touchstart', this.tapEvent, this);
      sf.base.EventHandler.add(this.getContent(), 'click', this.preventClickOnDrag, this);
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      sf.base.EventHandler.add(window, 'resize', this.windowResized, this);
      sf.base.EventHandler.add(this.element, 'contextmenu', this.mouseDownHandler, this);
      if (this.options.allowEditing) {
        sf.base.EventHandler.add(this.element, 'dblclick', this.doubleClickHandler, this);
      }
      if (this.options.enableColumnVirtualization && this.options.allowPaging) {
        // Custom event to handle shift+tab navigation in column virtualization with paging enabled when focus is on the pager
        // Bind a method to your custom event 'shiftTabNavigation'
        sf.base.EventHandler.add(this.element, 'shiftTabNavigation', this.handleShiftTabNavigation, this);
      }
    };
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    SfGrid.prototype.unWireEvents = function () {
      sf.base.EventHandler.remove(this.element, 'mousedown', this.mouseDownHandler);
      sf.base.EventHandler.remove(this.element, 'focus', this.gridFocus);
      sf.base.EventHandler.remove(document, 'click', this.delegateClickHandler);
      sf.base.EventHandler.remove(this.element, 'keydown', this.gridKeyDownHandler);
      sf.base.EventHandler.remove(this.element, 'keydown', this.keyDownHandler);
      sf.base.EventHandler.remove(document.body, 'keydown', this.delegateKeyDownHandler);
      sf.base.EventHandler.remove(this.element, 'dblclick', this.doubleClickHandler);
      sf.base.EventHandler.remove(this.getContent(), 'touchstart', this.tapEvent);
      sf.base.EventHandler.remove(this.getContent(), 'click', this.preventClickOnDrag);
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      sf.base.EventHandler.remove(window, 'resize', this.windowResized);
      sf.base.EventHandler.remove(this.element, 'contextmenu', this.mouseDownHandler);
      if (this.options.enableColumnVirtualization && this.options.allowPaging) {
        sf.base.EventHandler.remove(this.element, 'shiftTabNavigation', this.handleShiftTabNavigation);
      }
    };
    SfGrid.prototype.windowResized = function () {
      // eslint-disable-next-line @typescript-eslint/no-this-alias
      var _this = this;
      setTimeout(function () {
        var content = _this.element.querySelector('.e-content.e-yscroll');
        if (_this.options.frozenColumns && (_this.options.width === '100%' || 'auto')) {
          if (_this.options.allowTextWrap) {
            _this.freezeModule.refreshRowHeight();
          }
        }
        if (!sf.base.isNullOrUndefined(content) && content.scrollHeight > content.clientHeight) {
          _this.element.querySelector('.e-gridheader').style.paddingRight = getScrollBarWidth() - _this.scrollModule.getThreshold() + 'px';
        } else {
          _this.scrollModule.setPadding();
        }
        var gridContent = _this.element.querySelector('.e-gridcontent');
        var startIndex = gridContent.style.height.indexOf('- ') + 2;
        var endIndex = gridContent.style.height.indexOf('p');
        var sibilingsHeight = !sf.base.isNullOrUndefined(gridContent) && gridContent.style.height !== '' ? parseInt(gridContent.style.height.slice(startIndex, endIndex), 10) : 0;
        var height = !sf.base.isNullOrUndefined(gridContent) && gridContent.style.height !== '' ? getSiblingsHeight(gridContent) : 0;
        if (height !== sibilingsHeight) {
          _this.scrollModule.refresh();
        }
        if (!sf.base.Browser.isDevice) {
          var activeElement = document.activeElement;
          var isTextInput = !!activeElement && /^(INPUT|TEXTAREA)$/.test(activeElement.tagName);
          var isLandscapeInputFocused = _this.isLandScapeMobile() && isTextInput;
          if (!sf.base.isNullOrUndefined(_this.element) && (_this.element.querySelector('.e-filter-popup.e-popup-open') || _this.element.querySelector('.e-ccdlg.e-popup-open'))) {
            if (!isLandscapeInputFocused) {
              _this.dotNetRef.invokeMethodAsync('FilterPopupClose');
            }
          }
          // Close enhanced operator dropdowns on window resize
          _this.closeOperatorDropdownIfOpen();
        }
        _this.columnChooserModule.windowResized();
        if (_this.isResetDataTriggered && (_this.options.width === '100%' || _this.options.width === 'auto') && _this.options.enableVirtualization && !_this.options.enableColumnVirtualization && _this.options.allowResizing) {
          var contentTable = _this.getContentTable();
          var headerTable = _this.getHeaderTable();
          if (!sf.base.isNullOrUndefined(contentTable) && !sf.base.isNullOrUndefined(headerTable) && headerTable.offsetWidth > 0 && contentTable.offsetWidth > 0) {
            contentTable.style.width = headerTable.offsetWidth + 'px';
          }
        }
      }, 100);
    };
    /**
     * Closes enhanced operator dropdowns if any are open.
     * This method safely checks for the presence of open operator dropdowns
     * and triggers the close action via Blazor interop.
     *
     * @public
     * @returns {void}
     */
    SfGrid.prototype.closeOperatorDropdownIfOpen = function () {
      if (!sf.base.isNullOrUndefined(this.dotNetRef) && !sf.base.isNullOrUndefined(document)) {
        var operatorDropdowns = document.querySelectorAll('.e-enhanced-operator-dropdown.e-popup-open');
        if (!sf.base.isNullOrUndefined(operatorDropdowns) && operatorDropdowns.length > 0) {
          this.dotNetRef.invokeMethodAsync('CloseEnhancedOperatorDropdown');
        }
      }
    };
    SfGrid.prototype.doubleClickHandler = function (e) {
      if (e.target.tagName === 'TD') {
        e.target.blur();
      }
      this.toolTipModule.close();
    };
    SfGrid.prototype.setOptions = function (newOptions, options) {
      var oldOptions = sf.base.extend(options, {});
      this.options = newOptions;
      if (!oldOptions.allowResizing && newOptions.allowResizing) {
        this.resizeModule.render();
      }
      if (!oldOptions.allowGrouping && newOptions.allowGrouping || !oldOptions.allowReordering && newOptions.allowReordering || newOptions.showDropArea) {
        if (!sf.base.isNullOrUndefined(this.headerDragDrop)) {
          this.headerDragDrop.initializeHeaderDrag();
          this.headerDragDrop.initializeHeaderDrop();
        }
        if (!sf.base.isNullOrUndefined(this.groupModule)) {
          this.groupModule.initializeGHeaderDrag();
          this.groupModule.initializeGHeaderDrop();
        }
        if (newOptions.allowReordering && !sf.base.isNullOrUndefined(this.reorderModule)) {
          this.reorderModule.createReorderElement();
        }
      }
      if (oldOptions.clipMode !== newOptions.clipMode || newOptions.isColumnClipModeChanged) {
        this.toolTipModule.updateEvents();
      }
      if (!oldOptions.allowGrouping && newOptions.allowGrouping) {
        this.contentDragDrop.initializeContentDrop();
      }
      if (!oldOptions.allowRowDragAndDrop && newOptions.allowRowDragAndDrop) {
        this.rowDragAndDropModule.initializeDrag();
        if (this.options.isRenderedFromTreeGrid) {
          // eslint-disable-next-line @typescript-eslint/no-explicit-any
          var treeGridObj = this.element.parentElement.blazor_instance;
          treeGridObj.rowDragAndDropModule.updateRowDragEvents();
        }
      } else if (oldOptions.allowRowDragAndDrop && !newOptions.allowRowDragAndDrop) {
        this.rowDragAndDropModule.destroy();
      }
      if (!this.isRendered) {
        this.isRendered = this.options.isPrerendered;
      }
      if (oldOptions.groupCount !== newOptions.groupCount || oldOptions.showGroupedColumn !== newOptions.showGroupedColumn) {
        var cell = this.getHeaderTable().querySelector('.e-emptycell');
        if (!cell) {
          return;
        }
        cell.classList.remove('e-indentRefreshed');
      }
    };
    SfGrid.prototype.documentClickHandler = function (e) {
      var _this_1 = this;
      var CCButton = parentsUntil(e.target, 'e-cc-toolbar');
      var formElement = parentsUntil(e.target, 'e-gridform');
      var toolbar = parentsUntil(e.target, 'e-toolbar-item');
      var cellElement = parentsUntil(e.target, 'e-rowcell');
      var isExcel = document.querySelector('.e-dialog.e-excelfilter.e-popup-open');
      this.virtualContentModule.selectedCellNavigation = -1;
      var gridElementId = sf.base.isNullOrUndefined(parentsUntil(e.target, 'e-grid')) ? null : parentsUntil(e.target, 'e-grid').id;
      var targetGridId = !sf.base.isNullOrUndefined(parentsUntil(cellElement, 'e-grid')) ? parentsUntil(cellElement, 'e-grid').id : null;
      if (parentsUntil(e.target, 'e-inline-edit') && gridElementId === this.element.id) {
        var cell = parentsUntil(e.target, 'e-rowcell');
        this.editedCellIndex = cell == null ? null : cell.cellIndex;
      } else {
        if (sf.base.isNullOrUndefined(parentsUntil(e.target, 'e-popup-open'))) {
          this.editedCellIndex = null;
        }
      }
      if (!this.options.enableAdaptiveUI && !this.targetIsFilterDialog(e) && !e.target.classList.contains('e-cc-cancel') && !e.target.classList.contains('e-choosercheck') && !e.target.classList.contains('e-icon-filter') && !CCButton && (this.element.querySelectorAll('.e-filter-popup.e-popup-open').length || this.element.querySelectorAll('.e-ccdlg.e-popup-open').length)) {
        if (this.element.querySelector('.e-datetimepicker') != null) {
          this.element.querySelector('.e-datetimepicker').blur();
        }
        if (!sf.base.isNullOrUndefined(isExcel)) {
          setTimeout(function () {
            _this_1.dotNetRef.invokeMethodAsync('FilterPopupClose');
          }, 10);
        } else {
          this.dotNetRef.invokeMethodAsync('FilterPopupClose');
        }
      }
      if (this.options.editMode === 'Batch' && sf.base.isNullOrUndefined(formElement) && (!cellElement || !sf.base.isNullOrUndefined(targetGridId) && this.element.id !== targetGridId) && !toolbar && !this.targetIsFilterDialog(e) && this.element.querySelector('.e-gridform')) {
        this.dotNetRef.invokeMethodAsync('UpdateChanges');
      }
      if (!sf.base.Browser.isDevice) {
        this.toolTipModule.close();
      }
      var checkboxfilter = parentsUntil(e.target, 'e-checkboxfilter');
      if (!sf.base.isNullOrUndefined(checkboxfilter) || !sf.base.isNullOrUndefined(parentsUntil(e.target, 'e-excelfilter'))) {
        var filter = checkboxfilter || parentsUntil(e.target, 'e-excelfilter');
        if (sf.base.isNullOrUndefined(filter)) {
          return;
        }
        var selectAll = filter.querySelector('.e-selectall');
        var selectAllWrap = selectAll ? parentsUntil(selectAll, 'e-ftrchk') || selectAll.closest('.e-ftrchk') : null;
        // Detect if header (Select All) row was clicked
        var rowWithClick = e.target.closest('.e-ftrchk') || e.target.closest('li');
        var isHeaderClick = !!(rowWithClick && selectAll && rowWithClick.contains(selectAll));
        if (isHeaderClick) {
          if (selectAllWrap && !selectAllWrap.classList.contains('e-chkfocus')) {
            selectAllWrap.classList.add('e-chkfocus');
          }
          if (selectAll) {
            selectAll.focus();
          }
        } else {
          if (selectAllWrap) {
            selectAllWrap.classList.remove('e-chkfocus');
          }
          var item = e.target.closest('li[uid]') || e.target.closest('[uid]');
          var valueCheckbox = item ? item.querySelector('.e-chk-hidden') : null;
          if (valueCheckbox) {
            valueCheckbox.focus();
          }
        }
      }
    };
    SfGrid.prototype.targetIsFilterDialog = function (e) {
      var popupElement = parentsUntil(parentsUntil(e.target, 'e-filter-popup'), 'e-popup-open');
      var filterDropdown = parentsUntil(parentsUntil(e.target, 'e-ddl'), 'e-popup-open');
      var filterCheckbox = parentsUntil(parentsUntil(e.target, 'e-selectall'), 'e-ftrchk');
      var searchClear = parentsUntil(e.target, 'e-chkcancel-icon');
      var isBlankCheckbox = e.target.classList.contains('e-check') || e.target.classList.contains('e-uncheck');
      var ccPopupElement = parentsUntil(parentsUntil(e.target, 'e-ccdlg'), 'e-popup-open');
      var mentionElement = parentsUntil(parentsUntil(e.target, 'e-ccdlg-mention'), 'e-popup-open');
      var dropDownTreeElement = parentsUntil(parentsUntil(e.target, 'e-ddt'), 'e-popup-open');
      var zoomInElement = parentsUntil(e.target, 'e-zoomin');
      if (popupElement || filterDropdown || filterCheckbox || searchClear || this.isOutsideGridComponent(e) || zoomInElement || isBlankCheckbox || ccPopupElement || mentionElement || dropDownTreeElement) {
        return true;
      } else {
        return false;
      }
    };
    SfGrid.prototype.isOutsideGridComponent = function (e) {
      var _this_1 = this;
      var elements = [{
        className: 'e-datepicker',
        popupSuffix: '_popup'
      }, {
        className: 'e-datetimepicker',
        popupSuffix: '_popup'
      }, {
        className: 'e-timepicker',
        popupSuffix: '_popup'
      }, {
        className: 'e-daterangepicker',
        popupSuffix: '_popup'
      }];
      var isPickerInGrid = elements.some(function (_a) {
        var className = _a.className,
          popupSuffix = _a.popupSuffix;
        var pickerElement = _this_1.element.querySelector(".".concat(className));
        var pickerInEvent = parentsUntil(e.target, className);
        var pickerElements = _this_1.element.querySelectorAll(".".concat(className));
        if (!sf.base.isNullOrUndefined(pickerInEvent) && pickerElements.length > 0) {
          pickerElement = Array.from(pickerElements).find(function (el) {
            return '' + el.id + popupSuffix === pickerInEvent.id;
          });
        }
        var isExpanded = !sf.base.isNullOrUndefined(pickerInEvent) && !sf.base.isNullOrUndefined(pickerElement) && "".concat(pickerElement.id).concat(popupSuffix) === pickerInEvent.id ? _this_1.options.isWebAssembly ? 'true' : pickerElement.getAttribute('aria-expanded') : 'false';
        return isExpanded === 'true';
      });
      return isPickerInGrid;
    };
    SfGrid.prototype.documentKeyHandler = function (e) {
      var isMacLike = navigator.userAgent.indexOf('Mac') !== -1;
      // Handle Ctrl+Z (Undo) and Ctrl+Y (Redo) for Undo/Redo functionality
      if ((e.ctrlKey || isMacLike && e.metaKey) && (e.keyCode === 90 || e.keyCode === 89 || e.shiftKey && e.keyCode === 90)) {
        // Z (90) = Ctrl+Z for Undo, Y (89) = Ctrl+Y for Redo, Shift+Z (90) = Ctrl+Shift+Z for Redo
        var targetGrid = parentsUntil(e.target, 'e-grid');
        if (!sf.base.isNullOrUndefined(targetGrid) && targetGrid.id === this.element.id) {
          e.preventDefault(); // Prevent browser's default undo/redo
          this.dotNetRef.invokeMethodAsync('GridKeyDown', {
            key: e.key,
            code: e.code,
            ctrlKey: e.ctrlKey,
            shiftKey: e.shiftKey,
            altKey: e.altKey,
            metaKey: e.metaKey
          }, false, false, false, this.editedCellIndex, null, null, false, false);
          return;
        }
      }
      //TODO: handle alt+w
      // 74 - J
      if (e.altKey && e.keyCode === 74 && !sf.base.isNullOrUndefined(this.element)) {
        var grids = document.querySelectorAll('.e-grid');
        var targetGrid = parentsUntil(e.target, 'e-grid');
        if (grids.length > 1) {
          if (sf.base.isNullOrUndefined(targetGrid)) {
            grids[0].focus();
            return;
          }
          targetGrid.focus();
        } else {
          this.element.focus();
        }
        this.dotNetRef.invokeMethodAsync('GridFocus', e);
      }
      if (e.altKey && e.keyCode === 87 && !sf.base.isNullOrUndefined(this.element)) {
        var isPagerFocused = !sf.base.isNullOrUndefined(parentsUntil(e.target, 'e-pager'));
        var isSearchInput = false;
        var targetGrid = parentsUntil(e.target, 'e-grid');
        if (!sf.base.isNullOrUndefined(targetGrid)) {
          return; // returned here since this action will get performed on Grid KeyDown handler.
        }
        this.dotNetRef.invokeMethodAsync('GridKeyDown', {
          key: e.key,
          code: e.code,
          ctrlKey: e.ctrlKey,
          shiftKey: e.shiftKey,
          altKey: e.altKey
        }, isSearchInput, isPagerFocused, false, this.editedCellIndex, null, null, false, false);
      }
    };
    SfGrid.prototype.iterateTemplateElementsForward = function (columnTemplateElements) {
      for (var i = 0; i < columnTemplateElements.length; i++) {
        var currentElement = columnTemplateElements[parseInt(i.toString(), 10)];
        if (currentElement.tabIndex === 0) {
          this.firstFocusableTemplateElement = currentElement;
          break;
        } else if (!sf.base.isNullOrUndefined(currentElement.children) && currentElement.children.length !== 0) {
          this.iterateTemplateElementsForward(currentElement.children);
          break;
        }
      }
      return this.firstFocusableTemplateElement;
    };
    SfGrid.prototype.iterateTemplateElementsBackward = function (columnTemplateElements) {
      for (var i = columnTemplateElements.length - 1; i >= 0; i--) {
        var currentElement = columnTemplateElements[parseInt(i.toString(), 10)];
        if (currentElement.tabIndex === 0) {
          this.lastFocusableTemplateElement = currentElement;
          break;
        } else if (!sf.base.isNullOrUndefined(currentElement.children) && currentElement.children.length !== 0) {
          this.iterateTemplateElementsBackward(currentElement.children);
          break;
        }
      }
      return this.lastFocusableTemplateElement;
    };
    SfGrid.prototype.keyDownHandler = function (e) {
      var _a;
      var gridElement = parentsUntil(e.target, 'e-grid');
      var elementTag = e.target.tagName;
      var isPagerFocused = !sf.base.isNullOrUndefined(parentsUntil(e.target, 'e-pager'));
      var isGridToolbarFocused = !sf.base.isNullOrUndefined(parentsUntil(e.target, 'e-toolbar-items'));
      var isSearchInput = false;
      var focusTemplateCell = false;
      var cellIndex;
      var rowIndex;
      //when shift+tab pressed from first content cell, focus get stuck in filterBar cell
      if (gridElement) {
        var headerCells = Array.from(gridElement.querySelectorAll('.e-headercell')).filter(function (cell) {
          return cell.offsetParent !== null;
        });
        var filterBarCell = parentsUntil(document.activeElement, 'e-filterbarcell');
        if (e.shiftKey && e.key === 'Tab' && filterBarCell) {
          var previousFilterBarCell = filterBarCell.previousElementSibling;
          var currentCell = filterBarCell;
          var target = e.target;
          var hasEnhancement = currentCell.querySelector('.e-enhancement-filterbar') !== null;
          var isFilterIcon = (target === null || target === void 0 ? void 0 : target.classList.contains('e-icons')) && (target.classList.contains('e-filter') || target.classList.contains('e-filter-clear'));
          // Handle first filter bar edge case
          if (hasEnhancement && !previousFilterBarCell && isFilterIcon) {
            previousFilterBarCell = currentCell;
          }
          if (previousFilterBarCell) {
            var isEnhancedPrevCell = previousFilterBarCell.querySelector('.e-enhancement-filterbar') !== null;
            if (!isEnhancedPrevCell) {
              var filterBarInput = (_a = previousFilterBarCell.querySelector('[tabindex="0"]')) !== null && _a !== void 0 ? _a : previousFilterBarCell.querySelector('input');
              if (filterBarInput) {
                filterBarInput.focus();
                e.preventDefault();
              }
            }
          } else if (!sf.base.isNullOrUndefined(headerCells)) {
            headerCells[headerCells.length - 1].focus();
            e.preventDefault();
          }
        }
        if (e.key === 'Tab' && !e.shiftKey) {
          var filterBarRow = parentsUntil(document.activeElement, 'e-filterbar');
          if (filterBarRow && filterBarRow.lastElementChild === filterBarCell) {
            var firstRow = this.element.querySelectorAll('.e-row:not(.e-masked-row.e-hidden)')[0];
            if (firstRow && firstRow.classList.contains('e-insertedrow')) {
              var firstVisibleCell = this.getFirstVisibleCell(firstRow);
              if (firstVisibleCell) {
                firstVisibleCell.focus();
                e.preventDefault();
              }
            }
          } else if (filterBarCell && this.element.querySelectorAll('.e-insertedrow').length > 1) {
            var nextFilterBarCell = filterBarCell.nextElementSibling;
            if (nextFilterBarCell) {
              var inputElement = nextFilterBarCell.querySelector('input');
              if (inputElement) {
                inputElement.focus();
              }
            }
          }
        }
      }
      if (gridElement && gridElement.id !== this.element.id || e.key === 'Shift' || e.key === 'Control' || e.key === 'Meta' || e.key === 'Alt') {
        return;
      }
      if ((elementTag === 'INPUT' || elementTag === 'TEXTAREA') && e.code === 'Delete') {
        return;
      }
      if (!sf.base.isNullOrUndefined(gridElement) && !sf.base.isNullOrUndefined(gridElement.querySelector('.e-templatecell')) && !sf.base.isNullOrUndefined(parentsUntil(e.target, 'e-templatecell'))) {
        var templateCell = parentsUntil(e.target, 'e-rowcell') || this.options.isRenderedFromTreeGrid && !sf.base.isNullOrUndefined(e.code) && e.code === 'Escape' && parentsUntil(e.target, 'e-templatecell');
        if (sf.base.isNullOrUndefined(templateCell) || !templateCell.firstElementChild) {
          return;
        }
        cellIndex = templateCell.cellIndex;
        rowIndex = Number(parentsUntil(e.target, 'e-row').getAttribute('aria-rowIndex')) - 1;
        var templateElements = templateCell.firstElementChild.children;
        var firstFocussableElement = this.iterateTemplateElementsForward(templateElements);
        var lastFocussableElement = this.iterateTemplateElementsBackward(templateElements);
        var isTabKey = !e.shiftKey && e.code === 'Tab';
        var isShiftTabKey = e.shiftKey && e.code === 'Tab';
        var isEscapeKey = e.code === 'Escape';
        if (e.target === firstFocussableElement && isShiftTabKey || e.target === lastFocussableElement && isTabKey || isEscapeKey || firstFocussableElement === null && lastFocussableElement === null && !e.target.classList.contains('e-templatecell') && (isTabKey || isShiftTabKey)) {
          focusTemplateCell = true;
        }
      }
      if (e.target.classList.contains('e-searchinput') && e.key === 'Enter') {
        isSearchInput = true;
      }
      var allow = this.isPopUpOpened(e);
      if (allow && (e.key === 'Escape' || e.key === 'Enter') && document.activeElement.tagName === 'BODY') {
        e.target.focus();
      }
      if (e.key === 'Escape' && allow && e.target.getAttribute('aria-expanded') === 'true') {
        return;
      }
      if (!sf.base.isNullOrUndefined(parentsUntil(e.target, 'e-rowcell'))) {
        this.editedCellIndex = parentsUntil(e.target, 'e-rowcell').cellIndex === this.editedCellIndex ? this.editedCellIndex : null;
      }
      //Batch edit with multi select with Enter key and arrow keys scenario handled.
      var isMultiSelectPopupRendered = false;
      if (!sf.base.isNullOrUndefined(parentsUntil(e.target, 'e-batchrow')) && allow && e.key === 'Enter' && e.target.classList.contains('e-multiselect')) {
        var popupOpened = document.getElementsByClassName('e-popup-open');
        if (popupOpened.length > 0) {
          for (var i = 0; i < popupOpened.length; i++) {
            if (popupOpened[parseInt(i.toString(), 10)].id === e.target.getAttribute('aria-owns')) {
              isMultiSelectPopupRendered = true;
            }
            break;
          }
        }
      }
      if (!sf.base.isNullOrUndefined(parentsUntil(e.target, 'e-ccdlg')) && (e.key === 'Tab' || e.key === 'ArrowDown' || e.key === 'ArrowUp')) {
        this.focusColumnChooserDialogElements(e);
        return;
      }
      //filter
      if ((e.key === 'Tab' || e.key === 'ArrowDown' || e.key === 'ArrowUp') && (!sf.base.isNullOrUndefined(parentsUntil(e.target, 'e-excelfilter')) || !sf.base.isNullOrUndefined(parentsUntil(e.target, 'e-checkboxfilter')))) {
        this.focusFilterDialogElements(e);
        return;
      }
      var isDialogOpen = !!this.element.querySelector('.e-popup-open');
      var ccDlgOpen = isDialogOpen && !!this.element.querySelector('.e-ccdlg');
      var editedRowNavigation = !!parentsUntil(e.target, 'e-editedrow') && (e.key === 'Tab' || e.key === 'Tab' && e.shiftKey);
      if (focusTemplateCell || e.key === 'F2' || e.key === 'Insert' || e.key === 'Delete' || e.key === 'p' && e.ctrlKey || (!isDialogOpen || ccDlgOpen) && e.key === 'Escape' || editedRowNavigation || isMultiSelectPopupRendered || e.altKey && e.keyCode === 87 || e.action === 'pageUp' || e.action === 'pageDown' || e.action === 'ctrlAltPageUp' || e.action === 'ctrlAltPageDown' || e.action === 'altPageUp' || e.action === 'altPageDown') {
        this.dotNetRef.invokeMethodAsync('GridKeyDown', {
          key: e.key,
          code: e.code,
          ctrlKey: e.ctrlKey,
          shiftKey: e.shiftKey,
          altKey: e.altKey,
          metaKey: e.metaKey
        }, isSearchInput, isPagerFocused, isGridToolbarFocused, this.editedCellIndex, rowIndex, cellIndex, focusTemplateCell, isMultiSelectPopupRendered);
      }
    };
    SfGrid.prototype.focusColumnChooserDialogElements = function (e) {
      var dialog = parentsUntil(e.target, 'e-dialog');
      var items = Array.from(dialog.querySelectorAll('.e-chk-hidden, .e-btn, .e-ccsearch'));
      if (!items.length) {
        return;
      }
      e.preventDefault();
      // clear previous focus
      var removeFocusClass = function removeFocusClass(selector) {
        dialog.querySelectorAll(selector).forEach(function (el) {
          el.classList.remove('e-colfocus');
        });
      };
      var current = document.activeElement;
      removeFocusClass('.e-cclist.e-colfocus');
      var tabCount = items.indexOf(current);
      if (tabCount === -1) {
        tabCount = e.shiftKey || e.key === 'ArrowUp' ? items.length : -1;
      }
      var nextIdx = e.shiftKey || e.key === 'ArrowUp' ? tabCount <= 0 ? items.length - 1 : tabCount - 1 : tabCount >= items.length - 1 ? 0 : tabCount + 1;
      var next = items[parseInt(nextIdx.toString(), 10)];
      if (next) {
        next.focus();
        var focusElement = parentsUntil(next, 'e-cclist') || parentsUntil(next, 'e-ccheck');
        if (focusElement) {
          focusElement.classList.add('e-colfocus');
        }
      }
    };
    SfGrid.prototype.focusFilterDialogElements = function (e) {
      var gridElement = parentsUntil(e.target, 'e-grid');
      var dialog = parentsUntil(e.target, 'e-dialog');
      var items = Array.from(dialog.querySelectorAll('.e-chk-hidden, .e-btn, .e-searchinput'));
      // Clear previous highlights
      var selElements = gridElement.querySelectorAll('.e-ftrchk.e-chkfocus');
      selElements.forEach(function (el) {
        el.classList.remove('e-chkfocus');
      });
      var current = document.activeElement;
      var tabCount = items.indexOf(current);
      if (tabCount === -1) {
        tabCount = e.shiftKey || e.key === 'ArrowUp' ? items.length : -1;
      }
      var nextIdx = e.shiftKey || e.key === 'ArrowUp' ? tabCount <= 0 ? items.length - 1 : tabCount - 1 : tabCount >= items.length - 1 ? 0 : tabCount + 1;
      var next = items[parseInt(nextIdx.toString(), 10)];
      if (next) {
        e.preventDefault(); // prevent default tab behavior
        next.focus();
        // Add e-colfocus to the parent .e-ftrchk if applicable
        var focusElement = parentsUntil(next, 'e-ftrchk');
        if (focusElement) {
          focusElement.classList.add('e-chkfocus');
        }
      }
    };
    SfGrid.prototype.getFirstVisibleCell = function (row) {
      for (var i = 0; i < row.cells.length; i++) {
        var cell = row.cells[parseInt(i.toString(), 10)];
        var isEmptyIndentCell = cell.classList.contains('e-indentcell') && cell.classList.contains('e-updatedtd');
        var firstVisiblecell = !isEmptyIndentCell && !cell.classList.contains('e-hide');
        if (firstVisiblecell) {
          return cell;
        }
      }
      return undefined;
    };
    SfGrid.prototype.isLastCell = function (e) {
      var normalEditDiv = e.target.closest('.e-normaledit');
      if (!normalEditDiv) {
        return false;
      } else {
        var visibleTds = Array.from(normalEditDiv.querySelectorAll('.e-rowcell')).filter(function (e) {
          return !(e.classList.contains('e-hide') || e.querySelector('.e-disabled'));
        });
        return visibleTds[visibleTds.length - 1] === parentsUntil(e.target, 'e-rowcell');
      }
    };
    SfGrid.prototype.isPopUpOpened = function (e) {
      var datePicker = e.target.classList.contains('e-datepicker');
      var dateTimePicker = e.target.classList.contains('e-datetimepicker');
      var timePicker = e.target.classList.contains('e-timepicker');
      var daterangePicker = e.target.classList.contains('e-daterangepicker');
      var multiSelect = e.target.classList.contains('e-multiselect');
      var dropDownList = !sf.base.isNullOrUndefined(parentsUntil(e.target, 'e-ddl'));
      var autoComplete = e.target.classList.contains('e-autocomplete');
      var comboBox = e.target.classList.contains('e-combobox');
      return datePicker || dateTimePicker || timePicker || daterangePicker || multiSelect || dropDownList || autoComplete || comboBox;
    };
    // custom event used for column virtualization with paging when shift + Tab is given from pager.
    SfGrid.prototype.handleShiftTabNavigation = function (e) {
      var _this_1 = this;
      var pagerTarget = e.detail.currentTarget;
      setTimeout(function () {
        if (Math.round(_this_1.getContent().scrollLeft + _this_1.getContent().clientWidth) < _this_1.getContent().scrollWidth && !sf.base.isNullOrUndefined(pagerTarget) && pagerTarget.classList.contains('e-pager') && document.activeElement.parentElement.tagName.toLocaleLowerCase() === 'tr') {
          _this_1.getContent().scrollLeft = _this_1.getContent().scrollWidth;
          _this_1.virtualContentModule.focusFromPager = true;
        } else {
          _this_1.virtualContentModule.focusFromPager = false;
        }
      }, 30);
    };
    SfGrid.prototype.gridKeyDownHandler = function (e) {
      var popupElement = parentsUntil(e.target, 'e-filter-popup');
      var elementTag = e.target.tagName;
      this.toolTipModule.close();
      if (!sf.base.isNullOrUndefined(popupElement) && popupElement.classList.contains('e-popup-open') && e.key !== 'Escape') {
        e.stopPropagation();
        if ((e.key === 'Tab' || e.key === 'shiftTab' || e.key === 'Enter' || e.key === 'shiftEnter') && (elementTag === 'INPUT' || elementTag === 'TEXTAREA')) {
          var evt = new Event('change', {
            bubbles: false,
            cancelable: true
          });
          e.target.dispatchEvent(evt);
        }
      }
      // handling the focus for the template used inside edit settings.
      var shouldReturn = false;
      if (this.options.hasTemplateInEditSettings && parentsUntil(e.target, 'e-normaledit')) {
        var editFormInputElements = this.element.querySelector('.e-normaledit').querySelectorAll('input');
        var editFormTextareaElements = this.element.querySelector('.e-normaledit').querySelectorAll('textarea');
        var editForm = __spreadArray(__spreadArray([], Array.from(editFormInputElements), true), Array.from(editFormTextareaElements), true);
        shouldReturn = true;
        if (!sf.base.isNullOrUndefined(editForm)) {
          var firstInput = editForm[0];
          var lastInput = editForm[editForm.length - 1];
          var isShiftTabKey = e.shiftKey && e.key === 'Tab';
          var isTabKey = !e.shiftKey && e.key === 'Tab';
          if (document.activeElement === firstInput && isShiftTabKey || document.activeElement === lastInput && isTabKey) {
            shouldReturn = false;
          }
        }
      }
      // Handling the fix for the mouse click issue while using the EditTemplate in normal editing refer task: 876403
      if (e.key === 'Tab') {
        var normalEditDiv_1 = this.element.querySelector('.e-normaledit');
        if (!sf.base.isNullOrUndefined(normalEditDiv_1)) {
          var targetElement = e.target;
          var isAutoCompleteOrMultiSelectOrTimePicker = targetElement.classList.contains('e-autocomplete') || targetElement.classList.contains('e-multiselect') || targetElement.classList.contains('e-timepicker');
          var visibleTds = Array.from(normalEditDiv_1.querySelectorAll('.e-rowcell')).filter(function (e) {
            return !(e.classList.contains('e-hide') || e.querySelector('.e-disabled'));
          });
          if (isAutoCompleteOrMultiSelectOrTimePicker && (e.key === 'Tab' && !e.shiftKey && visibleTds[visibleTds.length - 1] !== parentsUntil(targetElement, 'e-rowcell') || e.key === 'Tab' && e.shiftKey && visibleTds[0] !== parentsUntil(targetElement, 'e-rowcell'))) {
            return;
          }
        }
      }
      //TODO: datepicker in dialog editing
      //NOTE: The below if condition is added for this task BLAZ-7200, while ensuring this task suspecting the below condition is redundant need to ensure this.
      if ((e.key === 'Tab' || e.key === 'Escape' || e.key === 'shiftTab' || e.key === 'Enter' || e.key === 'shiftEnter') && (elementTag === 'INPUT' || elementTag === 'TEXTAREA' || e.target.classList.contains('e-datepicker') || e.target.classList.contains('e-datetimepicker')) || e.target.classList.contains('e-rowcell') && e.key === 'F2') {
        var targetElement_1 = e.target;
        // Prevent blur and allow Enter key to work for multiline input
        if (!(e.key === 'Enter' && elementTag === 'TEXTAREA' && this.options.editMode === 'Normal')) {
          if (!(e.key === 'Tab' && (targetElement_1.classList.contains('e-datepicker') || targetElement_1.classList.contains('e-datetimepicker')) || e.key === 'Enter' && targetElement_1.classList.contains('e-autocomplete') || targetElement_1.classList.contains('e-multiselect'))) {
            if (e.key === 'Escape' || e.key === 'Tab' && this.isLastCell(e)) {
              targetElement_1.blur();
            } else {
              // Firefox needs additional delay for Tab navigation to work correctly
              var delay = (e.key === 'Tab' || e.key === 'shiftTab') && sf.base.Browser.info.name === 'mozilla' ? 10 : 0;
              setTimeout(function () {
                targetElement_1.blur();
              }, delay);
            }
          }
        }
      }
      if (e.key === 'Shift' || e.key === 'Control' || e.key === 'Alt') {
        e.stopPropagation(); //dont let execute c# keydown handler for meta keys.
      }
      var isMacLike = navigator.userAgent.indexOf('Mac') !== -1;
      if (e.keyCode === 67 && (e.ctrlKey || isMacLike && e.metaKey) && !this.options.isRenderedFromTreeGrid) {
        this.clipboardModule.copy();
      } else if (e.keyCode === 72 && (e.ctrlKey || isMacLike && e.metaKey) && e.shiftKey && !this.options.isRenderedFromTreeGrid) {
        this.clipboardModule.copy(true);
      }
      if (e.keyCode === 86 && (e.ctrlKey || isMacLike && e.metaKey) && !this.options.isEdit) {
        var rowElement = parentsUntil(e.target, 'e-rowcell');
        if (!sf.base.isNullOrUndefined(rowElement) && !rowElement.classList.contains('e-templatecell')) {
          e.stopPropagation();
        }
        this.clipboardModule.pasteHandler();
      }
      var normalEditDivs = this.element.querySelectorAll('.e-normaledit');
      var normalEditDiv;
      if (!sf.base.isNullOrUndefined(normalEditDivs)) {
        normalEditDivs.forEach(function (element) {
          if (element.contains(e.target)) {
            normalEditDiv = element;
          }
        });
      }
      if (parentsUntil(e.target, 'e-showAddNewRow') && e.shiftKey) {
        return;
      }
      //Preventing Duplicate records inserted in ShowAddNewRow when pressing tab continously
      if (e.key === 'Tab' && this.options.showAddNewRow) {
        var addNewRow = this.getContent().querySelector('.e-showAddNewRow');
        if (!addNewRow || this.previousTarget === e.target) {
          this.previousTarget = null;
          return;
        }
        var targetRow = e.target ? parentsUntil(e.target, 'e-row') : null;
        if (!targetRow) {
          return;
        }
        if (addNewRow === targetRow && this.isLastCell(e)) {
          this.previousTarget = e.target;
        }
      }
      if (!sf.base.isNullOrUndefined(normalEditDiv) && e.key === 'Tab') {
        var visibleTds = Array.from(normalEditDiv.querySelectorAll('.e-rowcell')).filter(function (e) {
          return !(e.classList.contains('e-hide') || e.querySelector('.e-disabled') || e.querySelector('.e-checkbox-disabled'));
        });
        if (shouldReturn) {
          return;
        }
        var commandColumnDiv_1 = parentsUntil(e.target, 'e-unboundcelldiv');
        var targetCell = parentsUntil(e.target, 'e-rowcell');
        var isShiftPressed = e.shiftKey;
        var isCommandColumnNull_1 = sf.base.isNullOrUndefined(commandColumnDiv_1);
        var isLastCell = visibleTds[visibleTds.length - 1] === targetCell;
        var isFirstCell = visibleTds[0] === targetCell;
        var commandColumnChildCount = commandColumnDiv_1 ? commandColumnDiv_1.childElementCount : 0;
        var isCommandColumnCheck = function isCommandColumnCheck(index) {
          if (!isCommandColumnNull_1 && (typeof index !== 'number' || index < 0 || index >= commandColumnDiv_1.children.length)) {
            return false;
          }
          return isCommandColumnNull_1 || !isCommandColumnNull_1 && commandColumnDiv_1.children.item(index) === e.target;
        };
        if (!isShiftPressed && isLastCell && isCommandColumnCheck(commandColumnChildCount - 1) || isShiftPressed && isFirstCell && isCommandColumnCheck(0)) {
          this.dotNetRef.invokeMethodAsync('EndEdit', {
            key: e.key,
            code: e.code,
            ctrlKey: e.ctrlKey,
            shiftKey: e.shiftKey,
            altKey: e.altKey
          });
          e.preventDefault();
        }
      }
      if (this.element.querySelector('.e-batchrow')) {
        //new - for batch editing keys
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        var keys = ['a', 'control', 'Delete', 'Insert'];
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        var actions = ['altPageUp', 'altPageDown', 'ctrlAltPageDown', 'ctrlAltPageUp', 'ctrlPlusP'];
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        var keyboardNavKeys = ['ArrowLeft', 'ArrowRight', 'ArrowUp', 'ArrowDown', 'Home', 'End'];
        if (!sf.base.isNullOrUndefined(e.key) && (keys.includes(e.key === 'A' ? 'a' : e.key) || e.key === 'F2' && !e.shiftKey || actions.includes(e.action) || (e.shiftKey || e.ctrlKey) && keyboardNavKeys.includes(e.key))) {
          e.stopPropagation();
        }
        if (e.key === 'Tab' || e.key === 'shiftTab' || e.key === 'Enter' || e.key === 'shiftEnter') {
          e.preventDefault();
          if (elementTag === 'INPUT' || elementTag === 'TEXTAREA') {
            var evt = new Event('change', {
              bubbles: false,
              cancelable: true
            });
            e.target.dispatchEvent(evt);
          }
        }
      }
      if (this.options.selectionMode === 'Cell' && this.options.editMode === 'Batch' && parentsUntil(e.target, 'e-gridform')) {
        var rows = parentsUntil(parentsUntil(e.target, 'e-gridform'), 'e-row');
        var cells = rows.querySelectorAll('.e-rowcell:not(.e-hide)');
        var currentCell = parentsUntil(e.target, 'e-rowcell');
        if (!e.shiftKey && e.key === 'Tab') {
          for (var i = 0; i < cells.length; i++) {
            var cell = cells[parseInt(i.toString(), 10)];
            if (cell.cellIndex === currentCell.cellIndex) {
              if (i < cells.length - 1) {
                cells[i + 1].tabIndex = 0;
              }
              break; // This will exit the for loop once the condition is satisfied
            }
          }
        } else if (e.shiftKey && e.key === 'Tab') {
          for (var i = cells.length - 1; i >= 0; i--) {
            var cell = cells[parseInt(i.toString(), 10)];
            if (cell.cellIndex === currentCell.cellIndex) {
              if (i > 0) {
                cells[i - 1].tabIndex = 0;
              }
              break; // This will exit the for loop once the condition is satisfied
            }
          }
        }
      }
      if (this.options.enableColumnVirtualization) {
        this.virtualContentModule.columnVirtualizationKeyDownHandler(e);
      }
    };
    SfGrid.prototype.mouseDownHandler = function (e) {
      var _a;
      var _this_1 = this;
      this.preventSaveCellOnDragRelease = false;
      this._mouseDownX = e.clientX;
      this._mouseDownY = e.clientY;
      var gridElement = parentsUntil(e.target, 'e-grid');
      if (this.options.enableVirtualization || this.options.enableColumnVirtualization) {
        this.virtualContentModule.observer.isWheelScrolling = false;
        this.virtualContentModule.observer.isTouchScrolling = false;
      }
      if (gridElement && gridElement.id !== this.element.id) {
        return;
      }
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      var editFormElement = this.options.editMode === 'Normal' ? 'e-editedrow' : this.options.editMode === 'Batch' ? 'e-editedbatchcell' : null;
      if (!sf.base.isNullOrUndefined(editFormElement) && editFormElement && parentsUntil(e.target, editFormElement)) {
        this.preventSaveCellOnDragRelease = true;
      }
      if (!this.options.enableAdaptiveUI && !this.targetIsFilterDialog(e) && e.target.classList.contains('e-content') && (this.element.querySelectorAll('.e-filter-popup.e-popup-open').length || this.element.querySelectorAll('.e-ccdlg.e-popup-open').length)) {
        setTimeout(function () {
          _this_1.dotNetRef.invokeMethodAsync('FilterPopupClose');
        }, 0);
      }
      this.closeOperatorDropdownIfOpen();
      var allowShiftSelection = function allowShiftSelection(gridObj) {
        var activeElement = document.activeElement;
        return gridObj.element.querySelector('.e-editedrow, .e-editedbatchcell, .e-addedrow, .e-showAddNewRow') && activeElement && gridObj.element.contains(activeElement) && (activeElement.tagName === 'INPUT' || activeElement.tagName === 'TEXTAREA');
      };
      if ((e.shiftKey || e.ctrlKey) && !allowShiftSelection(this)) {
        e.preventDefault(); //prevent user select on shift pressing during selection
      }
      // e.button = 2 for right mouse button click
      // e.button = -1 for long press in touchscreen
      // This is used to handle cases where macOS users perform a "right-click" equivalent
      // by holding the Control key and clicking the left mouse button.
      var contextMenu = document.querySelector('.e-sfcontextmenu');
      var isContextMenuElement = !sf.base.isNullOrUndefined(contextMenu) ? contextMenu.classList.contains('e-grid-menu') : false;
      var isColumnMenuHide = !sf.base.isNullOrUndefined(contextMenu) ? contextMenu.classList.contains('e-hide-menu') : false;
      var isShowColumnMenu = !sf.base.isNullOrUndefined(contextMenu) ? contextMenu.classList.contains('e-grid-column-menu') : false;
      var isLeftClickWithMenu = isContextMenuElement && !isColumnMenuHide && !isShowColumnMenu && e.button === 0;
      var isMacControlLeftClick = e.button === 0 && e.ctrlKey && !e.metaKey && this.isMacOS() && isContextMenuElement;
      if (e.button !== -1 && e.button !== 2 && (parentsUntil(e.target, 'e-headercell') && !isMacControlLeftClick || parentsUntil(e.target, 'e-detailcell')) || parentsUntil(e.target, 'e-detailrowexpand') || parentsUntil(e.target, 'e-detailrowcollapse') || e.target.classList.contains('e-headercontent') || sf.base.closest(e.target, '.e-groupdroparea') || sf.base.closest(e.target, '.e-gridpopup') || sf.base.closest(e.target, '.e-summarycell') || sf.base.closest(e.target, '.e-rhandler') || sf.base.closest(e.target, '.e-filtermenudiv') || sf.base.closest(e.target, '.e-filterbarcell') || sf.base.closest(e.target, '.e-groupcaption')) {
        var defaultParams = [null, null, null, null];
        (_a = this.dotNetRef).invokeMethodAsync.apply(_a, __spreadArray(['MouseDownHandler'], defaultParams, false));
      } else if (e.button === 2 || isLeftClickWithMenu || e.type === 'contextmenu' && e.button === -1 || isMacControlLeftClick) {
        var target = null;
        var cellUid = null;
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        var rowUid = null;
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        var cellColIndex = null;
        var editForm = parentsUntil(parentsUntil(e.target, 'e-gridform'), 'e-grid');
        if (parentsUntil(e.target, 'e-editcell') || editForm && editForm.id === gridElement.id) {
          target = 'Edit';
        } else if (parentsUntil(e.target, 'e-pager')) {
          target = 'Pager';
        } else if (parentsUntil(e.target, 'e-headercontent')) {
          target = 'Header';
          cellUid = parentsUntil(e.target, 'e-headercell') ? parentsUntil(e.target, 'e-headercell').getAttribute('data-uid') : null;
        } else if (parentsUntil(e.target, 'e-content')) {
          target = 'Content';
          rowUid = parentsUntil(e.target, 'e-row') ? parentsUntil(e.target, 'e-row').getAttribute('data-uid') : null;
          cellColIndex = parentsUntil(e.target, 'e-rowcell') ? parentsUntil(e.target, 'e-rowcell').getAttribute('aria-colindex') : null;
          cellColIndex = parseInt(cellColIndex, 10);
        }
        if (target === 'Header' || target === 'Content' || target === 'Pager' || target === 'Edit') {
          this.dotNetRef.invokeMethodAsync('MouseDownHandler', target, cellUid, rowUid, cellColIndex);
        }
      }
    };
    SfGrid.prototype.preventClickOnDrag = function (e) {
      if (this._mouseDownX != null && this._mouseDownY != null && this.preventSaveCellOnDragRelease) {
        var positionChanged = this._mouseDownX != null && this._mouseDownY != null && (this._mouseDownX !== e.clientX || this._mouseDownY !== e.clientY);
        this._mouseDownX = null;
        this._mouseDownY = null;
        if (positionChanged) {
          e.stopPropagation();
        }
      }
    };
    SfGrid.prototype.gridFocus = function (e) {
      if (!sf.base.isNullOrUndefined(this.element.querySelector('.e-gridform')) && this.element.querySelector('.e-gridform').classList.contains('e-editing')) {
        return;
      }
      this.dotNetRef.invokeMethodAsync('GridFocus', e);
    };
    SfGrid.prototype.keyActionHandler = function (e) {
      var _this_1 = this;
      var elementTag = e.target.tagName;
      var isSelectTag = false;
      var isGridEditForm = false;
      isSelectTag = !sf.base.isNullOrUndefined(this.element.querySelector('.e-gridform')) && this.element.querySelector('.e-gridform').classList.contains('e-editing') && elementTag === 'SELECT';
      if (e.action === 'pageUp' || e.action === 'pageDown' || e.action === 'ctrlAltPageUp' || e.action === 'ctrlAltPageDown' || e.action === 'altPageUp' || e.action === 'altPageDown' || e.action === 'altDownArrow' && !isSelectTag || e.action === 'ctrlPlusP') {
        e.preventDefault();
      }
      var allow = this.isPopUpOpened(e);
      var inputElement = parentsUntil(e.target, 'e-autocomplete');
      if (!sf.base.isNullOrUndefined(inputElement)) {
        var id = inputElement.id + '_popup';
        if (!sf.base.isNullOrUndefined(document.getElementById(id))) {
          return;
        }
      }
      if (parentsUntil(e.target, 'e-unboundcelldiv') && e.action === 'enter' && e.target.classList.contains('e-Savebutton')) {
        return;
      }
      if (!sf.base.isNullOrUndefined(this.element.querySelector('tr.e-showAddNewRow')) && (parentsUntil(e.target, 'e-filterbarcell') || e.target.classList.contains('e-searchinput'))) {
        return;
      }
      var gridForms = this.element.querySelectorAll('.e-gridform');
      if (!sf.base.isNullOrUndefined(gridForms)) {
        gridForms.forEach(function (gridForm) {
          if (gridForm.classList.contains('e-editing') || gridForm.classList.contains('e-adding') && parentsUntil(e.target, 'e-showAddNewRow')) {
            isGridEditForm = true;
          }
        });
      }
      if ((e.action === 'enter' && elementTag !== 'TEXTAREA' || e.action === 'ctrlPlusEnter' && elementTag === 'TEXTAREA') && isGridEditForm && this.options.editMode !== 'Batch' && (allow && e.target.getAttribute('aria-expanded') === 'false' || !allow)) {
        if (this.options.showAddNewRow && this.options.showColumnChooser) {
          e.stopPropagation();
        }
        setTimeout(function () {
          e.target.blur();
          _this_1.dotNetRef.invokeMethodAsync('EndEdit', {
            key: e.key,
            code: e.code,
            ctrlKey: e.ctrlKey,
            shiftKey: e.shiftKey,
            altKey: e.altKey
          });
        }, 40);
      }
    };
    SfGrid.prototype.updateFixedcolumns = function () {
      var cols = this.columnModel;
      var FixedColumns = [];
      var unFixedColumns = [];
      // Separate columns into locked and unlocked
      cols.forEach(function (column) {
        (column.fixedColumn ? FixedColumns : unFixedColumns).push(column);
      });
      // Combine locked and unlocked columns, locked columns first
      this.columnModel = FixedColumns.concat(unFixedColumns);
    };
    SfGrid.prototype.checkFixedColumns = function (columns) {
      for (var _i = 0, columns_2 = columns; _i < columns_2.length; _i++) {
        var column = columns_2[_i];
        if (column.columns && this.checkFixedColumns(column.columns)) {
          return true; // Stop further iteration if a locked column is found
        } else if (column.fixedColumn) {
          return true; // Return immediately when a locked column is found
        }
      }
      return false; // No locked column found
    };
    SfGrid.prototype.destroy = function () {
      this.unWireEvents();
      this.frozenDragDropModule.unwireEvents();
      this.virtualContentModule.removeEventListener();
      this.addScrollEvents(false);
      this.editedCellIndex = null;
      this.toolTipModule.destroy();
      this.keyModule.destroy();
      this.columnChooserModule.removeMediaListener();
      this.selectionModule.removeEventListener();
      this.rowDragAndDropModule.destroy();
      this.headerDragDrop.destroy();
      this.scrollModule.destroy();
      this.infiniteScrollModule.destroy();
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      window.sfBlazor.disposeWindowsInstance(this.dataId);
    };
    /**
     * Retrieves the column indexes currently in view.
     *
     * @private
     * @returns {number[]} The column indexes currently in view.
     */
    SfGrid.prototype.getColumnIndexesInView = function () {
      return this.inViewIndexes;
    };
    /**
     * Sets the column indexes to be displayed in view.
     *
     * @private
     * @param {number[]} indexes - The new column indexes to set.
     * @returns {void}
     */
    SfGrid.prototype.setColumnIndexesInView = function (indexes) {
      this.inViewIndexes = indexes;
    };
    SfGrid.prototype.getRowHeight = function (isVirtual) {
      var rowHeight = this.options.rowHeight ? this.options.rowHeight : getRowHeight(this.element);
      if (isVirtual && this.options.rowHeight === 0 && this.getContent().querySelectorAll('tr')[0].classList.contains('e-row') && this.options.enableVirtualMaskRow && this.options.overscanCount === 0) {
        var rows = this.getContent().querySelectorAll('.e-row:not(.e-masked-row)');
        if (rows.length > 0 && !this.options.isRenderedFromTreeGrid) {
          rowHeight = Math.round(rows[0].getBoundingClientRect().height);
        }
      }
      return rowHeight;
    };
    SfGrid.prototype.clientActions = function () {
      var gridInitializeResults = {
        rowHeight: null,
        isMacDevice: null,
        indentWidth: null,
        isRowDragCell: null
      };
      if ((this.options.enableVirtualization || this.options.enableColumnVirtualization) && (this.options.pageSize === 12 || this.options.width === 'auto' || this.options.width === '100%')) {
        gridInitializeResults.rowHeight = this.getRowHeight();
        this.virtualContentModule.ensurePageSize();
      }
      if (this.getColumns().some(function (col) {
        return col.hideAtMedia !== '';
      })) {
        this.columnChooserModule.setMediaColumns();
      }
      return gridInitializeResults;
    };
    SfGrid.prototype.print = function () {
      this.removeColGroup();
      var printWind = window.open('', 'print', 'height=' + window.outerHeight + ',width=' + window.outerWidth + ',tabbar=no');
      printWind.moveTo(0, 0);
      printWind.resizeTo(screen.availWidth, screen.availHeight);
      sf.base.print(this.element, printWind);
    };
    SfGrid.prototype.removeColGroup = function () {
      var depth = this.options.groupCount;
      var element = this.element;
      var id = '#' + this.element.id;
      if (!depth) {
        return;
      }
      var groupCaption = element.querySelectorAll('.e-groupcaption');
      var colSpan = groupCaption[depth - 1].getAttribute('colspan');
      for (var i = 0; i < groupCaption.length; i++) {
        groupCaption[parseInt(i.toString(), 10)].setAttribute('colspan', colSpan);
      }
      var colGroups = element.querySelectorAll("colgroup".concat(id, "colGroup"));
      var contentColGroups = element.querySelector('.e-content').querySelectorAll('colgroup');
      var headerColGroups = element.querySelector('.e-headercontent').querySelectorAll('colgroup');
      this.hideColGroup(colGroups, depth);
      this.hideColGroup(contentColGroups, depth);
      this.hideColGroup(headerColGroups, depth);
    };
    SfGrid.prototype.hideColGroup = function (colGroups, depth) {
      for (var i = 0; i < colGroups.length; i++) {
        for (var j = 0; j < depth; j++) {
          colGroups[parseInt(i.toString(), 10)].children[parseInt(j.toString(), 10)].style.display = 'none';
        }
      }
    };
    SfGrid.prototype.isLandScapeMobile = function () {
      var isLandscape = window.matchMedia("(orientation: landscape)").matches;
      var isSmallScreen = window.matchMedia("(max-width: 1024px)").matches;
      var hasTouch = window.matchMedia("(pointer: coarse)").matches;
      return isLandscape && isSmallScreen && hasTouch;
    };
    /**
     * Checks if the current browser is Safari on a macOS device.
     *
     * @returns {boolean} - Returns `true` if the browser is Safari and the operating system is macOS, otherwise `false`.
     */
    SfGrid.prototype.isMacSafariBrowser = function () {
      var userAgent = navigator.userAgent;
      var isSafariBrowser = /^((?!chrome|android).)*safari/i.test(userAgent); // Check the browser is Safari
      return this.isMacOS() && isSafariBrowser;
    };
    /**
     * Checks if the current operating system is macOS.
     *
     * @returns {boolean} - Returns `true` if the operating system is macOS, otherwise `false`.
     */
    SfGrid.prototype.isMacOS = function () {
      var userAgent = navigator.userAgent;
      var isMacOS = userAgent.indexOf('Mac OS') !== -1; // check whether it is on macOS
      return isMacOS;
    };
    /**
     * For internal use only - Get the module name.
     *
     * @private
     * @returns {string} The module name.
     */
    SfGrid.prototype.getModuleName = function () {
      return 'grid';
    };
    return SfGrid;
  }();
  var gridKeyConfigs = {
    pageUp: 'pageup',
    pageDown: 'pagedown',
    ctrlAltPageUp: 'ctrl+alt+pageup',
    ctrlAltPageDown: 'ctrl+alt+pagedown',
    altPageUp: 'alt+pageup',
    altPageDown: 'alt+pagedown',
    altDownArrow: 'alt+downarrow',
    altUpArrow: 'alt+uparrow',
    ctrlDownArrow: 'ctrl+downarrow',
    ctrlUpArrow: 'ctrl+uparrow',
    ctrlPlusA: 'ctrl+A',
    ctrlPlusP: 'ctrl+P',
    ctrlPlusC: 'ctrl+C',
    ctrlShiftPlusH: 'ctrl+shift+H',
    enter: 'enter',
    ctrlPlusEnter: 'ctrl+enter'
  };

  /**
   * Blazor grid interop handler
   */
  // tslint:disable
  var Grid = {
    initialize: function initialize(dataId, element, options, dotnetRef, focusEditableCellsParams) {
      sf.base.enableBlazorMode();
      new SfGrid(dataId, element, options, dotnetRef);
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (gridInstance.options.showAddNewRow && focusEditableCellsParams != null) {
        var editableCellField = focusEditableCellsParams.editableCellField || '';
        var editableIsAdd = focusEditableCellsParams.isAdd || false;
        var editableFrozenEdit = focusEditableCellsParams.editableCellFrozenEdit || false;
        this.focusCell(dataId, editableCellField, editableIsAdd, editableFrozenEdit);
      }
      return gridInstance.getInitModulesResults();
    },
    contentReady: function contentReady(dataId, options, action, isResetData) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      var contentReadyResults;
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var instance = gridInstance.element.blazor__instance;
        instance.setOptions(options, instance.options);
        instance.options = options;
        if (!sf.base.isNullOrUndefined(instance.scrollModule)) {
          instance.scrollModule.setPadding();
        }
        contentReadyResults = instance.contentReady(action, isResetData);
        if (instance.options.isColumnWidthChanged) {
          var widthService = new ColumnWidthService(instance);
          widthService.setWidthToTable();
        }
        if (options.height === '100%') {
          instance.scrollModule.refresh();
        }
      }
      return contentReadyResults;
    },
    refreshPivotRowHeight: function refreshPivotRowHeight(dataId) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var instance = gridInstance.element.blazor__instance;
        instance.freezeModule.refreshFreeze({
          "case": 'textwrap'
        });
      }
    },
    customFilterDialog: function customFilterDialog(dataId, dlgID, isExcel) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var dialogElement = document.querySelector('#' + dlgID);
        if (gridInstance.options.isRenderedFromTreeGrid && sf.base.isNullOrUndefined(dialogElement)) {
          dialogElement = document.getElementById(dlgID);
        }
        dialogElement.style.maxHeight = '100%';
        dialogElement.style.border = '1px';
        dialogElement.style.top = '0px';
        if (isExcel) {
          var contextMenuElements = gridInstance.element.querySelectorAll('.e-sfcontextmenu');
          var filterMenu = Array.from(contextMenuElements).find(function (el) {
            return el.classList.contains('e-grid-filter-menu');
          });
          if (!sf.base.isNullOrUndefined(filterMenu)) {
            var caretElement = filterMenu.querySelector('.e-caret');
            if (!sf.base.isNullOrUndefined(caretElement)) {
              caretElement.style.paddingRight = '8px';
            }
          }
        }
      }
    },
    setCustomFilterDialogPadding: function setCustomFilterDialogPadding(element, field) {
      var gridInstance = window.sfBlazor.getCompInstance(element);
      var setPadding = document.querySelector('#' + field);
      if (gridInstance.options.isRenderedFromTreeGrid && sf.base.isNullOrUndefined(setPadding)) {
        setPadding = document.getElementById(field);
      }
      setPadding.style.padding = '16px';
    },
    searchClear: function searchClear(dataId, inputId, inputValue) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var inputElement = gridInstance.element.querySelector('#' + inputId);
        if (gridInstance.options.isRenderedFromTreeGrid && sf.base.isNullOrUndefined(inputElement)) {
          inputElement = document.getElementById(inputId);
        }
        //Note: The variable 'inputValue' is used to update checkbox input element values when the input value is changed using events.
        if (!sf.base.isNullOrUndefined(inputElement)) {
          inputElement.value = !sf.base.isNullOrUndefined(inputValue) ? inputValue : '';
          if (!sf.base.isNullOrUndefined(inputElement.id) && !inputElement.id.includes('CCSearchBox')) {
            inputElement.focus();
          }
        }
      }
    },
    updateTableWidth: function updateTableWidth(dataId, columns) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var instance = gridInstance.element.blazor__instance;
        instance.options.columns = columns;
        if (instance.options.allowResizing && instance.options.isResizedGrid) {
          var widthService = new ColumnWidthService(instance);
          var tablewidth = columns.some(function (x) {
            return x.width === '' || x.width === null;
          });
          widthService.setWidthToTable(columns, tablewidth);
        }
      }
    },
    preventResizeAction: function preventResizeAction(dataId, isCancel) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var instance = gridInstance.element.blazor__instance;
        instance.resizeModule.preventResizeAction(isCancel);
      }
    },
    preventFreezeLineMoving: function preventFreezeLineMoving(dataId, isCancel) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var instance = gridInstance.element.blazor__instance;
        instance.frozenDragDropModule.preventFreezeLineMoving(isCancel);
      }
    },
    freezeLineMovedActions: function freezeLineMovedActions(dataId, freezeLineMovingClientOptions) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var instance = gridInstance.element.blazor__instance;
        instance.options.actualFrozenColumns = freezeLineMovingClientOptions.actualFrozenColumns;
        instance.options.columns = freezeLineMovingClientOptions.columns;
        instance.options.frozenRightCount = freezeLineMovingClientOptions.frozenRightCount;
        instance.options.frozenLeftCount = freezeLineMovingClientOptions.frozenLeftCount;
        instance.options.frozenLeftColumnsCount = freezeLineMovingClientOptions.frozenLeftColumnsCount;
        instance.options.frozenColumns = freezeLineMovingClientOptions.frozenColumns;
        instance.options.isColumnReordered = freezeLineMovingClientOptions.isColumnReordered;
        instance.freezeLineMovedAction();
        instance.freezeModule.setFrozenHeight();
      }
    },
    frozenHeight: function frozenHeight(dataId, options) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var instance = gridInstance.element.blazor__instance;
        instance.freezeModule.setFrozenHeight();
        if (options.allowTextWrap) {
          instance.freezeModule.refreshRowHeight();
          instance.freezeModule.refreshFreeze({
            'case': 'textwrap'
          });
        }
        if (options.allowResizing) {
          instance.freezeModule.updateResizeHandler();
        }
      }
    },
    updateVirtualColumns: function updateVirtualColumns(dataId, virtualizedColumns) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var instance = gridInstance.element.blazor__instance;
        instance.options.virtualizedColumns = virtualizedColumns;
        if (instance.options.allowResizing && instance.options.frozenColumns === 0 && instance.getContent().querySelector('table').style.width !== '') {
          var widthService = new ColumnWidthService(instance);
          widthService.setWidthToTable(null, false, 'resize');
        }
      }
    },
    updateOptions: function updateOptions(dataId, options) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var instance = gridInstance.element.blazor__instance;
        instance.setOptions(options, instance.options);
      }
    },
    virtualHeight: function virtualHeight(dataId, options, totalItemCount) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var instance = gridInstance.element.blazor__instance;
        instance.options = options;
        instance.options.totalItemCount = totalItemCount;
        instance.virtualContentModule.refreshOffsets();
        instance.virtualContentModule.setVirtualHeight();
      }
    },
    lazyGroupExpand: function lazyGroupExpand(dataId, options) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var instance = gridInstance.element.blazor__instance;
        instance.setOptions(options, instance.options);
        instance.options = options;
        if (instance.options.enableVirtualization) {
          instance.virtualContentModule.onDataReady();
        }
        if (instance.options.enableInfiniteScrolling) {
          instance.infiniteScrollModule.infiniteOnDataReady();
          instance.infiniteScrollModule.isLazyChildLoad = false;
          instance.infiniteScrollModule.currentRowIndex = 0;
        }
      }
    },
    viewRefresh: function viewRefresh(dataId, columns) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var widthService = new ColumnWidthService(gridInstance);
        if (gridInstance.options.enableColumnVirtualization) {
          columns = gridInstance.options.virtualizedColumns;
          gridInstance.virtualContentModule.refreshOffsets();
        }
        columns = columns.filter(function (x) {
          return x.visible;
        });
        var tablewidth = columns.some(function (x) {
          return x.width === '' || x.width === null || x.width === 'auto';
        });
        gridInstance.getColumns();
        widthService.setWidthToTable(columns, tablewidth);
        gridInstance.addTableBorderClass();
      }
    },
    virtualDisconnect: function virtualDisconnect(dataId, options) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance.virtualContentModule)) {
        gridInstance.element.blazor__instance.options.enableVirtualization = options.enableVirtualization;
        gridInstance.element.blazor__instance.virtualContentModule.observer.disconnect();
      }
    },
    reorderColumns: function reorderColumns(dataId, fromFName, toFName) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        gridInstance.element.blazor__instance.reorderModule.reorderColumns(fromFName, toFName);
      }
    },
    reorderColumnByIndex: function reorderColumnByIndex(dataId, fromIndex, toIndex) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        gridInstance.element.blazor__instance.reorderModule.reorderColumnByIndex(fromIndex, toIndex);
      }
    },
    reorderColumnByTargetIndex: function reorderColumnByTargetIndex(dataId, fieldName, toIndex) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        gridInstance.element.blazor__instance.reorderModule.reorderColumnByTargetIndex(fieldName, toIndex);
      }
    },
    renderColumnChooser: function renderColumnChooser(dataId) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      var positions = ['0', '0'];
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        positions = gridInstance.element.blazor__instance.columnChooserModule.renderColumnChooser();
      }
      return positions;
    },
    renderColumnMenu: function renderColumnMenu(dataId, uid, isFilter, key) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        return gridInstance.element.blazor__instance.columnMenuModule.renderColumnMenu(uid, isFilter, key);
      } else {
        return {
          Left: 1,
          Top: 1
        };
      }
    },
    renderAdaptiveMenuItems: function renderAdaptiveMenuItems(dataId) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      var columnMenuElement = document.getElementsByClassName("e-".concat(gridInstance.element.id, "-column-menu"))[0];
      var element = !sf.base.isNullOrUndefined(columnMenuElement) ? columnMenuElement.getElementsByTagName('ul')[0] : null;
      var e = document.getElementById(gridInstance.element.id + '_responsivetoolbaritems');
      var btnOffset = e.getBoundingClientRect();
      var left = btnOffset.left + scrollX;
      var top = btnOffset.bottom + scrollY;
      var popupOffset = element.getBoundingClientRect();
      var docElement = document.documentElement;
      if (btnOffset.bottom + popupOffset.height > docElement.clientHeight) {
        if (top - btnOffset.height - popupOffset.height > docElement.clientTop) {
          top = top - btnOffset.height - popupOffset.height;
        }
      }
      if (btnOffset.left + popupOffset.width > docElement.clientWidth) {
        if (btnOffset.right - popupOffset.width > docElement.clientLeft) {
          left = left + btnOffset.width - popupOffset.width;
        }
      }
      left = e.getAttribute('data-index') === '0' ? left - element.getBoundingClientRect().width + popupOffset.width : left - element.getBoundingClientRect().width + btnOffset.width - getScrollBarWidth();
      columnMenuElement.style.left = Math.ceil(left + 1) + 'px';
      columnMenuElement.style.top = Math.ceil(top + 1) + 'px';
    },
    filterPopupRender: function filterPopupRender(dataId, dlgID, uid, type, isColumnMenu) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      var positions = ['0', '0'];
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        positions = gridInstance.element.blazor__instance.filterModule.filterPopupRender(dlgID, uid, type, isColumnMenu);
      }
      return positions;
    },
    clientHeight: function clientHeight(dataId) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        return Math.max(gridInstance.element.blazor__instance.content.clientHeight, window.innerHeight || 0);
      }
      return 0;
    },
    clientTransformUpdate: function clientTransformUpdate(dataId, xPosition, yPosition, isOverscan, isBottomAdd) {
      if (isOverscan === void 0) {
        isOverscan = false;
      }
      if (isBottomAdd === void 0) {
        isBottomAdd = false;
      }
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance.virtualContentModule)) {
        gridInstance.element.blazor__instance.virtualContentModule.updateTransform(xPosition, yPosition, isOverscan, isBottomAdd);
      }
    },
    autoFitColumns: function autoFitColumns(dataId, columns, fieldNames, isAutoFit, isColumnResized) {
      if (isColumnResized === void 0) {
        isColumnResized = false;
      }
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var instance = gridInstance.element.blazor__instance;
        if (isAutoFit && !sf.base.isNullOrUndefined(instance.resizeModule)) {
          instance.options.columns = columns;
          instance.resizeModule.autoFitColumns(fieldNames);
        }
        if (isColumnResized) {
          var widthService = new ColumnWidthService(instance);
          widthService.setPersistedWidth(columns[0]);
        }
      }
    },
    autoFit: function autoFit(dataId) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var instance = gridInstance.element.blazor__instance;
        instance.resizeModule.autoFit();
        instance.scrollModule.setPadding();
      }
    },
    refreshColumnIndex: function refreshColumnIndex(dataId, columns) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var instance = gridInstance.element.blazor__instance;
        instance.options.columns = columns;
        instance.virtualContentModule.refreshColumnIndexes();
      }
    },
    focus: function focus(dataId, rowuid, celluid, action, keyCombination, headeruid, cellColIndex, isSelectionMethodInvoked, isLastBatchEditCell) {
      if (cellColIndex === void 0) {
        cellColIndex = -1;
      }
      if (isSelectionMethodInvoked === void 0) {
        isSelectionMethodInvoked = false;
      }
      if (isLastBatchEditCell === void 0) {
        isLastBatchEditCell = false;
      }
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (sf.base.isNullOrUndefined(gridInstance)) {
        return;
      }
      var cell = getCellByRowUidAndColIndex(gridInstance.element, rowuid, cellColIndex);
      cell = cell == null ? gridInstance.element.querySelector('[data-uid="' + celluid + '"]') : cell;
      var headerCell = gridInstance.element.querySelector('[e-mappinguid="' + headeruid + '"]');
      if (sf.base.isNullOrUndefined(cell) && sf.base.isNullOrUndefined(headerCell)) {
        return;
      }
      if (!sf.base.isNullOrUndefined(headerCell) && !sf.base.isNullOrUndefined(headerCell.parentElement) && !sf.base.isNullOrUndefined(headerCell.parentElement.parentElement)) {
        headerCell.parentElement.parentElement.focus();
        return;
      }
      var expandCollapseCell = cell.classList.contains('e-recordplusexpand') || cell.classList.contains('e-recordpluscollapse');
      var isTemplateCell = isSelectionMethodInvoked || (keyCombination == null && action == null ? !cell.classList.contains('e-templatecell') : true);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance) && !sf.base.isNullOrUndefined(cell) && isTemplateCell) {
        var instance = gridInstance.element.blazor__instance;
        var _a = instance.options,
          enableVirtualization = _a.enableVirtualization,
          enableColumnVirtualization = _a.enableColumnVirtualization;
        if (!enableVirtualization && !enableColumnVirtualization || !sf.base.isNullOrUndefined(action) && (['UpdateRecord', 'ScrollSelect', 'CancelEdit'].indexOf(action) !== -1 || enableColumnVirtualization && action === 'ScrollSelect') || expandCollapseCell) {
          if (!isLastBatchEditCell) {
            cell.focus();
          }
        } else {
          instance.virtualContentModule.handleCellFocusAndNavigation(cell, action, keyCombination);
        }
      }
    },
    scrollToFocusedCell: function scrollToFocusedCell(dataId) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      var element = document.activeElement;
      if (!element) {
        return; // Ensure the focused element exists
      }
      var grid = gridInstance.element;
      var gridRow = grid.querySelector('.e-row');
      var gridContent = gridInstance.content;
      var gridRect = grid.getBoundingClientRect();
      var elementRect = element.getBoundingClientRect();
      var getFrozenColumnWidth = function getFrozenColumnWidth(frozenSelector) {
        return Array.from(gridRow.querySelectorAll(frozenSelector)).reduce(function (totalWidth, col) {
          return totalWidth + col.offsetWidth;
        }, 0);
      };
      if (!sf.base.isNullOrUndefined(getFrozenColumnWidth)) {
        return;
      }
      var leftFrozenWidth = getFrozenColumnWidth('.e-leftfreeze');
      var rightFrozenWidth = getFrozenColumnWidth('.e-rightfreeze');
      if (elementRect.left < gridRect.left + leftFrozenWidth && !element.classList.contains('e-leftfreeze')) {
        gridContent.scrollLeft -= gridRect.left + leftFrozenWidth - elementRect.left;
      } else if (elementRect.right > gridRect.right - rightFrozenWidth && !element.classList.contains('e-rightfreeze')) {
        gridContent.scrollLeft += elementRect.right - (gridRect.right - rightFrozenWidth) + 20;
      }
    },
    blurActiveElement: function blurActiveElement(dataId) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (sf.base.isNullOrUndefined(gridInstance) || sf.base.isNullOrUndefined(gridInstance.element)) {
        return;
      }
      var gridElement = gridInstance.element;
      // If grid has an empty row placeholder, move focus into that cell instead of blurring the active element.
      var emptyCell = gridElement.querySelector('.e-emptyrow td[tabindex]');
      if (!sf.base.isNullOrUndefined(emptyCell) && gridInstance.options.allowPaging) {
        emptyCell.focus();
        return;
      }
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance) && !sf.base.isNullOrUndefined(parentsUntil(document.activeElement, 'e-grid'))) {
        document.activeElement.blur();
      }
    },
    iterateElements: function iterateElements(detailTemplateElements, childGrid, isChildGridNull) {
      for (var i = detailTemplateElements.children.length - 1; i >= 0; i--) {
        if (detailTemplateElements.children[parseInt(i.toString(), 10)].classList.contains('sf-grid') && !isChildGridNull) {
          var isPagerNull = sf.base.isNullOrUndefined(childGrid.querySelector('.e-pagercontainer'));
          var isLastPageIconNull = sf.base.isNullOrUndefined(childGrid.querySelector('.e-lastpage'));
          if (isPagerNull) {
            childGrid.querySelectorAll('.e-rowcell:not(.e-hide)')[childGrid.querySelectorAll('.e-rowcell:not(.e-hide)').length - 1].focus();
          } else if (!isPagerNull && !isLastPageIconNull) {
            childGrid.querySelector('.e-lastpage').focus();
          } else if (!isPagerNull && isLastPageIconNull && childGrid.querySelectorAll('.e-numericitem')) {
            childGrid.querySelectorAll('.e-numericitem')[childGrid.querySelectorAll('.e-numericitem').length - 1].focus();
          }
          return;
        } else if (detailTemplateElements.children[parseInt(i.toString(), 10)].tabIndex === 0) {
          detailTemplateElements.children[parseInt(i.toString(), 10)].focus();
          return;
        } else if (!sf.base.isNullOrUndefined(detailTemplateElements.children[parseInt(i.toString(), 10)].children) && detailTemplateElements.children[parseInt(i.toString(), 10)].children.length !== 0) {
          this.iterateElements(detailTemplateElements.children[parseInt(i.toString(), 10)], childGrid, isChildGridNull);
          if (!document.activeElement.classList.contains('e-detailcell')) {
            return;
          }
        }
      }
    },
    focusDetailTemplateElements: function focusDetailTemplateElements(dataId, keyCombination, isDetailTemplateCell) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (sf.base.isNullOrUndefined(gridInstance.element) && sf.base.isNullOrUndefined(gridInstance.element.blazor__instance) && sf.base.isNullOrUndefined(parentsUntil(document.activeElement, 'e-grid'))) {
        return;
      }
      var childGrid = document.activeElement.querySelector('.e-grid');
      var isChildGridNull = sf.base.isNullOrUndefined(childGrid);
      if (keyCombination === 'Tab') {
        document.activeElement.blur();
      } else if (keyCombination === 'ShiftTab') {
        var detailTemplateElements = parentsUntil(document.activeElement, 'e-detailcell').firstElementChild;
        if (detailTemplateElements.children.length !== 0 && isDetailTemplateCell) {
          this.iterateElements(detailTemplateElements, childGrid, isChildGridNull);
        }
        if (document.activeElement.classList.contains('e-detailcell')) {
          var previousRowCells = document.activeElement.parentElement.previousElementSibling.querySelectorAll('.e-rowcell:not(.e-hide)');
          previousRowCells[previousRowCells.length - 1].focus();
          return;
        }
      }
    },
    updateFilterBarCell: function updateFilterBarCell(dataId, filteredFields, filteredActualValue) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      var filterRow = gridInstance.element.querySelector('.e-filterbar');
      if (sf.base.isNullOrUndefined(filterRow)) {
        return;
      }
      filteredFields.forEach(function (fieldName, i) {
        var filterCell = filterRow.querySelectorAll('#' + fieldName + '_filterBarcell');
        if (filterCell.length > 0) {
          filterCell[0].value = filteredActualValue[parseInt(i.toString(), 10)];
        }
      });
    },
    focusFilterBar: function focusFilterBar(dataId, keyCombination, isFilterTemplate, index) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      var filterRow = gridInstance.element.querySelector('.e-filterbar');
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance) && !sf.base.isNullOrUndefined(filterRow)) {
        if (keyCombination === 'Tab') {
          if (document.activeElement.classList.contains('e-headercell')) {
            var filterBar = null;
            // For enhanced filter bar - look for input in .e-input-group or .e-enhanced-filter-input
            var enhancedFilterInput = filterRow.querySelector('.e-input-group input:not([disabled])');
            if (!sf.base.isNullOrUndefined(enhancedFilterInput)) {
              filterBar = enhancedFilterInput;
            } else {
              // Fallback to old structure
              filterBar = filterRow.querySelector('.e-textbox.e-input:not([disabled])');
            }
            if (isFilterTemplate) {
              var filterInputDivs = filterRow.querySelectorAll('.e-fltrinputdiv');
              for (var i = 0; i < filterInputDivs.length; i++) {
                // eslint-disable-next-line security/detect-object-injection
                var divElement = filterInputDivs[i];
                if (divElement.children.length === 0) {
                  continue;
                }
                // Check for input in e-input-group (enhanced filter) first
                var filterTemplateInput = divElement.querySelector('.e-input-group input') || divElement.querySelector('[tabindex="0"]') || divElement.querySelector('input');
                if (!sf.base.isNullOrUndefined(filterTemplateInput)) {
                  filterTemplateInput.focus();
                  break;
                } else {
                  divElement.children[0].focus();
                  break;
                }
              }
            } else if (!sf.base.isNullOrUndefined(filterBar)) {
              filterBar.focus();
            }
          }
        } else if (keyCombination === 'ShiftTab') {
          var activeElement = document.activeElement;
          var currentEditCell = parentsUntil(activeElement, 'e-updatedtd');
          var isBatchEdit = gridInstance.options.editMode === 'Batch';
          var hasNoError = currentEditCell && currentEditCell.querySelectorAll('.e-griderror').length === 0;
          if (activeElement && activeElement.classList.contains('e-rowcell') || isBatchEdit && hasNoError || activeElement.classList.contains('e-recordplusexpand') || activeElement.classList.contains('e-recordpluscollapse')) {
            var filterBar = null;
            // For enhanced filter bar - look for inputs in .e-input-group
            var enhancedFilterInputs = filterRow.querySelectorAll('.e-input-group input:not([disabled])');
            if (enhancedFilterInputs.length > 0) {
              filterBar = enhancedFilterInputs;
            } else {
              // Fallback to old structure
              filterBar = filterRow.querySelectorAll('.e-textbox.e-input:not([disabled])');
            }
            if (isFilterTemplate) {
              var filterElements = filterRow.querySelectorAll('.e-fltrinputdiv');
              var lastFilterElement = filterElements[filterElements.length - 1];
              // Check for input in e-input-group (enhanced filter) first
              var lastFilterTemplate = lastFilterElement.querySelector('.e-input-group input') || lastFilterElement.querySelector('input');
              if (!sf.base.isNullOrUndefined(lastFilterTemplate)) {
                lastFilterTemplate.focus();
              } else {
                lastFilterElement.children[0].focus();
              }
            } else if (!sf.base.isNullOrUndefined(filterBar) && filterBar.length > 0) {
              filterBar[filterBar.length - 1].focus();
            }
          }
        } else if (keyCombination === 'ArrowUp' || keyCombination === 'ArrowDown') {
          if (document.activeElement.classList.contains('e-groupcaption') || document.activeElement.classList.contains('e-recordplusexpand')) {
            filterRow.querySelector('.e-textbox.e-input:not([disabled])').focus();
          } else if (isFilterTemplate) {
            var filterTemplateInput = filterRow.querySelectorAll('.e-fltrinputdiv')[index === -1 ? parseInt(document.activeElement.getAttribute('aria-colIndex'), 10) - 1 : index].querySelector('input');
            if (!sf.base.isNullOrUndefined(filterTemplateInput)) {
              filterTemplateInput.focus();
            } else {
              filterRow.querySelectorAll('.e-fltrinputdiv')[index === -1 ? parseInt(document.activeElement.getAttribute('aria-colIndex'), 10) - 1 : index].children[0].focus();
            }
          } else {
            filterRow.querySelectorAll('.e-input')[index === -1 ? parseInt(document.activeElement.getAttribute('aria-colIndex'), 10) - 1 : index].focus();
          }
        }
      }
    },
    focusAddForm: function focusAddForm(dataId, keyCombination) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      var showAddNewRow = gridInstance.element.querySelector('.e-showAddNewRow');
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance) && !sf.base.isNullOrUndefined(showAddNewRow)) {
        if (keyCombination === 'Tab') {
          if (document.activeElement.classList.contains('e-headercell')) {
            var inputTextBox = showAddNewRow.querySelector('.e-input:not([disabled])');
            if (!sf.base.isNullOrUndefined(inputTextBox)) {
              inputTextBox.focus();
            }
          }
        } else if (keyCombination === 'ShiftTab') {
          if (document.activeElement.classList.contains('e-rowcell') || document.activeElement.classList.contains('e-recordplusexpand') || document.activeElement.classList.contains('e-recordpluscollapse')) {
            var inputTextBox_1 = showAddNewRow.querySelectorAll('.e-input:not([disabled])');
            var tds = showAddNewRow.querySelectorAll('.e-rowcell:not(.e-hide)');
            var commonElements_1 = [];
            tds.forEach(function (td) {
              var inputInTd = td.querySelector('.e-input:not([disabled])');
              if (inputInTd && Array.from(inputTextBox_1).some(function (input) {
                return input === inputInTd;
              })) {
                commonElements_1.push(inputInTd);
              }
            });
            if (!sf.base.isNullOrUndefined(tds)) {
              commonElements_1[commonElements_1.length - 1].focus();
            }
          }
        } else if (keyCombination === 'ArrowUp' || keyCombination === 'ArrowDown') {
          if (document.activeElement.classList.contains('e-groupcaption') || document.activeElement.classList.contains('e-recordplusexpand') || document.activeElement.classList.contains('e-recordpluscollapse')) {
            var inputTextBox = showAddNewRow.querySelector('.e-input:not([disabled])');
            if (!sf.base.isNullOrUndefined(inputTextBox)) {
              inputTextBox.focus();
            }
          } else {
            var colIndex = parseInt(document.activeElement.getAttribute('aria-colIndex'), 10) - 1;
            var index = 0;
            if (colIndex !== null) {
              index = colIndex;
            }
            var inputElement = showAddNewRow.querySelectorAll('.e-input')[parseInt(index.toString(), 10)];
            inputElement.focus();
          }
        }
      }
    },
    focusFirstGroupHeader: function focusFirstGroupHeader(dataId) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var firstGroupHeader = gridInstance.element.querySelector('.e-groupheadercell');
        if (!sf.base.isNullOrUndefined(firstGroupHeader)) {
          firstGroupHeader.focus();
        }
      }
    },
    focusExcelInput: function focusExcelInput(dataId, celluid) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      var excelPopup = document.querySelector('#' + celluid + '_excelDlg');
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance) && !sf.base.isNullOrUndefined(excelPopup)) {
        setTimeout(function () {
          var searchElement = excelPopup.querySelector('#' + gridInstance.element.id + '_SearchBox');
          if (!sf.base.isNullOrUndefined(searchElement)) {
            searchElement.focus();
          }
        }, 10);
      }
    },
    refreshOnDataChange: function refreshOnDataChange(dataId) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance.virtualContentModule)) {
        gridInstance.element.blazor__instance.virtualContentModule.refreshOnDataChange();
      }
    },
    updateAutofillPosition: function updateAutofillPosition(dataId, cellindex, index) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var _this = gridInstance.element.blazor__instance;
        return _this.selectionModule.updateAutofillPosition(cellindex, index);
      } else {
        return null;
      }
    },
    createBorder: function createBorder(dataId, rowIndex, cellIndex) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var _this = gridInstance.element.blazor__instance;
        return _this.selectionModule.createBorder(rowIndex, cellIndex);
      } else {
        return null;
      }
    },
    removePersistItem: function removePersistItem(dataId, id) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance) && !sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var _this = gridInstance.element.blazor__instance;
        _this.getHeaderTable().style.width = '';
        _this.getContentTable().style.width = '';
        if (_this.options.aggregatesCount !== 0 && !sf.base.isNullOrUndefined(_this.getFooterContent().querySelector('.e-table'))) {
          _this.getFooterContent().querySelector('.e-table').style.width = '';
        }
        if (_this.options.frozenColumns > 0) {
          var movableHeader = _this.element.querySelector('.e-movableheader');
          var movableContent = _this.element.querySelector('.e-movablecontent');
          if (!sf.base.isNullOrUndefined(movableHeader) && !sf.base.isNullOrUndefined(movableContent) && !sf.base.isNullOrUndefined(movableHeader.querySelector('.e-table')) && !sf.base.isNullOrUndefined(movableContent.querySelector('.e-table'))) {
            _this.element.querySelector('.e-movableheader').querySelector('.e-table').style.width = '';
            _this.element.querySelector('.e-movablecontent').querySelector('.e-table').style.width = '';
          }
        }
      }
      localStorage.removeItem(id);
    },
    focusChild: function focusChild(dataId, rowuid, celluid, cellColIndex) {
      if (cellColIndex === void 0) {
        cellColIndex = -1;
      }
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      var cell = getCellByRowUidAndColIndex(gridInstance.element, rowuid, cellColIndex);
      cell = cell == null ? gridInstance.element.querySelector('[data-uid=\'' + celluid + '\']') : cell;
      var childElements = cell.firstElementChild.children;
      var firstFocusableElement = gridInstance.iterateTemplateElementsForward(childElements);
      /* Select the first focusable child element
       * if no child found then select the cell itself.
       * if Grid is in editable state, check for editable control inside child.
       */
      if (!sf.base.isNullOrUndefined(firstFocusableElement)) {
        firstFocusableElement.focus();
      } else {
        gridInstance.element.querySelector('[data-uid="' + celluid + '"]').focus();
      }
      return !sf.base.isNullOrUndefined(firstFocusableElement) ? true : false;
    },
    exportSave: function exportSave(filename, bytesBase64) {
      var nav = navigator;
      if (nav.msSaveBlob) {
        //Download document in Edge browser
        var data = window.atob(bytesBase64);
        var bytes = new Uint8Array(data.length);
        for (var i = 0; i < data.length; i++) {
          bytes[parseInt(i.toString(), 10)] = data.charCodeAt(i);
        }
        var blob = new Blob([bytes.buffer], {
          type: 'application/octet-stream'
        });
        nav.msSaveBlob(blob, filename);
      } else {
        var link = document.createElement('a');
        link.download = filename;
        link.href = 'data:application/octet-stream;base64,' + bytesBase64;
        document.body.appendChild(link); // Needed for Firefox
        link.click();
        document.body.removeChild(link);
      }
    },
    destroy: function destroy(dataId, isRerendered) {
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        gridInstance.element.blazor__instance.destroy(isRerendered);
      }
    },
    validation: function validation(dataId, results, isAdd, newRowPosition) {
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        if (!sf.base.isNullOrUndefined(newRowPosition) && newRowPosition !== '' && gridInstance.options.newRowPosition !== newRowPosition) {
          gridInstance.options.newRowPosition = newRowPosition;
        }
        gridInstance.element.blazor__instance.editModule.createTooltip(results, isAdd);
      }
    },
    focusCell: function focusCell(dataId, field, isAdd, frozenEdit) {
      if (frozenEdit === void 0) {
        frozenEdit = false;
      }
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      var complexField = "#".concat(field.replace(/[.]/g, '___'));
      complexField = complexField + ':not(.e-disabled)';
      if (sf.base.isNullOrUndefined(gridInstance)) {
        return;
      }
      if (frozenEdit) {
        var forms = [].slice.call(gridInstance.element.querySelectorAll('form'));
        var td = void 0;
        for (var i = 0; i < forms.length; i++) {
          td = forms[parseInt(i.toString(), 10)].querySelector('td:not(.e-hide)');
          td.style.height = sf.base.closest(td, '.e-row').getBoundingClientRect().height + 'px';
        }
      }
      if (!field) {
        return;
      }
      var complexElement = gridInstance.element.querySelector(complexField);
      if (field !== '' && complexElement === null && complexField.includes('___')) {
        complexField = complexField.split('___').pop();
        complexElement = gridInstance.element.querySelector('#' + complexField);
      }
      if (field === '' && gridInstance.element.querySelector('input.e-boolcell')) {
        gridInstance.element.querySelector('input.e-boolcell').focus();
      } else if (field !== '' && complexElement) {
        var lastAddedRow = gridInstance.getContent().querySelector('tbody').classList.contains('e-addedrow');
        var preventScrolling = gridInstance.options.enableVirtualization && gridInstance.options.allowEditing && gridInstance.options.isEdit && !lastAddedRow;
        complexElement.focus({
          preventScroll: preventScrolling
        });
      }
    },
    CurrentPageFocus: function CurrentPageFocus(dataId, key, currentPage) {
      var pagerInstance = window.sfBlazor.getCompInstance(dataId);
      var numericContainer = pagerInstance.element.querySelector('.e-numericcontainer');
      if (key === 'PreviousPage' || numericContainer.querySelectorAll('.e-link:last-child')[0].innerText !== currentPage) {
        numericContainer.querySelector('.e-link').focus();
      } else {
        numericContainer.querySelectorAll('.e-link:last-child')[0].focus();
      }
    },
    pagerFocus: function pagerFocus(dataId, key) {
      var pagerInstance = window.sfBlazor.getCompInstance(dataId);
      var pagerContainer = pagerInstance.element.querySelector('.e-gridpager').querySelector('.e-pagercontainer');
      var numericContainer = pagerContainer.querySelector('.e-numericcontainer');
      var firstPage = pagerContainer.querySelector('.e-firstpage.e-pager-default');
      var previousPage = pagerContainer.querySelector('.e-prevpage.e-pager-default');
      if (key === 'ArrowDown') {
        if (firstPage) {
          firstPage.focus();
          return 'FirstPage';
        } else if (previousPage) {
          firstPage.focus();
          return 'PreviousPage';
        } else {
          numericContainer.querySelectorAll('.e-link')[1].focus();
          return '1';
        }
      } else if (key === 'ArrowRight') {
        if (firstPage !== null && firstPage.classList.contains('e-focused')) {
          previousPage.focus();
          return 'PreviousPage';
        } else if (previousPage != null && previousPage.classList.contains('e-focused') || pagerContainer.querySelector('.e-pp.e-focused') != null) {
          if (pagerContainer.querySelector('.e-pp') != null && !pagerContainer.querySelector('.e-pp').classList.contains('e-focused')) {
            pagerContainer.querySelector('.e-pp').focus();
            return 'PreviousPagerCount';
          } else {
            numericContainer.querySelectorAll('.e-link')[0].focus();
            return numericContainer.querySelectorAll('.e-link')[0].innerText;
          }
        } else if (numericContainer.querySelectorAll('.e-link.e-focused').length > 0 && pagerContainer.querySelector('.e-link.e-focused') != null && pagerContainer.querySelector('.e-link.e-focused').nextElementSibling != null) {
          numericContainer.querySelector('.e-link.e-focused').nextElementSibling.focus();
          return numericContainer.querySelector('.e-link.e-focused').nextElementSibling.innerText;
        } else if (numericContainer.querySelectorAll('.e-link.e-focused').length > 0 && pagerContainer.querySelector('.e-np') != null && pagerContainer.querySelector('.e-np.e-focused') == null) {
          pagerContainer.querySelector('.e-np').focus();
          return 'NextPagerCount';
        } else if (numericContainer.querySelectorAll('.e-link.e-focused').length > 0 || pagerContainer.querySelectorAll('.e-np.e-focused').length > 0) {
          if (pagerContainer.querySelector('.e-nextpage') != null) {
            pagerContainer.querySelector('.e-nextpage').focus();
            return 'NextPage';
          } else {
            numericContainer.querySelector('.e-link.e-focused').focus();
            return numericContainer.querySelector('.e-link.e-focused').innerText;
          }
        } else if (pagerContainer.querySelector('.e-nextpage.e-focused') != null) {
          pagerContainer.querySelector('.e-lastpage').focus();
          return 'LastPage';
        } else {
          pagerContainer.querySelector('.e-lastpage').focus();
          return 'LastPage';
        }
      } else if (key === 'ArrowLeft') {
        if (previousPage != null && previousPage.classList.contains('e-focused')) {
          firstPage.focus();
          return 'FirstPage';
        } else if (previousPage && pagerContainer.querySelector('.e-pp.e-focused')) {
          previousPage.focus();
          return 'PreviousPage';
        } else if (numericContainer.querySelectorAll('.e-link')[0].classList.contains('e-focused')) {
          if (pagerContainer.querySelector('.e-pp') != null) {
            pagerContainer.querySelector('.e-pp').focus();
            return 'PreviousPagerCount';
          } else if (previousPage) {
            previousPage.focus();
            return 'PreviousPage';
          } else {
            numericContainer.querySelectorAll('.e-link')[0].focus();
            return '1';
          }
        } else if (numericContainer.querySelectorAll('.e-link.e-focused').length > 0) {
          numericContainer.querySelector('.e-link.e-focused').previousElementSibling.focus();
          return numericContainer.querySelector('.e-link.e-focused').previousElementSibling.innerText;
        } else if (pagerContainer.querySelectorAll('.e-nextpage.e-focused').length > 0 && pagerContainer.querySelector('.e-np') != null) {
          pagerContainer.querySelector('.e-np').focus();
          return 'NextPagerCount';
        } else if (pagerContainer.querySelectorAll('.e-nextpage.e-focused').length > 0 || pagerContainer.querySelectorAll('.e-np.e-focused').length > 0) {
          var page = numericContainer.querySelectorAll('.e-link').length;
          numericContainer.querySelectorAll('.e-link')[page - 1].focus();
          return numericContainer.querySelectorAll('.e-link:last-child')[0].innerText;
        } else if (pagerContainer.querySelector('.e-lastpage.e-focused') !== null) {
          pagerContainer.querySelector('.e-nextpage').focus();
          return 'NextPage';
        } else {
          if (!firstPage.classList.contains('.e-disabled')) {
            firstPage.focus();
            return 'FirstPage';
          }
          return '0';
        }
      } else {
        return '0';
      }
    },
    setFrozenHeight: function setFrozenHeight(element) {
      element.querySelector('.e-frozencontent').style.height = element.querySelector('.e-movablecontent').offsetHeight - getScrollBarWidth() + 'px';
    },
    printGrid: function printGrid(dataId) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        gridInstance.element.blazor__instance.print();
      }
    },
    updateMediaColumns: function updateMediaColumns(dataId, mediaColumnsUid) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        gridInstance.element.blazor__instance.columnChooserModule.updateMediaColumns(mediaColumnsUid);
      }
    },
    copyToClipBoard: function copyToClipBoard(dataId, withHeader) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        gridInstance.element.blazor__instance.clipboardModule.copy(withHeader);
      }
    },
    preventCopyToClipBoard: function preventCopyToClipBoard(dataId, cancelValue, value, action) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      var gridclipBoardElement = gridInstance.clipboardModule;
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        if (action === 'Copy') {
          gridclipBoardElement.clipBoardData(cancelValue, value);
        } else if (action === 'Paste') {
          gridclipBoardElement.pasteAction(value, gridclipBoardElement.getSelectedRowCellIndexes()[0].rowIndex, gridclipBoardElement.getSelectedRowCellIndexes()[0].cellIndexes[0], cancelValue);
        }
      }
    },
    preventPasteAction: function preventPasteAction(dataId, rowIndex, columnField, value, columnIndex, cancelValue) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        gridInstance.element.blazor__instance.clipboardModule.pasteData(rowIndex, columnField, value, columnIndex, cancelValue);
      }
    },
    setMediaColumns: function setMediaColumns(dataId, isResetPersistData) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        gridInstance.element.blazor__instance.columnChooserModule.setMediaColumns(isResetPersistData);
      }
    },
    gridFocus: function gridFocus(dataId, isFocusGrid, isLastBatchEditCell, keyCombination) {
      if (isFocusGrid === void 0) {
        isFocusGrid = false;
      }
      if (isLastBatchEditCell === void 0) {
        isLastBatchEditCell = false;
      }
      if (keyCombination === void 0) {
        keyCombination = '';
      }
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance) && !sf.base.isNullOrUndefined(gridInstance.element) && !isLastBatchEditCell) {
        var groupHeaderCells = gridInstance.element.querySelectorAll('.e-groupheadercell').length;
        if (gridInstance.element.querySelectorAll('.e-toolbar-item').length !== 0) {
          var visibleToolbarItems = gridInstance.element.querySelectorAll('.e-toolbar-item:not(.e-overlay)');
          var focusElement = null;
          if (!sf.base.isNullOrUndefined(visibleToolbarItems) && visibleToolbarItems.length !== 0) {
            var lastToolbarIndex = visibleToolbarItems.length - 1;
            if (!sf.base.isNullOrUndefined(visibleToolbarItems[parseInt(lastToolbarIndex.toString(), 10)].querySelector('.e-tbar-btn'))) {
              for (var i = lastToolbarIndex; i >= 0; i--) {
                if (!sf.base.isNullOrUndefined(visibleToolbarItems[parseInt(i.toString(), 10)].querySelector('.e-tbar-btn')) && visibleToolbarItems[parseInt(i.toString(), 10)].querySelector('.e-tbar-btn').tabIndex === 0) {
                  focusElement = visibleToolbarItems[parseInt(i.toString(), 10)].querySelector('.e-tbar-btn');
                  break;
                }
              }
              if (!sf.base.isNullOrUndefined(focusElement)) {
                focusElement.focus();
                return;
              }
            } else if (!sf.base.isNullOrUndefined(visibleToolbarItems[parseInt(lastToolbarIndex.toString(), 10)].querySelector('.e-searchinput.e-input'))) {
              visibleToolbarItems[parseInt(lastToolbarIndex.toString(), 10)].querySelector('.e-searchinput.e-input').focus();
              return;
            }
          }
        } else if (groupHeaderCells > 0 && (document.activeElement.classList.contains('e-headercell') || document.activeElement.classList.contains('e-recordplusexpand') || document.activeElement.classList.contains('e-recordpluscollapse'))) {
          var unGroupButtons = gridInstance.element.querySelectorAll('.e-ungroupbutton');
          unGroupButtons[unGroupButtons.length - 1].focus();
          return;
        } else if (groupHeaderCells === 0 && gridInstance.options.allowGrouping && document.activeElement.classList.contains('e-headercell')) {
          gridInstance.element.querySelector('.e-groupdroparea').focus();
          return;
        } else if (keyCombination !== null && keyCombination === 'ShiftTab') {
          // Ensure next focusable element in DOM when Shift+Tab is pressed
          /* eslint-disable @typescript-eslint/no-explicit-any */
          var gridElement_1 = gridInstance.element;
          var focusableSelectors = 'a[href], area[href], input:not([disabled]), select:not([disabled]), ' + 'textarea:not([disabled]), button:not([disabled]), iframe, object, embed, ' + '[tabindex], [contenteditable]';
          var allFocusableElementsInDom = Array.prototype.slice.call(document.querySelectorAll(focusableSelectors)).filter(function (el) {
            var isVisible = el.offsetParent !== null;
            return isVisible && !gridElement_1.contains(el);
          });
          // Find the first focusable element before the Grid in DOM order
          var beforeGridElements = allFocusableElementsInDom.filter(function (el) {
            return !!(gridElement_1.compareDocumentPosition(el) & Node.DOCUMENT_POSITION_PRECEDING);
          });
          if (!sf.base.isNullOrUndefined(beforeGridElements) && beforeGridElements.length > 0) {
            beforeGridElements[beforeGridElements.length - 1].focus();
            return;
          }
        }
        if (parentsUntil(document.activeElement, 'e-grid') || isFocusGrid && !sf.base.isNullOrUndefined(gridInstance)) {
          gridInstance.element.focus();
        }
      }
    },
    isMacDevice: function isMacDevice() {
      return navigator.userAgent.indexOf('Mac OS') !== -1;
    },
    updateClonedMaskTranslates: function updateClonedMaskTranslates(dataId) {
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      var gObj = gridInstance.element.blazor__instance;
      var gridContent = gObj.getContent();
      var maskedTable = gridContent.querySelector('.e-masked-table');
      var minScrollTop = gridContent.scrollHeight - maskedTable.getBoundingClientRect().height;
      var scrollTop = gridContent.scrollTop <= minScrollTop ? gridContent.scrollTop : minScrollTop;
      maskedTable.style.transform = 'translate(0px,' + scrollTop + 'px)';
    },
    refreshScrollLeftPosition: function refreshScrollLeftPosition(dataId) {
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var gObj = gridInstance.element.blazor__instance;
        var scrollContent = gObj.getContent();
        var scrollLeft = scrollContent.scrollLeft;
        scrollContent.scrollLeft = scrollLeft + 20;
      }
    },
    refreshGridPageSize: function refreshGridPageSize(dataId) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance) && !sf.base.isNullOrUndefined(gridInstance.scrollModule)) {
        gridInstance.scrollModule.refresh();
      }
    },
    scrollIntoView: function scrollIntoView(dataId, columnIndex, rowIndex, rowHeight, isAddBottom, isFromAddForm) {
      if (isAddBottom === void 0) {
        isAddBottom = false;
      }
      if (isFromAddForm === void 0) {
        isFromAddForm = false;
      }
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      var gObj = gridInstance.element.blazor__instance;
      var scrollContent = gObj.getContent();
      var prevScrollTop = scrollContent.scrollTop;
      var rowHeightValue = rowHeight !== -1 ? rowHeight : gObj.getRowHeight();
      gObj.virtualContentModule.focusColumnIndex = columnIndex;
      if (isAddBottom) {
        scrollContent.scrollTop = scrollContent.scrollHeight;
      } else if (rowIndex !== -1) {
        scrollContent.scrollTop = rowIndex * rowHeightValue;
        gObj.virtualContentModule.selectedRowIndex = rowIndex;
        gObj.virtualContentModule.isScrollIntoview = true;
        if (!isFromAddForm && (rowIndex === 0 && prevScrollTop === 0 || prevScrollTop === scrollContent.scrollTop && !(gObj.virtualContentModule.scrollInfo.direction === 'left' || gObj.virtualContentModule.scrollInfo.direction === 'right'))) {
          gObj.dotNetRef.invokeMethodAsync('SelectRow', rowIndex, gObj.virtualContentModule.isScrollIntoview, gObj.virtualContentModule.focusColumnIndex);
          gObj.virtualContentModule.selectedRowIndex = -1;
        } else if (rowIndex >= gObj.options.totalItemCount) {
          gObj.virtualContentModule.selectedRowIndex = gObj.options.totalItemCount - 1;
        }
      }
      var columnOffsets = gObj.virtualContentModule.vHelper.cOffsets;
      var colOffsets = gObj.nColumnOffsets;
      setTimeout(function () {
        if (!gObj.options.frozenColumns && columnIndex !== -1) {
          if (gObj.options.enableColumnVirtualization) {
            scrollContent.scrollLeft = columnOffsets[columnIndex - 1];
          } else {
            scrollContent.scrollLeft = colOffsets[columnIndex - 1];
          }
        }
        if (gObj.options.enableColumnVirtualization && columnIndex > 0) {
          if (gObj.options.frozenColumns) {
            var customIndex = gObj.options.frozenLeftCount > 0 ? gObj.options.frozenLeftCount : gObj.options.frozenColumns;
            scrollContent.scrollLeft = columnOffsets[parseInt(customIndex.toString(), 10) - 1];
          }
        }
        if (gObj.options.enableVirtualMaskRow && gObj.virtualContentModule.startIndex === 0) {
          var virtualTable = gObj.content.querySelector('.e-virtualtable');
          var translateY = gObj.virtualContentModule.startIndex * rowHeightValue - gObj.options.pageSize * rowHeightValue;
          virtualTable.style.transform = 'translate(0px,' + translateY + 'px)';
        }
      }, 20);
    },
    lazyLoadGridHeight: function lazyLoadGridHeight(dataId) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      var height = 0;
      if (!sf.base.isNullOrUndefined(gridInstance.getContent())) {
        height = gridInstance.getContent().offsetHeight;
      }
      return height;
    },
    resetExpandCollapseAllScroll: function resetExpandCollapseAllScroll(dataId, requestType) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      gridInstance.options.requestType = requestType;
      if (!sf.base.isNullOrUndefined(gridInstance)) {
        gridInstance.infiniteScrollModule.resetInfniniteScrollPositions();
      }
    },
    updateResizeCursor: function updateResizeCursor(dataId) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance)) {
        gridInstance.resizeModule.updateHelper();
      }
    },
    getContentCell: function getContentCell(dataId, top, left) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance)) {
        return gridInstance.getContentCell(gridInstance.element, top, left);
      }
    },
    focusNextFrame: function focusNextFrame(dataId) {
      window.sfBlazor.getCompInstance(dataId);
      return new Promise(function (resolve) {
        requestAnimationFrame(function () {
          requestAnimationFrame(function () {
            return resolve(true);
          });
        });
      });
    },
    forceUpdateTranslate: function forceUpdateTranslate(dataId, translateY) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      var gridElement = gridInstance.element;
      if (!sf.base.isNullOrUndefined(gridElement)) {
        var gridBlazorInstance = gridElement.blazor__instance;
        if (!sf.base.isNullOrUndefined(gridBlazorInstance) && !sf.base.isNullOrUndefined(gridBlazorInstance.virtualContentModule)) {
          gridBlazorInstance.virtualContentModule.virtualEle.adjustTable(0, translateY);
        }
      }
    },
    syncTableWidthsAfterReset: function syncTableWidthsAfterReset(dataId) {
      var gridInstance = window.sfBlazor.getCompInstance(dataId);
      if (!sf.base.isNullOrUndefined(gridInstance) && !sf.base.isNullOrUndefined(gridInstance.element) && !sf.base.isNullOrUndefined(gridInstance.element.blazor__instance)) {
        var _this = gridInstance.element.blazor__instance;
        var contentTable = _this.getContentTable();
        var headerTable = _this.getHeaderTable();
        if (!sf.base.isNullOrUndefined(contentTable) && !sf.base.isNullOrUndefined(headerTable)) {
          var isResized = headerTable.querySelectorAll('.e-resized');
          if (!sf.base.isNullOrUndefined(isResized) && isResized.length > 0 && headerTable.offsetWidth > 0 && contentTable.offsetWidth > 0) {
            contentTable.style.width = headerTable.offsetWidth + 'px';
            _this.isResetDataTriggered = true;
          }
        }
      }
    },
    IsInputfocus: function IsInputfocus() {
      setTimeout(function () {
        var activeElement = document.activeElement;
        var isDropdownActive = activeElement.classList.contains('e-enhanced-operator-dropdown') || activeElement.closest('span.e-enhanced-operator-dropdown') !== null;
        if (activeElement && isDropdownActive) {
          var tdElement = activeElement.closest('td.e-filterbarcell');
          if (tdElement) {
            var inputElement = tdElement.querySelector('.e-enhanced-filter-input input.e-input');
            if (inputElement) {
              inputElement.focus();
            }
          }
        }
      }, 100);
    },
    getIsDeviceMode: function getIsDeviceMode() {
      try {
        // Access sf.base.Browser.isDevice which is available globally
        if (typeof window.sf !== 'undefined' && typeof window.sf.base !== 'undefined' && typeof window.sf.base.Browser !== 'undefined') {
          return window.sf.base.Browser.isDevice;
        }
      } catch (error) {
        console.warn('Error detecting device mode:', error);
      }
      return false;
    }
  };
  return Grid;
}();

/***/ })

}]);